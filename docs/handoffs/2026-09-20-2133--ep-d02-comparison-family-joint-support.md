# Handoff — both aligned identities selected; joint matrix breadth next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-21 01:33:21
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.123: whether the actual joint support of
state/boundary comparison type and overlap/member-disjoint aligned family
identity contains two, three, or all four cells. The owner selected C3c.122-C.
Preserve accepted EP-D02 wording until a complete replacement is replayed and
explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the
current full-assurance pass before the separate standalone expansion, audit,
and adaptation pass. Vanilla evidence and the byte-identical oracle remain in
their own lane.

## Selected direction — EP-D02-C3c.122-C

The owner selected

`T^{Xfam}_v={overlap,member-disjoint}`.

At least one actual aligned edge preserves and reassigns an exact complete-
family member, and at least one distinct aligned edge has member-disjoint
complete families. These provide motif migration and complete-family
reweaving at the clean same-artifact/one-coordinate boundary.

This is catalog-wide support only. It gives neither identity to every
definition, artifact, fiber, key, endpoint, owned instance, or C113 comparison
type. In particular, the prior Ashen-overlap/Dreamglass-disjoint example was a
feasibility witness; the owner's C did not select its pairing or two-cell joint
support.

Tight floors remain seven definitions, fifteen exact keys, and ninety six-
Soul rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree
remains six. Each edge retains its partial/neutral ideal ratings at the named
boundary; catalog coexistence is aggregate. The authoritative decision record
remains unchanged.

## Active owner choice — EP-D02-C3c.123

Let

`X^{overlap}_v(d)=X^O_v(d)`

and

`X^{member-disjoint}_v(d)=X^R_v(d)`.

Define actual occupied cells by

`J^X_v={(t,f) in {state,boundary} x {overlap,member-disjoint}`

`       : there exists d in D^t_{locked,v} with X^f_v(d) nonempty}`.

C113-A plus C121-C makes both comparison-type rows nonempty. C122-C makes
both family-identity columns nonempty. Therefore the joint support has exactly
two, three, or four occupied cells. No coarser prerequisite is missing.

```text
                         MOTIF MIGRATION       COMPLETE REWEAVE
                         overlap-bearing       member-disjoint
STATE-ONLY                     S,O                    S,R
BOUNDARY-ONLY                  B,O                    B,R
```

### A — exactly two cells

`|J^X_v|=2`.

The full row and column projections force one support-matrix perfect matching:

- `{(state,overlap),(boundary,member-disjoint)}`, or
- `{(state,member-disjoint),(boundary,overlap)}`.

This card does not choose which. “Matrix diagonal” must not be confused with
an earlier diagonal comparison in which both state and boundary differ.

A is the strongest simplicity countercase. Each comparison axis predicts one
family-change grammar, yielding two learnable compound dialects. Its cost is
permanent confounding: comparison axis and transformation depth cannot be
authored independently, and any rarity, balance, or access advantage of one
axis can masquerade as an advantage of its attached family identity.

### B — exactly three cells

`|J^X_v|=3`.

Exactly one cell is absent. One comparison type supports both aligned family
identities somewhere, while the other supports exactly one. This card selects
neither the rich row nor the missing cell.

B permits a controlled advanced-versus-teaching asymmetry, but has the
strongest hidden premium-axis risk, the least symmetric mental model, and a
token-rich-cell loophole.

### C — all four cells — recommended

`J^X_v={state,boundary} x {overlap,member-disjoint}`.

Both motif migration and complete-family reweaving occur somewhere under
state-only comparisons and somewhere under boundary-only comparisons.

C keeps comparison axis and family-change depth orthogonally available for
future Souls and Simulacra puzzles. Neither state comparison nor boundary
comparison dictates whether an exact motif survives. Costs are four cells to
author, validate, disclose, and teach; one obscure or dominated edge can
tokenize a cell; and full support does not guarantee within-name, within-
artifact, or per-key coexistence.

## Concrete witnesses and tight floors

C105's four-key geometry is:

```text
O edges: b0--b1, b0--b3, b1--b2
R edges: b0--b2, b1--b3
b2--b3 is not common
```

Single-identity aligned layouts are:

```text
O only: IDs {b0,b3} and {b1,b2}
R only: IDs {b0,b2} and {b1,b3}
```

Each matching covers all four keys. To realize both identities under one pure
comparison type, put all four keys in one instance fiber:

```text
S-OR: one boundary, four distinct states
B-OR: one state, four distinct boundaries
```

The sole-type graph is `K4`; intersecting it with the common graph yields all
three O edges and both R edges, covering every key. The typed `b2--b3` edge is
not common and therefore is not aligned.

Branch witnesses are:

```text
A: one O-only type definition + one R-only opposite-type definition
B: one OR type definition + one single-identity opposite-type definition
C: one S-OR definition + one B-OR definition
```

Every branch retains:

| Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | ---: | ---: | ---: | ---: |
| `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |

The outside base is five definitions and seven keys. C113-A already requires
one state-only and one boundary-only locked definition, while C105-C requires
at least four keys in each. The witnesses attain every C123 branch with those
same two four-key names. No branch changes complete families, Soul assignments,
palette, or degree.

## Ideal ratings, loopholes, and next dependency

At one fixed exact-key/Soul relationship, inherited fit is **direct**. Every
actual aligned edge is **partial** at the persistent-artifact/fixed-coordinate/
across-snapshot boundary. An overlap-bearing cell is **partial** at exact-
member continuity because a motif persists but moves to a disjoint Soul block.
A member-disjoint cell is **neutral** at exact-member continuity but
**partial** at the broader artifact/core/union/vocabulary boundary. Missing-
cell exclusion is **neutral/protective** only at that projection. Joint
correlation, asymmetry, and full breadth are **aggregate**. C is recommended
for independent gameplay dimensions, not because four separate cells directly
realize the standing ideal with one another.

C123 selects no A matching orientation, B missing cell, within-definition
dual-family prevalence, both identities per artifact/fiber/key/endpoint,
counts, frequency, accessibility, balance, power, cause, transition, path,
disclosure, agency, value, persistence, release, or implementation. State and
boundary remain static comparison coordinates, not causes.

After A, choose the perfect-matching orientation. After B, choose which row is
rich and which identity occupies the singleton row before asking local
coexistence. After C, the next coarser descendant is empty/proper/full
prevalence of locked definitions containing both aligned family identities;
per-artifact and per-key dual-family coverage are finer.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling: follow direct `AGENTS.md`
rules and do not treat the overnight plan as current guidance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C123 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
