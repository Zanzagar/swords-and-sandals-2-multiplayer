# Handoff — EP-D04 tenth direction round pending

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-10 20:42:10
**Branch:** `design/endless-progression-owner-packet`

Resume the six-choice EP-D04 R10 balance-envelope frontier in the owner packet.
Preserve accepted EP-D01, EP-D02, and EP-D07 and every selected-but-unaccepted
EP-D04 direction. Do not edit the authoritative record or implement anything.

## Session outcome

The owner's `I agree with rec` selected
`R9.1-A, R9.2-A, R9.3-A, R9.4-A`; it did not accept a final EP-D04
replacement. The selected evidence contract uses a closed two-point practical
equivalence band, non-resetting 99% simultaneous confidence for the complete
binding family and version lifecycle, an actual residual solver gap of at most
one point per claimant, and adversarial unresolved terminal mass capped at 5%
per format/view and 10% conditionally within a binding stratum. An unresolved
derived claim fails directly.

## Recomputed frontier

R10 now exposes all six linked balance choices:

1. **R10.1-A, recommended:** macro-average 1v1, 2v2, and 3v3 equally for the
   combined Tempered/Inscribed causal guarantee. Alternatives team-weight the
   modes `0.20/0.40/0.40` or require the floor separately in two formats.
2. **R10.2-A, recommended:** in every format, mixed is strictly better on at
   least 20% of structural mass and all-Legendary is strictly better on 35–60%.
3. **R10.3-A, recommended:** Tempered and Inscribed each independently own at
   least 10% of the selected structural combined measure through strict causal
   replacement wins. No exact leaf supplies over half and no member of the
   frozen exhaustive non-root causal-mechanics partition supplies over 75% of
   either rarity's certified mass.
4. **R10.4-A, recommended:** each format's all-Legendary aggregate clear-rate
   lead interval lies wholly in `(0.02,0.08]`.
5. **R10.5-A, recommended:** no canonical fixed all-Legendary entry package is
   near-optimal on over 60% of structural mass in any format.
6. **R10.6-A, recommended:** in each generated-experience format, mixed is
   strict on at least 10%, Legendary is strict on 20–60%, and no fixed
   Legendary package is near-optimal on over 70%. Tempered and Inscribed each
   own at least 5% causal experience under the R10.1 combination rule. The
   aggregate must be a practical tie or Legendary strict win, with any strict
   Legendary lead capped at ten points.

Every floor uses a simultaneous lower bound and every cap an upper bound.
Structural coverage and aggregate claims share one frozen legal coupled policy
pair. Fixed-package coverage uses universal one-sided regret: uncertain
proximity counts against the package, and the maximum ranges over every
canonical legal package. Under the experience view, a fixed package has its own
legal offer policy but cannot vary the package across contexts. An experience
causal ablation freezes the route selected by the baseline mixed policy before
replacement, so route switching cannot masquerade as item causality. Estimated
generator-weight uncertainty joins R9's simultaneous family.

One feasible exact witness is, per structural format, 40% Legendary-strict at
`+0.12`, 20% mixed-strict at `-0.04`, and 40% tied, giving a `+0.04` aggregate;
split each causal rarity's 10% across two leaves in different binding strata and
split package coverage 50/50. For experience, 20% Legendary-strict at `+0.10`,
10% mixed-strict at `-0.05`, and 70% tied gives a `+0.015` practical aggregate
tie. These are feasibility examples, not tuning targets or measured balance.

A write-nothing verifier found and the packet records corrections for four
draft loopholes: silently chosen equal-format weights; checkpoint shopping
between coverage and aggregate; a missing Legendary strict-experience cap that
allowed 90% small colour wins to cancel against 10% large mixed wins; and causal
route/package/concentration ambiguity. The final read-only check upheld the
corrected A envelope after the causal parent was defined as an exhaustive,
pairwise-disjoint, non-root partition. No evidence wave was launched.

After R10, choose migration/version failure authority, close the exact finite
support/taxonomy and team-communication authoring blockers, then present the
complete normative EP-D04 replacement for explicit accept/reject/revise replay.

## Verification

`git diff --check` passed after this handoff. The authoritative decision record
has no diff. No runtime code, tests, vanilla evidence, licensed assets,
installation, save, snapshot, capture, candidate, fixture, observation,
manifest, or golden changed. Tests were not run for this docs-only owner round.
No commit or push was made. Implementation remains blocked.
