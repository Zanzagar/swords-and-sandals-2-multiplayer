# Handoff — one primitive lineage per operation selected

**Session ID:** `7d172a4a-bd68-416a-841e-dea08736ccd3`
**UTC:** 2026-09-25 01:45
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`; present only `RCS-03C3B2A2A` next.

## Owner answer recorded

The owner's latest `A` selected `RCS-03C3B2A1-A`. `S^{split}_v` is empty:
every accepted operation occurrence has one primitive pre-episode action-root
lineage from commitment, if any, through every authored application it actually
realizes and its terminal disposition. Interrupted occurrences mint no phantom
lineage.

Distinct internal applications may later become addressable semantic children,
but retain that operation lineage and cannot fill independent-source positions
together. The operation and its manifestations participate **directly** in one
operative identity while retaining mechanically meaningful difference;
universal one-lineage prevalence is **aggregate**. This prevents a hidden
roots-per-button premium without flattening multi-hit, channel, or compound
actions.

A1-A decides no cross-operation, additive-relation-root, current-state,
build-root, addressability, claim, reuse, payoff, persistence, release, or
implementation policy. No later row derives or prunes. The authoritative
decision record remains unchanged and implementation remains blocked.

## Frontier and boundedness

Only two row statuses change: B2A1 becomes `DIR-SELECTED`, and B2A2A becomes
`OWNER-OPEN`. The 54-row register therefore contains 30 `SCREEN`, 1
`OWNER-OPEN`, 7 `PRUNED`, 13 `DIR-SELECTED`, 1 `DERIVED`, 1 `SPEC`, and 1
`EVALUATE`; `Phi_SR = 31`. `RCS-03C3B2A2A` is the sole active card.

## Next card

`RCS-03C3B2A2A` asks whether distinct, separately accepted operation
occurrences inside a prospectively authored finite source episode coarsen into
one canonical episode root. A qualifying episode form must disclose its
source-owned identity, exclusive membership grammar, and finite terminal,
abort, and restart rules no later than its first member's commitment. It cannot
be inferred from adjacency, raw causation, shared records, or a Relic match.
Each versioned form has one total, occurrence-stable policy: every realized
member coarsens or none do. Any partial/contextual variant requires a distinct
prospectively keyed form.

Present only:

- A: no qualifying source-episode form coarsens; every member operation keeps
  a distinct canonical root;
- B: every qualifying form coarsens its realized member lineages into one
  canonical episode root; or
- C: coarsened and uncoarsened source-episode forms coexist under stable
  versioned form rules.

Recommend B. “One authored source episode, one canonical source root” is
teachable, prevents integral multi-button lifecycles from farming independent
Relic sources, and gives the strongest **direct** local Achintya fit: distinct
commitments, payments, histories, and outcomes participate in one continuing
episode identity. Universal prevalence is **aggregate**. The cost is that every
qualifying lifecycle loses internal cross-source eligibility, so episode
authoring must remain narrow, prospective, and disclosed.

Plant Sigil prospectively opens `r_Rupture`; a later separately accepted
Detonate joins the same root. The actions retain primitive history and payment,
but cannot jointly occupy two independent-source positions. An unrelated Guard
root may still pair with `r_Rupture`. Emergent `Guard -> Heat` remains two roots
unless the source system authored that exact episode before play.

Coarsening is replacement/equivalence, not an additive third root. Unperformed
members supply no lineage; every re-initiation after terminal/abort opens a
fresh identity; and later membership never rolls back a committed result.
B2A2B separately owns additive relation-root promotion on whatever eligible
nonmerged compound-H complement remains and is pruned only if that domain is
empty.

## Validation

- Mechanical parsing confirms 54 rows: 30 `SCREEN`, 1 `OWNER-OPEN`, 7
  `PRUNED`, 13 `DIR-SELECTED`, 1 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`.
  `Phi_SR = 31`, and only `RCS-03C3B2A2A` is `OWNER-OPEN`.
- Three independent flag-only reviews pass. The final card includes the
  review-required whole-form/whole-occurrence merge invariant and fresh
  identity after every terminal/abort re-initiation; no undeclared partial or
  occurrence-dependent fourth branch remains.
- `git diff --check` passes.
- `node --test --test-concurrency=1` passes: 584 tests, 583 passed, 0 failed,
  and 1 expected raw-trace archive skip.
- The authoritative decision record remains byte-identical at SHA-256
  `e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
- No runtime, Ruffle, capture, installation, save, snapshot, fixture,
  candidate, observation, manifest, or golden work is authorized.
