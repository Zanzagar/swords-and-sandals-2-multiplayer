# Handoff — EP-D04 policy and burden elaboration

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-06 05:39:50
**Branch:** `design/endless-progression-owner-packet`

Resume the two still-open EP-D04 second-round choices together: confirm or
replace the R2.1 projection/state-transition collapse test, and confirm or
replace the fun-first R2.2 Legendary-commitment rule. Preserve all accepted
decisions and the selected-but-unaccepted D04 directions.

## Session outcome

The owner selected R2.3-A and R2.5-A as directions: compare legal player
policies, and define performance success-first with Pareto safeguards. The
owner also selected R2.4-A subject to examples: Tempered and Inscribed must
each own causal best-in-slot endgame niches. These remain worksheet directions,
not a final EP-D04 disposition.

The owner requested examples for R2.1, a fun version of a Legendary burden,
and elaboration of the policy/RL and lower-rarity proof methods. The owner
packet now includes:

- a collapse test distinguishing an always-on `+20% damage` shadow chassis from
  an event/state/timing-dependent behaviour;
- a proposed R2.2-A refinement that calls burdens **Legendary commitments** and
  rejects dull flat taxes, hidden punishment, and pure-upside v1 exceptions;
- concrete Tempered interruption-recovery and Inscribed reach/resource-stability
  causal-use examples; and
- a three-layer legal policy—Circuit-entry configuration, accepted
  post-preview Branch selection, and observable-history battle action/target
  choice—plus an exact/search/RL/human evaluation boundary.

The proposed commitment is item-local and mechanically live but need not make
the item net-negative. It can be a choice, setup dependency, opportunity cost,
resource conversion, timing or recovery window, target commitment, or opponent
response. Its payoff may greatly exceed the cost when mastered; compatible
commitments are the proposed source of the all-Legendary family's higher
ceiling. Pure-upside identities are deferred for an explicit later owner
choice only if the commitment rule repeatedly fails the fun playtest gate.

## Solver finding

RL should eventually be part of the balance harness once the simulator and
legal information/configuration boundaries are executable, but one trained
agent or best checkpoint cannot establish the policy surface. Use exact
enumeration, dynamic programming, or planning for tractable slices; multiple
seeded learning/search populations and approximate best responses elsewhere;
and held-out semantic cells/seeds, cross-policy matrices, coverage diagnostics,
and uncertainty intervals for evaluation. Report the omniscient per-cell
portfolio only as a diagnostic upper bound. RL discovers strong policies and
counterexamples; it does not define the metric, prove global optimality, or
replace human fun/readability/learnability testing.

Primary precedents linked in the packet are PSRO, OpenSpiel, AlphaStar's
league/exploiter system, and Agarwal et al.'s reliable RL evaluation work.

## Verification and remaining work

`git diff --check` passed after this handoff. The authoritative decision record
has no diff. No runtime code, tests, vanilla evidence, licensed assets,
installation, save, snapshot, capture, candidate, fixture, observation,
manifest, or golden changed. Tests were not run for this docs-only owner round.
No commit or push was made.

After R2.1 and R2.2 direction, recompute the frontier rather than skipping to a
full replacement. Exact semantic-cell grid, distribution weights, per-format
safeguards, numerical margins/ties, solver coverage, unresolved-cell treatment,
release/migration authority, and final normative replay remain open. The
authoritative decision record stays untouched until explicit owner acceptance;
implementation remains blocked.
