# Handoff — every represented copy has a lived foil; complete carrier next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-21 02:28:23
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.127: whether no locked name, some but not all
locked names, or every locked name has at least one represented persistent
artifact whose complete key set is covered by lived aligned edges. The owner
selected C3c.126-C. Preserve accepted EP-D02 wording until a complete
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the
current full-assurance pass before the separate standalone expansion, audit,
and adaptation pass. Vanilla evidence and the byte-identical oracle remain in
their own lane.

## Selected direction — EP-D02-C3c.126-C

Every persistent instance represented by completed locked keys contains at
least one internal aligned edge that the same owner and artifact materially
realize in one canonical nonforked fixed-version legal history:

`Ids^{hist-X}_v(d)=Ids_v(d)` for every locked definition `d`, and

`D^{all-Id-hist-X}_{locked,v}=D^{U=}_{locked,v}`.

This closes C125-C's showcase-copy loophole: the copy acquired cannot decide
whether the player ever experiences the name's lived relationship. It does not
cover every form. One token dyad can certify a larger fiber. Tight floors
remain nine definitions, twenty-three exact keys, and 138 Soul/key rows at six
Souls, with as few as four represented locked IDs and four lived aligned
edges. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree remains
six. Each lived edge is partial at the same-owner/same-artifact/across-history
boundary; universal instance incidence is aggregate. The authoritative
decision record remains unchanged.

## Active owner choice — EP-D02-C3c.127

For represented persistent ID `r` of locked definition `d`, define the keys
covered by lived aligned edges:

`K^{hist-X}_v(d,r)`

`  ={b in B_{v,r}(d)`

`      : there exists b' in B_{v,r}(d)`

`        with {b,b'} in X^{hist}_v(d)}`.

Define complete represented carriers and names containing one by

`Ids^{K-hist-X}_v(d)`

`  ={r in Ids_v(d):K^{hist-X}_v(d,r)=B_{v,r}(d)}`

and

`D^{exists-IdK-hist-X}_{locked,v}`

`  ={d in D^{U=}_{locked,v}:Ids^{K-hist-X}_v(d) is nonempty}`.

C126-C makes `K^{hist-X}_v(d,r)` nonempty for every represented artifact; it
does not make any fiber complete.

### A — no name has a lived-key-complete artifact

`D^{exists-IdK-hist-X}_{locked,v}=empty`. Every artifact has at least one
lived dyad but also at least one completed form whose aligned partners are
counterfactual-only in that artifact's lineage. This is the strongest
principled countercase: every simulacrum retains an alternate destiny it never
lives. It also mandates a stranded form in every artifact and forbids a clean
full-form teaching carrier for every name.

### B — complete-carrier and wholly incomplete names coexist

`D^{exists-IdK-hist-X}_{locked,v}` is a nonempty proper subset of the locked
domain. At least one name has a complete carrier; another name's every
represented artifact is positive but incomplete. This supports fully morphic
and fate-fractured names, but adds a likely premium-name taxonomy.

### C — every name has a lived-key-complete artifact — recommended

`D^{exists-IdK-hist-X}_{locked,v}=D^{U=}_{locked,v}`. Every name has at least
one represented artifact for which every completed form participates in a
lived aligned edge. Other represented artifacts of that name may remain
incomplete. This gives each name one coherent lived-form carrier without yet
flattening all same-name copies into identical coverage obligations.

## Concrete example and tight floors

```text
ONE FOUR-FORM S-O ARTIFACT

R17={b0,b1,b2,b3}
static aligned matching: b0--b3 and b1--b2

INCOMPLETE: one lawful R17 history contains b0 ... b3;
            b1--b2 remains split across mutually exclusive histories.

COMPLETE:   b0--b3 and b1--b2 each have a qualifying history.
            Those histories need not be the same history.
```

| Branch | Definitions | Keys | Rows at six Souls | Represented locked IDs | Minimum lived edges |
| --- | ---: | ---: | ---: | ---: | ---: |
| A | 9 | 23 | 138 | 4 | 4 |
| B | 9 | 23 | 138 | 4 | 5 |
| C | 9 | 23 | 138 | 4 | 8 |

C124-A requires four four-key locked names at the tight floor. Give each one
represented ID whose pure aligned graph is a two-edge matching. A negative
name needs one lived edge to satisfy C126-C while leaving two keys uncovered;
a positive name needs both matching edges. Thus A needs four, tight B needs
five, and C needs eight lived edges. Splitting into two two-key fibers does not
reduce the positive-name cost because C126-C requires one lived edge in each
fiber.

A positive-but-incomplete fiber can have three keys; do not reuse C120's
four-key static-alignment lower bound for this temporal subgraph. A strict
future separator between C127-C and all-fiber coverage can use a complete
two-key carrier plus a positive-but-incomplete three-key fiber.

Every lived edge is **partial** at the fixed-coordinate and same-owner/same-
artifact/across-history boundaries. A lived-uncovered key is **neutral/
protective** only at lived incidence; its static aligned edge remains partial.
Fiber completeness and name-level prevalence are **aggregate**.

C's remaining exploits are real: a tiny two-form carrier can certify a name,
and distinct covering edges may rely on mutually incompatible histories. C127
selects no all-fiber/global-key coverage, one common history, direct
transition, direction, reversibility, cause, agency, exact state/boundary
semantics, frequency, value, power, release, or implementation.

## Frontier discipline

Do not jump past the selected static ladder's corresponding order:

1. C125 definition-level lived-edge existence — selected C.
2. C126 represented-instance lived-edge incidence — selected C.
3. C127 definition-level complete-carrier existence — active.
4. If C, ask whether every represented fiber is lived-key-complete,
   equivalently whether every completed locked key has a lived aligned partner.

Exact state/boundary semantics are still prerequisite to claims about causal
transition or player agency. Incidence alone is well-defined without them.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling: follow direct `AGENTS.md` rules
and do not treat the overnight plan as current guidance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C127 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
