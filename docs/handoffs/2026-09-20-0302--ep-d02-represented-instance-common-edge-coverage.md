# Handoff — every name has a same-ID common edge; represented-fiber coverage next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-20 03:02:20
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.115: whether no, some but not all, or every union-
locked Soul Relic definition gives all of its represented persistent-instance
fibers at least one internal all-Soul-changing common edge. The owner selected
C3c.114-C. Preserve the accepted EP-D02 wording until a complete replacement
is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the current
full-assurance pass before the separate standalone expansion/audit/adaptation
pass. Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.114-C

For every locked definition `d`,

`W^{S,Id}_v(d)` is nonempty,

and therefore

`D^{Id-W}_{locked,v}=D^{U=}_{locked,v}`.

At least one represented persistent instance ID per locked definition occurs
on two completed snapshots for which every supported Soul's assigned noncore
map differs. This covers neither every represented ID nor every key. The edge
may be state-only, boundary-only, or diagonal and overlap-bearing or member-
disjoint. It establishes no cause, transition, common access, or control.

Tight floors remain seven definitions, fifteen exact keys, and ninety six-
Soul rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree
remains six. Each qualifying edge is partial at the represented-persistent-
artifact/across-snapshot boundary, while universal definition prevalence is
aggregate. The authoritative decision record remains unchanged.

## Frontier-order correction and independent checks

The first internal next-card draft moved directly to whether a common edge also
expresses the definition's sole supported one-coordinate type. Read-only
frontier review rejected that jump: C114-C guarantees only one positive
represented fiber per definition, leaving another fiber potentially negative.
Following C108/C109's existence-before-fiber-coverage order, represented-fiber
coverage is the coarser prerequisite; typed alignment remains later.

Independent construction and gameplay checks confirmed:

- empty, proper, and full definition prevalence are all feasible;
- every branch retains tight floors `7/15/90` and the same Soul roster/degree;
- C is recommended for systematic participation across represented fibers;
- A is the strongest principled countercase, while B is the variety compromise.

## Active owner choice — EP-D02-C3c.115

For locked definition `d`, define

`Ids^W_v(d)={r in Ids_v(d) : there exist {b,b'} in W^S_v(d) with inst_v(b)=inst_v(b')=r}`

and

`D^{all-IdW}_{locked,v}={d in D^{U=}_{locked,v} : Ids^W_v(d)=Ids_v(d)}`.

C114-C makes `Ids^W_v(d)` nonempty for every locked definition. An ID outside
that set is a common-edge-negative represented instance: every pair in its
completed fiber retains at least one Soul's map, though the retained Soul may
differ by pair. “Positive” below means only edge incidence, not value or power.

### A — every locked definition is fiber-hybrid

`D^{all-IdW}_{locked,v}` is empty. Every name has at least one positive fiber
from C114-C and at least one negative fiber. This is the strongest principled
countercase if artifact individuality should be affirmative and universal:
every name contains both a common-edge-bearing represented incarnation and a
pairwise witness-retaining incarnation.

Costs are at least two represented IDs per name, systematic same-name instance
taxonomy, and a structural incidence lottery because the name-level promise
does not reach every represented artifact.

### B — fully covered and fiber-hybrid definitions coexist

`D^{all-IdW}_{locked,v}` is nonempty and proper. At least one name gives every
represented ID an internal common edge, while another has both positive and
negative fibers. This can create deliberate variety if the two languages are
disclosed, accessible, and non-dominated, but adds another taxonomy across
C113-A's state/boundary split and risks premium names or instances.

### C — every represented instance fiber is common-edge-positive — recommended

`D^{all-IdW}_{locked,v}=D^{U=}_{locked,v}`. Every represented persistent ID in
every locked definition has at least one internal completed pair for which
every supported Soul's assigned noncore map differs. C closes C114's token-
fiber and represented-instance incidence-lottery loopholes without requiring
every key or pair to participate.

Costs are universal per-fiber authoring and proof burden, possible artificial
foil pairs or homogenization, and a residual one-obscure-edge-per-fiber
loophole. C establishes no player-facing value.

```text
C105 FOUR-KEY WITNESS

FIBER-HYBRID
R17={b0,b1}   overlap-bearing common edge   <- positive
R44={b2,b3}   not all-Soul-changing         <- negative; T retains acd

FULL, MEMBER-DISJOINT
R17={b0,b2}   member-disjoint common edge
R44={b1,b3}   member-disjoint common edge

FULL, OVERLAP-BEARING
R17={b0,b3}   overlap-bearing common edge
R44={b1,b2}   overlap-bearing common edge
```

Coordinate decoration can make both dyads state-only in a state-supporting
definition or boundary-only in a boundary-supporting definition. These are
static completed snapshots only; instance assignment creates no path or
access.

Every hybrid definition needs at least two represented fibers. C111-C gives
each fiber at least two keys, so any hybrid definition is forced to `[2,2]` at
the four-key floor. A forces both floor definitions to use it; B forces its
hybrid member to use it.

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |

At one fixed exact-key/Soul relationship, inherited fit remains **direct**.
Each actual same-ID common edge is **partial** at the represented-persistent-
artifact/across-snapshot boundary. A negative fiber is **neutral/protective**
at the internal whole-roster-change projection. Fiber coverage and definition
prevalence are **aggregate**. C is recommended for systematic participation,
not for a stronger direct doctrinal rating.

C115 selects no per-key same-ID common-edge coverage within a positive fiber,
every-pair coverage, typed/one-coordinate alignment, overlap/member-disjoint
identity, fiber count or topology, coordinate semantics, cause, transition,
path, access, disclosure, agency, value, power, persistence, release, or
implementation. Exact fiber count and size topology beyond the branch-derived
hybrid minima remain open.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C115 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
