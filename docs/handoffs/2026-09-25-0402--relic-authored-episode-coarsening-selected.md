# Handoff — universal authored-episode coarsening selected

**Session ID:** `1b7efff1-0561-4822-a925-84a0382490b9`
**UTC:** 2026-09-25 04:02
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`; present only `RCS-03C3B2A2B1` next.

## Owner answer recorded

The owner's latest `B` selected `RCS-03C3B2A2A-B`.
`M^{merge}_v=F^{xop}_v`, with at least one reachable functional witness:
every qualifying prospectively authored finite source episode opens one
canonical episode root, and every separately accepted operation it actually
realizes joins that root. The operations retain their primitive histories,
payments, outcomes, semantic applications, and complete ancestry, but cannot
occupy independent-source positions together.

The selected policy remains whole-form and whole-occurrence. Membership is
finite, disclosed, canonical, exclusive, source-owned, and fixed no later than
first-member commitment. Adjacency, causation, shared records, or a Relic match
never establishes membership. Unperformed members add no lineage; terminal or
abort re-initiation opens a fresh identity; later membership never rolls back
a committed result; and coarsening is replacement/equivalence rather than an
additive third root.

Plant Sigil prospectively opens `r_Rupture`; its later Detonate joins that
root. Both keep their costs and histories, but cannot fill two independent
relationship positions. An unrelated Guard root may still pair with
`r_Rupture`. The distinct operations and their authored episode participate
**directly** in one root identity while remaining mechanically different;
universal prevalence is **aggregate**. This prevents lifecycle root farming,
at the cost of internal cross-source eligibility for every qualifying episode.

B2A2A-B decides no additive relation root, current-state root, build root,
addressability, claim/reuse, payoff, persistence, release, or implementation
policy. The authoritative decision record remains unchanged and implementation
remains blocked.

## Twelfth prerequisite amendment

The post-answer screen proved that B2A2A-B determines neither absence nor
presence of an eligible nonmerged multi-root H order/trajectory language.

- Empty countermodel: required H positions use unary occurrence claims under
  unordered conjunction; width-two/three phase packages remain within single
  roots; Plant–Detonate supplies the required coarsened episode witness; no
  canonical nonmerged multi-root temporal claim exists.
- Nonempty countermodel: preserve that world and add canonical
  `Guard-before-Heat` across two separately rooted operations outside every
  prospectively authored source episode.

Both satisfy every selected predecessor, but they differ in timing, denial,
coordination, and counterplay. Silently opening promotion would invent the
second content family; silently pruning it would exclude that family.

Old `RCS-03C3B2A2B` is now a non-counting parent for counted
`RCS-03C3B2A2B1` (empty versus nonempty eligible support) and conditional
`RCS-03C3B2A2B2` (additive multi-parent relation-root promotion). Selecting
B2A2A-B reduced `Phi_SR` from 31 to 30; replacing one counted row with two then
added one potential row. The 55-row register contains 30 `SCREEN`, 1
`OWNER-OPEN`, 7 `PRUNED`, 14 `DIR-SELECTED`, 1 `DERIVED`, 1 `SPEC`, and 1
`EVALUATE`; `Phi_SR = 31`. Only B2A2B1 is active.

## Next card

`RCS-03C3B2A2B1` asks whether the completed catalog supports any canonical H
order/trajectory form whose inclusion-minimal indispensable operands retain at
least two distinct roots after A1-A and A2A-B. A qualifying form must be
reachable, materially effective, non-preempted, and genuinely temporal. Its
operands are inclusion-minimal, and a separate reachable matched separator
must hold the operand multiset, source roles, post-coarsening lineage topology,
and every non-temporal input fixed; reversing order or substituting a
non-equivalent same-endpoint trajectory must flip the claim. Raw adjacency,
co-occurrence, shared records/views, aliases, packets, same-root episode
members, and post-hoc Relic matching do not qualify. Every form retains all
indispensable operand lineages.

Present only:

- A: `E^{rel}_v` is empty; exclude all such nonmerged multi-root temporal
  forms; or
- B: `E^{rel}_v` is nonempty; require at least one such functional form.

Recommend B. At the predicate/relationship boundary, distinct source roots
retain their identities while participating **directly** in one irreducible
temporal claim. This gives the cross-source weaver a lived relationship rather
than only unordered co-presence and is the stronger Achintya Bheda Abheda fit.
Costs are order/freshness disclosure, logs and replay burden, coordination
pressure, denial-window edge cases, and formulaic sequence-recipe risk. B is a
support floor only; exact operators, forms, counts, distribution, and
prevalence remain AUTHOR/SPEC.

Example: Guard carries `r_G` and a later ally Heat carries unrelated `r_H`.
Neither belongs to one prospectively authored episode. “Guard completed before
Heat landed within the valid horizon” qualifies only if a reachable matched
Heat-before-Guard history holds operands, lineage topology, and non-temporal
inputs fixed but defeats the claim. It retains both roots. B2A2B1 does not
decide whether it also owns `r_G-before-H`; B2A2B2 asks that only if B is
selected.

Under A, B2A2B2 prunes and B2A3 follows. Under B, B2A2B2 becomes next. A/B are
exhaustive because the eligible support set is empty or nonempty; exact
prevalence is catalog authoring, not a third global support direction.

## Validation

- Mechanical parsing confirms 55 rows: 30 `SCREEN`, 1 `OWNER-OPEN`, 7
  `PRUNED`, 14 `DIR-SELECTED`, 1 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`.
  `Phi_SR = 31`, and only `RCS-03C3B2A2B1` is `OWNER-OPEN`.
- Independent countermodel reviews agree that selected predecessors force
  neither empty nor nonempty temporal support; the twelfth amendment restores
  the missing player-material boundary rather than guessing it.
- `git diff --check` passes.
- `node --test --test-concurrency=1` passes: 584 tests, 583 passed, 0 failed,
  and 1 expected raw-trace archive skip.
- The authoritative decision record remains byte-identical at SHA-256
  `e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
- Design commit `fe33018` is pushed and synchronized with
  `github/design/endless-progression-owner-packet`.
- PR #3 is **not merge-eligible**: after fetching main `a89704c`, GitHub
  reports `DIRTY`/`CONFLICTING`, and neither branch tip is the other's
  ancestor. A later deliberate integration must preserve both current
  `HANDOFF.md` lanes rather than resolving the overlap wholesale to one side;
  only a human may merge.
- No runtime, Ruffle, capture, installation, save, snapshot, fixture,
  candidate, observation, manifest, or golden work is authorized.
