---
handoff:      2026-09-26-0239--relic-semantic-child-addressability-selected
written:      2026-09-26 02:39 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    none this round; optional subagent route remained unavailable before work, so no failed run is counted as review
branch:       design/endless-progression-owner-packet
commits:      d458f15..HEAD
suite:        3,642 tests / 3,627 passed / 0 failed / 15 expected fresh-tree skips; register 59/59 and Phi_SR 25; decision-record hash unchanged
supersedes:   2026-09-25-2249--relic-exact-provenance-build-identity-selected
---

# Handoff — semantic-child Relic addressability selected

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`. The sole active Relic frontier is
`RCS-03C3B4B`; present that one card only.

## Where things stand

The owner selected `RCS-03C3B3A-B`. Semantic child applications alone may
furnish Relic source positions. Parent roots and exact-`Pi` classes remain
operative lineage/grouping identities but are not addressable atoms. B3B is
pruned. Every child retains complete parent/class/A4-basis proof lineage, so
siblings and intersecting proofs remain non-independent for one result.

The B4 prerequisite recheck initially appeared to expose a plural-site support
choice, but upstream selections already force it positive. B2B1-B supplies two
semantically distinct equal-`Pi` current occurrences, B2B2-B gives them one
class parent, and B3A-B retains their manifestations as the addressable layer.
`RCS-03C3B4A` therefore closes `DERIVED`; grouping those sites is B4B, not a
way to erase their primitive distinction.

The sixteenth-amendment register has 59 slots: 24 `SCREEN`, 1 `OWNER-OPEN`, 8
`PRUNED`, 22 `DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`;
`Phi_SR = 25`. The authoritative decision record remains unchanged because a
bare option label selects direction but does not complete acceptance.

## Highest-value next work

Present B4B's three final child-family topologies:

- A — semantic leaves only. Every meaningful primitive target/effect/
  application site is its own child atom.
- B — one nonoverlapping scale with authored compounds. At least one functional
  child groups several sites, but grouped members are not simultaneously
  addressable as descendants.
- C — a nested addressable child hierarchy. At least one compound and a proper
  descendant are both addressable at one cut, under one laminar acyclic family.

Recommend B. It lets signature sequences and genuinely unified multi-target
acts remain memorable recipe nouns without restoring parent-root
addressability or introducing broad-versus-precise scale shopping. A is the
cleanest precision rule but risks target/stage evidence-token noise. C is
expressively rich and has a strong direct ideal fit, but adds a second child
scale, candidate volume, scale-shopping, explanation ambiguity, and harder
claims/reuse/fan-out/UI semantics.

Use Twinstrike. Under A, `Opening Cut` and `Returning Cut` are separate child
atoms. Under B, they may prospectively form one `Completed Twinstrike` child,
with neither cut separately addressable at that cut. Under C, both `Completed
Twinstrike` and its `Returning Cut` descendant may be addressable, but complete
shared lineage prevents them from independently filling one result.

## Hard rules

- Primitive sites are authored player-meaningful manifestations, never fields,
  packets, callbacks, storage writes, labels, animation markers, or resolver
  microsteps.
- The final family is laminar: blocks are disjoint or one contains the other.
  Crossing selectors are recipe predicates, not additional canonical atoms.
- Family keys are prospective, deterministic, versioned, and source-owned.
  No Relic, recipe, result, payoff, claim, interruption, or favorable outcome
  may choose a grouping.
- Incomplete compounds furnish nothing. Same-cut completion settles before the
  coherent read; later sites cannot rewrite old assignments or settled results.
- Only C admits compound/descendant overlap, and complete shared lineage still
  blocks independent positions. No option restores parent addressability.
- B4B grants no fan-out, claims, reuse, payoff, UI realization, persistence,
  release, or implementation. C3C-C3F and RCS-08 retain those owners.
- Do not edit the authoritative decision record without an explicit
  `accepted`, `rejected`, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Verification and Git

The machine interruption occurred before any repository write. Recovery checks
found a clean worktree at `d458f15`, local and remote tips equal, no merge,
rebase, cherry-pick, lockfile, corrupt object, or missing object. `git fsck`
reported only ordinary unreachable historical objects.

The frozen register reparses to the exact 59-row distribution above and
`Phi_SR = 25`. The authoritative decision record retains SHA-256
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
`git diff --check` passes. The full serial suite passes with 3,642 tests: 3,627
passed, 0 failed, and 15 expected fresh-tree skips. Commit this round
atomically, push the feature branch, verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
