# Handoff — one fixed-union family-support class selected; identity next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-17 20:10:52
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.90: whether the sole forced-breadth class with
structural support for changing a complete noncore singleton-map family at one
fixed exact union is the fixed-width-capable class or the union-locked class.
The owner selected C3c.89-B. Preserve the accepted EP-D02 wording until a
complete replacement is replayed and explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate standalone-game expansion, audit, and adaptation
pass. Vanilla evidence and the byte-identical measurement oracle remain in
their SS2 lane.

## Selected direction

EP-D02-C3c.89-B fixes `|K^{M|U}_v|=1`. Exactly one of the fixed-width-capable
and union-locked forced-breadth classes contains at least one definition for
which some exact noncore union `U` admits two unequal legal complete map
families. Every definition in the other class is support-negative. Breathing
support remains independently derived and outside this residual class count.

C89-B selects structural capacity only. It does not identify the class,
definition, exact family, Soul-block assignment, predicate, actual pair, or
behavior. Tight floors remain six definitions, eight exact keys, and forty-
eight six-Soul rows. Supported-Soul counts remain
`{6,8,9,10,12,14,15}`. Fixed-only forces degree six; locked-only has a tight
degree-five witness but does not prohibit degree six elsewhere. Fixed-key fit
is direct, support neutral, the one-class allocation aggregate, and a later
actual same-definition/same-`U` unequal-family relation partial. The
authoritative decision record remains unchanged.

## Active owner choice — EP-D02-C3c.90

### A — fixed-width-capable only

`K^{M|U}_v={fixed}`. At least one fixed-width-capable definition is support-
positive; every union-locked definition has one legal complete family at most
for each exact `U`.

The strongest later realization would give capable-but-nonrotating Slate a
distinct job: Tide changes breadth, Ashen changes exact union, Slate retains
one exact union but may reweave its map family, and Veil remains fully anchored.
C90-A would not select Slate, however; the unique positive fixed tuple
`(q,k,rho)=(6,3,2)` could instead occur in a rotating Ashen-like definition.
It forces degree six, leaves one ambient noncore motif unused at its witness
`U`, and creates a later rotating/nonrotating subclass-participation choice.

### B — union-locked only — recommended

`K^{M|U}_v={locked}`. At least one union-locked definition is support-positive;
every fixed-width-capable definition has one legal complete family at most for
each exact `U`. Ashen may still rotate exact vocabulary under C86-A/C87-A.

The tight `(5,3,2)` witness keeps `U={a,b,c,d}` while legally supporting
`{{a,b},{c,d}}` and `{{a,c},{b,d}}`. This distributes the new structural axis
to the class that cannot vary exact union and opens a later lane in which a
Veil-like definition could keep one vocabulary while changing internal motif
coexistence. Its costs are leaving Slate's capability latent, softening the
“fixed covenant” metaphor, and becoming bookkeeping unless later actual
behavior, disclosure, predicates, agency, and non-dominance make it real.

Do not summarize B as “Veil reweaves.” C90 would not identify Veil, author a
second family as a key, or make the difference visible or material. It selects
support only.

A/B are exhaustive because C89-B makes `K` a singleton subset of
`{fixed,locked}`. Neither/both would reopen C89. Both options retain `6/8/48`,
the supported-Soul set, and add no exact key. Support is neutral, class identity
aggregate, fixed-key fit direct, and a future actual pair partial. B is
recommended for orthogonal class roles, lower structural complexity, and
thematic promise—not because latent locked support already realizes the ideal.

After A, ask whether exactly one or both of C87-A's nonempty rotating and
capable-but-nonrotating fixed subclasses participate; subtype identity and
prevalence follow separately. After B, ask whether locked support is nonempty
proper or universal. Actual fixed-union family behavior remains later.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C89-B
as selected and C90 as the sole active owner choice. The authoritative decision
record is unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04
directions remain unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain
selected, and R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Three bounded read-only checks
verified C90's exhaustive identity set, branch-dependent frontier, tight
construction floors, tuple constraints, ideal ratings, countercase, and narrow
B recommendation. The authoritative record was not edited.

Validation passed: tracked and current-untracked whitespace checks; the sole-
active-heading assertion; cross-document C89-B/C90 state assertions; stale-
state scans; authoritative-record no-diff; SVG XML parsing; Unicode format-
control scanning; exact floor-table assertions; and newest-handoff ordering.
Tests were not run because this was a documentation-only design round.
