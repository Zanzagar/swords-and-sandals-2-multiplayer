# Handoff — stable affinity cores selected; morphic repeat support next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-17 01:28:37
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.81: whether no, some but not all, or every role-
variable mixed (“morphic”) affinity definition has repeated-size support for a
later whole-role-map-preserving partner regrouping. The owner selected
C3c.80-A. Preserve the accepted EP-D02 wording until a complete replacement is
replayed and explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle remain in their SS2 lane.

## Selected direction

`D^kappa_{var,v}` is empty. Every affinity definition keeps one sole
singleton-backed temporal-by-basis cell label shared by every supported Soul
across all exact operative keys in one completed version. Partner-fluid equal
names may regroup Souls and morphic names may recast size roles around that
stable semantic heart.

Every fixed key retains inherited direct ideal fit. C80's marginal fit is
neutral/protective, and the cross-simulacrum continuity-with-difference analogy
on already-variable names is partial because the alternate forms remain
separate exact keys. C80-A retains supported-Soul counts
`{6,8,9,10,12,14,15}`. Tight local floors remain five definitions, seven keys,
and forty-two six-Soul rows. Affinity degree three and the branch-minimum prism
degree five remain tight. C39 still requires distinct instances and a separate
material shared-cell predicate difference; stable core identity alone is not
evidence. C80 selects no exact core label, noncore vocabulary, complete maps,
assignment, cause, transformation, disclosure, agency, power, or
implementation and changes no authoritative wording.

## Frontier audit

Let C77-B's nonempty morphic domain be

`D^morph_v=D^mu_{var,v}`.

For `d` in that domain, let `m_{r,v}(d)` count blocks of size `r` in its
C74-stable profile, and define

`D^{repeat-morph}_v={d in D^morph_v : exists r>=2 with m_{r,v}(d)>=2}`.

The relevant later comparison preserves the **entire** named-Soul role map:

`mu_v(b)=mu_v(b')` and `Pi_v(b)!=Pi_v(b')`.

One Soul retaining its size is insufficient. `FSVW|TR -> SVWT|FR` leaves `S`
in a size-four group but changes the whole role map because `F` and `T`
exchange roles.

For a profile with multiplicities `m_r`, complete role maps number

`|S_v|!/product_r((r*m_r)!)`,

and partitions compatible with one fixed complete role map number

`product_r((r*m_r)!/((r!)^m_r*m_r!))`.

The first count exceeds one for every mixed profile. The second exceeds one
exactly when some size repeats. Repeated size is therefore the exact support
condition for a later whole-role-map-preserving partition change. C81 selects
support only; it realizes no additional exact key.

Block-to-map assignment does not precede C81. It first needs separate treatment
of realized noncore-label breadth, exact label-set identity, and noncore map-
family identity. Instance/state/evaluation-boundary causation is later still.

## Active frontier

- **A, recommended — no morphic definition repeats a size:** every morphic
  whole role map uniquely fixes its partition. Equal-size names own pure
  partner fluidity, stable mixed names remain fixed covenants, and morphic
  names own visible size-role recasting. C81 is neutral/protective; fixed-key
  fit remains direct and existing cross-key role variation partial. The cost is
  excluding braided whole-role-map-preserving partner rewiring inside morphic
  names.
- **B — support-positive and support-negative morphic names coexist:** at least
  one morphic profile repeats a size and another uses every size once. This
  permits simple and structurally braided morphs, but forces another definition
  and a hidden subtype before any actual partner behavior is required. C81
  remains neutral and the catalog coexistence is aggregate.
- **C — every morphic definition repeats a size:** every morphic name can later
  support a fixed-whole-role-map partition change. This avoids B's subtype split
  but makes role information insufficient to identify partners for every
  morphic name, raises the smallest roster and affinity degree, and still adds
  no gameplay without a later actual-variation rule. The mandate is aggregate.

Eight-Soul boundary example:

```text
no-repeat Gale (6,2)
N1: FSVWTR | KM
N2: SVWTRK | FM       whole role map changes; fixed map has one partition

repeat-support Ashen (4,2,2)
R1: FSVW | TR | KM
R2: SVWT | FR | KM    whole role map changes; C77 witness
R1*: FSVW | TK | RM   same whole role map as R1, different partition
```

`R1*` is compatible but not required by C81. A definition realizing both C77
role variation and the later fixed-whole-role-map partner variation needs at
least three keys.

Tight support-only consequences are:

| Choice | Supported-Soul counts | Definitions | Exact keys | Minimum rows |
| --- | --- | ---: | ---: | ---: |
| A | `{6,8,9,10,12,14,15}` | 5 | 7 | 42 at six Souls |
| B | `{8,9,10,12,14,15}` | 6 | 9 | 72 at eight Souls |
| C | `{8,9,10,12,14,15}` | 5 | 7 | 56 at eight Souls |

Six Souls have no repeated mixed profile. The first locally eligible profile
is seven-Soul `(3,2,2)`, but prime seven cannot also support the equal-size
affinity required by C76-B. B/C first become complete at eight Souls through a
profile such as `(4,2,2)` and require a degree-four affinity. A remains tight
at degree three; the branch-minimum coreless prism remains degree five.

The base is Dreamglass prism, Echo one-only, two-key Tide, two-key Ashen, and
one-key Veil. B adds a second two-key morphic name on the other support side.
The same totals carry C39 only if a required pair also satisfies its distinct-
instance and separate-predicate conditions.

C81 selects no actual fixed-whole-role-map partner variation, exact profile or
partners, block assignment, noncore map vocabulary, variation cause,
transformation, coexistence, live switching, predicates, disclosure, agency,
sidegrade quality, power, acquisition, persistence, release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C80-A
as selected and C81 as the sole active owner choice. The authoritative decision
record is unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04
directions remain unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain
selected, and R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Three read-only audits
independently checked frontier order, mathematical feasibility/floors, and the
gameplay/thematic recommendation; follow-up audits of the actual diff found no
material discrepancy. `git diff --check`, untracked-file whitespace checks,
the sole-active-heading assertion, cross-document C80/C81 state assertions,
the authoritative-record no-diff check, SVG XML parsing, Unicode-format-control
scanning, and Node profile/construction/floor assertions all passed. The
repository test suite was not run because this was a documentation-only
decision round.
