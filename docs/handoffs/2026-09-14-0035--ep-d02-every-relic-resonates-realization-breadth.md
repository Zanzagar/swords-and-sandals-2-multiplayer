# Handoff — every Relic resonates; realization breadth next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-14 00:35:23
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.43, material Bound-Soul participation across
operative Relic instance-state realizations in `E+`. The owner selected
C3c.42-A. Preserve the accepted EP-D02 wording until a complete replacement is
replayed and explicitly accepted. Do not implement.

## Selected direction

Reuse nonempty `D+`, the supported Soul Relic definitions participating
materially in the six-cell `E+` input domain. C3c.42-A sets `D_S=D+`: every
supported definition must have at least one exact instance/state and fixed
Charm context where two supported Bound Souls produce different cell-tagged
extensional final-input portfolios solely because Bound Soul identity differs.

The comparison fixes every non-Soul fact and uses a nonempty common
reachability envelope `Ω` of identical canonical source/history/state
valuations. Both portfolios are built from legally composed predicates before
Soul-local reachability filtering. The distinguishing behavior's material,
bearer-compliant cell witness must occur inside `Ω`, so a Bound Soul's
independent combat behavior cannot fake resonance by changing reachability.

This derives at least two supported Bound Soul identities. It does not require
every Relic instance, Soul pair, Charm context, entry, or cell to differ, set a
power advantage, or choose an exact recipe. Every named Soul Relic nevertheless
makes Soul identity mechanically real somewhere in the six-cell core. This is
a direction rather than a complete accepted EP-D02 replacement and changes no
authoritative wording.

## Corrected frontier result

The first C3c.43 draft asked whether one exact hierarchy exposes one or several
simultaneous final input channels. Audit found that several internal result
slots can collapse to one material aggregate such as `P or Q`. Making the
slots materially distinct required the draft to silently select
player-disclosed recipe identities and separately observable progress/results.
That communication/consumer rule has not received its own owner choice, so the
multiplicity draft was withdrawn before owner presentation and selected
nothing. Return to multiplicity only after that prerequisite is resolved.

The prerequisite-ready frontier instead asks how broadly C3c.42-A's
definition-level promise applies across materially usable versions of each
Relic. Base-grammar ownership also remains deferred: a Relic-owned base plus an
unrestricted resonance transform and a pair-owned grammar can encode the same
final behavior. Any later ownership choice must carry a behavioral or durable-
lineage invariant rather than a storage label.

## Sole active choice

For each `d` in `D+`, nonempty `R_d+` contains every legal, reachable,
versioned, `E+`-operative realization of `d`. A realization fixes the exact
Relic instance, every input-affecting state fact, and one grammar-evaluation
boundary later authorized by configuration/evolution rules. It belongs only
when active at that boundary with a nonempty materially functional,
bearer-compliant `E+` portfolio under some supported Soul and legal fixed input-
Charm-or-empty context. Inactive inventory records, pending or invalid states,
unreachable records, and wholly `E+`-dormant realizations are excluded.

Let `S_d` contain each realization with a complete C3c.42 witness while that
realization, boundary, Charm context, version, and every non-Soul fact remain
fixed. C3c.42-A guarantees nonempty `S_d` for every definition. A realization
outside `S_d` is Soul-invariant only if at least one supported Soul pair and
legal fixed input-Charm-or-empty context admit a nonempty common envelope with
a material `E+` comparison for both worlds, and complete analysis of every
supported pair and context finds no material difference over any valid common
envelope. Zero comparable envelopes or incomplete analysis remains unresolved.
Let `U={d | S_d=R_d+}`.

- **A, recommended — every operative realization resonates:** `U=D+`. Every
  materially usable version of every Relic remains genuinely Soul-responsive.
  Different realizations may respond through different Soul pairs, Charms,
  cells, or predicates. This preserves per-copy qualitative variation but
  requires a resonance witness for every permitted operative facet, state, and
  boundary.
- **B — uniformly responsive definitions plus mixed definitions:** `U` is a
  nonempty proper subset of `D+`. Every operative realization of definitions in
  `U` responds; each other definition has both a responsive realization and a
  distinct operative realization proven Soul-invariant. This permits special
  unbound versions but adds a second taxonomy and can strand a combatant with a
  permanent, nontransferable version that ignores Soul.
- **C — every definition mixes both kinds:** `U` is empty. Every supported
  definition needs at least one responsive and one distinct operative Soul-
  invariant realization. This maximizes artifact variation, but every Relic
  name can yield a functional version that ignores Soul and therefore carries
  the preview, comparison, agency, and migration burden.

A/B/C exhaust `U` being all of nonempty `D+`, a nonempty proper subset, or
empty among fully classified legal designs. Zero-comparison or incompletely
analyzed realizations cannot satisfy B/C's invariant witness. B/C require at
least two distinct operative realizations for every mixed definition, but do
not require co-possession or authorize evolution. The card chooses no
precombat/in-combat horizon: if later rules authorize multiple relevant states
or boundaries, each operative tuple is classified separately.

C3c.39-C is orthogonal: it substitutes Relic realizations with Soul/context/
cell fixed, while C3c.43 fixes one realization and substitutes Soul. C's mixed
requirement for C3c.39's stable definition would therefore constrain a later
cross-cell, support, context, state, or evaluation-boundary rule rather than
manufacture a forbidden common fixed-cell predicate difference.

The card does not guarantee every legal record is operative, every Soul pair
differs, Charm-free resonance, equal power, sidegrade viability, or any state
transition. Exact evolution, disclosure, comparison, reroll/re-attunement,
cross-cell footprints, `E0`, payoff behavior, channel multiplicity and its
communication prerequisite, base-grammar ownership, content, acquisition,
horizons, persistence/migration, validation, release, and implementation remain
open.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record
C3c.42-A and identify the corrected C3c.43 as the sole active choice. The
authoritative decision record and normative system design remain unchanged.
EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04 directions remain
unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain selected, and
R10.3-R10.8 remain pending. No code tests were run because this is documentation
and vector work only. No runtime, capture, evidence, installation, save, or
snapshot was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`.

Three narrow read-only audits passed after one caught and repaired a vacuous-
invariance loophole. `git diff --check` passed; no-index whitespace checks for
the three new files reported no errors; the SVG parsed successfully with the
Python standard-library XML parser because `xmllint` is unavailable; and the
authoritative decision and system documents are clean.
