# Handoff — C72 anchor-count stability selected; affinity group count next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-15 21:58:33
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.73: whether no, some but not all, or every affinity
Relic definition may vary the number of singleton-map Soul groups across its
exact operative simulacra. The owner selected C3c.72-A. Preserve the accepted
EP-D02 wording until a complete replacement is replayed and explicitly
accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. If no option remains consistent,
offer or recommend a replacement rather than weaken the north star. Continue
the exact one-at-a-time A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle remain in their SS2 lane.

## Selected direction

`T^k_{var,v}` and `D^k_{var,v}` are empty. Every prism definition and every
affinity definition keeps one Soul-stable singleton-anchor count across all of
its exact operative keys in a completed version; every one-only definition is
already fixed at one. Counts may differ between definitions. Same-definition
exact keys may still change singleton labels, Soul groupings, sole-core labels,
and predicates while preserving that definition's total palette and count.

C72-A retains `3<=|S_v|<=15`, a local three-key floor, and a four-key floor in
a version carrying C39-C's variable-instance witness. It selects no exact
counts or labels, affinity partition, variation cause, transformation,
disclosure, power, or implementation.

## Active frontier

For affinity exact key `b`, define

`s ~_b t iff I_v(b,s)=I_v(b,t)`,

`Pi_v(b)=S_v / ~_b`, and `rho_v(b)=|Pi_v(b)|`.

This partition compares only exact singleton-backed cell-label sets. Souls in
one block may still differ materially in predicates, contexts, receipts, or
power under C43-A. Pair-complete prisms have `rho=|S_v|`; one-only keys have
`rho=1`; only affinities retain `2<=rho<=|S_v|-1`.

For affinity definition `d`, let

`Blocks_v(d)={rho_v(b): def_v(b)=d}`

and let `D^rho_var` contain definitions whose set has at least two values.

- **A, recommended — every affinity definition is group-count-stable:**
  `D^rho_var` is empty. A name keeps one amount of anchor-level Soul
  resolution while exact simulacra may still regroup Souls, change labels or
  the sole core, and differ in predicates. Inherited ideal fit is direct inside
  each fixed single-core affinity/Soul-roster relationship; C73's marginal fit
  is neutral and protective. Cost: a same-name affinity cannot expand or
  contract the number of anchor-map groups.
- **B — stable and variable affinity definitions coexist:** `D^rho_var` is a
  nonempty proper subset of `D_affinity`. Exceptional names may compress or
  split their Soul groups, while at least one other affinity name is stable.
  Marginal ideal fit is partial because the forms are separate simulacra, not
  simultaneous unity and difference. Cost: a second definition taxonomy, five
  exact keys minimum, and a potential higher-resolution premium roll.
- **C — every affinity definition varies group count:**
  `D^rho_var=D_affinity`. Every affinity name requires unequal-`rho` exact
  keys. The marginal fit is merely aggregate across alternate forms. This
  maximizes relational variety but universalizes comparison, authoring,
  disclosure, and migration burdens.

The empty/proper/full options are one homogeneous set-prevalence topology, as
in C63, C65, C69, and C71. They do not repeat C72's rejected bundling of
distinct archetype identities. Block count goes before block-size profile and
exact partition identity by coarse-to-fine discipline. Core-label continuity
is an independent ready sibling rather than a prerequisite.

With Souls `F/S/V/W`, stable palette `{a,d,e,f}`, stable count two, and sole
core `e`:

- `A2`: `F=V={a,e}`, `S=W={d,e}` gives `{F,V}/{S,W}`, so `rho=2`;
- `A2'`: `F=S={a,e}`, `V=W={d,e}` gives `{F,S}/{V,W}`, still `rho=2`;
- `A3`: `F=V={a,e}`, `S={d,e}`, `W={e,f}` gives three blocks.

A permits `A2/A2'`; B or C can use `A2/A3`. A Dreamglass prism using the four
three-of-four subsets of `{a,b,c,d}` and an Echo one-only key with palette
`{e,f}` and singleton `{f}` preserve the inherited prism/one-only witnesses,
coupled-only slack, and all-six-label singleton coverage.

Three Souls force `rho=2`, and degree three also fixes `rho=2`; therefore B/C
require at least four Souls and a variable affinity palette degree of at least
four. They do not tighten the fifteen-Soul ceiling. A/B/C Soul ranges are
`3..15/4..15/4..15`; local exact-key floors are `3/5/4`, C39-witness floors are
`4/5/4`, and minimum witness Soul/key rows are `12/20/16`. A variable pair can
reuse C39 only if its keys are distinct instances and additionally differ
materially in one shared-cell predicate; group-count variation alone is not
C39 evidence.

C73 selects no exact block count, block-size profile, block membership, common
Soul pairs, singleton or core labels, prism maps, variation cause,
transformation, coexistence, live switching, predicates, packages, contexts,
receipts, routes, disclosure, ease, frequency, power, acquisition,
persistence, release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C72-A
and C73 as the sole active owner choice. The authoritative decision record is
unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04 directions remain
unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain selected, and
R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Documentation, construction,
bound, pointer, and SVG validation passed: tracked and untracked whitespace
checks were clean; the authoritative decision record remained unchanged; the
SVG parsed as XML; the C73 A/B/C constructions preserved the inherited
structure and six-label coverage; the `rho<=10`, roster, exact-key, and witness-
row bounds reproduced; C73 was the sole active owner-packet heading; and this
file resolved as the newest handoff. This documentation-only turn did not run
the repository test suite.
