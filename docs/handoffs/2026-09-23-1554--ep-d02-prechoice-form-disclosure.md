# Handoff — One common hinge; pre-choice form disclosure next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-23 15:54:42
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.156: decision-time independent-form disclosure
support. The owner's latest `A` selected C3c.155-A and nothing in C156. This
remains Soul Relic design, specifically the player-information surface of the
selected same-opportunity, one-hinge result-rearm grammar. Preserve accepted
EP-D02 wording until a complete normative replacement is replayed and
explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Do not batch
support, per-tag overlap, history/path participation, opportunity saturation,
chooser authority, and value merely to shorten the ladder. Finish the current
full-assurance pass before the standalone expansion/audit/adaptation pass.

## Selected direction — EP-D02-C3c.155-A

Retain C154-A's common set

`Chi_PR(w,d)=Chi_P(w,d)=Chi_R(w,d)`

and let `K^{multi-chi}_v` contain dual tags with any live joint opportunity
whose common set contains at least two cuts.

The selected A makes `K^{multi-chi}_v` empty. Every live joint opportunity on
every legal history of every dual tag has a singleton `Chi_PR(w,d)`.
Equivalently, every complete `Pi_P(w,d) x Pi_R(w,d)` combination maps to the
same exact actual-anchored canonical decisive cut at that opportunity.
Different opportunities and histories may use different singleton cuts.

At the one hinge, continuing relationship context and every independently
predicate- and recurrence-isolating alternative coexist **directly** without
becoming one effect. Each isolation remains **direct**; downstream effects and
the larger recurrence remain **partial**; excluding multicut scheduling is
**neutral/protective**; and universal opportunity/path/tag singleton coverage
is **aggregate**.

C155-A adds no tag, history, opportunity, pair, continuation, cut, receipt-
width, menu, or visible-button floor. The tight candidate catalog remains one
dual tag plus the two opposite-polarity specialists. Every fixed catalog and
width floor remains unchanged.

## Why player information is next

C155-A completes the current evaluator-topology ladder but does not make that
topology knowable to a player. C44-A requires a committed semantic result's
identity to be truthfully learnable; it does not require the player to know
which available action distinction changes a relationship fact and which
changes the result's recurrence permission. C137-A through C141-A and C149-A
make the atomic choice, actual selected arm, and both causal comparisons real;
they do not provide either causal account before commitment.

Non-dominance is premature because no chooser-specific policy information,
payoff/objective, utility, or comparison horizon exists. Specific bearer,
ally, or opponent command authority can follow because C156 binds information
to the abstract actual command-holder without assigning that role.

## Active owner choice — EP-D02-C3c.156

For one dual tag `k`, legal history `w`, and live joint opportunity `d`, let
`u(w,d)` be the role legally authorized to make `d`'s atomic combat selection.
Let `O^-_u(w,d)` be that role's complete canonical, versioned authorized
information immediately before commitment. It may include a previously taught
rule or reachable in-game reference. It excludes hidden evaluator state,
debug access, data mining, and external guides.

Evaluate that information through the player-facing observation contract
available when the role is lawfully human-controlled. A role that cannot be
human-controlled supplies no witness, and private AI policy, model, script, or
evaluator state never establishes `Lambda_F`. This still does not decide
bearer, ally, or opponent command authority.

For `F in {P,R}`, a qualifying `pi_F in Pi_F(w,d)` is **pre-choice form-
legible** when that information lets the command-holder truthfully:

1. distinguish its two genuine legal action continuations;
2. understand that their isolated material difference is a predicate-read
   relationship fact for `P`, or the tag's recurrence authorization for `R`;
3. understand that this intervention can, conditional on later legal state,
   change whether or when the next occurrence becomes provisionally complete
   at `q2`.

The account need not reveal hidden future inputs, predict exact `q2` timing or
success, name an internal variable, expose the singleton hinge identity,
describe payoff, or recommend the optimal action. Let `Lambda_F(w,d)` contain
the qualifying pairs and define

`K^{pre-form}_v={F in {P,R} : some legal joint opportunity has
Lambda_F(w,d) nonempty}`.

### A — both forms have pre-choice support — recommended

`K^{pre-form}_v={P,R}`. Somewhere in the completed catalog, at least one
predicate pair and at least one recurrence pair are truthfully distinguishable
to their actual command-holders before commitment. The witnesses may be on
different tags, histories, opportunities, or pairs.

At each qualifying pre-choice boundary, continuing operative context and one
real causal distinction coexist **directly**; the later relation to `q2` and
the hinge remains **partial**; excluding total form opacity is **neutral/
protective**; and two-form catalog support is **aggregate**. A turns the
selected bond-versus-manifestation distinction into player-semantic vocabulary.
Its cost is explanation/localization/validation work and reduced mystery. Its
explicit weakness is token support: a buried P reference on one obscure tag
and an equally obscure R reference elsewhere satisfy A. Co-legibility, ease,
and fun remain later.

### B — exactly one form has pre-choice support

`|K^{pre-form}_v|=1`. Some qualifying pairs of one form are truthfully
explained before commitment; complete analysis proves none of the other form
are. Which polarity survives is the next dependent choice.

B's strongest case is an asymmetric mystery grammar: players knowingly repair
a covenant while manifestation permission remains occult, or knowingly invoke
an Echo while the bond change remains experiential. The supported form has
A's **direct** local fit; the hidden form is **neutral/unsupported** at the
player-information boundary; their asymmetry is **aggregate**. Costs are guide
dependence, one strategically opaque causal axis, and an unintended reading
that the hidden form is secondary.

### C — neither form has pre-choice support

`K^{pre-form}_v` is empty. No qualifying predicate or recurrence pair is
truthfully form-distinguishable to its command-holder before commitment.
C44-A's committed-result identity stays learnable, and post-choice discovery
remains possible.

C's strongest case is an occult Relic experience built around surprise and
experimentation. The hidden mechanical isolations remain **direct** at their
hinge, but player-facing distinction is **neutral/unsupported** before choice
and universal opacity is **aggregate**. Costs are blind first-use decisions,
external-guide advantage, opaque balance feedback, and failure to make the
selected P/R grammar part of intentional play.

## Concrete separator

```text
AUTHORIZED BEFORE ONE ATOMIC SELECTION

                relationship fact     recurrence permission
Converge              restores                 opens
Invoke Only           unchanged                opens
Rekindle Only         restores                unchanged

left comparison  -> P is pre-choice legible
right comparison -> R is pre-choice legible
```

A may explain both columns together or put them on separate witnesses. B
explains exactly one column somewhere. C provides no authorized pre-choice
account of either column, even if the player later sees “Echo Ward occurred.”

## Proof, anti-laundering, floors, and deferrals

A/B/C are exhaustive over the two-element support set: both, exactly one, or
neither. A needs one explicit canonical-information witness per form; they may
coincide. B needs one positive witness and exhaustive negative proof for the
absent form. C needs exhaustive negative proof for both. Incomplete analysis
is unresolved, not opacity.

Names, icons, colors, rarity labels, flavor, generic “affects the Relic” text,
aggregate success/failure, post-choice-only VFX or replay, debug state, data
mining, external guides, and another seat's uncommunicated information do not
count. A fused “does both” label proves neither form unless the legal
substitution and form-specific intervention are intelligible. A stable
authorized tutorial or in-game reference reachable before the choice may
count even off the combat HUD; discoverability and ease remain later.

C156 adds no tag, history, opportunity, pair, continuation, cut, receipt-width,
menu, or visible-button floor. One inherited three-continuation joint
opportunity and singleton cut can carry both A witnesses. The tight catalog
remains one dual plus both specialists. Every branch retains nine definitions,
twenty-three exact keys, 138 Soul/key rows at six Souls, four represented
locked IDs, eight lived aligned edges, four one-history-complete IDs, no
incomplete represented ID, Souls `{6,9,10,12,14,15}`, degree six, and C46-A's
same-boundary width two.

C156 does not choose B's polarity; per-tag, per-history, per-path, or per-
opportunity overlap; shared explanation; hinge legibility; bearer/ally/
opponent command authority or communication; modality, accessibility, or
discoverability; post-choice feedback; outcome preview; value, non-dominance,
payoff, power, fun, UI, release, or implementation. Under A, per-dual-tag two-
form disclosure overlap is next.

## Repository state

The owner packet, rigor audit, living head, and systems map carry C155-A and
make C156 the sole active choice. The authoritative decision record was not
edited and its SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.

This was a documentation-only design update. Validate `git diff --check`, SVG
XML parsing, a single active frontier, and the authoritative-record hash. Do
not run the Node suite because no code or executable contract changed.
