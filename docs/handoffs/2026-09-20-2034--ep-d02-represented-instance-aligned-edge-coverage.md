# Handoff — every name has an aligned edge; every artifact fiber next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-21 00:34:27
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.119: whether no, some but not all, or every union-
locked Soul Relic definition contains an aligned one-coordinate/all-Soul-
changing edge inside every represented persistent-instance fiber. The owner
selected C3c.118-C. Preserve the accepted EP-D02 wording until a complete
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the
current full-assurance pass before the separate standalone expansion/audit/
adaptation pass. Vanilla evidence and the byte-identical oracle remain in
their own lane.

## Selected direction — EP-D02-C3c.118-C

For every locked definition `d`,

`X^{1cW}_v(d)=A^{1coord}_v(d) intersection W^S_v(d)` is nonempty,

and therefore

`D^{1cW}_{locked,v}=D^{U=}_{locked,v}`.

Every locked name has at least one same-persistent-instance comparison that
holds exactly one of state and evaluation boundary fixed while changing every
supported Soul's assigned noncore map. Because C113-A's state-supporting-only
and boundary-supporting-only name classes are both nonempty, at least one
aligned edge of each type exists catalog-wide. Other fibers and keys may
remain unaligned.

Tight floors remain seven definitions, fifteen exact keys, and ninety six-Soul
rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree remains
six. Each aligned edge is partial at the same-persistent-artifact/fixed-
coordinate/across-snapshot boundary, while universal definition-level
prevalence is aggregate. The authoritative decision record remains unchanged.

## Active owner choice — EP-D02-C3c.119

Define

`Ids^X_v(d)={r in Ids_v(d):the r-fiber contains an edge in X^{1cW}_v(d)}`

and

`D^{all-IdX}_{locked,v}={d in D^{U=}_{locked,v}:Ids^X_v(d)=Ids_v(d)}`.

C118-C makes `Ids^X_v(d)` nonempty for every locked definition. It does not
make that set universal. C111-C and C117-C separately cover every key in every
represented fiber with one-coordinate and internal all-Soul-changing edges,
but those two graph covers can remain edge-disjoint.

### A — every locked name is fiber-hybrid

`D^{all-IdX}_{locked,v}` is empty. Every name has an aligned-positive artifact
and at least one orthogonal artifact whose one-axis and whole-roster relations
never occupy the same edge.

This is the strongest principled countercase if articulated and compound
incarnations are visible, accessible, agentic, and non-dominated sidegrades.
Otherwise it is a hidden copy-quality roll, especially because a combatant may
own at most one instance of a definition. It also forces at least two
represented IDs and six keys per locked name.

### B — all-fiber-positive and hybrid names coexist

`D^{all-IdX}_{locked,v}` is nonempty and proper. At least one name gives every
represented artifact an aligned edge, while another combines an aligned
carrier with an orthogonal artifact. This is the strongest variety compromise,
but adds name taxonomy over per-copy behavior and risks the systematic class
reading as premium.

### C — every represented fiber contains an aligned edge — recommended

`D^{all-IdX}_{locked,v}=D^{U=}_{locked,v}`. Every persistent instance ID
represented in the completed locked domain has an internal pair carrying its
definition's sole one-coordinate type and changing every supported Soul's map.

C makes the clean continuity-with-whole-roster-difference comparison a promise
of every represented artifact rather than one showcase copy. It closes C118's
token-artifact and per-copy comparison-lottery loopholes. Costs are universal
per-fiber foil authoring and validation, possible artificial snapshots, less
incarnation-level asymmetry, and a remaining token-dyad loophole: other keys in
a larger positive fiber may still lack an aligned partner.

## Verified witnesses and floors

The tight fiber-hybrid name is `[2,4]`:

```text
R17={b0,b3}          b0--b3 is common and typed: X-positive

R44={u0,u1,u2,u3}    typed/non-common: u0--u1, u2--u3
                      common/diagonal:  u0--u2, u0--u3,
                                        u1--u2, u1--u3
```

The six complete families are distinct. Overlap-bearing common edges
`b0--b3`, `u0--u2`, and `u1--u3` cover every key. Member-disjoint common edges,
including `b0--u1`, `b3--u0`, `u0--u3`, and `u1--u2`, also cover every key.
Coordinate decoration gives all typed dyads the definition's sole C113-A type
while keeping the R44 common edges diagonal.

An aligned-negative fiber needs at least four keys: its one-coordinate and
internal-common relations are disjoint no-isolate covers, which cannot coexist
on two or three vertices. The `u` fiber proves four tight. An aligned-positive
fiber needs two, so `[2,4]` is the negative-name minimum. A full four-key name
uses C105's aligned `[2,2]` witness.

With the inherited five-definition/seven-key outside base and C113-A's two
locked name classes:

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 7 | 19 | 114 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 17 | 102 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |

Independent read-only frontier, construction, and gameplay audits confirmed
that no coarser card is missing, `[2,4]` is feasible and tight at every
retained Soul profile, and C is the recommended branch while A is the strongest
principled countercase.

## Ideal ratings and exclusions

At one fixed exact-key/Soul relationship, inherited fit remains **direct**. An
actual aligned edge is **partial** at the same-persistent-artifact/fixed-
coordinate/across-snapshot boundary. An orthogonal fiber's typed edge remains
**partial** at the narrow boundary while retaining some Soul map; its diagonal
common edge is **partial** at the broader artifact boundary and **neutral** at
the omitted fixed-coordinate projection. Absence of the intersection is
**neutral/protective** only at the combined projection. Fiber and definition
prevalence are **aggregate**. C improves systematic availability of the
partial relationship; it does not upgrade it to direct.

C119 selects no key-complete aligned fiber, per-key or endpoint aligned
coverage, edge count, every-pair or connectedness rule, overlap-bearing versus
member-disjoint aligned identity, exact coordinate semantics, cause, path,
transition, common access, disclosure, agency, ease, value, power, non-
dominance, persistence, release, or implementation. It quantifies only over
represented completed-domain IDs, not every inventory instance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C119 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
