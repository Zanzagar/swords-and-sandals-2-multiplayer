# Handoff — temporally projection-pure Soul Relic positions

**Session ID:** `72534467-4493-4e32-9856-6f79ae638153`
**UTC:** 2026-09-24 16:03
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`; present only `RCS-03C3B1A1A` next.

## Owner answer recorded

The owner's latest `A` selected `RCS-03C3B1A0-A`. No source position may
materially use its opposite temporal form on any effective path. Every `T_H`
position is materially H-pure, every `T_S` position is materially S-pure, and
`P^{H-read}_v=T_H`. One entry may still relate distinct history and state
positions, and either kind of position may still read legal non-temporal
inputs.

At one position its temporal form and predicate participate **directly**; H/S
difference is mechanically real; excluding hidden opposite-form dependence is
**neutral/protective**; and universal exclusion is **aggregate**. History/state
unity remains available through the larger multi-position relationship. The
choice changes no authoritative decision wording and authorizes no
implementation.

## Eighth register amendment

Post-answer screening found that old `RCS-03C3B1A1` compressed two independent
boundaries:

1. whether every history position has any independently isolatable semantic
   phase; and
2. how many phases one phase-attributable position may independently isolate.

Over `p`'s reachable `(D,E)` valuations `{00,10,01}`, worlds
`U: p=D, q=E` and `M: p=D OR E, q=E` can agree that every history position is
phase-attributable, that catalog phase support is `{D,E}`, and that no
irreducibly multi-phase package exists. Only M lets `p` qualify independently
through two phases. Minimum attribution, local phase multiplicity,
irreducibly multi-phase supplements, and catalog support are therefore four
separate boundaries.

Old `RCS-03C3B1A1` is now a non-counting parent for
`RCS-03C3B1A1A` and conditional `RCS-03C3B1A1B`.
`RCS-03C3B1A2` follows the applicable child, then `RCS-03C3B1B` recomputes
catalog phase support.

**Agent correction:** the prior route treated minimum phase attribution,
supplemental irreducible coupling, and catalog phase union as exhaustive. The
U/M countermodel preserves all three while varying local independent phase
multiplicity. The omitted player-material boundary was restored before the
next choice was presented.

The register has 48 rows: 31 `SCREEN`, 1 `OWNER-OPEN`, 7 `PRUNED`, 7
`DIR-SELECTED`, 1 `SPEC`, and 1 `EVALUATE`; therefore `Phi_SR = 32`.

## Next card

`RCS-03C3B1A1A` asks whether each temporally H-pure history position must have
at least one semantic phase that can independently change its qualification.
The mutually exclusive and exhaustive semantic phase claims, relative to one
candidate source role, are:

- `D`: accepted declaration or commitment before performance;
- `E`: actual execution/performance, including bare ordinary completion but
  excluding adjudicated result and termination disposition/reason; and
- `O`: adjudicated result or termination disposition/reason.

These classify claims rather than cuts, records, fields, or atoms. `E` and `O`
may become authoritative at the same cut. One transition may be an outcome for
one source role and an activation for another without multiplying independent
source identity.

- A: every history position has at least one `D`-, `E`-, or `O`-isolation
  witness. Which phase, how many independently isolatable phases, and whether
  a separate irreducibly multi-phase path exists remain later choices.
- B: every history position is phase-coupled-only. Satisfaction can change
  through an H-isolation only when material claims from at least two phases
  vary together.
- C: phase-attributable and phase-coupled-only history positions coexist.

Recommend A. It gives every position one truthful causal phase anchor for
tooltips, replay, and counterplay while leaving richer multi-phase unity to the
two later registered choices. Exact facts, operators, roots, atoms, catalog
support, fan-out, reuse, payoff, UI, persistence, release, and implementation
remain separate.

## Validation

- Structural target: 48 register rows and `Phi_SR = 32`, with only
  `RCS-03C3B1A1A` marked `OWNER-OPEN`.
- Independent read-only reviews passed the phase partition, C3c.33 isolation
  refinement, A/B/C topology, countermodel, active pointers, and register
  arithmetic.
- Full suite: 584 tests, 583 passed, 0 failed, 1 expected raw-trace archive
  skip.
- `git diff --check` passed.
- `docs/design/endless-progression-decisions.md` remains byte-identical at
  SHA-256 `e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
- No runtime, Ruffle, capture, installation, save, snapshot, fixture,
  candidate, observation, manifest, or golden work was performed.
