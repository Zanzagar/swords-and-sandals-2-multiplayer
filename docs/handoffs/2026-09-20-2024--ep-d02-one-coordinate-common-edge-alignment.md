# Handoff — every fiber/key common-covered; exact typed alignment next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-21 00:24:30
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.118: whether no, some but not all, or every union-
locked Soul Relic definition contains at least one exact edge that is both its
sole one-coordinate comparison type and an all-Soul-changing common relation.
The owner selected C3c.117-C. Preserve the accepted EP-D02 wording until a
complete replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the
current full-assurance pass before the separate standalone expansion/audit/
adaptation pass. Vanilla evidence and the byte-identical oracle remain in
their own lane.

## Selected direction — EP-D02-C3c.117-C

For every locked definition `d`,

`Ids^{KW}_v(d)=Ids_v(d)`,

and therefore

`D^{all-IdKW}_{locked,v}=D^{U=}_{locked,v}`.

Every represented persistent-instance fiber is key-complete for internal all-
Soul-changing common edges. Equivalently, every completed locked exact key has
some same-ID partner on which every supported Soul's assigned noncore map
differs. This closes C116's token-carrier and per-copy incidence-lottery
loopholes without requiring every pair, graph connectedness, common access, or
typed-edge alignment.

Tight floors remain seven definitions, fifteen exact keys, and ninety six-Soul
rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree remains
six. Each internal common edge is partial at the represented-persistent-
artifact/across-snapshot boundary, while universal fiber/key coverage is
aggregate. The authoritative decision record remains unchanged.

## Active owner choice — EP-D02-C3c.118

Define

`X^{1cW}_v(d)=A^{1coord}_v(d) intersection W^S_v(d)`

and

`D^{1cW}_{locked,v}={d in D^{U=}_{locked,v}:X^{1cW}_v(d) is nonempty}`.

`A^{1coord}` already requires the same persistent instance ID. Under C113-A,
every locked name has exactly one one-coordinate type, state-only or boundary-
only. An edge in `X^{1cW}` therefore holds one modeled coordinate fixed inside
one persistent artifact while every supported Soul's assigned map differs.

C111-C and C117-C separately make every completed key incident to each edge
kind. They do not make the two partners identical. The next atomic question is
the empty/proper/full prevalence of their exact edge intersection.

### A — no locked definition has an aligned edge

`D^{1cW}_{locked,v}` is empty. Every one-coordinate edge retains at least one
Soul map, while every same-ID all-Soul-changing edge is diagonal in state and
evaluation boundary. This is the strongest principled countercase: controlled
one-axis comparisons preserve a relational witness, while a whole-roster
reweave requires a compound shift of both coordinates.

Costs are a less direct teaching comparison, a four-form minimum in every
represented fiber, and causal opacity: the static comparison does not show
which coordinate is responsible.

### B — aligned and orthogonal locked definitions coexist

`D^{1cW}_{locked,v}` is nonempty and proper. At least one name contains a clean
fixed-coordinate whole-roster contrast and at least one keeps the typed and
common relations edge-disjoint. This is the strongest variety compromise, but
adds hidden name taxonomy, may correlate with the state/boundary split, and
risks aligned names reading as premium.

### C — every locked definition has an aligned edge — recommended

`D^{1cW}_{locked,v}=D^{U=}_{locked,v}`. Every locked name has at least one
same-artifact one-coordinate comparison on which every supported Soul's map
differs.

C is recommended because artifact identity and one coordinate continue while
the entire Soul roster is relationally re-expressed on the exact same
comparison. It bridges each name's learnable comparison dialect to the
strongest existing roster contrast. Costs are an aligned-foil authoring burden
for every name, possible homogenization or artificial forms, and a token
loophole: one obscure edge can satisfy the rule.

## Verified witnesses and floors

The one-ID `u0..u3` construction realizes A:

```text
typed/non-common:  u0--u1, u2--u3
common/diagonal:   u0--u2, u0--u3, u1--u2, u1--u3
```

Each graph separately covers all four keys and the edge sets are disjoint.
Decorate the typed dyads state-only for one name and boundary-only for the
other.

C105's four-key `[2,2]` construction realizes positive alignment:

```text
R17={b0,b2}    sole dyad is common and typed
R44={b1,b3}    sole dyad is common and typed
```

Use two orthogonal names for A, one orthogonal plus one aligned for B, and two
aligned names for C. C113-A's two required comparison-type classes remain.

An alignment-negative fiber needs at least four keys. On two vertices both
no-isolate edge covers must use the sole edge; on three vertices each cover
needs at least two of three edges, so the covers intersect. The `[4]` `u`
witness proves four tight. Every branch nevertheless uses two four-key locked
names over the inherited five-definition/seven-key outside base:

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |

Independent read-only frontier, construction, and gameplay audits confirmed
that no coarser incidence card is missing, all three branches are feasible,
the four-key negative-fiber bound is tight, and C is the recommended branch
while A is the strongest countercase.

## Ideal ratings and exclusions

At one fixed exact-key/Soul relationship, inherited fit remains **direct**. An
actual aligned edge is **partial** at the same-persistent-artifact/fixed-
coordinate/across-snapshot boundary. A same-ID diagonal common edge is
**partial** at the broader artifact/across-snapshot boundary and **neutral** at
the omitted fixed-coordinate projection. Absence of exact alignment is
**neutral/protective** only at that combined projection. Definition prevalence
is **aggregate**. C improves legible same-comparison participation, not the
direct doctrinal rating.

C118 selects no per-fiber, per-key, endpoint, every-pair, connectedness, or
common-anchor aligned coverage; no overlap-bearing versus member-disjoint
identity; no exact coordinate semantics, cause, transition, path, common
access, disclosure, agency, value, power, non-dominance, persistence, release,
or implementation. It quantifies only over represented completed-domain
instances. Every snapshot remains independently legal and reachable; C118
grants no path and does not prove that the changing coordinate caused the Soul-
map changes.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C118 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
