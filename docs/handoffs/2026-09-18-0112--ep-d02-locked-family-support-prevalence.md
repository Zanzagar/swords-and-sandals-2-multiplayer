# Handoff — union-locked family support selected; prevalence next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-18 01:12:04
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.91: whether fixed-union map-family support is
nonempty proper or universal within the union-locked affinity class. The owner
selected C3c.90-B. Preserve the accepted EP-D02 wording until a complete
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate standalone-game expansion, audit, and adaptation
pass. Vanilla evidence and the byte-identical measurement oracle remain in
their SS2 lane.

## Selected direction

EP-D02-C3c.90-B fixes `K^{M|U}_v={locked}`. At least one union-locked affinity
definition has structural support for two unequal legal complete noncore map
families at some fixed exact union `U`, while every fixed-width-capable
definition is support-negative. Breathing support remains independently
derived.

C90-B selects capacity only. It identifies no Veil, authors no alternate exact
key or family, and creates no visible or material reweaving. Tight floors remain
six definitions, eight exact keys, and forty-eight six-Soul rows. Supported-
Soul counts remain `{6,8,9,10,12,14,15}`. A tight positive locked witness uses
`(q,k,rho)=(5,3,2)` and degree five without forbidding degree six elsewhere.
Fixed-key fit is direct, support neutral, class allocation aggregate, and a
later actual same-definition/same-`U` unequal-family relation partial. The
authoritative decision record remains unchanged.

## Active owner choice — EP-D02-C3c.91

Let `L^{M|U}_v` be the nonempty set of union-locked definitions with structural
fixed-`U` family support.

### A — support-positive and support-negative locked definitions coexist

`L^{M|U}_v` is a nonempty proper subset of the locked class. At least one
locked definition has an alternative legal family for some `U`, while a
distinct locked definition has none for any legal `U`.

This preserves a simple sealed covenant beside an advanced relationally open
name. At the tight six-Soul floor, the positive name can use `(5,3,2)` and the
negative name `(3,2,2)`. The cost is another hidden subtype, one additional
definition/key, and token risk: one obscure positive name can carry C90 while
most locked content remains structurally inert. Tight floors are `7/9/54`.

### B — every union-locked definition supports family reweaving — recommended

`L^{M|U}_v` is the whole locked class. Every locked definition has at least one
legal `U` with two unequal complete map families.

This turns locked-only C90 into a coherent class promise: exact vocabulary is
fixed, but every locked name has structural room for another internal
relationship. It avoids an extra support subtype and retains tight floors
`6/8/48`. Its costs are excluding simple `(3,2,2)` family-rigid covenants,
requiring richer geometry for every locked name, and creating universal latent
capacity that may produce no play if actual variation is later rejected.

Universal does not mean every `U` has alternatives, and neither answer means
“Veil reweaves.” C91 authors no second key, family assignment, predicate, or
visible/selectable/non-dominated behavior.

A/B are exhaustive because C90-B already makes `L` nonempty. Both retain Soul
counts `{6,8,9,10,12,14,15}`, attain a tight six-Soul maximum degree five while
leaving degree six legal, and add no exact key merely for support. Fixed-key fit
is direct, support neutral, prevalence aggregate, and a later actual pair
partial. B is recommended for class coherence and taxonomy control, not for a
stronger fit to the standing ideal.

After either answer, the structural support domain is complete. The next card
asks whether neither, exactly one, or both of the breathing and support-positive
union-locked classes actually realize a same-definition, same-`U`, unequal-
family exact-key pair. Class identity and within-class prevalence remain later
where required.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C90-B
as selected and C91 as the sole active owner choice. The authoritative decision
record is unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04
directions remain unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain
selected, and R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Three bounded read-only checks
verified C91's exhaustive prevalence split, absence of a hidden subclass
prerequisite, branch floors, roster/degree consequences, next frontier, ideal
ratings, strongest countercase, and narrow B recommendation. The authoritative
record was not edited.

Validation passed: tracked and current-untracked whitespace checks; the sole-
active-heading assertion; cross-document C90-B/C91 state assertions; stale-
state scans; authoritative-record no-diff; exact floor-table assertions; SVG
XML parsing; Unicode format-control scanning; and newest-handoff ordering.
Tests were not run because this was a documentation-only design round.
