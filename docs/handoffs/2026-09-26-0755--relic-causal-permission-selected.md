---
handoff:      2026-09-26-0755--relic-causal-permission-selected
written:      2026-09-26 07:55 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    two distinct bounded read-only prerequisite audits with mutual cross-challenge, plus one strict read-only final diff verifier with focused repair checks
branch:       design/endless-progression-owner-packet
commits:      123151e..HEAD
suite:        proportional serial checks 10/10 passed; register reparses 89/89 with Phi_SR 36; decision-record hash unchanged
supersedes:   2026-09-26-0737--relic-responsive-law-coexistence-selected
---

# Handoff — causal exact-permission response selected

Start with `HANDOFF.md` above its archive line and
`$ss2-progression-design`. Present E3 only; D1 is ready but queued by the
one-card protocol.

## Where things stand

The owner selected `RCS-03C3C3C2E2-B`. At least one responsive complete
intrinsic exact-permission law has a qualifying independently revisable lawful
decision whose full causal closure changes its certain permission bit while
the same law and eventual exact pair/proof remain fixed. This is existential:
it selects no universal actor, trigger, disclosure, success rate, or prevalence.

Two distinct read-only audits and mutual cross-challenge preserved E3's three
exhaustive stable-polarity branches. For the nonempty context-invariant class
`K_v`, its always-allow subset is empty, all of `K_v`, or nonempty proper.
Those cases yield responsive plus steadfast refusal (`R+D`), responsive plus
steadfast compatibility (`R+A`), or all three (`R+D+A`). “Always” is relative
to each complete law's own nonempty reachable domain.

The permission bit is gate-local. Allow passes this exact coordinate but does
not guarantee final co-survival or value. Deny means only “not both” and does
not choose a survivor, substitute, reroute, alternate proof, or benefit.
Responsive laws necessarily have reachable allow and deny contexts; a
constant-deny law is distinct because no reachable context can make it allow,
not because E2 guarantees opponent control.

The register has 89 unique rows: 35 `SCREEN`, 1 `OWNER-OPEN`, 13 `PRUNED`,
36 `DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`; `Phi_SR = 36`.
E3 is the sole presented card. The authoritative decision record remains
unchanged.

## Highest-value next work

Record exactly one owner answer to `RCS-03C3C3C2E3`:

- A — steadfast refusal only (`R+D`): every context-invariant law denies
  throughout its own domain. It supports context-robust severance but provides
  no steadfast compatibility family and puts every constant law under severe
  trap/value pressure.
- B — steadfast compatibility only (`R+A`): every context-invariant law allows
  throughout its own domain. It is the cleanest two-dialect catalog and leaves
  conditional refusal to responsive laws, but excludes invariant refusal
  builds.
- C — both steadfast polarities (`R+D+A`): require at least one always-allow
  law and one distinct always-deny law. Recommend C only with the hard viability
  obligation below; it gives three readable covenant philosophies but adds a
  third prediction dialect and ally-grief, counterplay, trap, and compensation
  burdens.

Concrete boundary: Stone Witness always passes the exact permission
coordinate; Severed Bell always returns “not both”; Ashen responds to `Guard`
versus `Burst`. None of those names selects final disposition or payoff.

Any answer moves E3 to `DIR-SELECTED`, presents D1 next, and reduces `Phi_SR`
to 35.

## Hard rules and traps

- Under C, every required polarity family must have a reachable, mechanically
  legible, non-token legal build or policy purpose and must not be everywhere
  weakly dominated under the later power/compatibility envelope.
- Constant refusal must be instrumentally useful or a play-shaping commitment
  or exploitable liability in a package with commensurate value. Polarity
  symmetry, lore, unreachable showcase, or voluntary handicap does not qualify.
- This viability obligation selects no exact law, rarity, actor, survivor,
  compensation, payoff, magnitude, count, prevalence, or launch placement.
  C3C3E/F and RCS-05/08 own mechanisms; SR-05/06 and RCS-17/18 validate them.
- If no always-deny family passes those later gates, reopen E3 with B as the
  bounded replacement; do not ship a token C witness.
- SC-09's high-ceiling Legendary direction does not make every E3 law
  Legendary or peak-power.
- Do not edit the authoritative decision record without an explicit accepted,
  rejected, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Verification and Git

Confirm 89 unique rows with the exact counts above and `Phi_SR = 36`. Confirm
the decision-record SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
Commit atomically with a `Co-Authored-By` trailer, push only the feature branch,
verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
