# Handoff — Every joint opportunity integrates; same-history multiplicity next

**Session ID:** `19f84717-dcd8-4377-9445-8992e919a310`
**UTC:** 2026-09-24 04:21:34
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.169: same-history joint-opportunity multiplicity
support. The owner's latest `A` selected C3c.168-A and nothing in C169. This
remains Soul Relic design, specifically whether a dual-form Relic can recur as
a joint relationship at two distinct decisions in one legal biography before
account identity across those decisions is considered. Preserve accepted
EP-D02 wording until a complete normative replacement is replayed and
explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready bounded choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Do not bundle
same-history opportunity multiplicity with account reuse or identity,
strategic-function identity, ordering, pacing, reference-asset identity,
simultaneous presentation, controller identity, value, fun, or UI.

## Selected direction — EP-D02-C3c.168-A

`U^{pre-all-op-int}_v=D^{dual}_v`. Every mechanically joint opportunity on
every exact legal history of every dual tag has at least one canonical
authorized semantic account that maps distinct qualifying predicate and
recurrence pairs and explicitly relates them as two faces of the same
continuing Soul–Relic choice.

At each exact `(k,w,d,u,O^-_u,xi)` boundary, relationship unity and both real
distinctions coexist **directly**. Continuity across opportunities using
different accounts remains **partial**; excluding fragmented or merely
colocated pseudo-integration is **neutral/protective**; and universal
opportunity/history/tag saturation is **aggregate**. Account identity,
wording, reference, and rendering may differ between opportunities.

C168-A raises no tight indexed, inherited catalog, or mechanical floor. Three
candidate tags, one dual history, one joint opportunity, one integrated
witness, two form memberships, two legible pairs, two mechanical pairs, and
three continuations remain sufficient. The authoritative decision record
remains unchanged.

## Frontier correction

The prior pointer to cross-opportunity canonical-account identity omitted a
prerequisite. C168-A still permits a catalog in which every legal history
contains exactly one joint opportunity. Asking whether one account recurs
would therefore be vacuous, or would silently smuggle in an opportunity-
multiplicity requirement.
C3c.169 resolves nontrivial same-history multiplicity first. Canonical account
identity remains wholly unselected.

## Active owner choice — EP-D02-C3c.169

For every `k in D^{dual}_v`, define

`W^{2j}_v(k)={w in W_v(k) : |D^{joint}(w)|>=2}`

and

`E^{2j}_v={k in D^{dual}_v : W^{2j}_v(k) is nonempty}`.

A history in `W^{2j}_v(k)` contains at least two distinct,
commitment-separated mechanically joint opportunities on one exact legal
history. C168-A gives each opportunity an integrated-account witness, but the
accounts may have the same or different canonical identity.

### A — every dual tag has a multi-opportunity history — recommended

`E^{2j}_v=D^{dual}_v`. Every dual tag has at least one exact legal history
containing at least two distinct mechanically joint opportunities. Other
histories may remain singleton.

Every dual Relic can recur as a joint relationship within at least one
biography rather than always collapsing into one isolated ritual. C168-A's
unity and distinction remain **direct** at each exact choice. Participation of
the same tag and history across two distinct choices is **partial**, because
account identity and cross-choice mechanics remain open. Rejecting counterfeit
multiplicity is **neutral/protective**, and universal per-tag participation is
**aggregate**.

Costs are added decision cadence, authoring and testing, possible
maintenance-loop fatigue, pacing pressure, and token compliance through a
second obscure or strategically empty choice.

Example: one Ashen Mercy history contains distinct Tend and Manifest
commitments separated by an authoritative state transition. Each has a C168
integrated-account witness; C169 does not say whether those account identities
are the same.

### B — every legal dual history has exactly one joint opportunity

`E^{2j}_v` is empty. C150-A already makes `D^{joint}(w)` nonempty on every
legal dual history, so every such history has cardinality exactly one.
Non-joint choices may still occur.

The strongest countercase is one dense, memorable integrated ritual per
biography instead of recurring Relic maintenance. The sole choice retains
C168-A's **direct** local fit. Cross-opportunity participation is
**neutral/unsupported**, protecting focus and avoiding repetition may be
**neutral/protective**, and universal singleton topology is **aggregate**.

Costs are no recurring joint relationship within one history, no nonvacuous
account-reuse play, and less room for the Relic's two faces to evolve across
decisions.

Example: every Ashen history contains exactly one Convergence joint choice.
Surrounding combat decisions may remain meaningful, but none is another
opportunity at which both forms jointly matter.

### C — recurring and singleton-only dual tags coexist

`E^{2j}_v` is a nonempty proper subset of `D^{dual}_v`. At least one dual tag
has a multi-opportunity history. Every legal history of at least one distinct
dual tag has exactly one joint opportunity.

Some Relics recur as an intra-biography relationship while others culminate
in one integrated ritual. Local cases inherit A/B's ratings, and class
coexistence is **aggregate**.

Costs are a cadence taxonomy, uneven cognitive and authoring burden, likely
agency or power premiums, exhaustive singleton proof for one class, and token
positive membership through one obscure two-opportunity history.

Example: Ashen has a Mercy history with distinct Tend and Manifest
opportunities. Every Dreamglass history has exactly one Study opportunity
where both forms jointly matter.

## Proof, anti-laundering, floors, and deferrals

A/B/C are exhaustive. C150-A makes `D^{joint}(w)` nonempty for every exact
legal history of every dual tag. A tag therefore has some history with at
least two joint opportunities or every one of its histories has exactly one.
`E^{2j}_v` is the whole dual domain, empty, or nonempty proper.

A requires, for every dual tag, one fully specified legal `w` and two distinct
`d_1,d_2 in D^{joint}(w)`. B requires exhaustive enumeration of every legal
history and its semantic opportunities, proving `|D^{joint}(w)|=1`
throughout. C combines one A-style positive tag with one B-style
singleton-only tag. Incomplete history or opportunity enumeration remains
unresolved.

Two qualifying predicate/recurrence pairs, three legal continuations, two
decisive cuts, two accounts, repeated text or rendering, nested UI steps, two
players, reload or replay, branch-exclusive alternatives, or choices on
different histories do not manufacture two opportunities. `d_1` and `d_2`
must be distinct atomic selections actually occurring on the same exact
history and separated by an authoritative commitment or state update.
Cosmetic steps before one commitment remain one opportunity; duplicate aliases
count once.

C169 selects multiplicity only. It does not require different arm sets,
different strategic functions, non-dominance, a minimum delay, a fixed order
of semantic roles, separate results, or separate accounts. The two integrated
accounts may have the same or different canonical identity, reference assets,
wording, views, or presentation timing.

Tight coattaining indexed incidences are:

| Branch | Candidate tags | Dual `(k,w)` incidences | Joint opportunities | Integrated witnesses | Joint-opportunity form memberships / legible pairs | Joint-opportunity mechanical pairs / continuations |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| A | 3 | 1 | 2 | 2 | 4 / 4 | 4 / 6 |
| B | 3 | 1 | 1 | 1 | 2 / 2 | 2 / 3 |
| C | 4 | 2 total | 3 total | 3 | 6 / 6 | 6 / 9 |

These minima are indexed incidences, not necessarily distinct global actions,
accounts, references, assets, panels, buttons, or definitions. A derives two
joint opportunities on one witness history per dual tag. B retains the
inherited singleton minimum. C requires two dual tags plus both specialists,
one multi-opportunity history, and one singleton history. No branch chooses
raw or canonical account count. No branch raises one opportunity's local
width or adds a result, receipt, decisive cut, definition, exact key, Soul
row, same-reference identity, or simultaneous presentation surface.

Every branch retains nine definitions, twenty-three exact keys, 138 Soul/key
rows at six Souls, four represented locked IDs, eight lived aligned edges,
four one-history-complete IDs, no incomplete represented ID, Souls
`{6,9,10,12,14,15}`, degree six, and C46-A's same-boundary width two.

C169 defers tag identities; exact opportunity counts above two; whether every
history of a positive tag is multi-opportunity; frequency, ordinary-route
prevalence, ordering, delay, and pacing; account reuse, commonality, and
identity; qualifying-pair identity; canonical reference-asset identity;
simultaneous presentation; account counts, salience, modality,
discoverability, accessibility, detail, and wording; command-holder identity,
actual controller, role stability, and communication; hinge disclosure;
strategic value, non-dominance, payoff, power, fun, UI, release, and
implementation.

Under A, same-history pairwise canonical integrated-account reuse
participation is the next dependent semantic atom. Use pairwise reuse rather
than one account common to every opportunity: pairwise-overlapping account
sets may have an empty total intersection. Under B/C, recompute the
branch-specific frontier.

## Repository state

The owner packet, rigor audit, living head, systems map, and this handoff carry
C168-A and make C169 the sole active owner choice. The authoritative decision
record was not edited and should retain SHA-256
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.

This is a documentation-only design update. Tracked and relevant untracked
whitespace checks pass; the SVG parses as XML; the owner packet has exactly one
active heading; this is the newest handoff; and the authoritative-record hash
matches. A read-only adversarial review found no blocking inconsistency and
prompted only a clarification that the floor table's form and mechanical
incidences are scoped to the listed dual joint opportunities, not whole-
catalog totals. The Node suite was not run because no code or executable
contract changed.
