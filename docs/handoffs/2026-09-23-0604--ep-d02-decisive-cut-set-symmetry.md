# Handoff — Every joint opportunity shares a cut; complete-set symmetry next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`  
**UTC:** 2026-09-23 06:04:16  
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.154: complete decisive-cut-set symmetry. The owner
selected C3c.153-A. This remains Soul Relic design, specifically same-tag
result-rearm topology. Preserve accepted EP-D02 wording until a complete
normative replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Ground abstract
terms in the Soul Relic player loop whenever needed. Finish the current full-
assurance pass before the standalone expansion/audit/adaptation pass.

## Selected direction — EP-D02-C3c.153-A

Retain

`X^{cross-op}_v(k)={(w,d) : w in W_v(k), d in D^{live}(w),
J^{both-op}(w,d), and not J^{same-chi}(w,d)}`

and

`K^{cross-op}_v={k in D^{dual}_v : X^{cross-op}_v(k) is nonempty}`.

The selected A makes `K^{cross-op}_v` empty. Every live joint opportunity on
every legal history of every dual tag has at least one predicate-isolating pair
and one distinct recurrence-isolating pair whose earliest decisive cuts map to
the same exact actual-history canonical boundary. Other pair combinations at
that opportunity may still use unequal cuts.

At each shared witness, continuing relationship context and both mechanically
independent margins coexist **directly** without becoming one effect. Each
isolation remains **direct**; unequal pair combinations, downstream effects,
and the larger recurrence remain **partial**; excluding strictly cross-cut
opportunities is **neutral/protective**; and universal opportunity/path/tag
saturation is **aggregate**.

C153 adds no tag, history, opportunity, continuation, receipt-width, menu, or
cut floor. The tight candidate catalog remains one dual tag plus the predicate-
only and recurrence-only specialists. Every fixed catalog and width floor
remains unchanged.

## Why complete-set symmetry is next

C153-A establishes nonempty intersection at every joint opportunity. It does
not prevent one form from supporting an additional decisive boundary that the
other form cannot support. Exact cut-set equality is the coarsest remaining
strengthening and must precede all-pairs alignment: all pairs align only when
both complete cut sets are the same singleton.

Generic materiality does not precede this card. C137/C140/C141 already make
each qualifying pair an actual-chosen-arm, `q2`-causal material comparison.
Visibility, chooser authority, policy value, and non-dominance are separate
downstream questions. C154 itself remains evaluator topology and is not proof
of fun or player legibility.

## Active owner choice — EP-D02-C3c.154

Define

`J^{equal-chi}(w,d) iff Chi_P(w,d)=Chi_R(w,d)`,

`X^{asym-chi}_v(k)={(w,d) : w in W_v(k), d in D^{live}(w),
J^{both-op}(w,d), and not J^{equal-chi}(w,d)}`,

and

`K^{asym-chi}_v={k in D^{dual}_v : X^{asym-chi}_v(k) is nonempty}`.

C153-A makes every asymmetric opportunity proper-overlap: its predicate and
recurrence cut sets share at least one exact actual-anchored cut, while at
least one additional cut belongs to only one form. Set equality ignores pair
multiplicity and requires no pairwise bijection.

### A — complete cut-set equality everywhere — recommended

`K^{asym-chi}_v` is empty. At every live joint opportunity on every legal
history of every dual tag, `Chi_P=Chi_R`. Every canonical decisive cut
supported by one independently isolated form is supported by the other.

At every supported boundary, continuing context and both independent forms
coexist **directly**; individual isolations remain **direct**; multicut
temporal structure, cross-cut pair combinations, and downstream effects remain
**partial**; excluding one-form-only cuts is **neutral/protective**; and
universal opportunity/path/tag equality is **aggregate**. Costs are
bidirectional matching and exhaustive proof, extra foil authoring for every
additional cut, reduced form-specific timing, and token matched foils. Equality
alone does not guarantee visibility, fun, value, or all-pairs alignment.

### B — every dual tag supports a proper-overlap opportunity

`K^{asym-chi}_v=D^{dual}_v`. Every dual tag has at least one joint opportunity
with overlapping but unequal complete cut sets.

B's strongest case is a common convergence boundary plus an extra
relationship-only or authorization-only adjudication stage. At the common cut
both forms coexist **directly**; at the exclusive cut its one isolation is
**direct** while two-form coexistence is **neutral/unsupported**; participation
across cuts is **partial**; and universal per-tag support is **aggregate**.
Costs are at least four semantic continuations at the witness, timing opacity,
form privilege, token extra cuts, and pressure for later visible or strategic
justification.

### C — cut-symmetric and asymmetry-capable tags coexist

`K^{asym-chi}_v` is a nonempty proper subset of `D^{dual}_v`. At least one dual
tag has equal complete cut sets at every joint opportunity, while another has
a proper-overlap witness.

C creates fully cut-symmetric and form-timing-variable Soul Relic archetypes.
The symmetric class inherits A's **direct** fit; the asymmetric witness
inherits B's direct/neutral/partial ratings; class coexistence is only
**aggregate**. Costs are another timing subtype, likely clarity or tempo
premiums, universal equality proof for one class, witness proof for the other,
and balance pressure between symmetric and form-privileged tags.

## Concrete separator

```text
EQUAL CUT SETS, NOT NECESSARILY ALL-PAIRS ALIGNED
Chi_P = {c0,c1}
Chi_R = {c0,c1}
predicate and recurrence each have a pair at c0 and c1
but predicate@c0 paired with recurrence@c1 remains unequal-cut

OVERLAPPING BUT UNEQUAL CUT SETS
Chi_P = {c0,c1}
Chi_R = {c0}
c0 supports both forms; c1 is predicate-only
```

Under A, only the first topology is legal, including its singleton special
case `{c0}`. Under B, every dual tag has at least one second-style witness.
Under C, at least one tag is A-like while another is B-like.

## Proof, floors, anti-laundering, and deferrals

A/B/C are exhaustive over the nonempty dual domain. C153-A excludes disjoint
sets, so every unequal witness overlaps properly. Equal multicut sets are not
all-pairs aligned and do not create a fourth C154 branch.

A requires complete enumeration of `Pi_P` and `Pi_R` at every live joint
opportunity on every legal dual history, followed by bidirectional equality of
the exact actual-anchored cut sets. B needs one fixed-field witness per dual
tag, an explicit common cut, and an explicit symmetric-difference cut; proving
the latter absent from the opposite form still requires complete enumeration.
C combines those proof modes. One common cut is insufficient for equality, and
one extra pair is insufficient for asymmetry until opposite-form absence is
proved. Incomplete analysis remains unresolved.

Every `chi` remains the pair's earliest decisive cut. Delayed evaluation,
matching phase names, ordinals, frames, timestamps, callbacks, or UI events
cannot manufacture exact boundary identity. Pairs cannot be stitched across
opportunities or histories; aliases and duplicate traces count once; a fused
pair proves neither independent form.

Keep combatant, Bound Soul, exact Relic realization, Charms, source
configuration, occurrence contract, evaluation boundary, and version fixed.
Changing a field cannot establish B/C support.

A adds no floor: equal singleton sets retain the inherited three semantic
continuations. B retains the tight three-tag catalog but each proper-overlap
witness derives at least two cuts, three qualifying pairs, and four semantic
continuations. C requires two dual tags plus both specialists and therefore a
tight four-tag minimum; its asymmetric witness has the same local floor. No
branch adds a history or opportunity floor. These are semantic continuations,
not necessarily visible buttons. An optional equal-multicut opportunity would
need at least two pairs per form and five semantic continuations, but A does
not require one.

Every branch retains nine definitions, twenty-three exact keys, 138 Soul/key
rows at six Souls, four represented locked IDs, eight lived aligned edges, four
one-history-complete IDs, no incomplete represented ID, Souls
`{6,9,10,12,14,15}`, degree six, and C46-A's same-boundary width two.

C154 does not choose singleton versus multicut support, all-pairs alignment,
asymmetry polarity or shape, cut distance or actionable intervals, visibility,
chooser authority, value, non-dominance, effect timing/order, response, UI,
payoff, release, or implementation. Under A, unequal pair-combination support/
all-pairs alignment is the next finer cut-topology atom. Recompute after any
answer.

## Repository state

The owner packet, rigor audit, living head, and systems map carry C153-A and
make C154 the sole active choice. The authoritative decision record was not
edited and its SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.

This was a documentation-only design update. Validate `git diff --check`, SVG
XML parsing, a single active frontier, and the authoritative-record hash. Do
not run the Node suite because no code or executable contract changed.
