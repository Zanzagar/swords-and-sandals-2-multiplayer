# Handoff — coupled and clean Soul Relic positions coexist

**Session ID:** `72534467-4493-4e32-9856-6f79ae638153`
**UTC:** 2026-09-24 20:35
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`; present only `RCS-03C3B1A2B` next.

## Owner answer recorded

The owner's latest `C` selected `RCS-03C3B1A2A-C`.
`J^{multi}_v` is a nonempty proper subset of `T_H`: at least one history
position has a reachable, materially effective, non-preempted
inclusion-minimal H package spanning multiple semantic phases, while at least
one distinct history position is proved clean across its declared reachable
domain. Every position retains its one or two independent phase anchors, and
the selection does not map coupling to the singular or exact-two class.

Within an admitted coupled package, indispensable claims from distinct phases
and the position transition participate **directly** while retaining their
semantic differences. Independent anchors protect distinction outside the
package; clean positions are **neutral/protective** anchors, and catalog
coexistence is **aggregate**. The answer changes no authoritative decision
wording and authorizes no implementation.

The 50-row register now contains 29 `SCREEN`, 1 `OWNER-OPEN`, 7 `PRUNED`, 11
`DIR-SELECTED`, 1 `SPEC`, and 1 `EVALUATE`; therefore `Phi_SR = 30`.
`RCS-03C3B1A2B` is the sole active card. Formal prerequisite review found no
eleventh register amendment: exact pair identity, package count, width
co-location, class mapping, incidence, prevalence, operators, and quotas are
catalog realization or later power/spec work rather than prerequisites that
remove or merge the current width options.

## Next card

For each admitted package `K`, with phase claims classified relative to one
candidate source role, let `w(K)` count the members of `{D,E,O}` containing at
least one indispensable claim. Let `W^{multi}_v` be the catalog set of such
widths. A2A-C makes it a nonempty subset of `{2,3}`. Present only:

- A: `W^{multi}_v={2}`; every coupled package spans exactly two phases.
- B: `W^{multi}_v={3}`; every coupled package indispensably spans all three.
- C: `W^{multi}_v={2,3}`; both package widths exist.

Recommend C. Width-two Vows give focused relational routes and can permit an
earlier readiness cut; three-phase Confluences give the narratively fullest
intent–deed–consequence package. Both are a **direct** local ideal fit;
catalog coexistence is **aggregate**, and width three is neither doctrinally
superior nor automatically more powerful. Clean positions selected by A2A-C
keep this broader vocabulary teachable.

Width is semantic, not necessarily elapsed timing: `{D,O}` and `{D,E,O}` may
both become ready at `O`, and `E`/`O` may share a cut. A witness must be
reachable, materially effective, non-preempted, and inclusion-minimal;
redundant third-phase co-variation does not establish width three. C needs two
distinct package witnesses but not distinct positions or Relics. Its costs are
a two-tier explanation/balance surface, explicit missing-phase/replay
disclosure, and the risks that width two makes width three ornamental or width
becomes a false rarity/power ladder.

Under B or C, all-three material phase support is derived for later
`RCS-03C3B1B`. Under A, B1B recomputes the remaining feasible catalog
phase-support alternatives.

## Validation

- Independent formal and gameplay reviews passed the C recording, width
  partition, feasibility, dependency route, no-amendment conclusion, and
  recommendation. They caught four draft overclaims: guaranteed early timing,
  guaranteed no-declaration support, unselected rarity, and an incoherent
  shared-pair phrase. All were corrected before commit.
- An independent actual-diff review passed the final wording, pointers,
  deferrals, and arithmetic.
- Structural target: 50 register rows and `Phi_SR = 30`, with only
  `RCS-03C3B1A2B` marked `OWNER-OPEN`.
- Full suite: 584 tests, 583 passed, 0 failed, 1 expected raw-trace archive
  skip.
- `git diff --check` passed.
- `docs/design/endless-progression-decisions.md` remains byte-identical at
  SHA-256 `e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
- No runtime, Ruffle, capture, installation, save, snapshot, fixture,
  candidate, observation, manifest, or golden work was performed.
