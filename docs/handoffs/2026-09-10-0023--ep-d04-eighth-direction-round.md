# Handoff — EP-D04 eighth direction round pending

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-10 04:23:36
**Branch:** `design/endless-progression-owner-packet`

Resume the two separable EP-D04 R8 adjudication choices in the owner packet.
Preserve accepted EP-D01, EP-D02, and EP-D07 and every
selected-but-unaccepted EP-D04 direction. Do not edit the authoritative record
or implement anything.

## Session outcome

The owner's `i agree` selected `R7.1-A, R7.2-A`; it did not accept a final
EP-D04 replacement.

`U_route` is now the primary family-neutral paired structural gate.
`G_offer -> E_pi` has one-way release-veto authority: generated/legal experience
may reject a structural pass but never rescue a failure. Within each separately
gated format, a frozen finite mechanics hierarchy gives canonical parents fixed
equal mass, recursively divides it among semantic children, and averages paired
roots only inside exact leaves. Descendant/file/sample count cannot increase a
parent's mass; a semantic hierarchy change versions and reruns the gate.

## Recomputed frontier

R8 has two producer/consumer choices under one compatibility rule: if the
selected policy-search method supplies no sound lower/upper value bounds, the
confidence-aware classifier returns unresolved rather than tie or win.

1. **R8.1-A, recommended — frozen hybrid policy search.** Use independent
   multi-start RL/search, legal scripted baselines, exact tractable subgames,
   and adversarial best-response/relaxation challengers. Train one legally
   coupled policy per claimant across hidden leaves sharing an information
   class. Privileged state may affect critics/teachers/training loss only, never
   deployed actor/checkpoint selection, parameters, recurrent initialization,
   messages, or action inputs. Execution obeys real offer, lock, per-seat
   authority, and the later accepted communication contract. Freeze every
   scored policy/challenger before its audit roots, or use fresh/nested roots or
   adaptive-valid inference. Missing/non-finite/excessive solution gaps are
   unresolved. B trusts one RL pipeline's best held-out checkpoint; C uses
   fixed scripts/exhaustive cases only.
2. **R8.2-A, recommended — confidence-aware four-way classification.** Given
   simultaneous sound all-Legendary bounds `[L_L,U_L]` and mixed bounds
   `[L_M,U_M]`, including paired sampling uncertainty, classify the optimal
   difference through `[L_L-U_M, U_L-L_M]`. The whole interval beyond the later
   `+/-epsilon_success` band is a strict win; wholly inside is a practical tie;
   missing or boundary-crossing intervals are unresolved. Apply this at leaf,
   parent, format aggregate, and causal Tempered/Inscribed replacement levels.
   Secondary Pareto dimensions remain separate. B uses point estimates with a
   band; C treats every nonzero estimate as strict.

A read-only verifier attacked the claim that R8.1/R8.2 were independent. It
wrote nothing and correctly found that the first draft allowed `R8.1-B` to feed
`R8.2-A` without a solver bound and did not freeze adaptively chosen challengers
before audit roots. The owner packet and `HANDOFF.md` correct both issues at the
instruction before this round was presented. No evidence wave was launched.

After R8, choose the practical equivalence margin, simultaneous confidence and
solver-gap tolerances, unresolved-mass rule, then dependent mixed coverage,
Legendary lead/concentration bounds, and experience-veto floors. Exact team
communication and mechanics taxonomy/support remain [U] inputs before final
replay.

## Verification

`git diff --check` passed after this handoff. The authoritative decision record
has no diff. No runtime code, tests, vanilla evidence, licensed assets,
installation, save, snapshot, capture, candidate, fixture, observation,
manifest, or golden changed. Tests were not run for this docs-only owner round.
No commit or push was made. Implementation remains blocked.
