# Handoff — mixed Soul Relic phase multiplicity

**Session ID:** `72534467-4493-4e32-9856-6f79ae638153`
**UTC:** 2026-09-24 16:39
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`; present only `RCS-03C3B1A1B2` next.

## Owner answer recorded

The owner's latest `C` selected `RCS-03C3B1A1B1-C`. For

`L^{phase}_v(p)={F in {D,E,O} | I^H_F(p)}`

and

`M^{plural}_v={p in T_H | |L^{phase}_v(p)| >= 2}`,

`M^{plural}_v` is a nonempty proper subset of `T_H`. At least one history
position is phase-singular and at least one distinct history position is
phase-plural. The selection is aggregate catalog topology: it does not require
both forms inside one Relic definition, entry, or result.

At each local position the qualifying phase-to-predicate links are
**direct**, continuity through the larger source operation is **partial**, the
phase distinctions remain mechanically real, and catalog-wide coexistence is
**aggregate**. The answer changes no authoritative decision wording and
authorizes no implementation.

The register remains at 49 rows but now contains 30 `SCREEN`, 1 `OWNER-OPEN`,
7 `PRUNED`, 9 `DIR-SELECTED`, 1 `SPEC`, and 1 `EVALUATE`; therefore
`Phi_SR = 31`. No charter amendment was required.

## Next card

Inside the inherited nonempty plural domain, define

`T^{tri}_v={p in M^{plural}_v | |L^{phase}_v(p)|=3}`.

Because `L^{phase}_v(p)` is a subset of exactly `{D,E,O}`, each plural
position is exact-two or tri-phase. Present only:

- A: `T^{tri}_v` is empty; every plural position is exact-two.
- B: `T^{tri}_v=M^{plural}_v`; every plural position is tri-phase.
- C: `T^{tri}_v` is a nonempty proper subset; exact-two and tri-phase plural
  positions coexist.

Recommend A. Inherited B1-C already provides singular teaching anchors plus
selected signature multi-face positions. Exact-two plurality preserves a
meaningful missing independent route, denial window, and authored pair
difference without making every plural position listen independently to the
whole act. This is only a partial Achintya Bheda Abheda fit because its
witnesses may occur on different histories. B1A2 separately owns a later
choice about an additional irreducibly multi-phase path.

“Exact-two” constrains the independent-isolation set, not every material read:
a later-admitted coupled path may still use the third phase. A tri-phase
position needs three positive isolation witnesses. An exact-two position needs
two positives plus complete reachable-domain proof that the third isolation is
absent. B or C necessarily supplies `{D,E,O}` catalog phase support; A leaves
catalog-wide support open. Exact pair identities remain catalog authoring.
Every answer makes `RCS-03C3B1A2` next.

## Validation

- Structural target: 49 register rows and `Phi_SR = 31`, with only
  `RCS-03C3B1A1B2` marked `OWNER-OPEN`.
- Independent read-only reviews passed the A/B/C partition, feasibility,
  proof burdens, register arithmetic, dependency separation, authored-pair
  routing, and recommendation framing.
- Full suite: 584 tests, 583 passed, 0 failed, 1 expected raw-trace archive
  skip.
- `git diff --check` passed.
- `docs/design/endless-progression-decisions.md` remains byte-identical at
  SHA-256 `e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
- No runtime, Ruffle, capture, installation, save, snapshot, fixture,
  candidate, observation, manifest, or golden work was performed.
