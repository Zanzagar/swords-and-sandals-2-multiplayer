# Handoff — universal empty-input dual access; six-cell grounding next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-14 04:22:04
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.51, six-cell grounding breadth for input-Charm-free
dual results. The owner selected C3c.50-A. Preserve the accepted EP-D02 wording
until a complete replacement is replayed and explicitly accepted. Do not
implement.

## Selected direction

C3c.50-A sets `F_v=P_v=R_v* x S_v=E_v` in one fixed completed version. Every
genuinely supported Bound Soul paired with every definition-tagged exact,
materially `E+`-operative Relic instance/state/evaluation-boundary realization
must have at least one legal anchored context with an empty input-Charm lane and
a reachable two-receipt result.

One exact payoff-Charm realization/state or the empty payoff lane remains fixed
in each witness. Its current data cannot create, split, merge, or rename input
receipts at that boundary, although an earlier payoff consequence can affect
reachability. Empty means zero active input-type Charm instances, not a null,
neutral, inactive, or no-op Charm. C3c.40-B still requires every functional
input-Charm definition to matter somewhere in `E+`; no Charm can merely be the
sole key to dual capability for a Soul/realization pair.

The selection grants no actual inventory or source access and chooses no ease,
frequency, power, payoff, exact mapping, acquisition, rebinding, migration,
release, or implementation. It is a direction, not a complete accepted EP-D02
replacement, and changes no authoritative wording.

## Frontier result

C3c.38-B guarantees six materially functional temporal-by-basis cells, while
C3c.50-A guarantees an empty-input dual route for every Soul/operative-
realization pair. Neither selected rule connects them. All pair witnesses could
use one cell, while predicates in the other five cells remain focused, work only
with an active input Charm, or disappear before the semantic receipt surface.
Catalog-wide six-cell grounding therefore precedes per-pair cell breadth,
stable mapping, and active-Charm dual participation.

The card uses a same-context, set-valued material-dependency relation. This is
important: a one-cell isolation rule would silently refuse genuinely coupled
multi-cell relationships, while mere predicate co-presence would give dormant
or correlated cells false credit. Receipt identity remains local to one
`J_h`; only cell labels are unioned across contexts.

## Sole active choice

Let

`C6={H/action,H/build,S/action,S/build,X/action,X/build}`.

For one fixed legal empty-input context `h`, `X_h` is the reachable canonical
source/history/state valuation set and `Z_h` is the complete reachability
relation inside `X_h x Y_h`. It is deliberately not assumed to be a function:
later player choice, priority, merge, or stochastic resolution can permit more
than one semantic final-input state for one valuation.

A nonempty `T` is a valid local cell-grounding package for receipt `j` only
when same-context reachable pairs `(x2,y2),(x0,y0)` show `j` present in the
two-receipt result `a_h(y2)` and absent in `a_h(y0)`; nonpredicate resolver and
exogenous branch coordinates are held fixed; predicate outputs outside `T`'s
cells agree; and completed analysis identifies an inclusion-minimal semantic
package `Q` of changed canonical predicate outputs for that exact receipt
toggle, with `T` exactly its cell projection. Every member of `Q` must be
indispensable. A raw read-set, declaration, correlated fact, choice, priority,
random branch, label, dormant row, or extensional alias proves nothing.

A singleton `T` is independent grounding. A larger minimal `T` is genuine
coupled grounding and counts each necessary cell without pretending any works
alone. Let `G_v` be the union of cells across all such packages for all dual
receipts, legal empty-input contexts, and pairs in `P_v`. C3c.50-A does not
derive `G_v` nonempty.

- **A, recommended — all six cells reach native dual play:** `G_v=C6`. Every
  cell grounds at least one empty-input dual receipt somewhere, alone or in a
  necessary coupled package. This keeps the complete recipe vocabulary causal
  at the flagship topology. The cost is six real authoring/validation
  obligations and token-witness/reskin pressure. It is catalog-wide, not every
  pair times every cell.
- **B — only some cells reach native dual play:** `G_v` is nonempty and proper,
  so one through five cells have valid grounding and at least one does not.
  This permits focus but risks a hidden premium-cell hierarchy. Exact subset
  and singleton-versus-coupled support follow later.
- **C — native dual play has no six-cell package grounding:** `G_v` is empty.
  Every pair still has an empty-input dual route, but no dual receipt has a
  valid package containing a six-cell predicate. Its nonconstant occurrence
  must involve a later-authorized non-six-cell factor, such as `E0` or a
  resolver coordinate, alone or jointly with cell behavior. C selects no such
  mechanism or new grammar category; it creates a later obligation to identify
  one.

For A's boundary, one context may change `{Ward}` to
`{Counterpoint,Ward}` through an `H/action` package. Another may change
`{Echo}` to `{Confluence,Echo}` only through a minimal coupled
`{S/build,X/action}` package; both cells count without a one-cell witness.
Other valid packages cover the remaining cells. Under B, an illustrative
supported subset is `{H/action,S/build,X/action}`. Under C, an illustrative
later mechanism is an `E0` timing relationship, but the option does not select
it.

A/B/C exhaust `G_v` being all six cells, a nonempty proper subset, or empty.
One context can contain several packages and one coupled package can cover
several cells, so no context, pair, Relic, or receipt count follows. A negative
requires complete enumeration and within-context dependency analysis. Failed
search is unresolved; cross-context receipt comparison is neither required nor
permitted. An owner-written replacement remains available.

This choice selects no per-pair cell breadth, exact pair/cell/receipt mapping,
singleton-versus-coupled breadth, package cap, cross-context receipt identity,
mapping stability under Soul or same-definition realization substitution,
active-input-Charm participation, `E0` or resolver mechanism, transformation,
precedence, cross-cell footprint, overlap/reuse, arbitration, power, payoff,
access, acquisition, custody, configuration horizon, persistence/migration,
validation, release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record
C3c.50-A and identify C3c.51 as the sole active queue item. The authoritative
decision record and normative system design remain unchanged. EP-D01, EP-D02,
and EP-D07 remain accepted; EP-D04 directions remain unaccepted, R9.4 remains
reopened, R10.1-A and R10.2-A remain selected, and R10.3-R10.8 remain pending.
Continue to present exactly one A/B/C choice per turn. No code tests were run
because this was documentation and vector work only. No runtime, capture,
evidence, installation, save, or snapshot was touched. Preserve the user's
existing change to `.agents/skills/ss2-progression-design/SKILL.md`.

Three read-only audits found and then verified repairs for hidden resolver
determinism, ambiguous dependency-package minimality, a contradictory cross-
context negative, active-Charm overreach, and an initially overclaimed `E0`
consequence. The repaired card uses relational reachability, exact-transition
minimal predicate packages, strictly local receipt identity, and makes C
select only absence of six-cell package grounding plus a later unidentified-
cause obligation. Final audits passed its formal relation, exhaustive topology,
frontier order, player-facing examples, recommendation/tradeoffs, mirrors, and
explicit deferrals.

`git diff --check` passed. No-index whitespace checks for the untracked rigor
audit, SVG, and this handoff returned the expected different-from-`/dev/null`
status with no diagnostics. The SVG parsed with Python's standard XML library,
the newest handoff resolves to this file, and the authoritative decision and
system documents are clean.
