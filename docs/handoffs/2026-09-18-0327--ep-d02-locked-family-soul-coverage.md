# Handoff — locked family pair completeness selected; Soul coverage next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-18 03:27:41
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.96: whether locked-family variation covers every
supported Soul inside every union-locked definition, covers every Soul only
somewhere across the class while leaving local gaps, or globally excludes at
least one Soul. The owner selected C3c.95-C. Preserve the accepted EP-D02
wording until a complete replacement is replayed and explicitly accepted. Do
not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting there. Separate states,
keys, definitions, catalog entries, labels, or cosmetics that respectively
supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate standalone-game expansion, audit, and adaptation
pass. Vanilla evidence and the byte-identical measurement oracle remain in
their SS2 lane.

## Selected direction

EP-D02-C3c.95-C makes every union-locked affinity definition pair-complete.
For each completed locked definition, the projection from operative exact key
to complete unordered singleton-map family is injective: every two distinct
keys carry unequal `M`. A new exact simulacrum of one locked name therefore
means a new complete internal relationship, not a repeated family separated
only by an unclassified finer axis.

C95-C does not author every legal family or select an exact family count. It
finitely caps exact keys by the stable tuple's legal family space and forbids
same-family assignment, predicate, state, or boundary variants inside the
locked class unless reopened. Tight floors remain six definitions, nine exact
keys, and fifty-four six-Soul rows. Supported-Soul counts remain
`{6,8,9,10,12,14,15}`; tight degree five is attained and degree six remains
legal. Fixed-key fit is direct, each unequal-family cross-key relation partial,
and universal pair completeness aggregate. The authoritative decision record
remains unchanged.

## Active owner choice — EP-D02-C3c.96

Remove each locked definition's stable sole core from a Soul's singleton map:

`J_v(b,s)=I_v(b,s)\{kappa_v(d)}`.

Let `(d,s)` belong to `E^S_v` when some two completed exact keys of locked
definition `d` assign supported Soul `s` different `J` maps. C94-B/C95-C derive
that every locked definition has at least one incident Soul. They do not derive
that every Soul is incident to every definition or even to the class.

```text
UNIVERSAL VEIL-LIKE WITNESS — degree 5, profile (4,2)
L0  F,S,V,W->{a,b}   T,R->{c,d}   M0={{a,b},{c,d}}
L1  F,S,V,W->{a,c}   T,R->{b,d}   M1={{a,c},{b,d}}

SOUL-INCOMPLETE SHROUD-LIKE WITNESS — degree 6, profile (2,2,2)
K0  F,S->{a,b,c}   V,W->{a,d,e}   T,R->{b,d,e}
K1  F,S->{a,b,c}   V,T->{a,b,d}   W,R->{c,d,e}
```

The stable core is omitted. Both names are pair-complete. Every Soul changes
in Veil. In Shroud, F and S retain `{a,b,c}` while V, W, T, and R change; the
exact partitions also change as C79-C requires.

### A — every Soul varies in every locked definition — recommended

`E^S_v` is the full locked-definition-by-Soul product. For each locked name and
each supported Soul, some key pair changes that Soul's assigned noncore map.
The witnessing pair may differ by Soul.

A makes the class's signature re-expression belong to every Soul and closes
the off-owner loophole. Its costs are a full definition-by-Soul authoring and
validation matrix, no permanently invariant Soul-map anchor inside a locked
name, and token risk if a changed map is hidden, inaccessible, or predicate-
dead. Tight floors are `6/9/54`; tight maximum degree is five.

### B — every Soul varies somewhere, with local definition/Soul gaps

`E^S_v` is proper while both its Soul projection and its already-derived
definition projection are complete. Every Soul participates somewhere, but at
least one Soul remains invariant throughout at least one locked definition.

B permits local Soul-specific continuity anchors without globally excluding a
Soul. Its costs are a hidden compatibility chart, a second locked definition,
and a token loophole in which one universal name covers every Soul while local
gaps add little. Use universal Veil beside Soul-incomplete Shroud. Tight floors
are `7/11/66`; tight maximum degree is six.

### C — at least one Soul never varies in any locked definition

The Soul projection of `E^S_v` is proper. At least one supported Soul retains
the same noncore map across every key of every locked definition, while every
definition still varies around some other Soul.

C's strongest case is a durable witness-Soul anchor that makes continuity
unusually legible. Its costs are an off-owner loophole and possible Soul caste:
the combatant's intrinsic Soul can miss the flagship locked-class verb
everywhere. Use Shroud as the sole tight-floor locked name; F and S are then
globally excluded. Tight floors are `6/9/54`; tight maximum degree is six.

A/B/C are exhaustive: with the definition projection already complete, the
relation is the full product, is proper with complete Soul projection, or has
proper Soul projection. Empty is impossible. All branches preserve Soul counts
`{6,8,9,10,12,14,15}`. At one fixed exact key, inherited fit remains direct.
Each present same-Soul/same-definition cross-key edge is partial; each coverage
topology is aggregate. A is recommended for participation and **Souls and
Simulacra** coherence, not because it improves the exact-boundary doctrinal
rating.

C96 does not require one common pair to change every Soul or every pair to
change every Soul. It fixes no family count or overlap, block-assignment rule,
material predicate, owner-Soul-specific effect, C39 witness, cause,
transformation, coexistence, live switching, disclosure, agency, sidegrade
quality, power, acquisition, persistence, release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C95-C
as selected and C96 as the sole active owner choice. The authoritative decision
record is unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04
directions remain unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain
selected, and R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Three bounded read-only checks
separated per-Soul incidence from the later common-pair quantifier, verified
the `6/9/54`, `7/11/66`, and `6/9/54` coattaining floors and degree-five/six
bounds, and confirmed the inherited Soul-count set. The authoritative record
was not edited.

Validation passed: tracked and current-untracked whitespace checks; the sole-
active-heading assertion; cross-document C95-C/C96 state and provenance
assertions; authoritative-record no-diff; exact C96 floor-table assertions;
SVG XML parsing; Unicode format-control scanning; and newest-handoff ordering.
Tests were not run because this was a documentation-only design round.
