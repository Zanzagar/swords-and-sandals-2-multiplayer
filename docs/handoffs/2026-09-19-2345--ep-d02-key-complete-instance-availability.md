# Handoff — every represented instance positive; one key-complete instance per name next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`  
**UTC:** 2026-09-19 23:45:03  
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.110: whether no, some, or every union-locked Soul
Relic definition contains at least one represented persistent instance-ID fiber
whose every completed key has a same-ID one-coordinate partner. The owner
selected C3c.109-C. Preserve the accepted EP-D02 wording until a complete
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy, not
theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the current
full-assurance pass before the separate standalone expansion/audit/adaptation
pass. Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.109-C

For every union-locked definition `d`, every persistent instance ID represented
by a completed exact key belongs to `Ids^{1coord}_v(d)`: its completed fiber
contains at least one family-distinct same-ID pair holding exactly one of
input-affecting state and grammar-evaluation boundary fixed. Other pairs inside
that fiber may remain diagonal. Inventory IDs absent from the completed
operative domain remain outside the rule.

Tight floors remain six definitions, eleven exact keys, and sixty-six six-Soul
rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree remains
six. A qualifying cross-snapshot relationship is partial at the same-artifact/
one-coordinate-continuous boundary; universal represented-fiber incidence is
aggregate. C109-C selects neither comparison type, covers no individual key,
and grants no cause, path, access, disclosure, or agency.

## Frontier correction and validation

The first internal C110 draft jumped directly to global per-key coverage. One
write-nothing frontier audit broke that ordering: after C109-C, the coarser
intermediate is whether each locked name has **some one represented fiber whose
every key is covered**. A `[2,3]` definition can have a complete two-key fiber
and a positive three-key fiber with one uncovered key. The too-fine draft was
withdrawn before owner presentation and selected nothing.

Follow-up math and gameplay reviews confirmed the corrected atom. Its
none/mixed/all floors remain `6/11/66`, `7/15/90`, and `6/11/66`; the five-key
strict-separation witness costs `6/12/72` locally but does not raise option C's
minimum. C remains the gameplay recommendation, while B is the strongest
countercase if the catalog distinction is explicit and strategically valuable.
State-only versus boundary-only support and exact fiber topology remain
independently ready siblings, not prerequisites.

## Active owner choice — EP-D02-C3c.110

For represented ID `r` of locked definition `d`, define

`B_{v,r}(d)={b in B_v(d) : inst_v(b)=r}`

and its keys incident to a one-coordinate pair by

`K^{1c}_v(d,r)={b in B_{v,r}(d) : there exists b' in B_{v,r}(d) with {b,b'} in A^{1coord}_v(d)}`.

Then define

`Ids^{K1c}_v(d)={r in Ids_v(d) : K^{1c}_v(d,r)=B_{v,r}(d)}`

and

`D^{exists-IdK}_{locked,v}={d in D^{U=}_{locked,v} : Ids^{K1c}_v(d) is nonempty}`.

A fiber is **key-complete** when all its completed keys are incident to a
one-coordinate pair. A **positive-but-incomplete** fiber still has C109-C's
required pair but also has at least one uncovered key. Neither term promises
acquisition, visibility, joint access, or a transition.

```text
NO KEY-COMPLETE FIBER — four-key [4]
R17/state0/A
R17/state0/B   <- qualifying pair
R17/state1/C   <- uncovered
R17/state2/D   <- uncovered

SOME COMPLETE FIBER, BUT NOT GLOBAL KEY COVERAGE — five-key [2,3]
R17/state0/A
R17/state0/B   <- R17 is key-complete

R44/state1/C
R44/state1/D   <- qualifying pair
R44/state2/E   <- R44 remains incomplete

FOUR-KEY QUALIFYING FLOOR — [2,2]
R17/state0/A
R17/state0/B
R44/state1/C
R44/state1/D   <- both positive dyads are key-complete
```

The fifth-key witness remains compatible with inherited C101/C105 geometry by
adding family `M4={abc,bcd,ade}` with assignment
`FS->abc,VW->ade,TR->bcd`. It has an authored member-disjoint partner and an
overlap-bearing common partner. Under C109-C, every positive two-key fiber is
automatically complete; hence `[2,2]` fully covers the four-key floor, while a
four-key nonqualifier must use incomplete `[4]`.

### A — no locked name contains a key-complete fiber

`D^{exists-IdK}_{locked,v}` is empty. Every represented fiber of every locked
name is positive-but-incomplete, forcing at least three completed keys per
fiber. A makes an irreducibly compound specialist realization part of every
persistent artifact, but removes every clean instance-level teaching carrier
and risks premium/dud forms. Tight floors are `6/11/66`.

### B — names with and without a key-complete fiber coexist

`D^{exists-IdK}_{locked,v}` is nonempty and proper. Some names contain at least
one complete represented fiber; others have only positive-but-incomplete
fibers. B is the strongest countercase if those name languages are disclosed,
strategically valuable, and protected from premium-copy readings. Otherwise it
adds hidden taxonomy to instance uncertainty. Tight floors are `7/15/90`.

### C — every locked name contains a key-complete fiber — recommended

`D^{exists-IdK}_{locked,v}=D^{U=}_{locked,v}`. Every locked name gets at least
one represented persistent artifact whose complete authored snapshot set has
one-coordinate comparison coverage. Other represented fibers may remain
positive-but-incomplete. C provides the smallest reliable per-name structural
foothold, but retains token-exemplar, tiny-fiber-laundering, premium-copy, and
museum-comparison risks. Tight floors are `6/11/66`.

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 6 | 11 | 66 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 6 | 11 | 66 at six Souls | 6 |

At one fixed exact-key/Soul relationship, inherited fit remains **direct**.
Same-ID contrasts remain **partial** at the broad persistent-artifact boundary.
A qualifying pair is **partial** at the same-artifact/one-coordinate-continuous
boundary because its facts occupy separate operative snapshots; an uncovered
key is **neutral/protective** there. Complete-fiber participation and
definition prevalence are **aggregate**. C is recommended for a systematic
structural foothold, not a stronger direct doctrinal rating.

C110 selects no count or prevalence of complete fibers within a qualifying
name, every-fiber/global per-key coverage, state-only versus boundary-only
support, exact fiber topology, partner count, every-pair rule, graph
connectedness, edge alignment, cause, path, access, disclosure, agency, value,
power, persistence format, migration, release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C109-C
as selected and corrected C110 as the sole active owner choice. The
authoritative `docs/design/endless-progression-decisions.md` remains unchanged.
EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04 directions remain
unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain selected, and
R10.3-R10.8 remain pending. Preserve the owner's existing uncommitted skill
edit. No implementation, commit, or push occurred.
