# Handoff — Same-opportunity participation selected; path saturation next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`  
**UTC:** 2026-09-23 04:31:39  
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.150: same-opportunity path saturation. The owner
selected C3c.149-A. Preserve accepted EP-D02 wording until a complete normative
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Finish the current
full-assurance pass before the standalone expansion/audit/adaptation pass.

## Selected direction — EP-D02-C3c.149-A

Retain C149's definitions:

`J^{both-op}(w,d) iff Pi_P(w,d) is nonempty and Pi_R(w,d) is nonempty`

and

`W^{both-op}_v(k)={w in W_v(k) : some d in D^{live}(w) has J^{both-op}(w,d)}`.

The selected A makes

`E^{both-op}_v=D^{dual}_v`.

Every dual tag has at least one fully specified legal actual recommit history
with one semantic opportunity at which two distinct C141 pairs sharing the
actual selected continuation independently establish `I_P` and `I_R`. Other
histories of that tag may still stage those forms at separate opportunities.

Same opportunity means one authoritative pre-choice snapshot and one atomic
selection. UI cannot merge separately committed decisions or cosmetically
split one semantic commitment. One fused pair changing both inputs proves
neither independent form; a positive opportunity needs two distinct pairs and
therefore at least three semantic continuations, not three visible buttons.

At the shared boundary and at each `chi`, participation is **direct**. Unequal
decisive or effect cuts are **partial**; separated histories are **neutral** at
this projection; and universal dual-tag participation is **aggregate**. The
tight three-tag floor remains one dual plus predicate-only and recurrence-only
specialists. All fixed catalog and width floors remain unchanged.

## Why path saturation is next

C149-A closes the no-joint-tag possibility but permits every dual tag's sole
joint history to be an obscure exception. Path prevalence must be resolved
before finer same-`chi`, effect-timing, or composite-command questions. This
repeats the prior participation-then-saturation discipline.

## Active owner choice — EP-D02-C3c.150

Define

`W^{sep-op}_v(k)=W_v(k) setminus W^{both-op}_v(k)`

and

`U^{all-both-op}_v={k in D^{dual}_v : W^{both-op}_v(k)=W_v(k)}`.

A history in `W^{sep-op}_v(k)` has no opportunity satisfying
`J^{both-op}`. C145-A still requires both independent forms somewhere on every
dual history, so such a history places at least one `I_P` and one `I_R` witness
at distinct opportunities. Fused attribution-bundled decisions may still
change both projections while proving neither form.

```text
JOINT HISTORY w-J
d-J actual Converge
    vs Invoke Only   -> I_P
    vs Rekindle Only -> I_R

INDEPENDENT-FORM-SEPARATED HISTORY w-S
d-P Rekindle vs Vent -> I_P
d-F Converge vs Withhold changes both inputs but proves neither form
d-R Invoke vs Bank   -> I_R
no opportunity on w-S contains both independent forms
```

The inherited fixed path-comparison unit remains one tag with the same
combatant, Bound Soul, exact Relic realization, Charms, source configuration,
occurrence contract, evaluation boundary, and version. Changing any fixed field
does not establish path mixing; cosmetic aliases or duplicate traces cannot
manufacture a second history. This topology selects no delay, interruption, or
response timing.

### A — every dual history contains a joint opportunity — recommended

`U^{all-both-op}_v=D^{dual}_v`. Every legal actual history of every dual tag
has at least one joint independent-form opportunity. Not every opportunity on
the history must be joint.

A closes C149-A's one-obscure-joint-history loophole and makes convergence
dependable on every played recommit path. At each joint `d`, the continuing
context and two independently isolated margins co-participate **directly**;
each `chi` is **direct**; unequal `chi` or effect timing is **partial**;
excluding joint-free paths is **neutral/protective**; and universal saturation
is **aggregate**. Costs are a locally dense choice with at least three semantic
continuations on every dual path, repetitive-maintenance pressure, contrived or
dominated counterfactuals, and menu or ritual friction.

### B — every dual tag mixes joint and separated histories

`U^{all-both-op}_v` is empty. C149-A supplies a joint history for every dual
tag, and each tag has a distinct history with no joint opportunity. C145-A
forces the separated history's forms onto at least two distinct opportunities.

B's strongest case is genuine history-sensitive decision topology: the same
relationship has a convergent route and a distributed route. The joint path
retains the **direct** rating. On the separated path each form is **direct** at
its `chi`; participation across distinct opportunities and the same-tag
contrast across histories are
**partial**, joint exclusion is **neutral/protective**, and universal mixing is
**aggregate**. Costs are at least two legal histories per dual tag, two
opportunity grammars, disclosure/testing burden, and token-route risk in both
directions.

### C — joint-saturated and path-mixed dual tags coexist

`U^{all-both-op}_v` is nonempty proper. At least one dual tag contains a joint
opportunity on every history; another has both a joint and an independently
opportunity-separated history.

C keeps dependable convergence for some relationships and history-sensitive
decision topology for others. Local histories inherit A/B's ratings; class
coexistence is **aggregate**. Costs are another dual subtype, ease or power-premium pressure,
disclosure/testing burden, and the mixed class's two-sided token-membership
risk.

## Proof, floors, and deferrals

A/B/C are exhaustive after C149-A. Every dual tag is saturated or path-mixed;
a never-joint class is impossible. Exact joint-history counts or fractions are
later quantitative refinements.

A positive path needs one legal `w`, one semantic `d`, the actual continuation,
and two distinct qualifying counterfactual continuations. A separated path
requires exhaustive analysis of every live opportunity and legal pair;
incomplete search is unresolved. Saturation requires positive proof over every
legal history. B needs one proved separated history per dual tag because C149-A
already supplies the joint history.

Every joint `d` has at least three semantic continuations. Every separated
history has at least two distinct qualifying opportunities. A/B retain the
tight three-tag floor; B also derives at least two histories per dual tag. C
requires two duals plus both specialists, deriving a four-tag floor, and each
mixed dual needs at least two histories.

All branches retain nine definitions, twenty-three exact keys, 138 Soul/key
rows at six Souls, four represented locked IDs, eight lived aligned edges, four
one-history-complete IDs, no incomplete represented ID, Souls
`{6,9,10,12,14,15}`, degree six, and C46-A's same-boundary width two.

C148-A makes all actual histories continuous only inside their post-`c1`
through pre-`q2` slice. It does not make counterfactual arms continuous through
`q2`. C150 does not choose tag identities, exact path fractions or dominance,
same-`chi`, simultaneous effects or order, mediator purity, composite commands,
deliberate modes, bundled-choice topology, counterfactual continuity,
specialist anti-token rules, actor, access, cadence, payoff, persistence, UI,
release, or implementation.

## Repository state

The owner packet, rigor audit, living head, and systems map carry C149-A and
make C150 the sole active choice. The authoritative decision record was not
edited and its SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.

This was a documentation-only design update. `git diff --check` passed, the SVG
parsed as XML, current living surfaces expose only C150 as active, and the
authoritative-record hash matched. The Node suite was not run because no code
or executable contract changed.
