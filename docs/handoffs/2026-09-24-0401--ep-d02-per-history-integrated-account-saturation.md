# Handoff — Per-tag semantic integration selected; history breadth next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-24 04:01:59
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.167: per-history integrated-account saturation.
The owner's latest `A` selected C3c.166-A and nothing in C167. This remains
Soul Relic design, specifically the player-semantic account surface of the
selected same-opportunity, one-hinge result-rearm grammar. Preserve accepted
EP-D02 wording until a complete normative replacement is replayed and
explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready bounded choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Do not batch
history integration with every-opportunity integration, canonical reference-
asset identity, simultaneous presentation, command-holder identity or
communication, hinge disclosure, accessibility, value, fun, or UI.

## Selected direction — EP-D02-C3c.166-A

`E^{pre-int}_v=D^{dual}_v`. Every mechanically dual tag has at least one exact
legal history and joint opportunity with one canonical authorized semantic
account that, by itself plus the neutral baseline:

- makes a qualifying predicate pair and distinct recurrence pair intelligible;
- explicitly relates those interventions as two faces of the same continuing
  Soul–Relic tag and atomic choice.

At the exact `(k,w,d,u,O^-_u,xi)` boundary, continuing relationship unity and
both real distinctions coexist **directly**. Their later relation to `q2`,
effects, and payoff remains **partial**; excluding fused or merely colocated
pseudo-integration is **neutral/protective**; and universal tag participation
is **aggregate**.

C166-A does not require integration on every history or opportunity, one
reference asset or screen, one account reused across histories, actual human
occupancy, or a selected controller identity. C165-A still makes both distinct
forms co-legible to the same actual command-holder at every joint opportunity.
One page holding separate modules is not integration; one semantic account may
render across explicitly linked views.

C166-A raises no inherited catalog or mechanical floor, but adds a tight
minimum of one integrated-account-witness incidence. Three candidate tags, one
dual tag-history, one co-legible joint opportunity, two form memberships, two
legible pairs, two mechanical pairs, and three continuations otherwise remain
tight. The authoritative decision record remains unchanged.

## Active owner choice — EP-D02-C3c.167

Retain C166's

`W^{pre-int}_v(k)={w in W_v(k) : some d in D^{joint}(w)
has J^{pre-int}(w,d)}`

and define

`W^{pre-int0}_v(k)=W_v(k) setminus W^{pre-int}_v(k)`

and

`U^{pre-all-int}_v={k in D^{dual}_v :
W^{pre-int}_v(k)=W_v(k)}`.

A history in `W^{pre-int0}_v(k)` is integration-negative: every exact joint
opportunity `d` on that history has `Gamma^{pre-int}_v(w,d)` empty. C165-A
still makes distinct P and R pairs intelligible to the same actual command-
holder at every such opportunity. Negativity means only that no one canonical
account at a target choice both supplies the exact mappings and explicitly
relates them as two faces. It may contain separate complete modules, one
jointly sufficient account with unrelated clauses, or a relational overview
missing an exact pair mapping.

### A — every legal dual-tag history has an integrated account — recommended

`U^{pre-all-int}_v=D^{dual}_v`. Every exact legal history of every dual tag has
at least one joint opportunity whose one authorized account satisfies
`Gamma^{pre-int}`. Account identity, opportunity, pairs, wording, and rendering
may differ between histories. Other opportunities on the same history may
remain integration-negative.

Every possible Soul–Relic biography therefore contains at least one direct
articulation of unity without erasing the two causal distinctions. This closes
C166-A's obscure-history loophole while preserving modular teaching elsewhere.
At each exact account boundary the fit is **direct**; continuity across
distinct histories remains **partial**; excluding wholly integration-negative
histories is **neutral/protective**; and universal history/tag saturation is
**aggregate**.

Costs are coverage proof, synthesis and localization across every history,
repetition or contrived integrated accounts, less route-specific mystery, and
token compliance through one buried account per history.

Example: Ashen's Mercy and Defiance histories each contain some Convergence
choice whose one account maps the exact P and R comparisons and explains their
unity. The two histories may use different accounts, references, and views.

### B — every dual tag mixes integrated and integration-negative histories

`U^{pre-all-int}_v` is empty. C166-A supplies every tag an integrated-positive
history, while every tag also has a distinct exact history in
`W^{pre-int0}_v(k)`. C165-A still exposes both forms at every joint choice on
that negative history.

The strongest countercase is biography-sensitive initiation. One route
explicitly names the unity; another gives the player complete modular truths
and lets lived play or interpretation synthesize them. Positive accounts
remain **direct**. The negative history's form accounts remain individually
**direct**, while explicit integration is **neutral/unsupported**. Same-tag
continuity across the histories is **partial**; deliberate mystery may be
**neutral/protective**; and universal mixing is **aggregate**.

Costs are route-dependent comprehension, guide advantage, apparent
inconsistency, and one-token positive or negative histories.

Example: Ashen's Mercy history contains the integrated Convergence account.
On its Defiance history, every joint choice explains both exact forms, but
exhaustive analysis finds only separate modules, unrelated joint clauses, or
incomplete relational overviews.

### C — history-saturated and history-mixed dual tags coexist

`U^{pre-all-int}_v` is a nonempty proper subset of `D^{dual}_v`. At least one
dual tag integrates on every legal history. At least one distinct dual tag has
both C166-A's positive history and an integration-negative history.

Selected Relics always articulate unity while others make that articulation
biography-dependent. Local cases inherit A/B's ratings and catalog coexistence
is **aggregate**. Costs are another information dialect, uneven learning,
clarity or interpretive-flexibility premiums, saturation and negative-proof
burdens, and token class membership.

Example: Ashen integrates on every history. Dreamglass has an integrated Study
history and a Facet history where P and R are fully legible but no qualifying
account relates them.

## Proof, anti-laundering, floors, and deferrals

A/B/C are exhaustive after C166-A. Every dual tag's
`W^{pre-int}_v(k)` is nonempty, so the tag is history-saturated or has both a
positive and an integration-negative history. The saturated subset is the
whole dual domain, empty, or nonempty proper. A never-integrated tag is
impossible without reopening C166-A.

A requires one exact `(d,u,O^-_u,xi,pi_P,pi_R)` integrated witness on every
legal `(k,w)`. B requires, for every dual tag, the inherited positive history
plus one distinct history whose every joint opportunity, canonical account,
and qualifying P/R pair combination is exhaustively negative. C combines
universal-history proof for one tag with the negative-history proof for
another. One nonintegrating account or one separate-module opportunity does
not prove a history negative. Incomplete history, account, pair, or content
enumeration remains unresolved.

Accounts cannot be stitched across opportunities or histories. A stable
generic account may witness multiple histories only when authorized in each
target choice's exact `O^-` and truthful for each exact arm set; each use is a
separate indexed witness. Changing combatant, Bound Soul, exact Relic
realization, Charms, source configuration, occurrence contract, evaluation
boundary, tag, or version does not manufacture two histories inside one fixed
comparison domain.

Same `referenceId`, asset, page, screen, heading, or simultaneous rendering is
neither necessary nor sufficient. Post-choice feedback, private AI state,
external guides, inference, data mining, and uncommunicated other-seat
information remain invalid. Every branch inherits the lawfully human-
controllable observation contract. A negative history cannot hide either form
or distribute form-legibility evidence across players; both form-legibility
witnesses remain inside the same actual command-holder's `O^-`. C167 adds no
controller identity/count and requires no actual human occupancy.

Tight indexed incidences are:

| Branch | Candidate tags | Dual `(k,w)` incidences | Integration-positive / negative histories | Co-legible joint opportunities | Integrated account witnesses | Form memberships / legible pairs | Mechanical pairs / continuations |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| A | 3 | 1 | 1 / 0 | 1 | 1 | 2 / 2 | 2 / 3 |
| B | 3 | 2 per dual tag | 1 / 1 per dual tag | 2 per dual tag | 1 per dual tag | 4 / 4 per dual tag | 4 / 6 per dual tag |
| C | 4 | 3 total | 2 / 1 total | 3 | 2 | 6 / 6 | 6 / 9 |

These are history- or opportunity-indexed witnesses, not necessarily distinct
global accounts, references, assets, panels, buttons, or action definitions.
No branch raises local joint-opportunity width or adds a result, receipt,
decisive cut, definition, exact key, Soul row, same-reference identity, or
simultaneous presentation surface.

Every branch retains nine definitions, twenty-three exact keys, 138 Soul/key
rows at six Souls, four represented locked IDs, eight lived aligned edges,
four one-history-complete IDs, no incomplete represented ID, Souls
`{6,9,10,12,14,15}`, degree six, and C46-A's same-boundary width two.

C167 defers tag identities; exact history counts, fractions, frequency,
access, and ordinary-route prevalence; every-opportunity integration; account
reuse/identity; reference-asset identity; simultaneous presentation; account
counts, salience, order, modality, discoverability, accessibility, detail, and
wording; command-holder identity, actual controller, role stability, and
communication; hinge disclosure; timing, feedback, preview, value, non-
dominance, payoff, power, fun, UI, release, and implementation.

Under recommended A, every-joint-opportunity integrated-account saturation is
the next dependent breadth atom. Under B/C, recompute the history-mixed
frontier.

## Repository state

The owner packet, rigor audit, living head, and systems map carry C166-A and
make C167 the sole active owner choice. The authoritative decision record was
not edited and should retain SHA-256
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.

This was a documentation-only design update. Validate tracked and untracked
whitespace, SVG XML parsing, one active frontier, and the authoritative-record
hash. Do not run the Node suite because no code or executable contract changed.
