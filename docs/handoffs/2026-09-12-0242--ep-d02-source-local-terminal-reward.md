# Handoff — source-local terminal Relic fallback selected

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-12 06:42:39
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.19, the initial display order of a committed
two- or three-candidate duplicate-Relic substitute offer. The owner selected
C3c.18-A: a complete-pool Relic event reroutes through that reward source's
existing non-Relic economy. Preserve accepted EP-D02 until a complete
replacement is replayed and explicitly accepted. Do not implement.

## Selected direction

Every Relic-granting source declares a versioned, nonempty terminal fallback in
its existing non-Relic reward economy. If its eligible Relic pool is complete
for the receiving combatant, the otherwise-earned event atomically resolves
through that same source-local fallback. It creates no Relic item, mastery
receipt, or dedicated Relic currency. Exact fallback contents and value
equivalence remain open.

## Sole active choice

C3c.19 governs only initial presentation after candidate membership is fixed:

- **A — stable neutral catalog order:** the same set always follows one
  versioned catalog order, independent of source weight or combatant state.
- **B, recommended — uniform committed shuffle:** every permutation has equal
  probability; the realized order commits with the offer and cannot be changed
  by reload, reconnect, or replaying that settlement.
- **C — descending source affinity:** highest declared source weight appears
  first, with a later neutral tie rule and at least one reachable order that
  differs from A.

B distributes persistent position/first-card attention bias and avoids implying
that the first card is the source's recommendation. Its cost is a less
predictable layout and another random result persistence must preserve. An
owner replacement may use player-configurable or nonuniform authored ordering.
Candidate membership/weights, exact algorithm/PRNG/seed, layout, animation,
localization, disclosure beyond visible order, timeout/default, acquisition,
custody, transfer, loss, Charm duplicates, migration, and release remain open.

## Verification and preserved state

A narrow read-only audit passed C3c.19 as material, prerequisite-correct,
atomic, mutually exclusive, and coherent with source-weighted membership and
player selection. `git diff --check` passed before this handoff. No code tests
were run because the changes are documentation-only.

The authoritative decision record, system design, and readiness record remain
unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04 directions remain
unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain selected, and
R10.3-R10.8 remain pending. No runtime, capture, evidence, installation, save,
or snapshot was touched.
