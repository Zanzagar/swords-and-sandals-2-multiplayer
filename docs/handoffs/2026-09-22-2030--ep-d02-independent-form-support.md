# Handoff — Decisive-input isolation required; form support next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-22 20:30:06
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.142: whether the completed catalog independently
supports predicate-read relationship facts only, recurrence authorization only,
or both decisive-input forms. The owner selected C3c.141-A. Preserve accepted
EP-D02 wording until a complete replacement is replayed and explicitly
accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Finish the current
full-assurance pass before the standalone expansion/audit/adaptation pass.
Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.141-A

`F^{no-axis-iso}_v` is empty. Every tag and every legal adjacent same-tag
recommit witness contains at least one live/false opportunity whose actual
selected continuation has a C141-qualified pair establishing `I_P(w,d)` or
`I_R(w,d)` in the complete decisive input projection `X^-(chi)`.

`chi` is the earliest aligned canonical semantic evaluation boundary where the
arms differ in whether the next same-tag occurrence becomes provisionally
complete. `I_P` varies a nonempty predicate-read `H`/`S` projection while
recurrence control and every other `q2` input are identical. `I_R` varies
recurrence control while every predicate fact and other input are identical.
One pair changing both proves neither; one opportunity proves both only through
two distinct valid pairs. A semantic coordinate serving both relationship and
authorization roles belongs to both projections and cannot fake isolation.

Only one form is required on a path, and additional bundled comparisons remain
legal. At `chi`, continuing operative context, identical non-target inputs, and
one materially different relationship or authorization projection coexist
**directly**. The whole recurrence remains **partial**, bundled-only exclusion
is **neutral/protective**, and universal prevalence is **aggregate**. The
branch retains `9/23/138`, four represented IDs, eight lived edges, four
complete IDs, no incomplete represented ID, Souls `{6,9,10,12,14,15}`, and
degree six. The authoritative decision record remains unchanged.

## Why catalog form support is next

C141-A establishes the per-path disjunction `I_P or I_R`, but does not identify
which independent form exists anywhere in the completed catalog. Per-tag or
per-path distribution depends on first knowing whether one or both forms are
supported. Continuous-context breadth is orthogonal: temporary context loss
can coexist with any form-support set.

## Active owner choice — EP-D02-C3c.142

Let `K^{ind}_v` contain:

- `Pred` exactly when some candidate tag, legal adjacent-commit witness,
  live/false opportunity, and C141-qualified chosen-arm pair establishes
  `I_P`; and
- `Rec` exactly when some such reachable legal pair establishes `I_R`.

Only complete-projection isolations count. Bundled pairs supply neither form.
One opportunity supplies both only through two distinct valid pairs. Positive
membership needs a reachable witness; absence requires complete reachable-
domain analysis. C141-A and the inherited nonempty tag and recommit domains
make `K^{ind}_v` nonempty.

### A — independent predicate support only

`K^{ind}_v={Pred}`. At least one `I_P` witness exists, and complete analysis
proves that no `I_R` witness exists anywhere. C141-A therefore makes every
legal path contain `I_P`. Recurrence control may remain part of `q2` or a
bundled contrast; it is never the independently varying decisive input.

This is the strongest relationship-first countercase to C. Every independently
attributable reset explanation rebuilds the Soul/Relic bond while recurrence
permission stays a common background seal. It creates one coherent language
and prevents a token permission-button subgame. At `chi`, fixed permission and continuing
context coexist with different relationship facts **directly**. The trajectory
is **partial**, exclusion **neutral/protective**, and universal predicate-only
prevalence **aggregate**. Costs: no pure Invoke/Bank decision after relationship
facts have converged, plus exhaustive proof that no `I_R` witness exists.

### B — independent recurrence support only

`K^{ind}_v={Rec}`. At least one `I_R` witness exists, and complete analysis
proves that no `I_P` witness exists anywhere. C141-A therefore makes every
legal path contain `I_R`. Relationship predicates remain part of `q2`; none is
ever the independently varying decisive input.

This is the strongest invocation-first countercase. Combat evolution may repair
the bond while every independently attributable reset explanation concerns
whether the Soul authorizes another manifestation. At `chi`, identical
relationship facts and continuing context
coexist with different manifestation permission **directly**. The trajectory
is **partial**, exclusion **neutral/protective**, and universal recurrence-only
prevalence **aggregate**. Cost: the relationship can feel mechanically
secondary while play centers on permission management, plus exhaustive proof
that no `I_P` witness exists.

### C — both independent forms supported — recommended

`K^{ind}_v={Pred,Rec}`. At least one valid `I_P` witness and one valid `I_R`
witness exist. C141-A continues to require one form on every path, but C does
not require both per tag, path, or opportunity. The witnesses may belong to
different tags, or one opportunity may prove both through distinct pairs.

C opens two truthful Souls and Simulacra vocabularies: relationship rebuilding
changes what the bond means, while invocation changes whether an already
sufficient bond may manifest again. Each qualifying boundary retains C141-A's
**direct** local fit, and the whole recurrence remains **partial**. Two-form
catalog support is only **aggregate**, not a simultaneous local realization of
the ideal. Cost: two UI/replay/test languages and a sharp token loophole—one
obscure witness of one form formally suffices until a later distribution card.

## Concrete separators

- **`Pred`:** Rekindle versus Vent leaves only predicate-read Heat different at
  `chi`; recurrence control and every other `q2` input are fixed.
- **`Rec`:** Invoke versus Bank leaves every predicate fact and other input
  fixed; only P's occurrence authorization differs.
- **C without breadth:** almost every legal path uses `Pred`, while one obscure
  R17/Soul/Charm/history combination supplies the catalog's sole `Rec` witness.
  That passes C142-C and remains a later distribution problem.

An owner-written replacement may add a genuinely third form only by defining a
semantic projection and complete-projection isolation distinct from `I_P` and
`I_R`; renaming context, phase, a wrapper, or a bundled combination does not
create one. Otherwise A/B/C exhaust the nonempty subsets of `{Pred,Rec}`. A and B derive universal
per-path support for their sole form; C supplies only catalog existence.

## Compatibility, floors, and deferrals

Every branch retains `9/23/138`, four represented IDs, eight lived edges, four
complete IDs, no incomplete represented ID, Souls `{6,9,10,12,14,15}`, and
degree six. C142 adds no receipt, structural content, opportunity, or result-
width requirement.

C142 chooses no per-tag/path/opportunity distribution, minimum count,
prevalence, visibility, controllability, or anti-token breadth; requirement
that one opportunity prove both; extra bundled transitions or their non-target
controls; continuous `c1`-to-`q2` context or outage count; predicate-internal
`H` versus `S` form, exact predicate coordinates, retained strands, source
atoms/reuse, or relationship operator; mediator-path purity; actual evaluator
invocation or failed-evaluation schedule; opportunity count/duration; actor or
authority; sole/immediate/beneficial/complete causation; recurrence phase,
cadence, cooldown, cap, or cross-encounter recurrence; consumption, payoff,
access, power, persistence, release, disclosure/UI implementation, or
implementation generally.

If C is selected, form distribution is the next dependent attribution atom.
Under A or B, recompute the frontier because the sole supported form already
occurs on every legal path. Continuous-context breadth remains an orthogonal
later sibling.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling: follow direct `AGENTS.md` rules
and do not treat the overnight plan as current guidance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C142 only. Record
the selected branch and its consequences, recompute the frontier, and present
exactly one next prerequisite-ready choice. Do not infer acceptance of the
whole EP-D02 replacement or start implementation.
