# Handoff — EP-D04 third direction round pending

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-06 06:32:57
**Branch:** `design/endless-progression-owner-packet`

Resume the corrected three-question EP-D04 R3 frontier in the owner packet.
Preserve accepted EP-D01, EP-D02, and EP-D07 and every selected-but-unaccepted
EP-D04 direction. Do not edit the authoritative decision record yet.

## Session outcome

The owner's `Sounds good` selected the remaining round-two directions; it did
not accept a final replacement. R2 now selects:

1. the projection/state-transition collapse test for chassis versus payload;
2. fun item-local Legendary commitments, with pure-upside v1 exceptions
   deferred absent failed human playtests and an explicit reopened choice;
3. legal player policies rather than fixed-kit or omniscient portfolio results;
4. separate causal best-in-slot guarantees for Tempered and Inscribed; and
5. combat/full-clear success first, with later Pareto safeguards.

## Prominent correction to this session's first R3 draft

The root initially drafted four questions and called them independent. That was
wrong. A read-only adversarial check showed that the comparison horizon
determines the cell hierarchy, which determines whether uniform semantic
weighting is even defined. The draft's compound format choice also silently
turned R2.4's two combined-grid rarity guarantees into six
rarity-by-format obligations, and the legal-policy tuple omitted route-offer
choice. The wrong draft was replaced at the instruction in both the owner
packet and `HANDOFF.md`; it was never presented as an owner choice.

The corrected legal policy is
`pi = (pi_offer, pi_entry, pi_branch, {pi_seat})`. It separates raw generated
offers from legally selected/experienced routes and prevents centralized
training from claiming a result through hidden shared planner state or
cross-seat authority unavailable to distinct human custodians. The precise
communication contract remains `[U]`. One evaluation run must also pin all
definition, generator, rule, Capacity, Circuit/adaptation, RNG/information,
Branch, and Team-Tactic versions; versions are never pooled.

## Correct R3 frontier

1. **Primary evaluation episode:** one unassisted paid-route Circuit with one
   immutable attempt per encounter (`A`, recommended); a finite multi-attempt
   live journey (`B`); or unbounded eventual completion (`C`). Defeat or
   Concede fails A's primary episode; Rematch, Assistance, and Recovery are
   separate robustness views. The eventual D03 length/transition version is
   referenced rather than assuming four permanently.
2. **All-Legendary format scope:** require its higher endpoint ceiling
   separately in 1v1, 2v2, and 3v3 (`A`, recommended); use a pooled gate with
   per-format floors (`B`); or gate only first-playable 2v2 (`C`). D06 can still
   order implementation proof around 2v2 without weakening the catalog rule.
3. **Lower-rarity format scope:** require each Tempered/Inscribed niche in every
   format (`A`); require some strict mixed win in every format while Tempered
   and Inscribed each satisfy its separate guarantee over the combined grid
   (`B`, recommended); or permit formats with no mixed strict win (`C`).

Recommendations are `1A, 2A, 3B`. After these answers, recompute the cell
hierarchy. If R3.1-A is selected, use ordered Circuit fingerprints with nested
encounter cells and seeds as replicates; then decide uniform-mechanics, raw
offer-generator, and legally experienced distribution authority. Numeric
coverage, all-Legendary lead/concentration bounds, ties, solver sufficiency,
unresolved-cell treatment, migration/release authority, and final replay remain
later dependencies.

## Verification

`git diff --check` passed after this handoff. The authoritative decision record
has no diff. No runtime code, tests, vanilla evidence, licensed assets,
installation, save, snapshot, capture, candidate, fixture, observation,
manifest, or golden changed. Tests were not run for this docs-only owner round.
No commit or push was made. Implementation remains blocked.
