---
handoff:      2026-09-26-1919--relic-intrinsic-related-permission-selected
written:      2026-09-26 19:19 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    three bounded read-only D2D prerequisite audits, two recommendation cross-challenges, and one strict read-only final diff verifier
branch:       design/endless-progression-owner-packet
commits:      b59ac5f..HEAD
suite:        proportional serial checks 10/10 passed; register reparses 94/94 with Phi_SR 35; decision-record hash unchanged
supersedes:   2026-09-26-1901--relic-authoritative-related-exposure-selected
---

# Handoff — intrinsic related-only permission selected

Start with `HANDOFF.md` above its archive line and
`$ss2-progression-design`. Present D2D only. Do not skip to D2E incidence or
G2 cross-pair coupling.

## Where things stand

The owner selected `RCS-03C3C3D2A-A`. There is no matched reachable related-
only opportunity `q∈Q_v` and pair of lawful prospective participant settings
that change its complete permission law while the versioned Relic/relationship
contract, facts, selected authoritative tuple, and unordered pair remain
fixed. `Q_v` may be empty. Different Relics, relationships, and factual
contexts may still carry different intrinsic laws. D2A-A selects no
determinism, incidence, result, or payoff.

D2A is `DIR-SELECTED`; D2B, D2C1, and D2C2 are `PRUNED`; D2D is
`OWNER-OPEN`. The 94-row register contains 34 `SCREEN`, 1 `OWNER-OPEN`, 16
`PRUNED`, 39 `DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`;
`Phi_SR = 35`. The authoritative decision record remains unchanged.

Three bounded read-only audits independently tested D2D's smallest semantic
topology, gameplay value, and timing/replay/downstream authority. Two
cross-challenges attacked their only disagreement, the recommendation. They
agreed D2D is one prerequisite-ready empty-aware card with no hidden row.

## Present exactly this next

Let `W^{Q-perm-law}_v` contain one projection for each canonical complete
intrinsic permission law with a nonempty reachable related-only domain
`O^Q_w`. `W` may be empty. Contexts, instances, callbacks, helper functions,
eager sampling, reload copies, or separate allow/deny handlers cannot multiply
one semantic law.

For each law and opportunity, fix the versioned contract/law, Relic and
relationship identities, all prospective bindings, genuine law-defining
committed variants, selected authoritative tuple, unordered pair and proof
relation, complete factual context, and all earlier committed authoritative
state regardless of random origin. Let `pi^Q_w(q)` be the allow probability
immediately before the earliest semantic realization committing this bit.
Define

`G^{Q-perm-stoch}_v={w∈W^{Q-perm-law}_v : some q∈O^Q_w has
0<pi^Q_w(q)<1}`.

- **A — deterministic-only related-only laws:** `G=∅`; `W` and `Q_v` may be
  empty. Every extant projection returns a certain zero or one on every fixed
  opportunity, although the law may respond deterministically across factual
  contexts. This is the safest coherent option and C's mandatory fallback. It
  protects coordination, replay trust, causal explanation, and policy
  comparison, but gives up oracular suspense and makes contextual authoring do
  all the work.
- **B — every extant related-only law is stochastic-capable:** `W≠∅` and
  `G=W`. Every projection has at least one reachable nondegenerate case, though
  not every opportunity must roll. This supplies pervasive numinous uncertainty
  but taxes every related-only build with variance and universalizes trust,
  explanation, simulation, and balance burdens. Do not recommend B.
- **C — deterministic-only and stochastic-capable related-only laws coexist:**
  `∅⊊G⊊W`. At least one complete projection is deterministic throughout its
  reachable `Q_v` domain and at least one distinct projection has a reachable
  nondegenerate draw. C entails at least two projections; deterministic hybrids
  lie outside the denominator and cannot supply its comparator. **Recommend C
  conditionally.** It creates reliable Witness and optional Oracle Relic
  identities rather than making chance universal.

Examples:

- **Stone Choir (A/deterministic side of C):** the related-only `Guard Pulse`
  and `Riposte Mark` pair certainly passes while visible Vow Integrity remains
  intact and certainly fails after it breaks. The same fixed opportunity always
  returns the same bit.
- **Dreamglass catalog (B):** every related-only law has some visible Unsettled
  Resonance region with a real draw—illustratively one fixed case is 70/30—
  although other contexts may remain certain.
- **Veiled Chorus (stochastic side of C):** one intrinsic related-only region
  receives one visible nondegenerate draw. Choosing the Oracle, managing
  exposure, and hedging denial can form a risk policy; D2D does not require
  direct odds control or decide the downstream reward.

One law random in one context and certain elsewhere is one stochastic-capable
law, not C. One law returning deterministic zero and one in different contexts
remains deterministic. Positive B/C entail related-only support. A stronger
requirement that every related-only opportunity roll is a replacement path,
not hidden inside B.

## Conditional-C obligation

C carries a deferred hard validation obligation:

- both required families must furnish reachable, mechanically legible, viable,
  non-token build or policy purposes;
- the stochastic identity must causally and materially change at least one
  truthful disclosed legal build or play policy;
- neither required family may be everywhere weakly dominated in the actual
  lawful build/route policy space; and
- plan-relevant risk must be understandable before the relevant irreversible
  choice and attributable afterward.

An epsilon-probability showcase, outcome-equivalent coin, opaque gotcha,
voluntary handicap, or raw-power package whose stochastic identity never
changes a preferred policy fails. Existing SR-05/SR-06/SR-09,
RCS-02/SR-10, SR-12, and RCS-17/RCS-18 own authoring and evaluation. If either
required family or their coexistence fails this obligation, reopen D2D with A
as the bounded replacement; do not fall back silently.

The distinct children and their real shared parent—not randomness—supply any
direct *Achintya Bheda Abheda* fit. Determinism is neutral/protective,
randomness is at most partial/expressive, and C's coexistence is aggregate.

## Hard boundaries

- One `q` receives one committed realization. Callback duplication,
  serialization, reload, reconnect, replay, denial, or routing fallback cannot
  redraw it, retry selection, or substitute another ledger.
- An opaque deterministic hash of fixed authoritative inputs is deterministic.
  Earlier random acquisition or persisted variants are already-fixed state. A
  generator may implement the one declared permission draw.
- D2A-A forbids a matched-case odds setting or direct odds command. The one-
  commit replay invariant separately forbids rerolls. D2D does not choose seed
  authority; later seed rules may not create outcome-shopping or redraw.
  An immutable law may read independently meaningful factual contexts, but D2D
  does not require action-responsive odds; D2E audits that later.
- Exact and hybrid permission remains intrinsic and deterministic. Never add a
  second related draw, vote, veto, or precedence rule to a hybrid.
- G2 alone owns correlation across distinct simultaneous nonconstant pair
  marginals. E/F own false-bit results and selection authority. RCS-02/SR-10
  own information; RCS-15/RCS-16 own persistence and migration; RCS-17/RCS-18
  own balance, simulation/RL, fun, trust, exploit, and playtest acceptance.
- D2D selects no odds, randomizing context, actor, factual-response requirement,
  entropy source, disclosure surface, incidence, result, compensation, payoff,
  rarity, placement, or implementation.
- Do not edit the authoritative decision record without an explicit
  `accepted`, `rejected`, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Projected transition

Any answer moves D2D to `DIR-SELECTED` and sends D2E through its fresh
prerequisite audit. Before any D2E amendment:

- B/C produce 33 `SCREEN`, 1 `OWNER-OPEN`, 16 `PRUNED`, 40 `DIR-SELECTED`,
  2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`; `Phi_SR = 34`.
- A also prunes G2 immediately because every exact, hybrid, and related-only
  pair marginal becomes deterministic: 32 `SCREEN`, 1 `OWNER-OPEN`, 17
  `PRUNED`, 40 `DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`;
  `Phi_SR = 33`.

B/C do not activate G2; they prove neither three-plus contenders nor two
simultaneous nonconstant marginals.

## Verification and Git

Confirm 94 unique rows with the exact current counts above and `Phi_SR = 35`.
Confirm the decision-record SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
Commit atomically with a `Co-Authored-By` trailer, push only the feature branch,
verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
