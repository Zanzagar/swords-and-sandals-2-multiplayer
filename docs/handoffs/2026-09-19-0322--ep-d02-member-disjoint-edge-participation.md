# Handoff — disjoint-family support selected; actual participation next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-19 03:22:28
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.103: whether none, some, or every union-locked
definition actually authors a member-disjoint common edge. The owner selected
C3c.102-C. Preserve the accepted EP-D02 wording until a complete replacement
is replayed and explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting there. Separate states,
keys, definitions, catalog entries, labels, or cosmetics that respectively
supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate standalone-game expansion, audit, and adaptation
pass. Vanilla evidence and the byte-identical measurement oracle remain in
their SS2 lane.

## Selected direction — EP-D02-C3c.102-C

Every union-locked definition structurally supports two member-disjoint
complete noncore map families. Each name therefore has both C99's capacity for
an overlapping-family common pair and capacity for complete family-member
replacement. C102-C authors no exact key or edge.

Supported-Soul counts are `{6,9,10,12,14,15}`; eight Souls are excluded. Tight
floors remain six definitions, nine exact keys, and fifty-four six-Soul rows,
and every locked definition now requires degree six. Fixed-key fit remains
direct, C102 support neutral, and universal support prevalence aggregate. The
authoritative decision record remains unchanged.

## Frontier audit

Actual definition participation is the immediate prerequisite-ready
descendant. Define the relation directly:

`R^W_v(d)={{b,b'} in W^S_v(d):M_v(b) intersect M_v(b') is empty}`

and measure it inside the capability domain:

`D^{act-disj-W}_{locked,v}={d in D^{cap-disj-M}_{locked,v}:R^W_v(d) is nonempty}`.

C102-C makes that denominator equal to the whole locked class. The direct
definition is safer than defining `R` only as `W\O`: under C95-C they are
equal, but a future reopening of family injectivity must not misclassify an
equal-family edge as complete family-member replacement.

C101-C already gives every completed key an overlap-bearing common partner.
Therefore no two-key definition can participate in `R`: its sole pair cannot
be both overlap-bearing and member-disjoint. A participant needs at least three
keys, and the witness below proves three sufficient. Connectedness remains
lateral; predicate/receipt continuity and controlled substitution still need
a cross-key semantic correspondence, comparable boundary, exact-key cause,
and comparable acquisition/access.

## Active owner choice — EP-D02-C3c.103

```text
THREE-KEY SIX-SOUL PARTICIPANT — degree 6, profile (2,2,2)
core h omitted; U={a,b,c,d,e}

b0  F,S->abc   V,W->abd   T,R->cde   M0={abc,abd,cde}
b1  V,T->abc   F,W->cde   S,R->abe   M1={abc,abe,cde}
b2  F,V->abe   S,T->acd   W,R->bcd   M2={abe,acd,bcd}

b0/b1 is an overlap-bearing common edge.
b1/b2 is an overlap-bearing common edge.
b0/b2 is a member-disjoint common edge.
```

### A — no locked definition realizes complete family-member replacement

`D^{act-disj-W}_{locked,v}` is empty. Every authored common edge retains at
least one exact family member despite every locked name's latent disjoint
capacity.

A gives the cleanest authored motif-survival covenant, best protects
continuity at the narrow exact-family-member boundary, and permits two-key
names. Its cost is leaving C102-C entirely latent and forbidding the sharpest
same-name contrast. Tight floors are `6/9/54`.

### B — realizing and nonrealizing definitions coexist

`D^{act-disj-W}_{locked,v}` is a nonempty proper subset of the capability
class. At least one name contains both edge types, while another capable name
exhaustively authors only overlapping-family common edges.

B reserves complete family-member replacement for advanced names beside
simpler motif-survival names. Its costs are another hidden behavioral subtype,
at least two locked definitions and five locked keys, and token risk unless the
difference becomes visible and valuable. Tight floors are `7/12/72`.

### C — every locked definition realizes it — recommended

`D^{act-disj-W}_{locked,v}=D^{cap-disj-M}_{locked,v}=D^{U=}_{locked,v}`. Every
locked name contains a member-disjoint common edge while C101-C still gives
every completed key an overlap-bearing common partner.

C earns universal capacity and gives every name both authored relational
dialects: motif migration and complete family-member replacement. No form loses
its continuity-bearing foil. Costs are a three-key minimum plus heavier
comparison, disclosure, and validation work; the disjoint edge could still be
obscure, inaccessible, or dominated. Tight floors are `6/10/60`.

## Exact boundary

All three branches retain supported-Soul counts `{6,9,10,12,14,15}` and tight
maximum degree six. The inherited outside-locked base has five definitions and
seven keys. A adds a two-key nonparticipant; B adds a two-key nonparticipant
and a distinct three-key participant; C adds one three-key participant. The
triangle construction extends over the selected legal profiles for every
remaining roster, and the alternate `(degree,k,rho)=(6,4,5)` support geometry
at ten and fifteen Souls admits the same overlap/overlap/disjoint topology.

At one fixed exact-key/Soul relationship, inherited fit remains direct. C101's
overlap-bearing same-Soul and shared-member relations are partial. A C103
member-disjoint edge is partial at the broad stable-definition/core/exact-
union/atomic-vocabulary boundary but neutral at exact-family-member continuity
because no exact member survives. Definition participation prevalence is
aggregate. C is recommended for gameplay contrast, not a stronger doctrinal
rating; A is the strongest narrow thematic countercase.

C103 selects no member-disjoint-edge coverage of every key, edge count or
fraction, every-edge rule, graph connectedness, exact family or assignment,
predicate/receipt continuity, controlled substitution, comparable access,
C39 witness, transformation, disclosure, agency, value, sidegrade quality,
power, persistence, release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C102-C
as selected and C103 as the sole active owner choice. The authoritative
decision record is unchanged. EP-D01, EP-D02, and EP-D07 remain accepted;
EP-D04 directions remain unaccepted, R9.4 remains reopened, R10.1-A and
R10.2-A remain selected, and R10.3-R10.8 remain pending.
