# Handoff — every represented fiber has a common edge; key-complete carrier next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-20 03:20:03
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.116: whether no, some but not all, or every union-
locked Soul Relic definition has at least one represented persistent-instance
fiber whose every completed key has an internal all-Soul-changing partner. The
owner selected C3c.115-C. Preserve the accepted EP-D02 wording until a complete
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the current
full-assurance pass before the separate standalone expansion/audit/adaptation
pass. Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.115-C

For every locked definition `d`,

`Ids^W_v(d)=Ids_v(d)`,

and therefore

`D^{all-IdW}_{locked,v}=D^{U=}_{locked,v}`.

Every represented persistent instance ID in the completed fixed-version domain
contains at least one internal pair of snapshots on which every supported
Soul's assigned noncore map differs. This does not cover every key or pair in a
larger fiber and does not quantify over inventory IDs absent from the completed
domain. It establishes no cause, transition, common access, or control.

Tight floors remain seven definitions, fifteen exact keys, and ninety six-Soul
rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree remains
six. Each qualifying edge is partial at the represented-persistent-artifact/
across-snapshot boundary, while universal fiber and definition prevalence is
aggregate. The authoritative decision record remains unchanged.

## C116 frontier correction

The first internal post-C115 draft returned to one-coordinate/common-edge
alignment. Read-only frontier review rejected that jump before owner
presentation. C115-C guarantees one edge in every represented fiber but does
not guarantee that even one fiber covers all of its keys. This is the direct
common-edge analogue of the existence-before-all-fiber order used at C110 and
C111. The typed draft selected nothing and remains a later refinement.

Independent frontier, construction, and gameplay checks confirmed:

- empty, proper, and full definition prevalence are all feasible;
- a tight nonqualifying definition needs two three-key fibers;
- A/B/C have exact floors `7/19/114`, `7/17/102`, and `7/15/90`;
- all branches retain Souls `{6,9,10,12,14,15}` and degree six;
- C is recommended for one structurally self-contained carrier per name;
- A is the strongest principled artifact-individuality countercase.

## Active owner choice — EP-D02-C3c.116

For represented persistent instance ID `r` of locked definition `d`, define

`K^W_v(d,r)={b in B_{v,r}(d):there exists b' in B_{v,r}(d) with {b,b'} in W^S_v(d)}`,

`Ids^{KW}_v(d)={r in Ids_v(d):K^W_v(d,r)=B_{v,r}(d)}`,

and

`D^{exists-IdKW}_{locked,v}={d in D^{U=}_{locked,v}:Ids^{KW}_v(d) is nonempty}`.

C115-C makes `K^W_v(d,r)` nonempty for every represented fiber. It need not
equal that fiber. A key outside it has no internal same-ID all-Soul-changing
partner, though C98-C still gives it a same-definition partner under another
persistent instance ID.

### A — no locked definition has a key-complete fiber

`D^{exists-IdKW}_{locked,v}` is empty. Every represented fiber in every locked
name has an internal common edge and an internally uncovered key. This is the
strongest principled countercase if every artifact should contain both a
radical internal contrast and a witness-retaining specialist form.

Costs are at least two represented IDs and six exact keys per locked name,
mandatory artifact-local incompleteness, and reliance on cross-ID foils whose
common acquisition and access are unselected.

### B — qualifying and wholly incomplete definitions coexist

`D^{exists-IdKW}_{locked,v}` is nonempty and proper. At least one name has a
key-complete persistent-artifact carrier, while another name's every fiber is
positive-but-incomplete. This can support deliberate variety if the two name
languages are disclosed, accessible, and non-dominated, but adds hidden
taxonomy and premium-name or premium-copy risk.

### C — every locked definition has a key-complete fiber — recommended

`D^{exists-IdKW}_{locked,v}=D^{U=}_{locked,v}`. Every locked name has at least
one represented persistent instance for which every completed form has some
internal all-Soul-changing partner. Other represented fibers may remain
positive-but-incomplete.

C gives every name one structurally self-contained comparison carrier without homogenizing
every represented artifact yet. Costs are a token carrier loophole, tiny-two-
key-fiber laundering, and premium or inaccessible carrier risk until access,
disclosure, and value are selected.

## Verified witnesses and floors

The tight nonqualifier is a six-key `[3,3]` definition:

```text
n0  FT->abc   SV->abe   WR->cde
n1  FR->abc   SV->acd   WT->bde
n2  FR->ace   SV->abe   WT->bcd
n3  FR->ade   SV->abc   WT->bde
n4  FT->abd   SW->abc   VR->cde
n5  FV->abd   SR->bce   WT->ace

R17={n0,n1,n2}   sole internal W edge n1--n2; n0 uncovered
R44={n3,n4,n5}   sole internal W edge n3--n5; n4 uncovered
```

Overlap-bearing common edges `n0--n3`, `n1--n4`, and `n2--n5` cover every
key. Member-disjoint common coverage includes `n0--n5`, `n1--n2`, `n2--n3`,
`n2--n4`, and `n3--n5`. Thus C98, C101, C105, and C115-C all hold while no
fiber is key-complete. Fix one coordinate across each triplet and vary the
other for a C113-A sole-type clique; transpose coordinates for the other name
class.

The C105 four-key witness partition
`R17={b0,b2}, R44={b1,b3}` gives two key-complete member-disjoint common dyads.
The overlap-bearing partition `{b0,b3}/{b1,b2}` works as well.

A separate verified five-key `[2,3]` construction gives one complete fiber and
one positive-but-incomplete fiber. It proves C does not imply all-fiber or
global per-key same-ID coverage:

```text
c0 [R17] FS->bcd  VT->ade  WR->abe
c1 [R17] FV->ace  SR->bde  WT->abd

c2 [R44] FV->bde  SR->ace  WT->bcd
c3 [R44] FT->cde  SV->abd  WR->abe
c4 [R44] FR->cde  SW->abc  VT->bde
```

`R17` is complete through `c0--c1`. Inside `R44`, only `c2--c3` is common,
leaving `c4` uncovered. Every key still has overlap-bearing and member-
disjoint global partners.

A nonqualifying fiber needs at least three keys because a positive two-key
fiber is automatically complete. A nonqualifying definition needs at least
two fibers because, with one ID, C98-C's global per-key coverage is wholly
internal. Hence six keys per negative name is tight. The inherited outside-
locked base has five definitions and seven keys; C113-A requires two locked
names.

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 7 | 19 | 114 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 17 | 102 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |

At one fixed exact-key/Soul relationship, inherited fit remains **direct**.
Each actual internal common edge is **partial** at the represented-persistent-
artifact/across-snapshot boundary. An internally uncovered key is **neutral/
protective** at the fiber-local whole-roster-change projection. Its C98-
guaranteed cross-ID foil is **partial** at the broader stable-definition/
across-persistent-simulacra boundary. Fiber completeness and definition
prevalence are **aggregate**. C is recommended for systematic carrier
participation, not for a stronger direct doctrinal score.

C116 selects no all-fiber or global per-key same-ID common-edge coverage,
every-pair coverage, typed/one-coordinate alignment, overlap/member-disjoint
identity, exact fiber count or size beyond branch-derived minima, graph
topology, semantics, cause, transition, path, access, disclosure, agency,
value, power, persistence, release, or implementation.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C116 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
