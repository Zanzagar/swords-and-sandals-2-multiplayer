# Handoff — universal Soul Relic phase attribution

**Session ID:** `72534467-4493-4e32-9856-6f79ae638153`
**UTC:** 2026-09-24 16:19
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`; present only `RCS-03C3B1A1B1` next.

## Owner answer recorded

The owner's latest `A` selected `RCS-03C3B1A1A-A`. Every temporally H-pure
history position has at least one C3c.33-valid isolation confined to accepted
declaration/commitment (`D`), execution/performance (`E`), or authoritative
outcome/disposition (`O`) claims. No history position may be phase-coupled-only.

At each position, one phase claim and the predicate participate **directly**;
the witnessed phase remains mechanically distinct; continuity with the larger
source operation is **partial**; excluding coupled-only local identity is
**neutral/protective**; and universal attribution is **aggregate**. The answer
does not choose the phase identity, additional independent phase routes, or a
separate irreducibly multi-phase path. It changes no authoritative decision
wording and authorizes no implementation.

## Ninth register amendment

Post-answer screening found that old `RCS-03C3B1A1B` compressed two
player-material boundaries:

1. whether positions with at least two independently isolatable phases are
   absent, universal, or mixed with phase-singular positions; and
2. whether plural positions independently isolate exactly two phases or all
   three.

On reachable `(D,E,O)` valuations `{000,100,010,001}`, worlds
`X2: p=D OR E, q=E OR O` and `X3: p=D OR E OR O, q=E OR O` agree that every
position is plural, support catalog phases `{D,E,O}`, and have no irreducibly
multi-phase package. Only X3 gives `p` three independent timing routes. The
difference changes readiness timing, counterplay, disclosure, and
cheapest-route dominance; it is not an exact cardinality used only to make a
proof stronger.

Old `RCS-03C3B1A1B` is now a non-counting parent for
`RCS-03C3B1A1B1` and conditional `RCS-03C3B1A1B2`. `RCS-03C3B1A2` follows the
applicable child, then `RCS-03C3B1B` recomputes catalog phase support.

Exact pair identities `{D,E}`, `{D,O}`, and `{E,O}` remain catalog authoring
after topology and support are selected. A named-pair mandate, incidence quota,
or exact percentage does not become another owner card without a separate
global player-contract consequence.

**Agent correction:** the eighth-amendment row was named for local phase
multiplicity while making only singleton-versus-plural membership explicit.
The X2/X3 countermodel holds minimum attribution, irreducible coupling, and
catalog support fixed while changing one position's legal timing surface. The
missing tri-phase boundary was restored before the next choice.

The register has 49 rows: 31 `SCREEN`, 1 `OWNER-OPEN`, 7 `PRUNED`, 8
`DIR-SELECTED`, 1 `SPEC`, and 1 `EVALUATE`; therefore `Phi_SR = 32`.

## Next card

For each `p` in `T_H`, let

`L(p)={F in {D,E,O} | I^H_F(p)}`

and let `M={p in T_H | |L(p)| >= 2}`. A1A-A makes every `L(p)` nonempty.
Distinct members require distinct C3c.33-valid isolation pairs; multiple claims
inside one phase, record aliases, shared cuts, and cross-role views of one
transition do not multiply routes or sources.

- A: `M` is empty. Every history position independently isolates exactly one
  phase. This gives the clearest temporal identity and denial window.
- B: `M=T_H`. Every history position independently isolates at least two
  phases. This maximizes flexibility but gives every position multiple timing
  and explanation surfaces.
- C: `M` is a nonempty proper subset. Phase-singular and phase-plural positions
  coexist.

Recommend C. Singular positions teach precise timing and clean counters;
selected plural positions provide signature multi-face Relic play without
making every remembered source listen to multiple stages. Coexistence is
**aggregate**, and multiplicity itself earns only a **partial** Achintya Bheda
Abheda fit because its witnesses may occur on different histories. Under A,
conditional B1A1B2 is pruned; under B or C, it next decides
exactly-two-versus-all-three prevalence. B1A2 separately owns jointly required
cross-phase paths.

## Validation

- Structural target: 49 register rows and `Phi_SR = 32`, with only
  `RCS-03C3B1A1B1` marked `OWNER-OPEN`.
- Independent read-only reviews passed the X2/X3 countermodel, A/B/C topology,
  proof burdens, source-role guard, gameplay examples, dependency order, active
  pointers, and register arithmetic.
- Full suite: 584 tests, 583 passed, 0 failed, 1 expected raw-trace archive
  skip.
- `git diff --check` passed.
- `docs/design/endless-progression-decisions.md` remains byte-identical at
  SHA-256 `e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
- No runtime, Ruffle, capture, installation, save, snapshot, fixture,
  candidate, observation, manifest, or golden work was performed.
