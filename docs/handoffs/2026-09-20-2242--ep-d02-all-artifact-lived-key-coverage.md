# Handoff — one complete lived carrier per name; all-artifact coverage next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-21 02:42:31
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.128: whether every represented artifact is lived-
key-complete in no locked definitions, some but not all locked definitions, or
every locked definition. The owner selected C3c.127-C. Preserve accepted
EP-D02 wording until a complete replacement is replayed and explicitly
accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the
current full-assurance pass before the separate standalone expansion, audit,
and adaptation pass. Vanilla evidence and the byte-identical oracle remain in
their own lane.

## Selected direction — EP-D02-C3c.127-C

Every locked name contains at least one represented artifact whose complete
key fiber is covered by internal lived aligned edges:

`D^{exists-IdK-hist-X}_{locked,v}=D^{U=}_{locked,v}`.

Other represented artifacts may remain lived-positive but incomplete. Separate
covering edges may use mutually incompatible histories, so the answer does not
put all forms in one history. Tight floors remain nine definitions, twenty-
three exact keys, and 138 Soul/key rows at six Souls, with as few as four
represented locked IDs and eight lived aligned edges. Soul counts remain
`{6,9,10,12,14,15}` and tight maximum degree remains six. Each edge is partial
at the fixed-coordinate and same-owner/same-artifact/across-history boundaries;
carrier completeness and definition prevalence are aggregate. The
authoritative decision record remains unchanged.

## Active owner choice — EP-D02-C3c.128

Define

`D^{all-IdK-hist-X}_{locked,v}`

`  ={d in D^{U=}_{locked,v}`

`      : Ids^{K-hist-X}_v(d)=Ids_v(d)}`.

Because represented fibers partition the completed key set and every lived
aligned edge is same-ID, this is exactly equivalent to every completed locked
key having a lived aligned partner. C127-C makes the complete-carrier set
nonempty for every name but does not make it universal across represented
artifacts.

### A — every name is artifact-hybrid

`D^{all-IdK-hist-X}_{locked,v}=empty`. Every name contains at least one
complete carrier and at least one positive-but-incomplete artifact. This is the
strongest principled countercase: complete and fate-fractured simulacra are an
intentional property of every lineage. Under one-copy, no-transfer, permanent
ownership it otherwise mandates a copy-quality lottery.

### B — all-complete and artifact-hybrid names coexist

The set is nonempty and proper. At least one name makes every represented
artifact complete, while another contains both a complete carrier and an
incomplete artifact. This adds a likely premium-name taxonomy over the
remaining premium-copy surface.

### C — every represented artifact is lived-key-complete — recommended

`D^{all-IdK-hist-X}_{locked,v}=D^{U=}_{locked,v}`. Every completed form of
every represented artifact has at least one internal lived aligned partner.
The acquired copy can no longer exclude one of its completed forms from the
name's lived relationship. This closes the remaining premium-copy and orphan-
form loopholes, at the cost of universal foil authoring and less room for
deliberately fate-fractured incarnations.

## Separator and tight floors

```text
ONE NAME — C127-COMPLETE BUT C128-HYBRID

R17={a0,a1}       a0--a1 is lived; R17 is complete

R44={b0,b1,b2}    static aligned edges cover all three forms;
                   only b0--b1 is lived, leaving b2 uncovered
```

The name passes C127 through R17 and both artifacts are C126-positive. It fails
C128 through R44. Exact `[2,3]` constructions were independently enumerated
for both overlap-only and member-disjoint-only names while preserving broader
opposite-family key coverage and every retained Soul profile.

| Branch | Definitions | Keys | Six-Soul rows | Represented locked IDs | Lived edges |
| --- | ---: | ---: | ---: | ---: | ---: |
| A | 9 | 27 | 162 | 8 | 8 |
| B | 9 | 24 | 144 | 5 | 8 |
| C | 9 | 23 | 138 | 4 | 8 |

A full name is tight at four keys on one ID with two lived matching edges. A
hybrid name is tight at `[2,3]`: its complete carrier needs two keys and its
positive-but-incomplete fiber needs three, with one lived edge in each. C124-A
requires four pure compound names. A therefore uses four hybrids; B uses one
hybrid and three full names; C uses four full names. C120's four-key lower
bound concerned the static aligned intersection and does not apply to this
sparser temporal subgraph. `[2,4]` is legal but non-tight.

Every lived edge is **partial** at both the fixed-coordinate and same-owner/
same-artifact/across-history boundaries. A lived-uncovered form is **neutral/
protective** only at lived incidence; its static aligned relationship remains
partial. All-fiber/global-key prevalence is **aggregate**.

Even C permits one obscure partner per form, disconnected lived dyads, and
different covering edges witnessed by mutually incompatible histories. C128
selects no one common history, direct transition, direction, causation,
reversibility, player choice, disclosure, frequency, value, power, release, or
implementation. Exact state/boundary semantics are prerequisites before causal
or agency cards, not before this incidence classification.

## Frontier discipline

The lived-incidence ladder is now:

1. C125 definition-level lived-edge existence — selected C.
2. C126 represented-instance lived-edge incidence — selected C.
3. C127 complete-carrier existence per name — selected C.
4. C128 all-fiber/global-key lived coverage — active.

Do not silently turn C128 into one common history, graph connectedness, direct
transformation, or player-controlled evolution. Those are separate descendants
or siblings with additional prerequisites.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling: follow direct `AGENTS.md` rules
and do not treat the overnight plan as current guidance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C128 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
