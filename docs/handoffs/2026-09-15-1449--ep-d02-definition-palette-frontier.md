# Handoff — C70 class purity selected; definition palette next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-15 18:49:57
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.71: whether every named Relic keeps one total
native-dual six-cell palette across all of its exact operative realizations in
a completed version. The owner selected C3c.70-A. Preserve the accepted EP-D02
wording until a complete replacement is replayed and explicitly accepted. Do
not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedābheda*) as the enduring design ideal. At any operative
relationship represented by a mechanic, prefer meaningful continuity or
participation and real, mechanically legible distinction coexisting at that
same boundary. Merely distributing “unity” and “difference” across different
states, keys, definitions, catalog entries, labels, or cosmetics does not
satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. If no option remains consistent,
offer or recommend a replacement rather than weaken the north star. Continue
the exact one-at-a-time A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle remain in their SS2 lane.

## Selected direction

`D_{cross,v}` is empty. Every operative Relic definition is one-/multi-anchor-
class-pure inside a completed version. The required prism-multi, affinity-multi,
and one-anchor classes therefore occupy at least three separate definitions:

```text
Dreamglass -> PRISM-MULTI only
Ashen      -> AFFINITY-MULTI only
Echo       -> ONE-ANCHOR only
```

C69-A continues to keep each multi-support definition wholly prism or wholly
affinity across its multi keys. C70-A does not forbid same-definition
variation inside one class and selects no variation cause, transformation,
coexistence, live switching, label continuity, disclosure, power, or
implementation.

## Count correction that must remain scoped

The first C70 presentation called the local A/B/C exact-key minima `3/3/4` the
tight general floor without qualifying C39-C. C70's per-version class-support
minima really are `3/3/4`. C39-C additionally requires two materially different
same-definition instances in one fixed witness version. Therefore the selected
C70-A branch needs at least four exact keys in **some C39-witness version**,
although another completed version may still attain the three-key local floor.
C39 is existential across the global versioned domain, not universal over each
fixed `R_v^*`; C47 explicitly warns against projecting global support into an
arbitrary version slice. Do not replace this with an unconditional per-version
four. The owner selected A's topology, not the earlier count phrasing; no
direction changed.

## Active frontier

C57-A already defines one Soul-stable total palette `C_v(b)` for every exact
operative key. Let

`D_v^+=def_v(R_v^*)`

and

`D^C_{var,v}={d in D_v^+ : some b_1,b_2 have def_v(b_1)=def_v(b_2)=d and C_v(b_1)!=C_v(b_2)}`.

- **A, recommended — every definition is palette-stable:**
  `D^C_var` is empty. Each named Relic keeps one outer six-cell recipe
  vocabulary; C39's personal artifact differences remain possible through
  materially different predicates within shared cells. C3c.52-B's required
  palette variation occurs between definitions. Inherited ideal fit is direct
  inside each fixed key/Soul relationship; C71's marginal fit is neutral and
  protective. Cost: a same-name key cannot open or close an entire cell.
- **B — stable and palette-variable definitions coexist:** `D^C_var` is
  nonempty proper. This permits rare transformative simulacra but adds a
  definition taxonomy and token-edge risk. Its marginal fit is a partial
  alternate-key metaphor, not simultaneity.
- **C — every definition is palette-variable:** `D^C_var=D_v^+`. Every name
  needs at least two palettes. This maximizes form variety but makes comparison,
  disclosure, authoring, and migration burdens universal. Its marginal fit is
  merely aggregate; the stable ID can become only a label.

For cell labels `a..f` and Souls `F/S/V`, a prism key can use
`F={a,b}, S={b,c}, V={a,c}` and an affinity key
`F=V={d,e}, S={d,f}` with sole core `d`. A one-only key can keep sole
singleton `{a}` inside total palette `{a,d}`. Under A, two Dreamglass instances
keep the same `{a,b,c}` palette but may differ materially in their fixed-cell
predicate. Under B, Dreamglass may instead use `{a,b,c}` and `{a,b,d}` while
Ashen/Echo stay stable. Under C, Dreamglass, Ashen, and Echo each have two
different palettes while retaining their selected prism, affinity, and
one-only classes.

A/B/C are the empty/nonempty-proper/full partition of `D^C_var`. Tight local
per-version exact-key minima are `3/4/6`; in a C39 witness version they are
`4/4/6`. C71 does not decide total-degree stability under B/C, singleton count
or labels, affinity Soul partition, instance/state/boundary attribution,
transformation, predicates, packages, routes, disclosure, power, release, or
implementation.

If A is selected, definition-level total degree is automatically stable and the
next likely atom is definition-level singleton-count stability. If B/C is
selected, total-degree stability intervenes first. Affinity Soul-partition
stability is an independent later sibling; exact singleton-label stability
waits for both count and partition.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C70-A,
the scoped count correction, and C71 as the sole active owner choice. The
frozen `2026-09-15-1011` handoff retains its historical bare count statement
and is superseded here rather than edited. The authoritative decision record is
unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04 directions remain
unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain selected, and
R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Documentation and SVG
validation passed: `git diff --check` was clean; no-index whitespace checks for
the untracked rigor audit, SVG, and this handoff produced no diagnostics; the
SVG parsed as XML; executable assertions verified all three palette examples,
archetype/class constraints, six-label coverage, and the `4/4/6` C39-witness-
version constructions; the newest-handoff lookup resolved to this file; and a
current-pointer scan found only C71 active. No code tests were run because this
was documentation and vector work only.
