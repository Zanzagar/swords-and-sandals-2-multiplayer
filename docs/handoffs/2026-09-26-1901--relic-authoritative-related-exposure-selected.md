---
handoff:      2026-09-26-1901--relic-authoritative-related-exposure-selected
written:      2026-09-26 19:01 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    three bounded read-only D2 prerequisite audits with mutual cross-challenge, plus one strict read-only final diff verifier
branch:       design/endless-progression-owner-packet
commits:      ff72eba..HEAD
suite:        proportional serial checks 10/10 passed; register reparses 94/94 with Phi_SR 39; decision-record hash unchanged
supersedes:   2026-09-26-1501--relic-three-permission-philosophies-selected
---

# Handoff — authoritative related exposure selected

Start with `HANDOFF.md` above its archive line and
`$ss2-progression-design`. Present D2A only. The six-row D2 subtree is frozen;
do not skip ahead to coordinate, language, branching, randomness, or incidence.

## Where things stand

The owner selected `RCS-03C3C3D1-B`. At least one reachable actual evaluation
under one actually bound lawful complete stance/configuration must select a
genuine distinct-related pair in its authoritative tuple. The witness may be
related-only or hybrid and grants no joint survival, survivor, result, payoff,
prevalence, or participant actionability. A hybrid remains one pair with one
eventual permission bit.

Three bounded read-only audits asked different questions about D2: authority
inheritance, minimum decision-subtree width, and gameplay value of the first
card. Mutual cross-challenge resolved their only domain disagreement. Define
`E_v=E^{resolved-exact}_v`, `R_v=R^{resolved-related}_v`,
`H_v=E_v∩R_v`, and `Q_v=R_v setminus E_v`.

Every hybrid in `H_v` inherits its single permission bit from the exact lane.
C2A-A makes it participant-intrinsic, C2D-A makes it deterministic once
authoritative inputs are fixed, and E1/E2/E3 classify its exact law. D2 may
constrain hybrid placement but cannot add a related grant, veto, draw, or
precedence rule. The related-only residual `Q_v` retains independent permission
authority, coordinate, language, branching, and randomness questions.

The thirtieth prerequisite correction makes old D2 a non-counting parent for:

1. D2A — participant-configurability prevalence over related-only-capable
   treatment contracts;
2. D2B — independent recombination of a related permission coordinate;
3. D2C1 — constructive related permission-language prevalence;
4. D2C2 — participant-composed related context branching;
5. D2D — stochastic support over complete `Q_v` law projections; and
6. D2E — final permission topology over all `R_v`.

D2A-A prunes D2B/D2C1/D2C2 and activates D2D. D2A-B/C activates D2B; every
D2B answer activates D2C1. D2C1-A prunes D2C2 and activates D2D; D2C1-B/C
activates D2C2, then D2D. D2D precedes a fresh D2E audit. There is no separate
`Q_v≠∅` card: positive D2A/D2D branches entail the support their distinct
authority/randomness requires, while bare residual coverage adds no independent
player contract.

The corrected register has 94 unique rows: 38 `SCREEN`, 1 `OWNER-OPEN`, 13
`PRUNED`, 38 `DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`;
`Phi_SR = 39`. D2A is the sole presented card. The authoritative decision
record remains unchanged.

## Present exactly this next

Let `S^{rel-only-perm}_v` contain versioned treatment contracts with at least
one reachable related-only opportunity `q∈Q_v`; it may be empty. Let
`P^{rel-only-perm-config}_v⊆S^{rel-only-perm}_v` contain the contracts where
two lawful prospectively bindable participant settings change the complete
permission law on one matched `q`, holding the version, Relic, evaluation
facts, selected authoritative tuple, and unordered pair fixed. Merely selecting
a different tuple does not count.

- **A — no participant-configurable related-only permission:** `P=∅`; `S` may
  be empty. Recommend A. Participants already bind a finite whole-policy
  routing orientation; the resolver selects the proof relation from lawful
  inputs, while the Relic/relationship covenant owns compatibility. This does
  not promise participant control of one matched proof form. It protects
  artifact identity, team trust, prediction, and UI economy without forcing
  shared exact/related law identity, determinism, or incidence. Costs are less
  downstream control, frustration under intrinsic refusal, and a heavier
  disclosure obligation.
- **B — every related-only-capable contract offers a configurable case:**
  `S≠∅` and `P=S`. Every such contract has at least one matched `Q_v` case
  whose complete permission law differs under two prospective settings. This
  enables mastery and precommitted risk postures, but duplicates routing agency,
  creates hybrid asymmetry, and adds second-optimizer, quarterbacking, grief,
  migration, and obvious-allow pressures.
- **C — configurable and intrinsic related-only-capable contracts coexist:**
  `∅⊊P⊊S`. At least one `Q_v`-capable contract has a configurable matched
  witness and another has none. This broadens artifact personality but teaches
  another authority dialect and risks making configurable contracts the premium
  class. A hybrid-only contract lies outside `S` and cannot supply C's intrinsic
  comparator.

Use the Twin Vow example if needed: a selected `Guard Pulse a` and distinct
`Riposte Mark b` share one nonmultiplying causal parent but no exact child. A
makes its permission intrinsic. B permits prospectively bound `Concord` versus
`Severance` to change the complete law while the same pair remains selected.
C requires that configurable related-only contract plus a different genuinely
related-only contract whose permission no participant setting changes; a
hybrid-only Stone Witness is not enough.

A/B/C are mutually exclusive and exhaustive. Positive B/C openly entail
related-only support. D2A chooses no holder, consent, information, default,
binding horizon, coordinate independence, policy language, randomness,
form-wide incidence, survivor, substitute, result, payoff, release placement,
or implementation. A live proc-time prompt remains incompatible with cut-
atomic automatic invocation.

## Hard boundaries

- Keep the standalone north star **Souls and Simulacra** and
  *achintya bheda abheda* as the ideal, but do not use the ideal to collapse
  independent player-contract questions. D2A-A is neutral/protective at this
  authority boundary; a later allowed distinct pair with one real causal unity
  can still express the ideal directly.
- Do not let hybrid-only contracts manufacture D2A-C or deterministic/stochastic
  coexistence. D2A through D2D use `Q_v`; D2E returns to all `R_v`.
- Do not create a second permission bit, vote, draw, or precedence rule for a
  hybrid. One semantic opportunity is treated once.
- Do not treat a changed selected tuple as participant permission control.
- Do not edit the authoritative decision record without an explicit
  `accepted`, `rejected`, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Verification and Git

Confirm 94 unique rows with the exact counts above and `Phi_SR = 39`. Confirm
the decision-record SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
Commit atomically with a `Co-Authored-By` trailer, push only the feature branch,
verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
