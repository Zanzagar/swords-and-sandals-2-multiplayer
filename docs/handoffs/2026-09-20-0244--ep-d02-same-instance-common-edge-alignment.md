# Handoff — comparison types separated by name; same-instance common-edge alignment next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-20 02:44:10
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.114: whether no, some but not all, or every union-
locked Soul Relic definition contains a same-instance all-Soul-changing common
edge. The owner selected C3c.113-A. Preserve the accepted EP-D02 wording until
a complete replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the current
full-assurance pass before the separate standalone expansion/audit/adaptation
pass. Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.113-A

No locked definition contains both one-coordinate comparison types:

`D^{SB}_{locked,v}=empty`.

The nonempty state-supporting-only and boundary-supporting-only classes form a
disjoint partition of `D^{U=}_{locked,v}`. C111-C already covers every key, so
every represented instance fiber and every completed key is incident to its
definition's sole type and to no edge of the opposite type. Both-type
prevalence is therefore empty at definition, represented fiber, represented
persistent-artifact, key, and endpoint levels in the completed fixed-version
domain.

Inside a state-supporting fiber, state is injective and equal-boundary blocks
induce disjoint cliques of size at least two. Boundary-supporting fibers have
the dual form. A pure three-key typed path is impossible: it closes to a
triangle or introduces the excluded type. Counts, block sizes, degree, and
component topology remain open.

Tight floors are now seven definitions, fifteen exact keys, and ninety six-
Soul rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree
remains six. Each typed edge is partial at its same-artifact/fixed-coordinate
boundary, definition-level separation is neutral/protective, and the catalog
partition is aggregate. The authoritative decision record remains unchanged.

## Independent frontier, construction, and gameplay checks

Three write-nothing checks separated the next static incidence question from
the later lifecycle work:

- the frontier audit found same-ID/common-edge intersection prevalence to be
  ready, while direct typed-edge alignment is too fine;
- the construction audit verified empty, proper, and full branches at the
  unchanged `7/15/90` floor;
- the gameplay analysis supports C as the systematic per-definition same-ID
  direction,
  with B as the strongest deliberately heterogeneous countercase.

C98-C gives every completed key an all-Soul-changing common partner. C111-C
gives every completed key a same-ID one-coordinate partner. They need not be
the same partner. Define

`W^{S,Id}_v(d)={{b,b'} in W^S_v(d) : inst_v(b)=inst_v(b')}`

and

`D^{Id-W}_{locked,v}={d in D^{U=}_{locked,v} : W^{S,Id}_v(d) is nonempty}`.

A qualifying edge may be state-only, boundary-only, or diagonal, and may be
overlap-bearing or member-disjoint. A diagonal qualifying edge needs a larger
same-ID fiber than the displayed `[2,2]` witness: in a two-key fiber, C111-C
forces its sole dyad to be one-coordinate. C114 does not yet choose any of
those finer identities.

## Active owner choice — EP-D02-C3c.114

### A — no locked definition aligns them

`D^{Id-W}_{locked,v}` is empty. Every same-ID comparison retains at least one
supported Soul's noncore map, and every all-Soul-changing common edge crosses
instance IDs. This gives every same-ID pair a retained-map witness, but the
witness may be a different Soul on each pair; it creates no global fiber or
owner-Soul anchor. The strongest catalog comparison remains cross-ID, and its
player-facing availability is unselected.

### B — aligned and unaligned definitions coexist

`D^{Id-W}_{locked,v}` is nonempty and proper. At least one name contains a
same-ID all-Soul-changing pair while another keeps all common edges cross-ID.
This is the strongest countercase if pairwise witness-retaining definitions and
same-ID radical-contrast definitions are disclosed, accessible, and non-dominated
peers. Its cost is another name taxonomy and a likely premium reading for
aligned names.

### C — every locked definition aligns them — recommended

`D^{Id-W}_{locked,v}=D^{U=}_{locked,v}`. Every locked name contains at least
one persistent instance ID represented by two completed snapshots on which
every supported Soul's noncore map differs. This aligns persistent artifact
identity with the established whole-roster comparison promise: at least one
represented same-ID fiber in every locked definition contains the static
contrast. It does not cover every key or instance and grants no path, access,
or control. Costs are a one-edge token loophole and added authoring, proof,
preview, and disclosure burden.

```text
UNALIGNED WITNESS
u0  FR->bde  ST->ade  VW->abc
u1  FT->ace  SW->ade  VR->bcd
u2  FW->cde  SV->abe  TR->abc
u3  FV->cde  ST->abd  WR->ace

Fibers {u0,u1} and {u2,u3} are non-common same-ID dyads. All overlap-bearing
and member-disjoint common edges cross fibers.

ALIGNED WITNESS — C105 b0..b3
Fibers {b0,b2} and {b1,b3} make the same-ID dyads member-disjoint common edges.
Fibers {b0,b3} and {b1,b2} instead make them overlap-bearing common edges.
```

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |

At one fixed exact-key/Soul relationship, inherited fit remains **direct**. A
same-instance all-Soul-changing edge is **partial** at the persistent-artifact/
across-snapshot boundary: definition, instance ID, core, exact union, atomic
vocabulary, and Soul identity continue while every Soul's assigned map differs.
If overlap-bearing, exact motif survival adds another partial continuity; if
member-disjoint, exact-family-member continuity is neutral. Definition-level
prevalence is **aggregate**. C is recommended for aligning two selected
structural promises, not for a stronger direct doctrinal rating.

C114 selects no one-coordinate-versus-diagonal alignment, state-versus-
boundary identity, overlap-versus-member-disjoint identity, per-key coverage,
topology, coordinate semantics, cause, transition, path, access, disclosure,
agency, value, power, persistence, release, or implementation. Every snapshot
remains independently legal and reachable somewhere; C114 grants no path.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C114 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
