# Handoff — finite Soul Relic history freshness selected

**Session ID:** `49440671-6bb3-4302-b4a9-c43f70f6aadf`
**UTC:** 2026-09-24 07:48
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`; present only `RCS-03C3B0` next.

## Owner answer recorded

The owner selected `RCS-03C3A-A`: every materially H-reading position is
freshness-bounded. Every materially qualifying occurrence on every effective
H-reading path, including any permitted coupled H+S addition, has a disclosed
finite nonterminal player-semantic age horizon and ages out on a sufficiently
long legal continuation because that horizon passes. Exact horizon unit/value,
source-owned clearing, claim/consumption, reuse, payoff, and UI remain separate.

The remembered occurrence and current Soul/Relic/Charm relationship
participate **directly** at invocation; continuity across time is **partial**;
expiry is **neutral/protective**; universal coverage is **aggregate**. The
direction prevents permanent priming and stale opaque manifestations while
requiring generous, visible semantic windows. The authoritative decision
record remains unchanged and no implementation is authorized.

## Sixth register amendment

Adversarial re-derivation proved that old `RCS-03C3B` still compressed seven
independent boundaries:

1. additional coupled H+S evidence admission;
2. within-H phase attribution;
3. occurrence-history phase support;
4. action/transition source-root identity;
5. build-provenance source-root identity;
6. root-versus-child atom addressability; and
7. conditional semantic child-application partitioning.

It is now a non-counting parent for `RCS-03C3B0`, `RCS-03C3B1A`,
`RCS-03C3B1B`, `RCS-03C3B2A`, `RCS-03C3B2B`, `RCS-03C3B3`, and
`RCS-03C3B4`. Root/child addressability precedes detailed child partitioning so
a root-only answer can prune the latter. Every eventual child retains its root
lineage; same-root siblings cannot fill two independent-source positions of one
result. Existing C3C–C3F/RCS-08 still own fan-out, claims, and reuse.

**Agent correction:** the first internal amendment draft omitted the coupled-
path and phase-attribution rows while relying on both. Adversarial review caught
the overcompression before owner presentation. The temporary 43-slot/
`Phi_SR = 29` route is superseded.

The register has 45 slots: 30 `SCREEN`, 1 `OWNER-OPEN`, 7 `PRUNED`, 5
`DIR-SELECTED`, 1 `SPEC`, and 1 `EVALUATE`, so `Phi_SR = 31`.

## Next card

`RCS-03C3B0` decides whether a position already required to have an
independently attributable H-only or S-only path may also contain an additional
material H+S path whose meanings cannot be isolated in reachable play:

- A: no position admits an additional coupled path;
- B: every position has at least one; or
- C: positions with and without coupled supplements coexist.

Recommend A. It preserves both history and current-state play through distinct
positions while keeping each local causal explanation real. It still permits
ordinary causation and entries containing separate H and S positions; it
excludes only inseparable supplements. Phase attribution/support, root identity,
atom factorization, child partitioning, exact families, fan-out, claims, reuse,
payoff, persistence, release, and implementation remain separate.

## Validation

- Structural target: 45 Relic register rows and `Phi_SR = 31`, with exactly one
  `OWNER-OPEN` row.
- Full suite: 584 tests, 583 passed, 0 failed, 1 expected archive skip.
- `git diff --check` passed.
- `docs/design/endless-progression-decisions.md` must remain byte-identical at
  SHA-256 `e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
- No runtime, Ruffle, capture, installation, save, snapshot, fixture,
  candidate, observation, manifest, or golden work is authorized or required.

Process note: one write-nothing verifier accidentally redirected a diff-check
report into `/tmp`; the main session later made the same class of mistake while
re-deriving the register count. Neither changed the repository, installation,
save, capture, or Git state. Both mistakes are recorded rather than hidden.
