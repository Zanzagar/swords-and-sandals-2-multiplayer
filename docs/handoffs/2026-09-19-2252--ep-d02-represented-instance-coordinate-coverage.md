# Handoff — one-coordinate comparison universal by name; instance fibers next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`  
**UTC:** 2026-09-19 22:52:56  
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.109: whether no, some, or every union-locked Soul
Relic definition gives every represented persistent instance-ID fiber at least
one same-instance, one-coordinate comparison. The owner selected C3c.108-C.
Preserve the accepted EP-D02 wording until a complete replacement is replayed
and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy, not
theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the current
full-assurance pass before the separate standalone expansion/audit/adaptation
pass. Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.108-C

Let `A^{1coord}_v(d)` contain family-distinct completed pairs carrying the same
persistent instance ID for which exactly one of complete input-affecting state
equality and grammar-evaluation-boundary equality holds. The owner selected

`D^{1coord}_{locked,v}=D^{U=}_{locked,v}`.

Every union-locked definition therefore contains at least one such pair. Other
instance fibers and other pairs may remain diagonal, meaning both coordinates
change. C108-C selects neither state-only nor boundary-only comparison and
grants no causal mechanism, transition, common access, disclosure, or agency.

Tight floors remain six definitions, eleven exact keys, and sixty-six six-Soul
rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree remains
six. A qualifying cross-snapshot relationship is partial at the same-artifact/
one-coordinate-continuous boundary; universal definition incidence is
aggregate.

## Frontier validation

Three write-nothing reviews independently checked C109. Frontier review found
the next coarser-to-finer question is per-represented-instance-fiber incidence:
C108-C has quantifier order “every definition has some positive fiber,” while
C109 asks whether every represented fiber is positive. Per-key coverage is
strictly stronger. State-only versus boundary-only support and exact fiber
topology are independently ready siblings, not prerequisites. Math review
confirmed the `[2,2]` witnesses, the `[4]` loophole, and floors `6/11/66`,
`7/15/90`, and `6/11/66`. Gameplay review favored C for reducing per-copy
comparison-class lottery and identified B as the strongest countercase if the
name-level taxonomy is explicit and strategically valuable.

Use **one-coordinate-positive**, not “readable,” for structural incidence:
C109 supplies no player disclosure or access.

## Active owner choice — EP-D02-C3c.109

For locked definition `d`, let

`Ids_v(d)={r : there exists b in B_v(d) with inst_v(b)=r}`

and

`Ids^{1coord}_v(d)={r in Ids_v(d) : there exist {b,b'} in A^{1coord}_v(d) with inst_v(b)=inst_v(b')=r}`.

Define

`D^{Id-1coord}_{full,v}={d in D^{U=}_{locked,v} : Ids^{1coord}_v(d)=Ids_v(d)}`.

C107-C makes every represented ID fiber contain at least two completed keys.
C108-C makes its positive-ID set nonempty in every definition. “Represented”
only quantifies over IDs appearing in the completed operative domain; it does
not cover absent inventory records or promise acquisition or joint access.

```text
HYBRID [2,2]
Ashen R17 / state0 / boundaryA
Ashen R17 / state0 / boundaryB   <- R17 is one-coordinate-positive
Ashen R44 / state1 / boundaryC
Ashen R44 / state2 / boundaryD   <- R44 is diagonal-only

FULL [2,2]
Ashen R17 / state0 / boundaryA
Ashen R17 / state0 / boundaryB
Ashen R44 / state1 / boundaryC
Ashen R44 / state1 / boundaryD   <- both represented IDs are positive

FULL [4], BUT NOT PER-KEY
Ashen R17 / state0 / boundaryA
Ashen R17 / state0 / boundaryB   <- the required positive pair
Ashen R17 / state1 / boundaryC
Ashen R17 / state2 / boundaryD   <- these keys lack one-coordinate peers
```

The `[4]` layout is automatically fiber-full after C108-C because it has only
one represented ID. It proves that full fiber participation does not imply
per-key coverage.

### A — every locked name is fiber-hybrid

`D^{Id-1coord}_{full,v}` is empty. Every locked name has at least one positive
represented ID and at least one diagonal-only represented ID. A supports
systematic per-artifact individuality, but forces multiple IDs per name, gives
no fully covered teaching class, and risks hidden per-copy taxonomy. Tight
floors are `6/11/66`.

### B — fully covered and fiber-hybrid names coexist

`D^{Id-1coord}_{full,v}` is a nonempty proper subset of the locked class. B is
the strongest gameplay countercase if fully covered and advanced hybrid names
become an explicit, disclosed, strategically valuable catalog distinction.
Otherwise it layers name-level taxonomy over per-copy uncertainty. Tight floors
are `7/15/90`.

### C — every represented instance fiber is positive — recommended

`D^{Id-1coord}_{full,v}=D^{U=}_{locked,v}`. Every represented persistent ID has
at least one same-ID family contrast holding state or boundary fixed. Diagonal
pairs may remain. C reduces potential per-copy comparison-class lottery without
flattening compound contrasts. Costs are universal per-ID authoring and proof
burden plus a residual one-pair-per-fiber loophole. Tight floors are
`6/11/66`.

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 6 | 11 | 66 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 6 | 11 | 66 at six Souls | 6 |

At one fixed exact-key/Soul relationship, inherited fit remains **direct**.
Same-ID contrasts remain **partial** at the broad persistent-artifact boundary.
A qualifying pair is **partial** at the same-artifact/one-coordinate-continuous
boundary because its facts occupy separate operative snapshots; a diagonal-only
fiber is **neutral/protective** there. Fiber and definition prevalence is
**aggregate**. C is recommended for systematic within-artifact comparison
potential, not a stronger direct doctrinal rating.

C109 selects no state-only versus boundary-only support, requirement that both
types exist, exact fiber number or size, per-key or every-pair coverage,
comparison-edge alignment, cause, path, access, disclosure, agency, value,
power, inventory-instance universality, persistence format, migration, release,
or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C108-C
as selected and C109 as the sole active owner choice. The authoritative
`docs/design/endless-progression-decisions.md` remains unchanged. EP-D01,
EP-D02, and EP-D07 remain accepted; EP-D04 directions remain unaccepted, R9.4
remains reopened, R10.1-A and R10.2-A remain selected, and R10.3-R10.8 remain
pending. Preserve the owner's existing uncommitted skill edit. No
implementation, commit, or push occurred.
