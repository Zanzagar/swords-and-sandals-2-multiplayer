# Handoff — same-instance coverage universal; one-coordinate comparison next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`  
**UTC:** 2026-09-19 22:02:55  
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.108: whether no, some, or every union-locked Soul
Relic definition contains at least one same-persistent-instance family contrast
that holds exactly one of input-affecting state or grammar-evaluation boundary
fixed. The owner selected C3c.107-C. Preserve the accepted EP-D02 wording until
a complete replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy, not
theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the current
full-assurance pass before the separate standalone expansion/audit/adaptation
pass. Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.107-C

For every union-locked definition `d`, every completed exact key belongs to

`K^{inst}_v(d)={b in B_v(d):exists b'!=b, inst_v(b')=inst_v(b)}`.

Thus `D^{K-inst}_{full,v}=D^{U=}_{locked,v}`: every completed locked exact key
has a family-distinct completed partner carrying the same persistent
`soulRelicInstanceId`. Every represented instance-ID fiber has size at least
two. At the four-key C105 floor, `[4]` and `[2,2]` are legal. C107-C neither
requires one ID to carry every key nor quantifies over inventory instances
absent from the completed operative domain.

Tight floors remain six definitions, eleven exact keys, and sixty-six six-Soul
rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree remains
six. Each same-ID cross-snapshot relationship is partial at the persistent-
artifact boundary; universal exact-key incidence is aggregate.

## Frontier validation

Three write-nothing reviews independently checked C108. Frontier review found
no coarser prerequisite: definition-level existence correctly precedes
per-fiber and per-key coverage, and state-only versus boundary-only identity is
finer. Math review supplied tight `[2,2]` positive and negative witnesses and
confirmed floors `6/11/66`, `7/15/90`, and `6/11/66`. Gameplay review favored
C only as a structural-legibility guarantee and identified B as the strongest
countercase if diagonal-only names become an explicit compound-ritual class.

A diagonal pair proves only that no authored one-coordinate comparison exists.
It does not prove joint state-and-boundary causation. A one-coordinate pair
isolates an authored comparison coordinate but proves no causal mechanism,
transition, or common access.

## Active owner choice — EP-D02-C3c.108

Let `state_v(b)` project the complete input-affecting Relic state and
`bound_v(b)` the later-authorized grammar-evaluation boundary. Let

`A^{1coord}_v(d)`

contain distinct same-ID key pairs for which exactly one of state equality and
boundary equality holds. A pair is state-only when boundary stays fixed,
boundary-only when state stays fixed, and diagonal when both coordinates
change. Define

`D^{1coord}_{locked,v}={d in D^{U=}_{locked,v}:A^{1coord}_v(d) is nonempty}`.

```text
DIAGONAL-ONLY [2,2]
b0=R17/state0/boundaryA   b2=R17/state1/boundaryB
b1=R44/state0/boundaryA   b3=R44/state1/boundaryB

ONE-COORDINATE-POSITIVE [2,2]
b0=R17/state0/boundaryA   b2=R17/state0/boundaryB  <- boundary-only
b1=R44/state0/boundaryA   b3=R44/state1/boundaryB  <- diagonal
```

The positive definition still leaves `b1` and `b3` without a one-coordinate
peer. That is deliberate: definition-level existence is this card; per-key
coverage is a later descendant.

### A — every same-instance comparison is diagonal

`D^{1coord}_{locked,v}` is empty. Both state and boundary projections are
injective inside every represented instance-ID fiber. A can make every form an
intentionally compound state-by-boundary ritual, but it is not the simple
branch: after C107-C it forces multiple states and boundaries in every fiber,
offers no completed controlled comparison, and has the greatest explanation,
attribution, and proof burden. Tight floors are `6/11/66`.

### B — one-coordinate-readable and diagonal-only names coexist

`D^{1coord}_{locked,v}` is nonempty and proper. B is the strongest gameplay
countercase if the two artifact languages are explicit, disclosed, and
strategically valuable. Otherwise it adds hidden taxonomy and token-exemplar or
premium-name risk. Tight floors are `7/15/90`.

### C — every locked definition has a one-coordinate pair — recommended

`D^{1coord}_{locked,v}=D^{U=}_{locked,v}`. Every locked name gets at least one
controlled same-instance comparison that holds state or boundary fixed, while
other pairs may remain diagonal. C gives every name a teaching, balance, and
diagnostic foothold without forbidding compound forms. Costs are authoring and
proof burden plus a one-pair token loophole. It selects neither axis and does
not cover every key. Tight floors are `6/11/66`.

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 6 | 11 | 66 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 6 | 11 | 66 at six Souls | 6 |

At one fixed exact-key/Soul relationship, inherited fit remains **direct**.
Every same-ID cross-snapshot contrast is **partial** at the broad persistent-
artifact boundary. A one-coordinate pair is also **partial** at the narrower
same-artifact/one-coordinate-continuous boundary because its facts remain
separate operative snapshots. Diagonal absence is **neutral/protective** at
that narrower projection, and A/B/C definition prevalence is **aggregate**. C
is recommended for legibility, not a stronger direct doctrinal rating.

C108 selects no state-only versus boundary-only identity, requirement that both
types exist, per-instance/fiber/key one-coordinate coverage, C101/C105/all-Soul
edge alignment, exact state or boundary semantics, cause, transition,
transformation, evolution, reroll, live switching, simultaneous expression,
controlled substitution, comparable acquisition or access, disclosure,
preview, agency, ease, frequency, predicate or receipt continuity, sidegrade
quality, value, power, non-dominance, persistence format, migration, release,
or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C107-C
as selected and C108 as the sole active owner choice. The authoritative
`docs/design/endless-progression-decisions.md` remains unchanged. EP-D01,
EP-D02, and EP-D07 remain accepted; EP-D04 directions remain unaccepted, R9.4
remains reopened, R10.1-A and R10.2-A remain selected, and R10.3-R10.8 remain
pending. Preserve the owner's existing uncommitted skill edit. No
implementation, commit, or push occurred.
