# Handoff — universal actual foil coverage; persistent-instance carrier next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`  
**UTC:** 2026-09-19 14:15:37  
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.106: whether no, some, or every union-locked Relic
definition realizes family-different completed snapshots on one persistent Soul
Relic instance ID. The owner selected C3c.105-C. Preserve the accepted EP-D02
wording until a complete replacement is replayed and explicitly accepted. Do
not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Every remaining choice must use
*Achintya Bheda Abheda* (*acintya-bhedabheda*) as the enduring design ideal:
prefer meaningful continuity or participation and real, mechanically legible
distinction at the same operative relationship. This is an inspirational game-
design analogy, not theological equivalence, a silent invariant, or an automatic
selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the current
full-assurance pass before the separate standalone expansion/audit/adaptation
pass. Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.105-C

For every union-locked definition `d`, every completed exact key belongs to

`K^R_v(d)=union R^W_v(d)`.

Thus `D^{K-R}_{full,v}=D^{U=}_{locked,v}`: each completed key has an authored
member-disjoint common partner. C101-C separately gives every key an overlap-
bearing common partner, and the two partners must be distinct. Every locked
name therefore needs at least four exact keys.

The tight floor is six definitions, eleven exact keys, and sixty-six six-Soul
rows. Supported-Soul counts remain `{6,9,10,12,14,15}` and maximum palette
degree remains six. Overlap edges remain partial. Member-disjoint edges are
partial at the broad stable definition/core/union/vocabulary boundary and
neutral at exact-family-member continuity. Universal dual-edge incidence is
aggregate. C105 creates static authored comparisons, not legal transitions.

## Frontier-order correction

The first internal post-C105 draft would have asked whether every key supports
a second member-disjoint family. Its finite geometry was sound: every current
`rho=3` family has eleven disjoint candidates, while every C104-legal `rho=5`
family has exactly one. The order was wrong for gameplay. It would have selected
a second latent foil before that foil had any chosen purpose, continuing an
unbounded connectivity ladder after C105 had already completed the meaningful
incidence promise.

That branching-support card is deferred unless actual member-disjoint degree
two becomes a desired descendant. C106 instead begins the already-deferred
split among the exact key's persistent-instance, input-affecting-state, and
evaluation-boundary carriers.

## Active owner choice — EP-D02-C3c.106

C3c.43's exact operative realization is the tuple

`(soulRelicInstanceId, input-affecting state, evaluation boundary)`.

Let `inst_v(b)` project completed locked exact key `b` to its persistent instance
ID, and define

`D^{same-inst-M}_{var,v}={d in D^{U=}_{locked,v}:exists b!=b' in B_v(d), inst_v(b)=inst_v(b')}`.

C95-C already makes the complete families of two distinct completed keys
unequal. A same-ID witness therefore means one persistent artifact occurs on two
independently legal and reachable family-different snapshots. Those snapshots
must differ in input-affecting state, evaluation boundary, or both. C106 does
not select which coordinate differs, create a transition or common access
between snapshots, make them simultaneous, or authorize switching.

```text
SAME FOUR C105 KEYS; ONLY THE CARRIER CHANGES

A — INSTANCE-FIXED
b0 = Ashen R17 / state0 / boundaryA
b1 = Ashen R44 / state0 / boundaryA
b2 = Ashen R58 / state0 / boundaryA
b3 = Ashen R63 / state0 / boundaryA

SAME-INSTANCE WITNESS
b0 = Ashen R17 / state0 / boundaryA
b2 = Ashen R17 / state1 / boundaryA
or
b2 = Ashen R17 / state0 / boundaryB
```

### A — no locked name varies family within one instance

`D^{same-inst-M}_{var,v}` is empty. In one fixed version, each persistent
instance ID occurs on at most one completed operative locked exact key—not just
one family. The four C105 keys use distinct IDs in alternate legal worlds.

A gives the clearest immutable artifact identity and simplest persistence. Its
cost is that one-per-definition possession, duplicate substitution, permanence,
and no transfer can leave every rich foil outside one combatant's collection.
The comparison graph may be only counterfactual. Tight floors are `6/11/66`.

### B — instance-fixed and internally variable names coexist

`D^{same-inst-M}_{var,v}` is nonempty and proper. At least one locked name has a
same-ID family-different pair, while another has none in the completed operative
domain.

B is the strongest gameplay countercase if immutable covenant artifacts and
morphic artifacts become explicit, readable identities. Otherwise it creates a
hidden persistence and comparison taxonomy. Tight floors are `7/15/90`.

### C — every locked name has a same-instance pair — recommended

`D^{same-inst-M}_{var,v}=D^{U=}_{locked,v}`. Every locked name has at least one
persistent instance ID on two family-different completed snapshots. It does not
say every instance varies, one instance carries all four keys, or the witness
supplies both comparison dialects.

C prevents the whole C105 comparison graph from being merely a catalog of
mutually exclusive duplicate outcomes and gives every name within-artifact
simulacrum potential. Costs are universal state/boundary multiplicity,
persistence/migration/disclosure burden, and a token loophole because one
exceptional instance and one pair can satisfy a name. Tight floors remain
`6/11/66`.

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 6 | 11 | 66 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 6 | 11 | 66 at six Souls | 6 |

The inherited outside-locked base is five definitions and seven keys. C105-C
requires four keys per locked name. A/C classify one such name; B needs distinct
positive and negative four-key names. Reassigning instance IDs changes no
family geometry, Soul roster, or degree bound.

At one fixed exact-key/Soul relation, inherited fit remains **direct**. A same-
instance cross-snapshot family difference is **partial** at the persistent-
artifact boundary: definition, instance ID, core, exact union, and atomic
vocabulary continue while the complete family differs, but the facts occupy
separate snapshots. A is **neutral/protective**. Prevalence is **aggregate**.
C is recommended for player-facing potential and thematic coherence, not a
direct simultaneous doctrinal fit. B is the strongest gameplay countercase; A
is the strongest clarity, persistence-simplicity, and singular-artifact
countercase.

C106 selects no state-versus-boundary identity, endpoint transition,
transformation, evolution, reroll, live switching, simultaneous expression,
per-instance prevalence, same-instance key coverage, controlled substitution,
comparable access, disclosure, agency, value, power, persistence format,
migration, release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C105-C
as selected and C106 as the sole active owner choice. The authoritative
`docs/design/endless-progression-decisions.md` remains unchanged. EP-D01,
EP-D02, and EP-D07 remain accepted; EP-D04 directions remain unaccepted, R9.4
remains reopened, R10.1-A and R10.2-A remain selected, and R10.3-R10.8 remain
pending. Preserve the owner's existing uncommitted skill edit. No implementation,
commit, or push occurred.
