# Handoff — C71 palette stability selected; definition anchor count next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-15 20:28:19
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.72: whether neither, exactly one, or both
multi-anchor Relic archetypes admit definitions whose exact operative
simulacra have different singleton-anchor counts. The owner selected C3c.71-A.
Preserve the accepted EP-D02 wording until a complete replacement is replayed
and explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedābheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. If no option remains consistent,
offer or recommend a replacement rather than weaken the north star. Continue
the exact one-at-a-time A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle remain in their SS2 lane.

## Selected direction

`D^C_{var,v}` is empty. Every exact operative key carrying one stable Relic
definition ID has the same total native-dual six-cell palette `C_v(d)` in a
completed version. Definition-level total degree `d_v(d)=|C_v(d)|` is therefore
derived stable and receives no redundant decision card. C3c.52-B's required
palette variation occurs between definitions. C39-C's selected personal
artifact variation can still change a fixed-cell predicate inside that shared
vocabulary. C71-A does not join singleton count or labels, affinity Soul
partition, predicates, packages, receipts, or power and selects no variation
cause or transformation.

The scoped C70 count correction remains: the selected branch has at least
three exact keys in every completed version and at least four in some C39
variable-instance witness version. C39 does not require four in every version.

## Active frontier

C59-A already makes each exact key's singleton count Soul-stable, so write
`k_v(b)=|I_v(b,s)|`. C69-A/C70-A partition definitions into nonempty prism-
multi, affinity-multi, and one-anchor-only classes. The last class is
automatically count-stable at one and remains outside C72.

For each multi-support definition define

`Counts_v(d)={k_v(b) : def_v(b)=d}`.

Let `T^k_var` contain `prism` and/or `affinity` exactly when that archetype has
some definition whose `Counts_v(d)` contains at least two integers.

- **A, recommended — neither archetype varies:** `T^k_var` is empty. Every
  named prism or affinity keeps one quantity of independently grounded recipe
  anchors across its exact simulacra; different definitions may use different
  counts. Exact keys may still change which labels are anchors and how their
  predicates behave. This avoids a hidden higher-count “better copy.” Inherited
  ideal fit is direct inside each fixed key/Soul relationship; C72's marginal
  fit is neutral and protective. Cost: no same-name simulacrum can expand or
  contract this quantitative breadth.
- **B — exactly one archetype varies:** `|T^k_var|=1`. Some definition in
  exactly prism or affinity has unequal-count keys, while every definition in
  the other archetype is stable. C72-B intentionally leaves the participating
  identity for the immediate next card. This is a partial alternate-key
  metaphor, not simultaneous bhedābheda, and creates a potential quality-roll
  problem in one dialect.
- **C — both archetypes vary:** `T^k_var={prism,affinity}`. At least one prism
  definition and one affinity definition vary count; other definitions may
  remain stable. This is merely aggregate ideal fit across separate catalog
  witnesses. It maximizes quantitative form variety but makes comparison and
  balance burdens unavoidable in both dialects and tightens the Soul ceiling.

For Souls `F/S/V`, a stable-palette Dreamglass can compare:

- count-two prism `P2`: `F={a,b}`, `S={b,c}`, `V={a,c}`;
- count-three prism `P3`: `F={a,b,c}`, `S={c,d,e}`, `V={a,d,e}`.

Both use total palette `{a,b,c,d,e}`, remain pair-complete, pairwise
overlapping, and coreless. A keeps one of those counts across Dreamglass keys;
B can permit both only in its later-selected prism branch; C requires some
prism definition like this.

An affinity on stable `{b,c,d,e,f}` can compare count-two
`F=V={b,c}, S={b,d}` with count-three
`F=V={b,c,d}, S={b,e,f}`; both keep sole core `b`. Echo remains one-only at
count one. These constructions retain the C71 palette, cover all six singleton
labels, preserve an exact-degree-two Echo footprint, and can align one
variable pair with C39's predicate witness without requiring that reuse.

A/B/C are the zero/one/both projection over the two nonempty multi archetypes.
A naive empty/proper/full set over definitions was rejected because its middle
case bundled prism-only, affinity-only, and both-archetype participation—the
same defect corrected at C60. Tight local exact-key floors are `3/4/5`; in a
C39 witness version they are `4/4/5`.

Any variable-count multi definition needs total palette degree at least four.
A variable prism needs at least five. Across six labels, a variable prism can
support at most ten Souls because its two coreless pairwise-intersecting
uniform set families must use different counts; the best shared ceiling is ten
for counts three and four. Thus C derives `3<=|S_v|<=10`. B derives that bound
only if prism is selected next; affinity-only B and A retain
`3<=|S_v|<=15`.

C72 selects no B identity, within-class prevalence, exact count, exact labels,
affinity Soul partition, common-core label continuity, instance/state/
evaluation-boundary cause, transformation, coexistence, live switching,
predicates, packages, contexts, receipts, routes, disclosure, ease, frequency,
power, acquisition, persistence, release, or implementation. If A is selected,
affinity partition block-count stability is the likely next independent atom.
If B is selected, prism versus affinity identity is next.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C71-A
and C72 as the sole active owner choice. The authoritative decision record is
unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04 directions remain
unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain selected, and
R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Documentation, construction,
bound, pointer, and SVG validation passed: tracked and untracked whitespace
checks were clean; the SVG parsed as XML; the A/B/C constructions satisfied
the inherited C52-C71 structural constraints; exhaustive six-label prism
search reproduced the `3/10/15/6` count-two-through-five maxima and the
ten-Soul unequal-count ceiling; C72 was the sole active owner-packet heading;
the authoritative decision record remained unchanged; and this file resolved
as the newest handoff. This documentation-only turn did not run the repository
test suite.
