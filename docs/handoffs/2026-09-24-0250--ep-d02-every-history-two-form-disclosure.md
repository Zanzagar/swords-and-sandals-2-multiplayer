# Handoff — Every-history foothold selected; two-form saturation next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-24 02:50:51
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.161: every-history two-form pre-choice disclosure
saturation. The owner's latest `A` selected C3c.160-A and nothing in C161. This
remains Soul Relic design, specifically the player-information surface of the
selected same-opportunity, one-hinge result-rearm grammar. Preserve accepted
EP-D02 wording until a complete normative replacement is replayed and
explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready bounded choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Do not batch
two-form history saturation with same-opportunity co-legibility, communication,
accessibility, value, or fun.

## Selected direction — EP-D02-C3c.160-A

Retain C159's form-supported history sets and define

`U^{pre-all+}_v={k in D^{dual}_v :
H^P_v(k) union H^R_v(k)=W_v(k)}`.

C160-A makes `U^{pre-all+}_v=D^{dual}_v`. Every exact legal history of every
mechanically dual tag carries at least one qualifying pre-choice P or R
account. Histories may be P-only, R-only, or both, but none is P/R-pre-choice-
opaque. Each account is **direct** at its exact `O^-` boundary; a singleton-
supported history's relationship to the complete dual vocabulary remains
**partial**; excluding history-level opacity is **neutral/protective**; and
universal tag/history coverage is **aggregate**.

C160-A adds no tag, history, opportunity, pair, continuation, cut, receipt-
width, menu, or visible-button floor. The tight candidate catalog remains one
dual tag plus both mechanical specialists. Every fixed catalog and width floor
remains unchanged.

## Active owner choice — EP-D02-C3c.161

Define

`W^{pre1}_v(k)={w in W_v(k) : |K^{pre-hist}_v(k,w)|=1}`

and

`U^{pre-all-both}_v={k in D^{dual}_v :
W^{pre-both-hist}_v(k)=W_v(k)}`.

C159-A makes every `W^{pre-both-hist}_v(k)` nonempty. C160-A makes every
history's support set one of `{P}`, `{R}`, or `{P,R}`, never empty. Therefore
every tag outside `U^{pre-all-both}_v` has both a both-form history and a
distinct singleton-supported history. No opaque fourth case remains.

### A — every legal history exposes both forms — recommended

`U^{pre-all-both}_v=D^{dual}_v`. Every exact legal history of every dual tag
has a qualifying pre-choice P account and R account. The accounts may use
different opportunities, pairs, references, lawfully human-controllable
command-holder roles, or players.

Each account is **direct** at its own `O^-` boundary. When the accounts occur
at different opportunities, their participation within one continuing
Soul–Relic history is **partial**, not same-boundary direct. Excluding
singleton-supported histories is **neutral/protective**, and universal tag/
history saturation is **aggregate**.

A makes the mechanically dual grammar truthfully dependable in every lived
biography and closes token compliance through one fully explained history plus
focused residual histories. Costs are two explanation/localization/validation
obligations per history, cognitive load, possible repetition or a contrived
second account, less focused biography identity, and token compliance through
two obscure accounts.

Example: Ashen's Covenant, Ember, and Echo histories each explain both how a
choice changes the bond predicate and how a choice changes recurrence
permission. Ember may teach P early and R later; A does not require one prompt.

### B — every dual tag mixes both-form and singleton histories

`U^{pre-all-both}_v` is empty. Every dual tag has C159-A's both-form history
and a distinct history exposing exactly one form. C160-A forbids that focused
history from being opaque. B does not choose its singleton polarity or require
both singleton polarities.

This is the strongest focused-biography countercase. The supported form is
**direct**; the missing form is **neutral/unsupported** before choice on the
focused history; continuity between the tag's full and focused biographies is
**partial**; deliberate teaching focus may be **neutral/protective**; and
universal mixing is **aggregate**.

Costs are route-dependent understanding, one causal face hidden on some lived
histories, later polarity and prevalence choices, and exhaustive proof that the
missing account is truly absent.

Example: Ashen's Covenant history explains P and R; its Ember history explains
P only. Every other dual tag has an analogous both-form and singleton pair.

### C — saturated and history-mixed dual tags coexist

`U^{pre-all-both}_v` is a nonempty proper subset of `D^{dual}_v`. At least one
dual tag exposes both forms on every history. At least one distinct tag has a
both-form history and a singleton-supported history.

The classes inherit A/B's local ratings and their coexistence is **aggregate**.
Costs are another information subtype, likely clarity or flexibility premiums,
uneven learning burden, and token class membership.

Example: every Ashen history explains P and R. Dreamglass has a Study history
explaining both and a Facet history explaining only R.

## Proof, floors, and deferrals

A/B/C are exhaustive whole/empty/nonempty-proper topologies of the saturated
tag subset. C159-A supplies a both-form history for every tag; C160-A makes
every non-both history exactly P-only or R-only rather than opaque.

A needs positive `Lambda_P` and `Lambda_R` accounts on every exact legal
`(k,w)`. B needs, for every tag, one singleton history with one positive form
plus exhaustive absence of the other across every live joint opportunity,
qualifying pair, lawfully human-controllable observation, and reachable
authorized reference. C combines A-style universal proof for one tag with B's
singleton witness for another. Incomplete analysis remains unresolved.

Accounts cannot be stitched across histories; accounts at different
opportunities on one exact history may combine. A generic reference counts
only when authorized and available inside the actual command-holder's `O^-`
before the exact choice and truthful for that history's legal arms. Post-choice
feedback, private AI state, guides, inference, fused non-causal wording, and
another seat's uncommunicated information remain invalid. A does not require
same-opportunity co-legibility, one explanation, one role, or one player, and
other opportunities may remain silent.

Tight indexed incidences are:

| Branch | Candidate tags | `(k,w)` history incidences | Positive `(k,w,d)` information incidences | `(k,w,F)` form memberships |
| --- | ---: | ---: | ---: | ---: |
| A | 3 | 1 | 1 | 2 |
| B | 3 | 2 per dual tag | 2 per dual tag | 3 per dual tag |
| C | 4 | 3 total | 3 total | 5 total |

These are indexed witness incidences, not necessarily distinct global battle
histories, tutorial texts, reference assets, or UI explanations. No branch adds
a local mechanical pair, continuation, cut, receipt width, menu, visible
button, definition, exact key, or Soul row. All branches retain nine
definitions, twenty-three exact keys, 138 Soul/key rows at six Souls, four
represented locked IDs, eight lived aligned edges, four one-history-complete
IDs, no incomplete represented ID, Souls `{6,9,10,12,14,15}`, degree six, and
C46-A's same-boundary width two.

C161 defers singleton polarity under B/C; identities, counts, fractions,
frequency, access, player control, ordinary-route prevalence; same-opportunity
or every-opportunity co-legibility; shared explanation/reference/role/player
and communication; order, hinge visibility, modality, discoverability,
feedback, value, power, fun, UI, release, and implementation.

Under recommended A, resolve pre-choice same-opportunity co-legibility
participation next: whether each dual tag ever makes both form accounts
available at one joint opportunity, never does, or those tag classes coexist.
C161-A alone still permits every history's P and R accounts to occur at
different decisions.

## Repository state

The owner packet, rigor audit, living head, and systems map carry C160-A and
make C161 the sole active owner choice. The authoritative decision record was
not edited and should retain SHA-256
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.

This was a documentation-only design update. Validate tracked and untracked
whitespace, SVG XML parsing, one active frontier, and the authoritative-record
hash. Do not run the Node suite because no code or executable contract changed.
