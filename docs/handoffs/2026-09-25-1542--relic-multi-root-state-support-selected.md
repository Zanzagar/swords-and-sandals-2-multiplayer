---
handoff:      2026-09-25-1542--relic-multi-root-state-support-selected
written:      2026-09-25 15:42 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    /root/b2b_atomicity; /root/b2b_dependencies; /root/b2b_gameplay
branch:       design/endless-progression-owner-packet
commits:      1e0d6ac..HEAD
suite:        3,642 tests / 3,627 passed / 0 failed / 15 expected fresh-tree skips; handoff-navigation 7/7; design-record checks passed
supersedes:   2026-09-25-1424--relic-continuity-root-preservation-selected
---

# Handoff — irreducible multi-root current-state support selected

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`. The sole active Relic frontier is
`RCS-03C3B2B1`; present that one card only.

## Where things stand

The owner selected `RCS-03C3B2A4-B`. `E^{state-mroot}_v` is nonempty: at
least one prospectively versioned current-state source form must have a
reachable, committed, non-preempted, materially functional occurrence whose
inclusion-minimal direct-support basis contains multiple independent roots.
The roots are co-necessary at one authoritative cut, have disjoint complete
lineages, have no ancestor relationship, and share no nonmultiplying
authorization/accounting parent.

The occurrence remains one fact with one continuity identity and one Relic
position. Exact forms, support widths, counts, distribution, prevalence, and
redundant alternatives remain AUTHOR/SPEC. Root-atomic states remain legal.
`Tempered Formation`, jointly sustained by independent Guarding and Heat-ready
roots, is the boundary example. A4 mints and exposes no root and grants no
downstream use.

The post-answer audit proved old B2B non-atomic: prior answers do not determine
whether any one exact build provenance can supply two otherwise-distinct roots,
so an equivalence policy could be vacuous. Old B2B is now a non-counting parent
for support child `RCS-03C3B2B1` and conditional equivalence child
`RCS-03C3B2B2`. This fourteenth prerequisite correction changes the register
to fifty-seven slots: 27 `SCREEN`, 1 `OWNER-OPEN`, 7 `PRUNED`, 19
`DIR-SELECTED`, 1 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`; `Phi_SR = 28`.
The authoritative decision record remains unchanged because a bare option
label selects direction but does not complete its acceptance protocol.

## Highest-value next work

Present `RCS-03C3B2B1`: can one exact configured build source produce two
separately accepted and accounted combat-evidence occurrences that remain
distinct roots after all selected B2A canonicalization and are both materially
admissible pure-build candidates at one evaluation cut?

For evidence occurrence `x`, `Pi(x)` is its exact build-provenance signature:
source combatant, frozen legal configuration revision, every indispensable
exact configured causal origin, and each origin's configuration-side
ownership/custody provenance. It excludes occurrence identity and
occurrence-specific authorization, payment, and accounting lineages, which
must remain separate when `Pi` is equal. Equal item definitions, tags, actions,
or presentation do not make different origins equal. A qualifying pair must be
semantically distinct, have disjoint complete lineages and separate accounting,
and cannot be fields, targets, hits, phases, callbacks, packets, aliases,
action/build views, or children of one canonical root.

Shared immutable provenance/equip attribution is metadata rather than a
causal-lineage member or authorization/payment parent; reuse of one accepted
payment event disqualifies the pair. Candidate admission is B2B1's immediate
authoritative result: after the second occurrence, the source ledger either may
or may not contain both same-`Pi` roots as candidate records. It changes the
input to B2B2/B3 without granting position use; exact UI remains downstream.

Present exactly two exhaustive directions:

- A: `E^{build-repeat}_v` is empty. No exact provenance signature has two
  otherwise-independent post-B2A roots simultaneously admissible as pure-build
  candidates. Pure-build relationships must cross exact provenance signatures.
- B: `E^{build-repeat}_v` is nonempty. At least one prospectively versioned
  source form has a reachable functional witness with two distinct post-B2A
  roots carrying the same exact provenance signature. Exact forms, counts,
  distribution, and prevalence remain AUTHOR/SPEC.

Recommend B. Under frozen `Bastion#17` provenance, Aster separately authorizes
and pays for one Guard occurrence at `t_1` and another at `t_3`, each tied to a
different defended attack. Under A, the source ledger cannot admit both as a
same-provenance pair; under B, at least one form admits both as distinct
candidate roots with separate occurrence-accounting lineages and equal `Pi`.
B preserves repeat-source design space without promising a repeat-source build
or power gain. It is a **partial** local Achintya Bheda Abheda fit grounded in
direct same-provenance participation.
B2B2 decides only remain-distinct versus coarsen for build identity; even a
remain-distinct answer grants no position occupancy until B3.

Do not add a universal-versus-mixed C. Empty versus nonempty support is the
player-material boundary; catalog saturation remains AUTHOR/SPEC. If B is
selected, B2B2 becomes active. If A is selected, B2B2 is pruned and B3 becomes
active.

## Hard rules

- Resolve exactly one prerequisite-ready owner choice, then recompute, commit,
  and push before presenting another.
- B2B1 mints, promotes, coarsens, and exposes no root. B2B2 decides only
  remain-distinct versus coarsen for build identity; B3 owns position use.
- One operation's action/build views remain one lineage. An A4 multi-root fact
  remains one fact and one position; it cannot counterfeit a B2B1 pair.
- Different combatants, owners, configuration revisions, or exact causal
  origins do not share `Pi` merely through equal definition, tag, or action.
- Upgrades, reforges, unequip/re-equip, cross-encounter continuity,
  persistence, migration, addressability, child partition, nesting, fan-out,
  claims/reuse, payoff, UI, release, and implementation remain downstream.
- Do not edit the authoritative decision record without an explicit
  `accepted`, `rejected`, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Traps from this round

- Do not infer a nonempty repeated-provenance domain from pure-build entry
  support plus contingent evidence. Those rules admit one-shot provenance
  worlds.
- Do not collapse support existence into equivalence policy. A policy over an
  empty domain is not a player choice.
- Do not turn a definition/tag match into provenance equality or allow a
  build/action view to become an additive root.

## Verification and Git

The three read-only reviews independently checked B2B atomicity, upstream
ownership, and gameplay/ideal fit. The full serial suite passed with 3,642
tests: 3,627 passed, 0 failed, and 15 expected fresh-tree skips. The targeted
handoff-navigation suite passed 7/7, the register reparsed to the stated 57-row
distribution and `Phi_SR = 28`, and the authoritative decision record retained
SHA-256 `e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
PR #3 already exists. Push the feature branch; do not merge it.
