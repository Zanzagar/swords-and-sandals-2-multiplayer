# Handoff — Every dual tag has a both-form history; path saturation next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-22 21:53:48
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.145: whether every legal recommit history of every
dual-form fixed tag is both-form, every dual tag mixes both-form and single-
form histories, or saturated and mixed dual tags coexist. The owner selected
C3c.144-A. Preserve accepted EP-D02 wording until a complete replacement is
replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Finish the current
full-assurance pass before the standalone expansion/audit/adaptation pass.

## Selected direction — EP-D02-C3c.144-A

For each dual tag `k`, `W_v(k)` is its legal adjacent same-tag recommit
histories and `K^{path}_v(k,w)` is the set of C141-qualified independent forms
occurring on one actual history. Let `E^{both-path}_v` contain the dual tags
having at least one history whose set is `{Pred,Rec}`.

C144-A fixes `E^{both-path}_v=D^{dual}_v`: every dual tag has at least one
actual legal history containing both an `I_P` and an `I_R` opportunity. The
forms may occur at different opportunities and `chi` cuts. One opportunity
counts both only through two distinct isolation pairs. Other histories may
still be single-form.

Each actual `chi` is **direct**; two forms within one fixed-tag temporal
history are **partial** because their cuts may differ; other paths are
**neutral**; universal dual-tag participation is **aggregate**. C144-A's cost
is two-form authoring for every dual tag and a token loophole because one
obscure both-form history per tag suffices.

Every branch retains `9/23/138`, four represented IDs, eight lived edges, four
complete IDs, no incomplete represented ID, Souls `{6,9,10,12,14,15}`, degree
six, and C46-A's same-boundary maximum of two. The authoritative decision
record remains unchanged.

## Why path saturation is next

C144-A establishes tag participation before within-tag prevalence. A pooled
classification over all histories would conflate every dual tag being mixed
with one mixed tag beside saturated tags. Specialist polarity classifies tags
outside the dual class, and continuous context classifies availability along a
history; neither is a prerequisite for path prevalence inside dual tags.

## Active owner choice — EP-D02-C3c.145

For each `k in D^{dual}_v`, define

`B^{both}_v(k)={w in W_v(k) : K^{path}_v(k,w)={Pred,Rec}}`.

C144-A makes every `B^{both}_v(k)` nonempty. Define

`U^{all-both}_v={k in D^{dual}_v : B^{both}_v(k)=W_v(k)}`.

A tag outside `U^{all-both}_v` is path-mixed: it has at least one both-form
history and at least one distinct single-form history. The single-form history
may be predicate-only or recurrence-only; mixedness does not require both
singleton polarities.

Membership in `U^{all-both}_v` requires complete reachable-domain proof that
every legal history contains both forms. Nonmembership needs one legal history
whose complete opportunity domain establishes exactly one form. Incomplete
analysis remains unresolved.

### A — every dual tag is both-form on every path — recommended

`U^{all-both}_v=D^{dual}_v`. Every legal history of every dual tag has both an
`I_P` and an `I_R` opportunity, although actions, order, opportunities, and
`chi` cuts may differ.

A closes the one-obscure-both-form-history loophole and gives “dual” a
dependable played meaning. C143-C's specialists remain the focused one-form
teaching class. Every `chi` is **direct**; two forms in one temporal history
are **partial**; excluding single-form dual paths is **neutral/protective**;
universal tag-and-path prevalence is **aggregate**. Costs are universal two-
form authoring and proof, possible ritual/menu friction, flexibility-premium
pressure, and an artificial-second-comparison loophole on every history.

Example: every legal P history contains some Rekindle/Vent `I_P` comparison
and some Invoke/Bank `I_R` comparison before `q2`, though their order and exact
actions can differ.

### B — every dual tag mixes both-form and single-form paths

`U^{all-both}_v` is empty. For every dual tag, `B^{both}_v(k)` is a nonempty
proper subset of `W_v(k)`. Every dual tag therefore has at least two distinct
legal histories: one both-form and one single-form.

B is the strongest path-variety countercase. One persistent relationship can
support a rich two-lever history and a simpler focused history instead of
forcing a two-form ritual into every recurrence. Each `chi` is **direct**;
fixed-tag distinction across histories is **partial**; the focused path's
missing form is **neutral/protective**; universal path mixing is **aggregate**.
Costs are less predictive tag grammar and a two-sided token loophole: one
obscure single-form history among ordinary both-form histories or one obscure
both-form history among ordinary single-form histories suffices. Access and
player control over the split remain unselected.

Example: fixed tag P has a Rekindle-then-Invoke both-form history and a fully
analyzed Rekindle/Vent predicate-only history. Combatant, Soul, exact Relic
realization, Charms, source configuration, occurrence contract, evaluation
boundary, and version do not change.

### C — path-saturated and path-mixed dual tags coexist

`U^{all-both}_v` is a nonempty proper subset of `D^{dual}_v`. At least one dual
tag is both-form on every history; another has both-form and single-form
histories.

C offers the widest vocabulary, but creates a third behavioral class beside
specialists and saturated duals. Each `chi` is **direct**; same-history
participation and fixed-tag cross-history continuity are **partial**; focused-
path exclusion is **neutral/protective**; coexistence is **aggregate**. Costs
are disclosure/testing burden, power-premium risk, and the same two-sided token
risk inside the mixed class.

Example: every P history is both-form. Dual Q has one both-form and one
predicate-only history. Specialist R remains outside the domain.

## Compatibility and deferrals

A/B/C exhaust the nonempty dual domain's saturated subset being full, empty,
or nonempty proper. “Exactly one both-form history” is a later quantitative
refinement, not a peer topology; a singleton history set would otherwise make
“all” and “exactly one” coincide.

A adds no tag or tagged-history-count floor. B requires at least two histories
per dual tag. C requires at least two dual tags and at least two histories for
its mixed tag; with C143-C's specialist, the inherited three-tag floor makes C
feasible. No option adds a receipt, Relic definition, exact key, result, same-
opportunity requirement, or same-`chi` requirement.

C145 selects no saturated/mixed identities; singleton-form polarity or whether
both singleton polarities occur; exact counts, fractions, or ordinary-path
dominance; accessibility, visibility, control, frequency, or anti-token
thresholds; same-opportunity co-location, equal `chi`, order, or deliberate
mode choice; specialist polarity or both specialist classes; continuous
context or outages; predicate internals; retained sources; evaluator schedule;
actor or causation strength; cadence, payoff, persistence, UI, release, or
implementation. Under B/C, singleton-form polarity inside mixed dual tags is
next. Under A, recompute the frontier. Specialist polarity and continuous-
context breadth remain separate siblings.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C145 only. Record
the selected branch, recompute the frontier, and present exactly one next
prerequisite-ready choice. Do not infer acceptance of the whole EP-D02
replacement or start implementation.
