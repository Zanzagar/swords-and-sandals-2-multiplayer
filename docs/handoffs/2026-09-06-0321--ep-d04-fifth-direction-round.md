# Handoff — EP-D04 fifth direction round pending

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-06 07:21:11
**Branch:** `design/endless-progression-owner-packet`

Resume the sole EP-D04 R5 cell-unit choice in the owner packet. Preserve
accepted EP-D01, EP-D02, and EP-D07 and all selected-but-unaccepted EP-D04
directions. Do not edit the authoritative record or implement anything.

## Session outcome

The owner's `lets do 1A and 2A` selected both R4 recommendations; it did not
accept a final EP-D04 replacement.

1. The primary combat-ceiling gate gives all-Legendary and mixed families the
   same family-neutral preregistered selected route, exogenous attempt state,
   and disclosure schedule. A separate later-versioned offer-policy view begins
   at the offer set and may veto release, but it cannot rescue a failed paired
   combat gate. Neither policy chooses its own test route.
2. Rarity classifies the complete allied composition in team modes. Every one
   of each allied gladiator's sixteen evaluated hosts is Legendary for an
   all-Legendary claimant; a mixed strict winner contains a causally active
   Tempered or Inscribed payload. Team Tactic remains outside rarity. This
   creates a legal-catalog obligation for compatible all-Legendary 2v2/3v3
   compositions under team budgets.

## Recomputed frontier

R5 has one prerequisite choice before offer-set identity and weighting:

- **R5.1-A, recommended:** pin the complete version tuple and use a canonical
  hierarchy of mechanics-reporting parents, exact ordered-Circuit leaf
  fingerprints, nested encounter diagnostics, and paired roots/seeds as
  within-leaf replicates. Each leaf includes every exogenous canonical field
  that can change disclosed information, legal transitions, or outcomes;
  broad budget bands cannot replace exact breakpoint-relevant opponent
  projections. Cosmetic IDs do not split leaves.
- **R5.1-B:** treat every root/seed as an equal cell, permitting sample-count
  weighting attacks.
- **R5.1-C:** treat every authored route/display ID as a cell, permitting
  cosmetic-clone weighting attacks.

Under A, encounter order is semantic, and hidden variants can be distinct
leaves while sharing one player-visible information class. A policy sees only
authorized disclosures. Adding roots or renaming a route cannot change its
semantic weight; a mechanically changed recipe that retains the old fingerprint
fails closed.

The ceiling search may consider any legal player-authored persistent/build
projection, but every variable obeys its actual commitment horizon; persistent
stats cannot adapt to hidden leaves. Those build choices are policy candidates
across the grid, not environment cell axes. The lower-rarity causal test retains
its paired non-rarity/environment controls and legal re-optimization protocol.

After R5, define the offer set as a joint choice context composed of the selected
route units—not as independent routes—then choose distribution authority and
weights. Numeric coverage, Legendary lead/concentration bounds, ties, solver
sufficiency, unresolved-cell treatment, migration/release authority, and final
normative replay remain later.

## Verification

`git diff --check` passed after this handoff. The authoritative decision record
has no diff. No runtime code, tests, vanilla evidence, licensed assets,
installation, save, snapshot, capture, candidate, fixture, observation,
manifest, or golden changed. Tests were not run for this docs-only owner round.
No commit or push was made. Implementation remains blocked.
