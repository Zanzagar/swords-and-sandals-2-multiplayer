# Handoff — Per-history co-legibility selected; per-opportunity coverage next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-24 03:24:30
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.164: per-opportunity any-form pre-choice
disclosure coverage. The owner's latest `A` selected C3c.163-A and nothing in
C164. This remains Soul Relic design, specifically the player-information
surface of the selected same-opportunity, one-hinge result-rearm grammar.
Preserve accepted EP-D02 wording until a complete normative replacement is
replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready bounded choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Do not batch per-
opportunity any-form coverage with every-opportunity two-form saturation,
shared presentation, communication, accessibility, value, or fun.

## Selected direction — EP-D02-C3c.163-A

`U^{pre-all-both-op}_v=D^{dual}_v`. Every exact legal history of every
mechanically dual tag contains at least one atomic selection whose single
actual command-holder `O^-` makes a qualifying P pair and distinct qualifying
R pair intelligible before commitment. Other joint opportunities on that
history may expose one form or neither.

At the fixed tag/history/role/selection boundary, continuing Soul–Relic
context and both distinct forms coexist **directly**; their later relationship
to `q2`, effects, and the wider biography remains **partial**; excluding fully
opportunity-separated histories is **neutral/protective**; and universal
history/tag saturation is **aggregate**.

C163-A adds no tag, history, opportunity, pair, continuation, cut, receipt-
width, menu, or visible-button floor. The tight three-tag construction and all
fixed catalog and width floors remain unchanged.

## Active owner choice — EP-D02-C3c.164

For each legal `w in W_v(k)`, define

`D^{joint}(w)={d in D^{live}(w) : J^{both-op}(w,d)}`,

`J^{pre+}(w,d) iff J^{both-op}(w,d)
and (Lambda_P(w,d) is nonempty or Lambda_R(w,d) is nonempty)`,

`D^{pre+}_v(k,w)={d in D^{joint}(w) : J^{pre+}(w,d)}`,

`D^{pre0}_v(k,w)=D^{joint}(w) setminus D^{pre+}_v(k,w)`,

and

`U^{pre-all-op+}_v={k in D^{dual}_v : for every w in W_v(k),
D^{pre+}_v(k,w)=D^{joint}(w)}`.

C163-A makes `D^{pre+}_v(k,w)` nonempty for every history through at least one
co-legible choice. A joint opportunity in `D^{pre0}` is **P/R-pre-choice-
opaque**: both Lambda sets are empty. It may still show action names, costs,
generic lore or warnings, committed-result identity, and post-choice feedback.
Opacity means only that no qualifying independent P or R comparison is
intelligible to the actual command-holder before that atomic selection.

```text
ALL-JOINT-OPPORTUNITY ANY-FORM COVERAGE
d_converge: P and R accounts
d_bond:     P account only
d_manifest: R account only

OPACITY-BEARING TAG
d_converge: P and R accounts
d_veiled:   mechanically joint, but no qualifying P or R account
```

### A — every mechanically joint opportunity exposes a form — recommended

`U^{pre-all-op+}_v=D^{dual}_v`. For every dual tag, exact legal history, and
live mechanical joint opportunity, the actual command-holder's authorized
`O^-` makes at least one qualifying P or R pair intelligible before commitment.
An opportunity may expose P only, R only, or both.

At the exact tag/history/role/selection boundary, each disclosed form is
**direct**. A one-form opportunity's relationship to the complete dual grammar
is **partial**; excluding total opportunity-level opacity is
**neutral/protective**; and universal opportunity/history/tag coverage is
**aggregate**.

A prevents a mechanically dense dual-form choice from being wholly causally
unexplained while preserving focused one-form choices. Costs are explanation,
localization, and validation at every joint choice, less occult mystery,
repetitive-hint risk, and token compliance through one shallow account per
opportunity.

Example: Ashen's Converge choice explains P and R, a later Bond choice explains
P only, and Manifestation explains R only. Every mechanically joint choice has
at least one truthful causal foothold.

### B — every dual tag retains an opaque joint opportunity

`U^{pre-all-op+}_v` is empty. Every dual tag has at least one exact mechanical
joint opportunity with both Lambda sets empty. C163-A supplies a distinct co-
legible opportunity on that opaque witness's same history. B does not make
every history or every opportunity opaque.

This is the strongest occult-beat countercase. Positive accounts remain
**direct**; continuity between explained and veiled choices is **partial**;
the opaque choice's player-facing causal relationship is
**neutral/unsupported**; deliberate mystery may be **neutral/protective**; and
universal opacity-bearing topology is **aggregate**.

Costs are blind commitment at a mechanically dense choice, guide or data-
mining advantage, apparent information inconsistency, and token opacity
through one obscure opportunity.

Example: an Ashen history has a co-legible `d_converge`. A distinct `d_veiled`
still has inherited P and R mechanical pairs and three semantic continuations,
but its command-holder receives no qualifying pre-choice account of either.

### C — fully covered and opacity-bearing dual tags coexist

`U^{pre-all-op+}_v` is a nonempty proper subset of `D^{dual}_v`. At least one
dual tag exposes P or R at every joint opportunity. At least one distinct tag
has C163-A's co-legible opportunity on every history but also has an opaque
joint opportunity somewhere.

The classes inherit A/B's local ratings and their coexistence is
**aggregate**. Costs are a hidden information taxonomy, uneven learning
burden, likely clarity or mystery premiums, and token negative membership.

Example: every Ashen joint opportunity explains at least one form. Dreamglass
has a co-legible Study choice on every history but also one Veiled joint choice
explaining neither form.

## Proof, human-control consequence, floors, and deferrals

A/B/C are exhaustive because `U^{pre-all-op+}_v` is a subset of the nonempty
dual domain: whole, empty, or nonempty proper. Individual joint opportunities
may still support `empty`, `{P}`, `{R}`, or `{P,R}`; C164 projects the three
nonempty states together. C163-A makes every history's positive set nonempty,
so an opacity-bearing tag is never wholly opaque and its opaque opportunity is
distinct from the co-legible opportunity on that same history.

A requires, for every exact legal `(k,w,d)` satisfying `J^{both-op}`, one
positive `Lambda_P(w,d)` or `Lambda_R(w,d)` pair under the target `u(w,d)` and
`O^-`. B requires, for every tag, one exact joint opportunity and exhaustive
proof that both Lambda sets are empty across its qualifying pairs, lawfully
human-controllable observation contract, and reachable authorized references.
C combines universal A-style coverage for one tag with one B-style opaque
witness for another. One unclear pair, hidden menu, AI-controlled occurrence,
or incomplete search does not prove opacity; incomplete analysis is unresolved.

Accounts from another opportunity or history cannot rescue the target choice.
A reference counts only when authorized, reachable inside the target choice's
exact `O^-` before commitment, and truthful for its actual legal arms. Post-
choice feedback, private AI state, external guides, empirical inference, fused
non-causal labels, and another seat's uncommunicated information remain
invalid. Non-joint combat choices are outside C164.

C156 makes a Lambda witness depend on a lawfully human-controllable observation
contract. A therefore excludes a joint opportunity whose actual command-holder
role can never be lawfully human-controlled. This constrains role capability,
not bearer/ally/opponent identity, actual human occupancy on every occurrence,
or a persistent controller. Under B/C, opacity may instead come from missing
P/R information for a controllable role; the branch does not choose why.

Tight indexed incidences are:

| Branch | Candidate tags | Dual `(k,w)` incidences | Joint opportunities | Positive/co-legible opportunities | Opaque opportunities | Form memberships / legible pairs | Mechanical pairs / continuations |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| A | 3 | 1 | 1 | 1 | 0 | 2 / 2 | 2 / 3 |
| B | 3 | 1 per dual tag | 2 per dual tag | 1 per dual tag | 1 per dual tag | 2 / 2 per dual tag | 4 / 6 per dual tag |
| C | 4 | 2 total | 3 total | 2 total | 1 total | 4 / 4 total | 6 / 9 total |

Opaque opportunities add mechanical-pair and continuation incidences but zero
form-membership or legible-pair incidence. These are indexed witnesses, not
necessarily distinct global histories, actions, texts, references, assets, or
buttons. No branch raises local width or adds a receipt, result width, decisive
cut, definition, exact key, or Soul row.

All branches retain nine definitions, twenty-three exact keys, 138 Soul/key
rows at six Souls, four represented locked IDs, eight lived aligned edges,
four one-history-complete IDs, no incomplete represented ID, Souls
`{6,9,10,12,14,15}`, degree six, and C46-A's same-boundary width two.

C164 defers class identities, support polarity at singleton opportunities,
counts, fractions, frequency, access, actual controller, ordinary-route
prevalence, every-opportunity two-form saturation, one explanation/reference/
presentation surface, bearer/ally/opponent command-holder identity, role
stability or communication, hinge disclosure, modality, discoverability,
accessibility, wording, timing, feedback, preview, value, non-dominance,
payoff, power, fun, UI, release, and implementation.

Under recommended A, resolve every-opportunity two-form pre-choice disclosure
saturation next: after requiring some causal foothold at every joint choice,
ask whether P and R must both be co-legible at each one.

## Repository state

The owner packet, rigor audit, living head, and systems map carry C163-A and
make C164 the sole active owner choice. The authoritative decision record was
not edited and should retain SHA-256
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.

This was a documentation-only design update. Validate tracked and untracked
whitespace, SVG XML parsing, one active frontier, and the authoritative-record
hash. Do not run the Node suite because no code or executable contract changed.
