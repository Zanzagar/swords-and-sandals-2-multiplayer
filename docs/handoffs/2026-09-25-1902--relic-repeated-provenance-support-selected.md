---
handoff:      2026-09-25-1902--relic-repeated-provenance-support-selected
written:      2026-09-25 19:02 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    /root/b2b_atomicity; /root/b2b_dependencies; /root/b2b2_gameplay_clean
branch:       design/endless-progression-owner-packet
commits:      e881b76..HEAD
suite:        3,642 tests / 3,627 passed / 0 failed / 15 expected fresh-tree skips; handoff-navigation 7/7; design-record and register checks passed
supersedes:   2026-09-25-1542--relic-multi-root-state-support-selected
---

# Handoff — repeated same-provenance Relic support selected

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`. The sole active Relic frontier is
`RCS-03C3B2B2`; present that one card only.

## Where things stand

The owner selected `RCS-03C3B2B1-B`, requiring at least one functional
pure-build source form in which two separately committed and accounted combat
occurrences share exact configured provenance but retain distinct post-B2A
roots and disjoint lineages. This is candidate admission only; it grants no
position use and mints or merges no root.

The fifty-seven-slot register now has 26 `SCREEN`, 1 `OWNER-OPEN`, 7
`PRUNED`, 20 `DIR-SELECTED`, 1 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`;
`Phi_SR = 27`. The authoritative decision record remains unchanged because a
bare option label selects direction but does not complete acceptance.

## Highest-value next work

Present B2B2's three equivalence-relation policies over every distinct
equal-`Pi` candidate pair, including pairs spanning semantic source forms:

- A: relate no distinct pair; every separately paid root remains a singleton
  pure-build identity.
- B: relate every equal-`Pi` pair; exact provenance alone determines one
  canonical class, retaining every member's payment, history, outcome, and
  complete lineage.
- C: relate a nonempty proper subset through one stable disclosed finer
  equivalence key, with reachable related and still-separated equal-`Pi`
  witnesses.

Recommend B, “one exact configured provenance, one build identity.” It is the
strongest direct local Achintya Bheda Abheda fit and blocks roots-per-repeat
farming. State the real cost: same-source repetition no longer creates build
diversity and may increase partner-source pressure, which later content
authoring must control. Append-only class ancestry can also keep a later
partner with intersecting lineage non-independent because an expired member
remains historical ancestry until the RCS-15-bounded class lifetime ends. Use
the matched Bastion example in the owner packet: all four roots share exact
`Pi` across two semantic forms; A leaves four singletons, B makes one class,
and C may make two form-keyed classes.

## Hard rules

- Resolve exactly one prerequisite-ready owner choice, then recompute, commit,
  and push before presenting another.
- B2B2 is replacement/equivalence in the pure-build projection, never an
  additive root or a global B2A causal-root rewrite.
- The relation and class keys are prospective, reflexive, symmetric, and
  transitive. Joins are atomic and idempotent at
  member commitment, cannot rewrite settled results, and settle same-cut
  cohorts before the coherent evidence read.
- Coarsening cannot refresh H age, prolong S, resurrect evidence, open
  readiness, or merge different `Pi`.
- SR-07 owns whether source mutation creates a new configuration revision;
  RCS-15/SR-11 owns class lifetime across unchanged configurations and
  encounter boundaries.
- B3 owns addressability and position use; B4 owns child partition; C3C-C3F
  and RCS-08 own fan-out, claims, consumption, reuse, and team authority.
- Do not edit the authoritative decision record without an explicit
  `accepted`, `rejected`, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Traps from this round

- A same item definition, tag, or action is not equal `Pi`; source combatant,
  configuration revision, exact configured origin, and custody provenance all
  remain part of the signature.
- A late second member cannot retroactively validate an earlier recipe or
  refresh an expired first member.
- C is not barred catalog prevalence: it creates two authoritative identity
  classes within one exact provenance and therefore a different legal future
  build/counterplay surface.
- The first internal card draft incorrectly scoped cohorts by source form and
  omitted equal-`Pi` cross-form equivalence. Atomicity review caught it before
  commit; do not restore that nonexhaustive form-subset trichotomy.
- The first gameplay reviewer violated its explicit write-nothing brief by
  creating and then deleting two temporary files. No repository, Git, oracle,
  save, or capture state changed, but that pass was excluded. The named clean
  replacement review performed no writes and independently re-derived the
  recommendation.

## Verification and Git

Three clean read-only rechecks report no remaining atomicity, boundary, or
gameplay flags after the corrections above. The register reparses to the stated
57-row distribution and `Phi_SR = 27`; the authoritative decision record
retains SHA-256
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
`git diff --check` passes, the targeted handoff-navigation suite passes 7/7,
and the full serial suite passes with 3,642 tests: 3,627 passed, 0 failed, and
15 expected fresh-tree skips. Commit this round atomically, push the feature
branch, verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
