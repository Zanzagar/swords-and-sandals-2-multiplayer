# Handoff — C73 stable affinity group count selected; group-size profile next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-16 01:37:44
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.74: whether no, some but not all, or every affinity
Relic definition may vary its unlabeled singleton-map Soul-group-size profile
across exact operative simulacra. The owner selected C3c.73-A. Preserve the
accepted EP-D02 wording until a complete replacement is replayed and explicitly
accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. If no option remains consistent,
offer or recommend a replacement rather than weaken the north star. Continue
the exact one-at-a-time A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle remain in their SS2 lane.

## Selected direction

`D^rho_{var,v}` is empty. Every affinity definition keeps one number `rho` of
singleton-map Soul-equivalence blocks across all of its exact operative keys in
a completed version. Exact keys carrying one definition may still change the
block-size profile, exact Soul membership, singleton labels, sole-core label,
and predicates. Pair-complete prisms and one-only definitions retain their
derived counts `|S_v|` and one.

C73-A retains `3<=|S_v|<=15`, a local three-key floor, and a four-key floor in
a version carrying C39-C's variable-instance witness. It selects no exact
`rho`, profile, partition, labels, variation cause, transformation, disclosure,
power, or implementation. It changes no authoritative wording.

## Active frontier

For an affinity exact key `b`, define the unlabeled group-size profile

`lambda_v(b)=sort_desc(multiset{|B| : B in Pi_v(b)})`.

It records only the sorted block sizes. It forgets block order, Soul identities,
and which singleton-label map owns a block. Thus `{F,V}/{S,W}` and
`{F,S}/{V,W}` both have `(2,2)`, while `{F,S,V}/{W}` has `(3,1)`.

For affinity definition `d`, let `Profiles_v(d)` collect the `lambda` values of
its exact keys and let `D^lambda_var` contain definitions with at least two
values.

- **A, recommended — every affinity definition is profile-stable:**
  `D^lambda_var` is empty. A name keeps one relational silhouette while exact
  simulacra may regroup which Souls occupy its blocks. Inherited ideal fit is
  direct inside each fixed single-core affinity/Soul-roster relationship;
  C74's marginal fit is neutral and protective. It prevents a hidden `(2,2)`
  versus `(3,1)` copy-quality roll. Cost: one name cannot move between balanced
  and outlier shapes.
- **B — stable and profile-variable affinity definitions coexist:**
  `D^lambda_var` is a nonempty proper subset. Exceptional names may change
  silhouette while at least one other name is stable. Marginal fit is partial
  as an alternate-simulacrum metaphor. It can be exciting when visible and
  choosable, but adds a taxonomy, a potential token stable name, and preferred-
  copy pressure.
- **C — every affinity definition is profile-variable:**
  `D^lambda_var=D_affinity`. Every affinity name needs a profile-changing pair.
  Marginal fit is merely aggregate across separate forms. It maximizes contrast
  but universalizes comparison, disclosure, authoring, and migration burdens.

With Souls `F/S/V/W`, stable palette `{a,b,f}`, singleton count two, sole core
`a`, and stable `rho=2`:

- `A31`: `F=S=V={a,b}`, `W={a,f}` has profile `(3,1)`;
- `A31'`: `S=V=W={a,b}`, `F={a,f}` also has `(3,1)`;
- `A22`: `F=V={a,b}`, `S=W={a,f}` has `(2,2)`.

A permits `A31/A31'`; B or C can use `A31/A22`. B additionally needs a distinct
stable affinity name; Tide may use `F=S={c,e}` and `V=W={e,f}` on palette
`{c,e,f}`, with sole core `e` and profile `(2,2)`. A Dreamglass prism using the
four three-of-four subsets of `{a,b,c,d}` and an Echo one-only key using
singleton `{e}` on palette `{e,f}` preserve the inherited structural witnesses
and all-six-label coverage.

For `n=|S_v|`, profile variation at stable `rho` is possible exactly when
`2<=rho<=n-2`. B/C therefore require at least four Souls but, unlike C73
variation, require no new palette-degree floor; degree three already realizes
`(3,1)` and `(2,2)`. The fifteen-Soul ceiling does not tighten. A/B/C Soul
ranges are `3..15/4..15/4..15`; local exact-key floors are `3/5/4`, C39-witness
floors are `4/5/4`, and minimum witness Soul/key rows are `12/20/16`. A profile-
variable pair reuses C39 only if its keys are distinct instances and also differ
materially in one shared-cell predicate; profile change alone is not evidence.

C74 selects no exact profile, partition membership, persistent Soul pairs,
block-to-map assignment, singleton or core labels, instance/state/boundary
causation, transformation, coexistence, live switching, predicates, packages,
contexts, receipts, routes, disclosure, ease, frequency, power, acquisition,
persistence, release, or implementation.

Recompute the frontier from the answer. Under A, an ordinary exact-partition-
stability choice remains. Under B, profile-variable definitions are already
partition-variable, so a partition card must classify only the profile-stable
remainder. Under C, universal exact-partition variability is derived. Affinity
core-label continuity remains an independently ready sibling.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C73-A
and C74 as the sole active owner choice. The authoritative decision record is
unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04 directions remain
unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain selected, and
R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Documentation, construction,
bound, pointer, and SVG validation passed: tracked and untracked whitespace
checks were clean; the authoritative decision record remained unchanged; the
SVG parsed as XML; explicit A/B/C constructions preserved C57-C73 and all-six-
label coverage; integer-partition enumeration reproduced the profile theorem;
the roster, definition, key, and witness-row floors reproduced; C74 was the
sole active owner-packet heading; and this file resolved as the newest handoff.
Independent read-only checks found no mathematical, construction, atomicity, or
deferral defect in the final C74 card. This documentation-only turn did not run
the repository test suite.
