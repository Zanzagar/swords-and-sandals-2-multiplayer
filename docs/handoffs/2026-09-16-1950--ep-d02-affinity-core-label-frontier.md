# Handoff — partner-fluid equal affinities selected; sole-core label next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-16 23:50:59
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.80: whether every affinity Relic definition keeps
one sole all-Soul core cell label across its exact operative keys, stable-core
and variable-core names coexist, or every affinity name changes that core. The
owner selected C3c.79-C. Preserve the accepted EP-D02 wording until a complete
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle remain in their SS2 lane.

## Selected direction

`D^Pi_{var-equal,v}=D^equal_v`. Every equal-size affinity definition realizes
at least two unordered exact Soul partitions while every supported Soul keeps
the definition's common group-size role. The selected affinity dialects are:

- equal-size names are partner-fluid;
- size-role-stable mixed names are fixed asymmetric covenants; and
- role-variable mixed names are morphic.

Every fixed key retains direct ideal fit through one all-Soul core, universal
peer participation, and unequal singleton maps. Same-name cross-simulacrum fit
is partial because the alternate partitions live on separate exact keys, and
the universal catalog mandate is aggregate.

C79-C retains supported-Soul counts `{6,8,9,10,12,14,15}`. Tight local floors
are five definitions, seven exact keys, and forty-two Soul/key rows at six
Souls. Degree-three affinities and the degree-five prism remain tight. A
combined C39 witness still needs distinct instances and a separate material
shared-cell predicate difference; partition change alone is not C39 evidence.
C79 selects no exact partners, owner-Soul relevance, map assignment, label
continuity, cause, transformation, disclosure, agency, power, or
implementation and changes no authoritative wording.

## Frontier audit

For each affinity key `b`, C68-A gives

`G_v(b)=intersection_{s in S_v} I_v(b,s)={kappa_v(b)}`.

`kappa_v(b)` is the sole singleton-backed temporal-by-basis cell label shared
by every supported Soul. It is not an equipment Core, Ascendancy Core, stat,
or cosmetic tag. C71-A gives a definition one stable total cell palette, so
same-definition core labels are comparable. Define

`CoreLabels_v(d)={kappa_v(b):b in R_{affinity,v} and def_v(b)=d}`

and

`D^kappa_{var,v}={d in D_{affinity,v}:|CoreLabels_v(d)|>=2}`.

No support card is missing. C68 gives `1=g<k<d`, so every affinity has a
noncore singleton label. Swapping the current core `c` with such a label `x`
inside every Soul map preserves palette, singleton count, equality partition,
size-role map, pairwise overlap, and exactly-one-core status while changing the
core to `x`. This proves structural eligibility only; it does not authorize a
live transformation or prove player value.

The finer same-role/different-partner question inside morphic definitions does
not precede C80. It is possible only for role-variable mixed profiles with a
repeated group size. That domain first needs a separate repeated-size-support
choice, after which actual equal-role partner variation can be asked of the
eligible names. It is a refinement of definitions already known to be
partition-variable, not a prerequisite for the coarser core-label projection.

## Active frontier

- **A, recommended — every affinity definition keeps one core label:** a named
  affinity has one semantic heart across all exact simulacra. Tide can change
  partners and Ashen can recast size roles around that enduring motif. C80's
  marginal fit is neutral/protective; on already-variable definitions the
  cross-simulacrum continuity-with-difference analogy is partial, not direct.
  The cost is forbidding a same-name affinity from migrating its universal
  temporal/basis motif, and later content must keep noncore changes meaningful.
- **B — stable-core and variable-core names coexist:** at least one affinity
  changes its universal label and another keeps one. This permits exceptional
  “wandering-heart” names, but creates another hidden subtype and preferred-
  copy risk. Its cross-key fit is partial and catalog coexistence aggregate.
  If selected, a later choice must decide which of the three affinity dialects
  participate; C80-B does not silently assign them.
- **C — every affinity definition changes its core label:** every affinity name
  has at least two exact keys with different universal cell labels. Fixed-key
  fit remains direct, but cross-key fit is only partial and the universal
  mandate aggregate. This removes every definition-wide semantic heart, blurs
  the rooted-affinity contrast with coreless prisms, and forces even fixed
  Veil-like covenants to gain a second key solely to move their core.

Six-Soul examples on stable palette `{a,b,c}`:

```text
stable-core Tide
T1: FSV={c,a} | WTR={c,b}
T2: FSW={c,a} | VTR={c,b}   partners change; core c remains

variable-core Tide
T1: FSV={c,a} | WTR={c,b}   core c
T2: FSW={a,b} | VTR={a,c}   core a

variable-core Veil
V1: FSVW={c,a} | TR={c,b}   core c
V2: FSVW={a,b} | TR={a,c}   core a; partition still fixed
```

The bars are equality blocks of singleton maps, not teams or power tiers.

Tight consequences are:

| Choice | Definitions | Exact keys | Rows at six Souls |
| --- | ---: | ---: | ---: |
| A | 5 | 7 | 42 |
| B | 5 | 7 | 42 |
| C | 5 | 8 | 48 |

The base is Dreamglass prism, Echo one-only, two-key Tide, two-key morphic
Ashen, and one-key stable Veil. B can vary Tide's core on its existing pair;
C requires a second Veil key. All branches retain the parent roster and degree
floors. The same totals carry C39 only if a pair also satisfies its distinct-
instance and separate-predicate conditions; a core change alone is not enough.

C80 selects only empty/proper/full prevalence of sole-core-label variation
among affinity definitions in one completed version. It selects no exact core
label, cross-version continuity, noncore map vocabulary, block-to-map
assignment, finer morphic partner behavior, causal key axis, transformation,
coexistence, live switching, predicates, disclosure, agency, sidegrade,
power, acquisition, persistence, release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C79-C
and C80 as the sole active owner choice. The authoritative decision record is
unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04 directions remain
unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain selected, and
R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. One read-only adversarial
audit re-derived the frontier order, support construction, examples, and all
definition/key/row and degree floors. A read-only follow-up audit of the actual
diff found no material discrepancy and independently reconstructed the Tide and
Veil examples. `git diff --check`, untracked-file whitespace checks, the
sole-active-heading assertion, cross-document C79/C80 state assertions, the
authoritative-record no-diff check, SVG XML parsing, and Node construction/floor
assertions all passed. The repository test suite was not run because this was a
documentation-only decision round.
