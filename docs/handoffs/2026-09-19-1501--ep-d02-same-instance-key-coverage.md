# Handoff — same-instance variation universal by name; exact-key coverage next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`  
**UTC:** 2026-09-19 15:01:29  
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.107: whether no, some, or every union-locked Relic
definition gives every completed exact key a family-distinct same-persistent-
instance partner. The owner selected C3c.106-C. Preserve the accepted EP-D02
wording until a complete replacement is replayed and explicitly accepted. Do
not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Every remaining choice must use
*Achintya Bheda Abheda* (*acintya-bhedabheda*) as the enduring design ideal:
prefer meaningful continuity or participation and real, mechanically legible
distinction at the same operative relationship. This is an inspirational game-
design analogy, not theological equivalence, a silent invariant, or an automatic
selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the current
full-assurance pass before the separate standalone expansion/audit/adaptation
pass. Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.106-C

Let `inst_v(b)` project a completed locked exact key to its persistent
`soulRelicInstanceId`. Every union-locked definition now belongs to

`D^{same-inst-M}_{var,v}`:

it contains at least two distinct completed exact keys carrying one common
instance ID. C95-C makes their complete families unequal automatically. The
snapshots must differ in input-affecting state, evaluation boundary, or both,
but C106 chooses neither coordinate and creates no transition or common access.

Other instances may remain stable. The witness need not be a C101 overlap edge
or C105 member-disjoint edge. Tight floors remain six definitions, eleven keys,
and sixty-six six-Soul rows. Soul counts remain `{6,9,10,12,14,15}` and maximum
degree remains six. The same-ID cross-snapshot relation is partial at the
persistent-artifact boundary; universal definition prevalence is aggregate.

## Frontier-order correction

The first internal post-C106 draft moved to whether state or evaluation boundary
could be held fixed across a same-ID family contrast. Read-only review caught a
coarser missing incidence atom. C105-C requires at least four exact keys, while
C106-C guarantees only one repeated-ID fiber. A legal `[2,1,1]` carrier layout
leaves two exact keys with no same-instance partner.

Therefore C107 must decide same-instance exact-key coverage before coordinate
isolation. The latter remains the next finer structural question. When it
returns, a diagonal pair will prove only the absence of a completed one-axis
comparison, not that state and boundary are jointly causal.

## Active owner choice — EP-D02-C3c.107

Define

`K^{inst}_v(d)={b in B_v(d):exists b'!=b in B_v(d), inst_v(b')=inst_v(b)}`

and

`D^{K-inst}_{full,v}={d in D^{U=}_{locked,v}:K^{inst}_v(d)=B_v(d)}`.

C106-C makes `K^{inst}` nonempty for every locked name. A key outside it is an
**instance orphan** only in this completed operative projection: its instance ID
appears on no other completed locked key in the fixed version. It may still have
C101/C105 comparison partners on other IDs.

```text
SAME FOUR C105 KEYS; q=(input-affecting state,evaluation boundary)

NONFULL [2,1,1]
b0=R17/q0   b2=R17/q1   b1=R44/q0   b3=R63/q0
K^inst={b0,b2}; b1 and b3 are instance orphans.

FULL [2,2]
b0=R17/q0   b2=R17/q1   b1=R44/q0   b3=R44/q1
K^inst={b0,b1,b2,b3}.

FULL [4] is also legal: all four keys can use R17 with distinct q values.
```

Distinct `q` values inside one ID fiber prevent exact-tuple collisions. Every
snapshot is independently legal and reachable somewhere; none of these layouts
creates a path, transformation, live switch, or simultaneous expression.

### A — no locked definition has full coverage

`D^{K-inst}_{full,v}` is empty. Every locked name contains both C106's repeated
fiber and at least one singleton fiber. A therefore makes every name hybrid,
not immutable.

A can express a singular-incarnation anchor inside every otherwise morphic name.
Its cost is hidden per-copy quality and acquisition risk: one combatant may
receive a same-name instance with internal simulacrum potential while another
receives a singleton realization. Tight floors are `6/11/66`.

### B — fully covered and hybrid names coexist

`D^{K-inst}_{full,v}` is nonempty and proper. At least one name has no singleton
fiber, while another combines repeated and singleton fibers.

B is the strongest gameplay countercase if fully morphic versus singular-
incarnation names become explicit and strategically valuable. Otherwise it adds
another hidden persistence taxonomy and premium-name risk. Tight floors are
`7/15/90`.

### C — every locked definition has full coverage — recommended

`D^{K-inst}_{full,v}=D^{U=}_{locked,v}`. Every completed locked exact key has a
family-distinct completed partner carrying the same persistent instance ID.
Every represented instance-ID fiber therefore has size at least two.

C closes C106's exceptional-pair loophole and gives every authored simulacrum
within-artifact relational potential. It does not require one ID to carry all
four keys, govern inventory instances absent from the completed operative
domain, or make the peer accessible or valuable. Its costs are per-key
state/boundary authoring and proof burden plus a token loophole in which one
obscure alternate snapshot satisfies the rule. Tight floors remain `6/11/66`.

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 6 | 11 | 66 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 6 | 11 | 66 at six Souls | 6 |

At four keys the instance-ID partitions are `[4]`, `[3,1]`, `[2,2]`,
`[2,1,1]`, and `[1,1,1,1]`. C106-C excludes only the last. Full coverage holds
for `[4]` and `[2,2]`; nonfull coverage holds for `[3,1]` and `[2,1,1]`. The
inherited outside-locked base is five definitions and seven keys, so A/C add one
four-key locked name and B adds distinct full and nonfull four-key names. ID
decoration changes no family geometry.

At one fixed exact-key/Soul relation, inherited fit remains **direct**. Every
same-ID cross-snapshot family contrast is **partial** at the persistent-artifact
boundary. An instance orphan is **neutral/protective** at this projection, and
A/B/C prevalence is **aggregate**. C is recommended for systematic
participation and acquisition fairness, not because universal incidence across
separate snapshots directly realizes the standing ideal. B is the strongest
gameplay countercase; A is the strongest universal singular-anchor countercase.

C107 selects no fiber number or size beyond singleton incidence, same-ID peer
alignment with C101/C105 edges, all-Soul change, state-versus-boundary identity,
coordinate isolation, transition, transformation, evolution, reroll, live
switching, simultaneous expression, controlled substitution, comparable access,
disclosure, agency, value, power, persistence format, migration, release, or
implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C106-C
as selected and C107 as the sole active owner choice. The authoritative
`docs/design/endless-progression-decisions.md` remains unchanged. EP-D01,
EP-D02, and EP-D07 remain accepted; EP-D04 directions remain unaccepted, R9.4
remains reopened, R10.1-A and R10.2-A remain selected, and R10.3-R10.8 remain
pending. Preserve the owner's existing uncommitted skill edit. No implementation,
commit, or push occurred.
