# Handoff — C69 archetype purity selected; definition anchor class next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-15 10:11:36
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.70: whether a multi-support Relic definition may
also have one-anchor exact realizations. The owner selected C3c.69-A and made
*Achintya Bheda Abheda* the standing ideal for every future **Souls and
Simulacra** choice. Preserve the accepted EP-D02 wording until a complete
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedābheda*) as the enduring design ideal. At any operative
relationship represented by a mechanic, prefer meaningful continuity or
participation and real, mechanically legible distinction coexisting at that
same boundary. Merely distributing “unity” and “difference” across different
states, keys, definitions, catalog entries, labels, or cosmetic variants does
not satisfy the ideal.

This is an inspirational game-design analogy rather than theological
equivalence, a silent universal invariant, or an automatic answer. It supplies
no unstated rule, reopens no prior direction, and never bypasses the one-at-a-
time owner workflow. Every future recommendation must name its relationship and
boundary, rate its fit as direct, partial, neutral, or aggregate, and state the
gameplay tradeoff. Never describe an incompatible option as satisfying the
ideal; if no offered direction is consistent, add or recommend an explicit
replacement rather than weakening the north star.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle stay in their existing SS2
lane.

## Selected direction

`D_{dual,v}` is empty. Every Relic definition with multi-anchor exact
realizations is prism-pure or affinity-pure across all those realizations.
Because both archetypes exist globally, there are at least two multi-support
definitions and two multi-anchor exact keys; the inherited one-anchor class
makes at least three operative exact keys globally.

Exact labels, predicates, packages, and power may still vary inside the stable
archetype. This can later support one named dialect of simultaneous continuity
and distinction without silently selecting presentation. C69-A does not decide
whether that definition may also have a one-anchor key, what causes any exact-
key variation, or whether transformation or switching exists.

## Active frontier

Let `def_v: R_v^* -> \mathcal D_v` project every operative exact key to its
stable Relic definition ID. Reuse the nonempty one-/multi-anchor partition and
define

`D_{cross,v}={d in D_{multi,v} : some b_o in R_{one,v} has def_v(b_o)=d}`.

This domain includes only multi-support definitions. Definitions supported
exclusively by one-anchor exact keys remain outside the denominator, so B/C do
not silently decide whether any exist.

```text
A  Dreamglass -> PRISM-MULTI only
   Ashen      -> AFFINITY-MULTI only
   Echo       -> ONE-ANCHOR only

B  Dreamglass -> PRISM-MULTI + ONE-ANCHOR
   Ashen      -> AFFINITY-MULTI only

C  Dreamglass -> PRISM-MULTI + ONE-ANCHOR
   Ashen      -> AFFINITY-MULTI + ONE-ANCHOR
```

- **A, recommended:** `D_cross` is empty. Every operative definition is one-/
  multi-class-pure, so the global one-anchor witness needs a separate one-only
  definition. All choices inherit direct ideal fit inside each exact form's
  supported-Soul relationship; A's marginal C70 fit is neutral and protective.
  It prevents cross-key succession from masquerading as simultaneous
  bhedābheda and lets definition identity later teach one causal breadth. Its
  cost is no named Relic that expands or collapses across the threshold and a
  minimum of three operative definitions and exact keys.
- **B:** `D_cross` is nonempty proper. At least one multi-support definition
  also has a one-anchor key and at least one remains multi-only. Its marginal
  fit is a partial cross-key metaphor: an enduring definition can have
  genuinely different simulacra, but alternate keys do not themselves satisfy
  simultaneity. The benefit is rare threshold-changing Relics; the cost is a
  taxonomy and the risk of one hidden token form. Minimum total exact keys: 3.
- **C:** `D_cross=D_multi`. Every prism or affinity definition also has at
  least one one-anchor key; separate one-only definitions remain optional. Its
  marginal fit is merely aggregate. It maximizes transformational possibility
  and the literal Simulacra reading but imposes a universal variation/mapping
  burden and risks a dormant form tax. Minimum total exact keys: 4.

Concrete multi examples for Souls `F/S/V`: prism Dreamglass uses `{a,b}`,
`{b,c}`, `{a,c}`; affinity Ashen uses `F=V={d,e}`, `S={d,f}`. A one-anchor key
may keep sole singleton `{a}` inside total palette `{a,d}` for every Soul while
the inherited C43-A predicate witness supplies material Soul response below
the label projection. Under B Dreamglass can own that one-anchor key while
Ashen stays multi-only. Under C both Dreamglass and Ashen need distinct one-
anchor witnesses. C69-A continues to constrain all of each definition's multi
keys to its one prism/affinity archetype.

A/B/C are exhaustive as empty/nonempty-proper/full support of `D_cross` inside
nonempty `D_multi`. The tight general floor is
`|R_v^*|>=|D_{multi,v}|+max(1,|D_{cross,v}|)`, yielding total exact-key minima
3/3/4. All retain `3<=|S_v|<=15`.

The card selects no one-anchor-only prevalence under B/C, exact labels,
cross-key continuity, Soul partitions, variation cause, transformation,
coexistence, live switching, predicates, packages, disclosure, power,
acquisition, release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C3c.69-
A and identify C3c.70 as the sole active owner choice. EP-D01, EP-D02, and EP-
D07 remain accepted; EP-D04 directions remain unaccepted, R9.4 remains
reopened, R10.1-A and R10.2-A remain selected, and R10.3-R10.8 remain pending.

No code tests were run because this was documentation and vector work only. No
runtime, capture, evidence, installation, save, snapshot, or implementation was
touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`.

Two narrow read-only checks found no conflict between the new standing ideal
and selected C3c directions and confirmed C70's ordering, domain,
exhaustiveness, feasibility, tight counts, examples, and scope after correcting
the full domain of `def_v` and separating inherited ideal alignment from C70's
marginal fit. `git diff --check` passed. No-index whitespace checks for the
untracked rigor audit, SVG, and this handoff returned the expected different-
from-`/dev/null` status with no diagnostic output. The SVG parsed as XML,
executable assertions passed for all three C70 example projections, the newest-
handoff lookup resolved to this file, and a current-record scan confirmed C69-A
selected and only C70 active.
