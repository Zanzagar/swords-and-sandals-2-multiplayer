# Handoff — one key-complete fiber per name; every fiber/key next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-20 21:26:49
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.117: whether no, some but not all, or every union-
locked Soul Relic definition extends C116-C's key-complete common-edge coverage
from at least one represented persistent-instance fiber to every represented
fiber and therefore every completed key. The owner selected C3c.116-C. Preserve
the accepted EP-D02 wording until a complete replacement is replayed and
explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the current
full-assurance pass before the separate standalone expansion/audit/adaptation
pass. Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.116-C

For every locked definition `d`,

`Ids^{KW}_v(d)` is nonempty,

and therefore

`D^{exists-IdKW}_{locked,v}=D^{U=}_{locked,v}`.

Every locked name has at least one represented persistent instance whose every
completed key has some internal all-Soul-changing partner. Other represented
fibers may remain positive-but-incomplete. This closes definition-level absence
without covering every represented instance or every key.

Tight floors are seven definitions, fifteen exact keys, and ninety six-Soul
rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree remains
six. Each internal common edge is partial at the represented-persistent-
artifact/across-snapshot boundary, while universal definition-level existence
is aggregate. The authoritative decision record remains unchanged.

## Active owner choice — EP-D02-C3c.117

Define

`D^{all-IdKW}_{locked,v}={d in D^{U=}_{locked,v}:Ids^{KW}_v(d)=Ids_v(d)}`.

Because represented instance fibers partition the completed keys and an
internal edge cannot cross instance IDs, membership is exactly equivalent to
every completed locked key having some same-ID all-Soul-changing partner.
C116-C makes the complete-ID set nonempty in every name; it does not make that
set universal.

### A — every locked name is fiber-hybrid

`D^{all-IdKW}_{locked,v}` is empty. Every name has C116-C's complete carrier
and also a positive-but-incomplete represented artifact. This is the strongest
principled countercase if the complete-carrier/specialist-artifact contrast is
visible, accessible, and strategically valuable.

Costs are mandatory per-copy comparison asymmetry, five keys per locked name,
and an acquisition-quality lottery: one combatant may possess at most one
instance of a definition, while common access and relative value remain
unselected.

### B — all-fiber-complete and hybrid names coexist

`D^{all-IdKW}_{locked,v}` is nonempty and proper. At least one locked name
gives every represented artifact full internal key coverage, while another
retains a complete carrier plus an incomplete artifact. This is the strongest
catalog-variety compromise if those name languages become explicit and non-
dominated, but otherwise adds hidden taxonomy and a premium-name risk.

### C — every represented fiber is key-complete — recommended

`D^{all-IdKW}_{locked,v}=D^{U=}_{locked,v}`. Equivalently, every completed
locked form has an all-Soul-changing partner carrying the same persistent
instance ID.

C closes C116's token-carrier and per-copy incidence-lottery loopholes and
places continuity plus whole-roster distinction inside every represented
artifact. It is structural coverage only: it does not prove common player
access, a transition path, usefulness, sidegrade quality, or power. Costs are
universal partner authoring and validation, possible artificial foil forms,
and a remaining one-obscure-partner-per-key loophole.

## Verified witnesses and floors

The C116 five-key separator is a tight hybrid:

```text
R17={c0,c1}       c0--c1 is common; the fiber is complete
R44={c2,c3,c4}    c2--c3 is common; c4 is internally uncovered
```

The C105 four-key witness is a tight full name:

```text
R17={b0,b2}       member-disjoint common dyad
R44={b1,b3}       member-disjoint common dyad
```

Use the hybrid geometry in both C113-A name classes for A, one full and one
hybrid name for B, and the full geometry in both classes for C. In the tight
two-key fibers, each common dyad is necessarily the definition's sole typed
edge under C111-C. This does not settle typed alignment generally: give all
four verified `u0..u3` keys one instance ID, keep sole-type coverage on the two
non-common dyads, and use diagonal common cross-block edges to cover every key.

Under C116-C, a negative C117 name needs one complete fiber of at least two
keys plus one positive-but-incomplete fiber of at least three, so `[2,3]` is
tight. C113-A requires two locked definitions over the inherited five-
definition/seven-key outside base.

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 7 | 17 | 102 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 16 | 96 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |

Independent read-only frontier, construction, and gameplay audits confirmed
the equivalence, exact floors, Soul/degree feasibility, and that no coarser
support or incidence card is missing.

## Ideal ratings and exclusions

At one fixed exact-key/Soul relationship, inherited fit remains **direct**.
Each internal common edge is **partial** at the represented-persistent-artifact/
across-snapshot boundary. In A and B, an internally uncovered key is **neutral/
protective** at the fiber-local whole-roster-change projection; its C98-
guaranteed cross-ID foil is **partial** at the broader stable-definition/
across-persistent-simulacra boundary. Fiber/key coverage and definition
prevalence are **aggregate**. C improves systematic participation, not the
direct doctrinal rating.

C117 selects no typed/one-coordinate alignment, overlap-bearing versus member-
disjoint identity, partner count, every-pair rule, graph connectedness, common
anchor, exact coordinate semantics, cause, transition, path, common access,
disclosure, agency, value, power, persistence, release, or implementation. It
quantifies only over persistent instance IDs represented in the completed
fixed-version domain, not every inventory instance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C117 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
