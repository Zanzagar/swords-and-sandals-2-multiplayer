# Handoff — fixed-cell-stable input Charms; Bound-Soul breadth next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-13 23:52:35
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.42, material Bound-Soul participation across Soul
Relic definitions in `E+`. The owner selected C3c.41-A. Preserve the accepted
EP-D02 wording until a complete replacement is replayed and explicitly
accepted. Do not implement.

## Selected direction

Let nonempty `I` contain every materially functional input-Charm definition.
C3c.40-B already requires every member of `I` to have at least one material use
in the six-cell `E+` domain. C3c.41-A now sets `V_I` empty: every definition is
stable between legally comparable same-definition/version instances inside a
fixed `E+` temporal-by-basis cell.

Complete reachable-domain analysis must find no two such instances that return
different final-predicate Booleans on one identical canonical
source/history/state valuation reachable in both alternate worlds while the
exact Soul Relic instance/state, Bound Soul, payoff Charm or absence, every
non-input-Charm configuration fact, evaluation boundary, version, and cell
remain fixed. Incomplete analysis is unresolved. A definition with fewer than
two legally comparable instances in any common fixed-root/context/cell domain
has no nontrivial comparison; A does not promise obtainable interchangeable
copies. Unique instance IDs retain ownership, custody, provenance, and later-
permitted nonmechanical-history value.

This direction does not constrain cross-cell instance behavior, basis-neutral
`E0`, same-instance evolution, behavior with another Relic or resonance
context, or payoff-side behavior. It is not a complete accepted EP-D02
replacement and changes no authoritative wording.

## Corrected dependency frontier

The earlier queue put base-grammar ownership directly after Relic and input-
Charm instance granularity. Read-only re-derivation found that a Relic-owned
base followed by an unrestricted resonance transform and a pair-owned grammar
can encode exactly the same combat behavior. That fork would be a storage-
schema label until a material behavior or durable lineage invariant constrains
it.

C3c.3-B also guarantees only two materially distinct variants somewhere in the
complete Bound-Soul/Relic pair map. As written, different Relics can supply
that global diversity while all supported Souls behave identically for each
fixed Relic. C3c.42 therefore resolves material same-Relic Bound-Soul
participation before entry multiplicity, transformation power/precedence, and
any later non-vacuous authoring/lineage ownership choice. Same-definition Relic
and Charm cross-cell instance footprints remain separate later questions.

## Sole active choice

Reuse C3c.39's nonempty `D+`, the supported Relic definitions that participate
materially in `E+`. Put a definition in `D_S` only when two supported Bound
Souls have a reachable same-combatant alternate-world witness with one exact
Relic instance of that definition. Hold the exact Relic instance/state, exact
input Charm or empty lane, exact payoff Charm or absence, every non-Soul fact,
evaluation boundary, and version fixed. Compare each world's set of `(cell,
predicate behavior)` pairs over a nonempty common envelope `Ω` of identical
canonical valuations reachable under both Souls. Build the sets from legally
composed final predicate records before any Soul-local reachability filter, and
include a behavior only when its material cell-classification witness is
realizable inside `Ω`. Predicate behaviors are equal if and only if they return
the same Boolean throughout `Ω`. Extensionally identical entries collapse for
this comparison only. A witness requires different sets, so it needs no entry
pairing, count, combined acceptance, or arbitration choice; a Bound Soul's
separate combat behavior cannot fake resonance merely by changing reachability,
and the witness permits no in-game Bound-Soul rebinding.

- **A, recommended — every supported Relic definition materially resonates:**
  `D_S=D+`. Every named Relic needs at least one material same-Relic Soul
  portfolio witness somewhere. This derives at least two supported Souls but does not
  require every instance, Soul pair, Charm context, entry, or cell to differ.
  It makes “Soul Relic” a real gameplay promise at the cost of an authored and
  tested resonance surface for every definition.
- **B — only some definitions materially resonate:** `D_S` is a nonempty
  proper subset of `D+`, deriving at least two supported Relic definitions.
  Witnessed definitions respond; the complement must pass complete invariance
  analysis. This permits resonance-forward Relics and stable anchors but
  creates two thematic and teaching languages.
- **C — no definition materially resonates in `E+`:** `D_S` is empty under
  complete analysis. The current global pair-map diversity could then arise
  only when the Relic also changes or outside this scoped domain. This is the
  smallest matrix but defeats the intended resonance promise in the six-cell
  core and would require an explicit C3c.3-B repair or deliberate relocation
  during final replay.

A/B/C exhaust all/nonempty-proper/empty `D_S`. Incomplete analysis never counts
as invariance. Exact pair uniqueness, per-instance or per-entry breadth, equal
power, recipes, cell mapping, `E0`, payoff behavior, base ownership, entry
multiplicity, transformations/precedence, content, acquisition, horizons,
persistence, validation, release, and implementation remain open.

## Verification and preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record
C3c.41-A and identify C3c.42 as the sole active choice. The authoritative
decision record and normative system design remain unchanged. EP-D01, EP-D02,
and EP-D07 remain accepted; EP-D04 directions remain unaccepted, R9.4 remains
reopened, R10.1-A and R10.2-A remain selected, and R10.3-R10.8 remain pending.
No code tests were run because this is documentation and vector work only. No
runtime, capture, evidence, installation, save, or snapshot was touched.
