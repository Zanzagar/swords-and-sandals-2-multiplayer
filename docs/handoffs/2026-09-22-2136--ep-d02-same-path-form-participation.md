# Handoff — Mixed tag overlap selected; same-path participation next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-22 21:36:10
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.144: whether every dual-form fixed context/receipt
tag, no dual tag, or only some dual tags have at least one actual legal
recommit history containing both independent decisive-input forms. The owner
selected C3c.143-C. Preserve accepted EP-D02 wording until a complete
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Finish the current
full-assurance pass before the standalone expansion/audit/adaptation pass.

## Selected direction — EP-D02-C3c.143-C

For each inherited fixed context/receipt tag `k`, let `K^{ind}_v(k)` contain
the C141-qualified independent forms reachable for that same tag, and let
`D^{dual}_v` contain exactly the tags supporting both `Pred` and `Rec`.
C143-C makes `D^{dual}_v` a nonempty proper subset of the candidate tag domain:
at least one dual tag and at least one singleton-form specialist coexist.

C143-C does not require both specialist polarities because a dual tag can
supply both forms globally under C142-C. Each actual isolation remains
**direct** at `chi`; same-tag dual expression across distinct histories is
**partial**; specialist exclusion is **neutral/protective**; and catalog
coexistence is **aggregate**. Its costs are a dual/specialist taxonomy,
disclosure and validation burden, flexibility-premium pressure, and a token-
dual loophole until path breadth is constrained.

The fixed tag already includes combatant, Bound Soul, exact Relic realization,
Charms, source configuration, evaluation boundary, and version. Therefore the
valid token exploit is an obscure reachable combat-history/action route within
that fixed tag, not a different Soul/Charm configuration. The frozen 20:43
handoff used the latter wording; do not propagate it.

Every branch retains `9/23/138`, four represented IDs, eight lived edges, four
complete IDs, no incomplete represented ID, Souls `{6,9,10,12,14,15}`, degree
six, and C46-A's same-boundary maximum of two. The authoritative decision
record remains unchanged.

## Why same-path tag participation is next

A dual tag can currently earn that label through mutually exclusive histories:
one containing `I_P`, another containing `I_R`, and none containing both. The
next coarser layer is whether each dual tag has any both-form history. Asking
whether every, some, or exactly one history inside a participating tag is both-
form is a finer later decision. Continuous-context breadth and specialist
polarity remain separate frontier siblings.

## Active owner choice — EP-D02-C3c.144

For each `k in D^{dual}_v`, let `W_v(k)` be its legal adjacent same-tag
recommit histories. For `w in W_v(k)`, let `K^{path}_v(k,w)` contain `Pred`
when some opportunity on `w` establishes `I_P`, and contain `Rec` when some
opportunity establishes `I_R`. C141-A makes every path set nonempty.

A path is both-form when its set is `{Pred,Rec}`. The forms may occur at
different opportunities and different `chi` cuts. One opportunity supplies
both only through two distinct valid C141 isolation pairs; C144 does not select
one simultaneous action or effect. Define
`E^{both-path}_v={k in D^{dual}_v : some w in W_v(k) has K^{path}_v(k,w)={Pred,Rec}}`.
Positive membership needs one legal witness. Nonmembership needs complete
reachable-domain proof that none exists.

### A — every dual tag has a both-form path — recommended

`E^{both-path}_v=D^{dual}_v`. Every dual tag demonstrates both forms within at
least one actual history; other histories may remain single-form. One P history
can select Rekindle over Vent to establish `I_P`, later select Invoke over Bank
to establish `I_R`, and then recommit.

A makes every richer dual-tag identity behaviorally present in one actual
recurrence arc rather than only by stitching mutually exclusive histories
together. Each `chi` is **direct**; same fixed tag and temporal history carrying
two forms is **partial** because their cuts may differ; other paths are
**neutral**; universal dual-tag participation is **aggregate**. Costs are
two-form authoring and replay burden for every dual tag and a token loophole:
one obscure both-form history per tag suffices.

### B — no dual tag has a both-form path

`E^{both-path}_v` is empty. Every path of every dual tag is single-form.
Because each tag remains dual under C143-C, each necessarily has at least one
predicate-only and one distinct recurrence-only history.

B is the strongest path-identity countercase: one actual history teaches one
causal grammar. Each `chi` is **direct**; cross-history same-tag difference is
**partial**; excluding same-path co-participation is **neutral/protective**;
universal separation is **aggregate**. Costs are no within-history pivot and a
token loophole where one form survives only on a rare alternate history.

### C — participating and path-separated dual tags coexist

`E^{both-path}_v` is a nonempty proper subset. At least one dual tag has a
both-form history; another realizes its two forms only on distinct single-form
histories. A specialist remains outside this classification domain.

C supplies the widest authored vocabulary, but adds a third behavioral
taxonomy beyond specialists and duals. Each local `chi` is **direct**;
same-history participation is **partial**; path-separated exclusion is
**neutral/protective**; coexistence is **aggregate**. It requires at least two
dual tags and is feasible beside C143-C's specialist under the inherited
three-tag floor. Its costs are disclosure/validation burden, flexibility-
premium pressure, and token positive membership.

## Compatibility and deferrals

A/B/C exhaust `E^{both-path}_v` being the whole nonempty dual domain, empty,
or a nonempty proper subset. C144 distributes existing independent-form
witnesses and adds no receipt, structural content, opportunity, or result-
width requirement.

C144 selects no tag identities; all/some/exactly-one both-form path prevalence
inside participating tags; path-type counts or dominance; accessibility,
visibility, control, frequency, or anti-token breadth; same-opportunity or
same-cut co-location; order; deliberate mode choice; specialist polarity;
continuous context; predicate internals; retained sources; actor or causation
strength; cadence, payoff, persistence, release, UI, or implementation. Under
A or C, path prevalence inside participating dual tags is next. Under B,
recompute the frontier.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C144 only. Record
the selected branch, recompute the frontier, and present exactly one next
prerequisite-ready choice. Do not infer acceptance of the whole EP-D02
replacement or start implementation.
