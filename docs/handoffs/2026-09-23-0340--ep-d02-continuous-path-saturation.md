# Handoff — Every tag continuous-capable; path saturation next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`  
**UTC:** 2026-09-23 03:40:03  
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.148: continuous-context path saturation. The owner
selected C3c.147-A. Preserve accepted EP-D02 wording until a complete normative
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Finish the current
full-assurance pass before the standalone expansion/audit/adaptation pass.

## Selected direction — EP-D02-C3c.147-A

For each candidate tag `k=(h,j)`, `W_v(k)` remains its complete legal adjacent
same-tag recommit-history set satisfying C136 through C141, and
`W^{cont}_v(k)` contains histories where `h` stays operatively available in
every authoritative semantic state from post-`c1` through pre-`q2`.

Selected A makes `K^{cont}_v=K^{candidate}_v`: every candidate tag has at least
one continuous history. Other legal histories may contain outages. Each C141
`chi` remains **direct**; continuity of the same operative context across the
changing temporal relationship is **partial**; universal tag support is
**aggregate**; and allowed outage paths are **neutral** at this projection.
The inherited three-tag floor and all fixed catalog floors remain unchanged.

## Why path saturation is next

C147-A leaves a direct token loophole: one obscure continuous route can qualify
a tag whose ordinary recommit play always power-cycles its context. Resolve
that within-tag path prevalence before the finer dual-only same-opportunity
question, mirroring C144 participation followed by C145 saturation. Same-
opportunity participation remains ready immediately afterward.

## Active owner choice — EP-D02-C3c.148

Define

`U^{all-cont}_v={k in K^{candidate}_v : W^{cont}_v(k)=W_v(k)}`.

C147-A makes a tag outside this set path-mixed: it has at least one continuous
history and at least one distinct outage-bearing history.

### A — every recommit path of every tag is continuous — recommended

`U^{all-cont}_v=K^{candidate}_v`. Every legal adjacent same-tag recommit
history keeps its fixed context operative throughout the semantic slice.

This makes in-place renewal dependable and closes the obscure-route exploit.
Each `chi` is **direct**; same-context temporal continuity is **partial**;
in-slice outage exclusion is **neutral/protective**; and universal tag/path
prevalence is **aggregate**. Costs are universal proof and authoring, an
absolute ban on authoritative context disappearance inside the measured post-
`c1` through pre-`q2` slice, and possible repetitive maintenance pressure.
Post-`q2` behavior remains governed by C133/C134 and later rules.

Example: every P recommit route may let Heat fail and be rebuilt while R17
stays operative inside the measured slice. Silence remains legal generally; if
it makes R17 authoritatively unavailable after `c1` and before `q2`, that
continuation may not produce the adjacent same-tag `q2/c2` without violating A.
Outages outside the slice remain open.

### B — every tag mixes continuous and outage-bearing paths

`U^{all-cont}_v` is empty. Every tag has its C147-A continuous route and at
least one distinct outage/return route.

This is the strongest path-variety countercase: every relationship supports
both in-place renewal and remanifestation. Continuous and persistent-lineage
trajectories are **partial** at their respective boundaries; operative-context
continuity on an outage path is **neutral/unsupported**; and per-tag coexistence
is **aggregate**. It requires at least two histories per tag, two timing
languages, extra disclosure/testing, and permits token variety through one
obscure outage route.

Example: P has one continuous Rekindle/Vent route and another where R17 is
Silenced, restored while Heat remains false, then uses Invoke/Bank before
`q2`. Every other tag has both modes too.

### C — continuous-saturated and path-mixed tags coexist

`U^{all-cont}_v` is nonempty proper. At least one tag is continuous on every
path, while another has both path types.

Individual paths retain the ratings above; class coexistence is **aggregate**.
Costs are a hidden timing subtype, likely ease/power-premium pressure, and
token mixed membership through one obscure outage path.

Example: every P route is continuous; Q has one continuous route and one
Silence/restore route. This chooses no mapping to dual or specialist tags.

## Proof, floors, and deferrals

A/B/C exhaust the saturated subset. Saturated membership requires exhaustive
proof over every legal history. Mixed nonmembership needs one outage-bearing
witness because C147-A already provides its continuous witness. A adds no
history floor; B derives at least two histories per tag; C derives at least two
per mixed tag and no higher total-tag floor.

All branches retain nine definitions, twenty-three exact keys, 138 Soul/key
rows at six Souls, four represented locked IDs, eight lived aligned edges, four
one-history-complete IDs, no incomplete represented ID, Souls
`{6,9,10,12,14,15}`, degree six, and C46-A's same-boundary width two.

C148 chooses no identities or class mapping, exact path counts or dominance,
access/frequency/strategic value, outage details, retained semantic facts,
evaluator schedule, same-opportunity or same-`chi` relation, continuous-plus-
same-opportunity intersection, specialist representation, actor, cadence,
payoff, persistence, UI, release, or implementation.

The authoritative decision record hash remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
