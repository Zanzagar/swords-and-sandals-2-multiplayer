# Handoff — every pair has at least two cells; second singleton coverage next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-14 19:02:37
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.56, second singleton-foothold coverage across exact
pairs. The owner selected C3c.55-B. Preserve the accepted EP-D02 wording until
a complete replacement is replayed and explicitly accepted. Do not implement.

## Selected direction

Reuse each exact pair's native-dual cell footprint `C_v(p)` and define

`d_v(p)=|C_v(p)|` and `m_v=min_{p in P_v}d_v(p)`.

C3c.55-B fixes `m_v=2`. Every supported Bound Soul paired with every tagged
exact materially `E+`-operative Relic realization has at least two distinct
native-dual cell edges, and at least one exact pair has exactly two. This is a
total cell-footprint floor: one genuinely coupled package may establish
several edges, so two cells do not mean two contexts, receipts, routes,
independently triggerable tools, or simultaneous capabilities.

C3c.53-A gives every pair at least one singleton-backed edge, but the second
total edge may remain coupled-only. Thus the exact singleton minimum
`n_v=min_p|I_v(p)|` remains one or two. Every valid package for an exact-two row
is locally at most two cells wide, without creating a global package-size cap.

The selection fixes no exact pair or footprint, total-degree maximum or
regularity, exact singleton degree, package count, placement of C3c.54-B's
coupled-only edge, substitution stability, mapping, disclosure, control, ease,
frequency, power, payoff, Charm behavior, access, acquisition, or
implementation. This is a direction, not a complete accepted EP-D02
replacement, and changes no authoritative wording.

## Frontier result

Define the second-foothold-qualified pair set

`Q_v={p in P_v : |I_v(p)|>=2}`.

C3c.53-A makes every `I_v(p)` nonempty, so `Q_v` is all of `P_v`, a nonempty
proper subset, or empty. This all/some/none topology is the preferred next
nested support decision. It is one classification, not a bundled scalar choice.
Footprint mapping stability and the identity axis owning variation are
independently ready and remain unresolved.

A second singleton means a second distinct cell label with its own valid
singleton package somewhere for that exact pair. The witnesses may use
different contexts, sources, payoff states, transitions, or receipts; they do
not establish distinct routes, tools, easy triggers, visibility, control,
simultaneous availability, or power.

## Sole active choice

- **A — every pair has a second singleton foothold:** `Q_v=P_v`. Every exact
  pair has at least two distinct singleton-backed cells. This guarantees two
  cell-level causal references per pair, but imposes the full two-witness
  matrix and invites boilerplate or shadow witnesses. It fixes `n_v=2`; every
  exact-two row has `I_v(p)=C_v(p)`. C3c.54-B's coupled-only edge must occur in
  a different degree-three-or-higher row, forcing nonregular total degree and
  an exact maximum of at least three.
- **B, recommended — mix one-anchor and multi-anchor pairs:** `Q_v` is a
  nonempty proper subset of `P_v`. Some exact pairs have at least two
  singleton-backed cells, while some have exactly one and therefore at least
  one coupled-only edge. This preserves two genuinely different causal
  structures that later stability, disclosure, and trigger-quality rules could
  turn into a teachable clarity-versus-mastery contrast. Without those later
  safeguards, the variation may look like unexplained missing support. B fixes
  `n_v=1` and makes singleton degree nonregular while leaving total-degree
  regularity open.
- **C — exactly one singleton foothold per pair:** `Q_v` is empty. Every pair
  has exactly one singleton-backed cell and at least one coupled-only edge.
  This creates a uniform one-attributable-cell-plus-relational-expansion
  structure, but makes coupling complexity universal and carries the heaviest
  negative-validation burden. Full six-cell singleton projection forces at
  least six exact pair keys. Singleton degree is uniformly one; total-degree
  regularity remains open.

For B, Firebound/Dreamglass exact `R44` could have singleton-backed `H/action`
and `X/build` cells. Stonebound/Ashen exact `R17` could have
`C_v(p)={S/build,X/action}` but `I_v(p)={S/build}`, with `X/action` supported
only by a minimal `{S/build,X/action}` package. That is mixed cell-level causal
structure, not two playable routes for the first pair. Other exact pairs
supply the remaining global singleton labels.

Membership in `Q_v` needs two positive singleton-cell witnesses. Proving a
pair outside `Q_v` needs its positive anchor plus complete fixed-boundary
analysis showing no singleton package for any of the other five cell labels;
failed search remains unresolved. A applies the two-positive burden to every
pair. B needs at least one qualified pair and one exact-one negative. C needs
the exact-one negative for every pair.

B's strongest token implementation could put all six global singleton
exemplars on one showcase pair, then give every other pair the same obscure
`H/action` singleton plus one contrived coupled extension. One such row supplies
the exact-two minimum. Distribution, stable mapping, disclosure, trigger
quality, and content value remain necessary later safeguards.

An owner-written replacement may choose another explicit second-foothold
topology. The card selects no exact membership, mapping, package counts, route
count, active-Charm participation, receipt lineage, transformation, precedence,
base ownership, control, ease, frequency, power, acquisition, persistence,
release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record
C3c.55-B and identify C3c.56 as the sole active one-at-a-time owner card, with B
recommended. The authoritative decision record and normative system design
remain unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04 directions
remain unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain selected,
and R10.3-R10.8 remain pending. Continue exactly one A/B/C choice per turn.

No code tests were run because this was documentation and vector work only. No
runtime, capture, evidence, installation, save, or snapshot was touched.
Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`.

Three independent read-only audits checked the formal topology, frontier
ordering, examples, recommendation, proof burdens, token construction, and
derived degree/cardinality/package consequences. Review made the potential
clarity-versus-mastery benefit conditional on later safeguards, removed wording
that implied stable definition-level identity or player-facing routes, and
stated that mapping stability remains independently ready rather than a hard
dependency.

`git diff --check` passed. No-index whitespace checks for the untracked rigor
audit, SVG, and this handoff returned the expected different-from-`/dev/null`
status with no diagnostics. The SVG parsed with Python's standard XML library,
this file resolved as the newest handoff, the stale-C3c.55-active-pointer scan
found none, and the authoritative decision and system documents are clean.
