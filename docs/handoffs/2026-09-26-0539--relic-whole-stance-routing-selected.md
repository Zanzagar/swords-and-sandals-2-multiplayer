---
handoff:      2026-09-26-0539--relic-whole-stance-routing-selected
written:      2026-09-26 05:39 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    three bounded read-only B4A audits, one read-only denominator adjudication, and one strict replacement read-only diff verification
branch:       design/endless-progression-owner-packet
commits:      5a27423..HEAD
suite:        proportional serial checks 10/10 passed; register reparses 79/79 with Phi_SR 36; decision-record hash unchanged
supersedes:   2026-09-26-0518--relic-universal-routing-configuration-selected
---

# Handoff — closed whole-policy Relic routing selected

Start with `HANDOFF.md` above its archive line and
`$ss2-progression-design`. The sole active Relic frontier is
`RCS-03C3C3B4A`; present that one card only.

## Where things stand

The owner selected `RCS-03C3C3B3B1-A`: every routing-capable Relic exposes a
finite versioned menu of indivisible authored whole-policy routing stances.
B3B2 is pruned. A twenty-third prerequisite correction makes canonical
complete bound stance laws—not contracts or individual evaluation cases—the
B4A stochastic-support denominator. The register has 79 rows and
`Phi_SR = 36`; the authoritative decision record remains unchanged.

## Highest-value next work

Record the owner's B4A answer, then audit the resulting next row before
presenting it:

- A: every complete bound stance law is deterministic across its reachable
  domain; recommend A for stance ownership, causal legibility, team
  choreography, and replay trust;
- B: every stance law is stochastic-capable on at least one reachable fixed
  input, without requiring every invocation to roll; or
- C: deterministic-only and stochastic-capable stance laws coexist somewhere
  in the catalog, without guaranteeing both inside every Relic.

Under A, prune B4B and open C3C3C. Under B/C, screen B4B first: it is a real
owner question only if some reachable random law has at least two nonconstant
tag marginals. One random component beside constants makes correlation
vacuous.

## Hard rules and traps

- Deterministic does not mean static. B2-B already permits a whole stance to
  respond to changes in another contender or ledger domain; at fixed complete
  input, A returns one tuple.
- Classify normative tuple laws, not player ignorance or code. A seeded PRNG
  may realize a declared stochastic kernel; an opaque deterministic hash is
  not stochastic merely because it is hard to predict.
- Earlier random stance assignment is RCS-09/RCS-13 acquisition/configuration,
  not B4A. Exact randomizing inputs are AUTHOR/SPEC and odds are TUNE.
- No callback, reload, reconnect, replay, or UI reopening may redraw a committed
  tuple. No downstream permission, survivor, claim, payoff, or outcome may
  trigger rejection sampling, retry, or a switch.
- C does not promise a reliable/random toggle on every Relic. That stronger
  guarantee is an explicit replacement path.
- One initial diff verifier invalidated itself by creating and deleting a
  transient `/tmp/status-map-view`. Its findings were discarded; it made no
  repository edit. Only the strict no-write replacement verification counts.
- Do not edit the authoritative decision record without an explicit accepted,
  rejected, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Verification and Git

Confirm the register reparses to 79 rows with 35 `SCREEN`, 1 `OWNER-OPEN`, 9
`PRUNED`, 30 `DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`;
`Phi_SR = 36`. Confirm the decision-record SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
Commit the answer atomically with a `Co-Authored-By` trailer, push only the
feature branch, verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
