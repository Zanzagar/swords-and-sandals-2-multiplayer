# Handoff — C74 stable affinity profile selected; singleton-block support next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-16 02:31:19
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.75: whether no, some but not all, or every affinity
Relic definition has a C74-stable profile containing any size-one Soul group.
The owner selected C3c.74-A. Preserve the accepted EP-D02 wording until a
complete replacement is replayed and explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. If no option remains consistent,
offer or recommend a replacement rather than weaken the north star. Continue
the exact one-at-a-time A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle remain in their SS2 lane.

## Selected direction

`D^lambda_{var,v}` is empty. Every affinity definition keeps one unlabeled
sorted multiset of singleton-map Soul-group sizes across all of its exact
operative keys in a completed version. Exact keys carrying one definition may
still reassign named Souls among those sizes, regroup equal-size partners,
change the block-to-map assignment, singleton or sole-core labels, and
predicates. Prism and one-only profiles remain derived.

C74-A retains `3<=|S_v|<=15`, a local three-key floor, and a four-key floor in
a version carrying C39-C's variable-instance witness. It selects no exact
profile, Soul-indexed peer presence or size role, partition membership, labels,
variation cause, transformation, disclosure, power, or implementation. It
changes no authoritative wording.

## Frontier corrections

The first post-C74 note would have moved directly to exact-partition stability.
That was too fine. An intermediate replacement then defined the exact group-
size role `mu_v(b)(s)=|[s]_{Pi_v(b)}|`; adversarial review also rejected that
draft before owner presentation because it bundled no-peer/has-peer status with
pair-versus-trio size. A second replacement projected to the binary bit
`alpha_v(b)(s)=1 iff mu_v(b)(s)>=2`, but review caught a still-coarser
prerequisite before owner presentation: a C74-stable profile without any size-
one block forces every `alpha` bit to one. Such a definition is ineligible for
peer-map variation, not a genuine stable witness. None of the three superseded
drafts selected anything.

C75 therefore resolves support for a size-one Soul group before named-Soul
peer-map variation, exact peer-bearing size, or partner identity. Core-label
continuity is an independent ready sibling, not a prerequisite.

## Active frontier

For affinity definition `d`, let `m_{1,v}(d)` be the multiplicity of one in its
C74-stable profile and define

`D^solo_v={d in D_{affinity,v}:m_{1,v}(d)>=1}`.

A size-one Soul group means one Soul alone has its entire singleton-label map;
it does not mean a one-cell map. Every affinity key still has one all-Soul core
and at least two unequal maps.

- **A, recommended — no affinity definition has a one-Soul map group:**
  `D^solo_v` is empty. Every Soul in every affinity has an equal-map peer.
  C75 fit is direct at the fixed affinity-key/Soul-roster boundary: peer
  participation, unequal map groups, and the sole all-Soul core coexist there.
  It also creates a clean prism/affinity/one-only vocabulary. Cost: no
  “solitary witness within a chorus,” at least four Souls, and
  `rho<=floor(|S_v|/2)` for every affinity definition.
- **B — singleton-bearing and fully peer-covered affinities coexist:**
  `D^solo_v` is nonempty proper. This supports visible “solitary witness” and
  “choral” name dialects. C75's prevalence fit is aggregate across separate
  definitions even though each fixed affinity inherits direct core-plus-
  difference fit. Cost: another taxonomy, at least two affinity definitions,
  and disguised-tier risk.
- **C — every affinity definition has a one-Soul map group:**
  `D^solo_v=D_affinity`. C75 fit is direct at the fixed boundary: a solo Soul
  shares the universal core yet has a full map no other Soul has. It strongly
  supports rooted individuality and retains three Souls. Cost: every affinity
  repeats the outsider motif, and balanced profiles such as `(2,2)` are
  forbidden.

At four Souls, `FS|VW` has profile `(2,2)` and is fully peer-covered;
`FSV|W` has profile `(3,1)` and is singleton-bearing. A permits only the first
kind, B requires definitions of both kinds, and C permits only the second kind
or other profiles containing a one. Bars denote equal singleton-label maps,
not teams or power tiers.

A no-singleton profile with `rho` parts exists exactly when
`|S_v|>=2*rho`. A singleton-bearing profile exists for every inherited `rho`,
witnessed by `(|S_v|-rho+1,1,...,1)`. Thus A/B require at least four Souls and
C retains three; no option adds a palette-degree floor. Tight A/B/C definition
and local-key floors are `3/4/3`; C39-witness key floors are `4/5/4`; local
Soul/key row floors are `12/16/9`; C39-witness row floors are `16/20/12`.

C75 selects no exact singleton-block count, which Souls are alone, whether
those identities vary across exact keys, peer-bearing size or identity, exact
partition, labels, predicates, causation, transformation, disclosure, power,
or implementation.

Under A every named-Soul peer map is derived all-one, so no peer-variation card
exists. Under B/C only definitions in `D^solo_v` enter that later frontier; all
others are forced stable. If the eligible set has one definition, the later
choice is binary stable/variable; at least two admit empty/proper/full over the
eligible subset. Exact peer-bearing size and partners follow as finer
projections.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C74-A,
the three caught coarse-to-fine projection errors, and C75 as the sole active
owner choice. The authoritative decision record is unchanged. EP-D01, EP-D02,
and EP-D07 remain
accepted; EP-D04 directions remain unaccepted, R9.4 remains reopened, R10.1-A
and R10.2-A remain selected, and R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Partition feasibility,
construction coverage, definition/key/row floors, active-frontier pointers,
tracked and relevant-untracked whitespace, authoritative-record nonchange, and
SVG XML parsing all passed. This documentation-only turn did not run the
repository test suite.
