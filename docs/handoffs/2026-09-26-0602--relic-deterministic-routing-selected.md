---
handoff:      2026-09-26-0602--relic-deterministic-routing-selected
written:      2026-09-26 06:02 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    three bounded read-only C/D prerequisite audits, two disjoint bounded document writers, and one strict read-only diff verification
branch:       design/endless-progression-owner-packet
commits:      64a1d1d..HEAD
suite:        proportional serial checks 10/10 passed; register reparses 81/81 with Phi_SR 36; decision-record hash unchanged
supersedes:   2026-09-26-0539--relic-whole-stance-routing-selected
---

# Handoff — deterministic Relic routing selected

Start with `HANDOFF.md` above its archive line and
`$ss2-progression-design`. The sole active Relic frontier is
`RCS-03C3C3C1`; present that one card only.

## Where things stand

The owner selected `RCS-03C3C3B4A-A`: every complete bound routing-stance law
is deterministic on every reachable fixed nonrandom selector input. B4B is
pruned. A twenty-fourth prerequisite correction separates post-routing overlap
exposure from later joint-survival permission. The amended register has 81 rows
and `Phi_SR = 36`; the authoritative decision record remains unchanged.

## Highest-value next work

Record the owner's C1 answer, then audit the resulting next row before
presenting it:

- A: `E^{resolved-exact}_v` is empty. Exact-overlap tuples remain structurally
  supported before routing, but every lawful bound stance routes around them.
  Prune C2 and open D1.
- B: `E^{resolved-exact}_v` is nonempty. At least one reachable actual
  evaluation and lawful bound stance selects an authoritative exact-overlap
  pair. This grants no joint-survival permission. Recommend B because it makes
  the selected exact-child grammar playable routing texture rather than
  permanently latent proof space.

The illustrative boundary is P's exact `Guard + Heat` ledger versus its
independent `Guard + Vow` ledger beside Q's `Return + Heat`: one stance may
route Vow while another routes shared Heat. B requires some functional witness
of that kind, not those names, a particular count, or universal stance coverage.

## Hard rules and traps

- C1 is an exposure question only. It decides no permission, survivor,
  candidate, claim, consumption, payoff, persistence, or implementation.
- Count one resolved opportunity per unordered distinct-tag pair in one
  selected tuple. Alternate ledgers, aliases, callback splits, reload/replay,
  and multiple shared children between the same tags do not multiply it.
- A pair may be exact and distinct-related simultaneously, but it receives one
  shared permission bit. Never compose grant/veto votes by form.
- Before C2/D2, explicitly screen intrinsic, participant-prebound, and
  stochastic permission authority. B4A-A fixed selector randomness only.
- Permission false means only “not both”; later E/F or H/I owns the result.
  Denial cannot trigger selector retry or ledger substitution.
- Do not edit the authoritative decision record without an explicit accepted,
  rejected, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Verification and Git

Confirm the register reparses to 81 rows with 35 `SCREEN`, 1 `OWNER-OPEN`, 10
`PRUNED`, 31 `DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`;
`Phi_SR = 36`. Confirm the decision-record SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
Commit the answer atomically with a `Co-Authored-By` trailer, push only the
feature branch, verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
