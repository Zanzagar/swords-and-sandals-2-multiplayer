---
handoff:      2026-09-26-1501--relic-three-permission-philosophies-selected
written:      2026-09-26 15:01 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    two bounded read-only prerequisite audits with mutual cross-challenge, plus one strict read-only final diff verifier
branch:       design/endless-progression-owner-packet
commits:      38fb48b..HEAD
suite:        proportional serial checks 10/10 passed; register reparses 89/89 with Phi_SR 35; decision-record hash unchanged
supersedes:   2026-09-26-0755--relic-causal-permission-selected
---

# Handoff — three exact-permission philosophies selected

Start with `HANDOFF.md` above its archive line and
`$ss2-progression-design`. Present D1 only. D2 is conditional on B and requires
its own fresh prerequisite screen; E follows A after D2 is pruned.

## Where things stand

The owner selected `RCS-03C3C3C2E3-C`. The completed catalog must contain
responsive, steadfast-allow, and distinct steadfast-deny intrinsic exact-
permission law families (`R+D+A`). The selected hard viability obligation
remains: every required polarity family needs a legible, viable, non-token
purpose, and the steadfast-deny family may not be a dominated symmetry witness.
If later authoring cannot satisfy that gate, reopen E3 with B as the bounded
replacement.

Two read-only audits and mutual cross-challenge preserved D1 as exactly one
empty/nonempty exposure decision. For one resolved pair opportunity—one actual
evaluation, one actually bound lawful complete stance/configuration, its one
deterministic selected tuple, and one unordered tag pair—
`R^{resolved-related}_v` contains pairs whose selected ledgers use two distinct
final children with intersecting complete lineage or a shared nonmultiplying
authorization/accounting parent.

C2-C's pre-selector related support need not survive selection. A hybrid in
`E^{resolved-exact}_v∩R^{resolved-related}_v` may witness D1-B once and later
receives one permission bit. D1-B does not require a related-only route,
matched stance toggle, participant actionability, second form vote, result, or
payoff.

The register has 89 unique rows: 34 `SCREEN`, 1 `OWNER-OPEN`, 13 `PRUNED`,
37 `DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`; `Phi_SR = 35`.
D1 is the sole presented card. The authoritative decision record remains
unchanged.

## Highest-value next work

Record exactly one owner answer to `RCS-03C3C3D1`:

- A — no authoritative distinct-related route:
  `R^{resolved-related}_v=∅`. Every structurally supported related pair remains
  counterfactual to authoritative co-selection. This protects accounting
  clarity but makes the selected related grammar permanently latent. D2 prunes
  and E follows.
- B — require an authoritative distinct-related route:
  `R^{resolved-related}_v≠∅`. Recommend B under the existing SR-05/SR-06
  gates. At least one actual bound route selects a genuine related pair. This
  makes two distinct manifestations of one causal/accounting unity operative
  at exposure, without granting joint survival. Costs are one-payment
  compression, broad-lineage hubs, selector injection, explanation burden,
  and token-hybrid risk. D2 receives its fresh prerequisite screen.

Concrete B boundary: one bound `Chorus` stance selects P through `Twin Vow`'s
`Guard Pulse a` and Q through distinct `Riposte Mark b`; `a≠b` retain one
nonmultiplying operation lineage/payment. Both ledgers are authoritative, but
D2 still decides their pair's one permission bit.

## Hard rules and traps

- D1 classifies post-routing exposure only. It selects no permission-law
  authority, randomness, joint survival, candidate, survivor, substitute,
  claim, consumption, retry, benefit, payoff, prevalence, or placement.
- A says related pairs are never co-selected. It does not say their constituent
  proofs never appear or that avoidance is deliberate/player-configurable.
- B may use a related-only or hybrid witness. Requiring
  `R^{resolved-related}_v setminus E^{resolved-exact}_v` to be nonempty would
  be a stronger replacement, not part of B.
- At SR-05/SR-06 closure, the related fact must matter to something players can
  understand, plan around, or have treated in a viable non-token package. This
  is deferred validation, not a new D1 membership test or promised benefit.
- If no B witness can pass those existing gates, reopen D1 with A rather than
  ship inert incidence.
- Do not edit the authoritative decision record without an explicit accepted,
  rejected, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Verification and Git

Confirm 89 unique rows with the exact counts above and `Phi_SR = 35`. Confirm
the decision-record SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
Commit atomically with a `Co-Authored-By` trailer, push only the feature branch,
verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
