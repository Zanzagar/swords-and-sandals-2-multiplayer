# Handoff — immediate Soul Relic evaluation eligibility selected

**Session ID:** `ec00fd05-93e4-47f7-869f-5c5daf628c40`
**UTC:** 2026-09-24 06:59:01
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`; present only `RCS-03C2A` next.

## Owner answer recorded

The owner selected `RCS-03C1-A`: every legal maximal
non-cadence-readiness episode is evaluation-eligible at its opening cut `r`.
The completed design has no scheduled-gated or orphaned readiness episodes.
The relationship and its first opportunity to manifest therefore coincide;
no later Bell, phase, or other schedule gate is required.

This direction selects eligibility only. It does not decide whether an
eligible opportunity actually invokes, which participant may control it, when
that authority is expressed, or which contingent `H`/`S` evidence the
evaluator acquires. The authoritative decision record remains unchanged, and
no implementation is authorized.

## Register correction and next card

Re-deriving invocation authority found that old `RCS-03C2` still combined two
independent player-material axes: whether invocation is system-mandatory or
participant-contingent, and—if participant authority exists—whether it is
exercised live at the eligible cut or bound through an earlier precommitment.
The owner-visible register now replaces that parent with `RCS-03C2A` and
conditional `RCS-03C2B`, growing from 33 to 34 counted slots. Selecting C1-A
first reduced `Phi_SR` from 23 to 22; adding the one net split row returns
current `Phi_SR` to 23.

The sole active row is `RCS-03C2A`, evaluator invocation-control source. It
asks whether every legal evaluation opportunity invokes automatically, every
one remains materially participant-contingent, or both regimes coexist.
Recommend A, automatic invocation, because the selected source construction
and predicate/recurrence-control grammar already provide meaningful player
leverage while a second invocation command risks turning the Relic root into
another spell button.

Participant contingency includes live commands, carried precommitments,
grants, vetoes, and semantic coordinates that genuinely bundle invocation
control with another role. Merely causing Heat, Guard, or another readiness
fact is not invocation authority unless the authored action also carries that
unexhausted semantic role. An independent authorize/withhold pair is sufficient
proof of independent control, not a prerequisite for membership. This keeps
the A/B/C empty/whole/proper-subset partition exhaustive. A read-only
adversarial audit passed the corrected card.

`RCS-03C2B` remains conditional on a participant-supporting C2A answer. Exact
actor, consent/veto, communication, and information remain in `RCS-02`;
precommitment lock/rebinding remains in `RCS-13`; contingent evidence
acquisition remains in `RCS-03C3`.

## Validation

- Full suite: 584 tests, 583 passed, 0 failed, 1 skipped. The skip is the
  expected raw-trace archive check for this worktree profile.
- Structural audit: 20 `SC-*` cards with all seven required fields; 34 Relic
  register rows; `Phi_SR = 23`; exactly one `OWNER-OPEN` Relic row; 75 checked
  relative links and no missing target.
- `git diff --check` passed.
- `docs/design/endless-progression-decisions.md` remained byte-identical at
  SHA-256 `e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
- No runtime, Ruffle, capture, installation, save, snapshot, fixture,
  candidate, observation, manifest, or golden work occurred.
