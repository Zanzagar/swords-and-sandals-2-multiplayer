# Handoff — every form aligned; aligned family identity next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-21 01:24:09
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.122: whether actual aligned edges are overlap-
bearing only, member-disjoint only, or include both complete-family identities
somewhere in the completed locked catalog. The owner selected C3c.121-C.
Preserve accepted EP-D02 wording until a complete replacement is replayed and
explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the
current full-assurance pass before the separate standalone expansion, audit,
and adaptation pass. Vanilla evidence and the byte-identical oracle remain in
their own lane.

## Selected direction — EP-D02-C3c.121-C

The owner selected

`D^{all-IdKX}_{locked,v}=D^{U=}_{locked,v}`.

Every represented persistent-instance fiber is aligned-key-complete.
Equivalently, every completed exact key of every union-locked Relic definition
has a same-persistent-artifact partner in

`X^{1cW}_v(d)=A^{1coord}_v(d) intersection W^S_v(d)`.

The pair carries the definition's sole C113-A one-coordinate type and changes
every supported Soul's assigned noncore map. This closes C120's token-carrier,
premium-copy, and orphan-form loopholes, but selects neither more than one
partner nor every-pair alignment nor common access to both snapshots.

Tight floors are seven definitions, fifteen exact keys, and ninety six-Soul
rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree remains
six. Fixed exact-key/Soul fit remains direct; an actual aligned edge is partial
at the same-artifact/fixed-coordinate/across-snapshot boundary; universal key
and fiber incidence is aggregate. The authoritative decision record remains
unchanged.

## Active owner choice — EP-D02-C3c.122

Define actual aligned overlap-bearing and member-disjoint edges by

`X^O_v(d)=X^{1cW}_v(d) intersection O^W_v(d)`

and

`X^R_v(d)=X^{1cW}_v(d) intersection R^W_v(d)`.

Define the catalog's actual aligned family-identity set by

`T^{Xfam}_v={overlap : X^O_v(d) is nonempty for some locked d}`

`             union {member-disjoint : X^R_v(d) is nonempty for some locked d}`.

C95-C makes completed-key family identity injective. Hence overlap-bearing and
member-disjoint edges partition `W^S` and therefore partition `X`. C121-C
makes `T^{Xfam}_v` nonempty, so exactly three possibilities remain. This is an
actual authored-edge card; no latent-capability card is missing.

### A — overlap-bearing aligned edges only

`T^{Xfam}_v={overlap}`.

Every completed key has an aligned partner that preserves at least one exact
complete-family member and reassigns it to a disjoint Soul block. No member-
disjoint common edge is aligned, although C105-C still gives every key one
outside `X`.

This is the strongest countercase. It makes every clean same-artifact/fixed-
coordinate whole-roster comparison obey a recognizable motif-survival
covenant and is the simplest dialect to teach. The cost is that complete-
family reweaving can occur only on cross-ID or same-ID diagonal comparisons.

### B — member-disjoint aligned edges only

`T^{Xfam}_v={member-disjoint}`.

Every completed key has an aligned partner with no surviving complete-family
member. No overlap-bearing common edge is aligned, although C101-C still gives
every key one outside `X`.

This creates the sharpest controlled total-reweave comparison: persistent
artifact and one coordinate remain fixed while every family member changes.
The cost is losing exact-motif continuity at every aligned boundary.

### C — both aligned family identities occur — recommended

`T^{Xfam}_v={overlap,member-disjoint}`.

At least one actual aligned overlap-bearing edge and one distinct aligned
member-disjoint edge occur somewhere in the completed locked catalog.

C preserves both relational dialects at the clean aligned boundary: motif
migration and complete-family reweaving. It gives Souls and Simulacra broader
comparison and build language without treating separate edges as a stronger
direct doctrinal realization. Costs are two teaching languages, a token-
minority-edge loophole, and possible hidden segregation by name, tier,
comparison type, or access.

## Concrete witness and tight floors

C105's verified four-key geometry is:

```text
overlap-bearing common edges: b0--b1, b0--b3, b1--b2
member-disjoint common edges: b0--b2, b1--b3
not a common edge:             b2--b3
```

The exact layouts are:

```text
A: R17={b0,b3}  R44={b1,b2}   overlap-aligned matching
B: R17={b0,b2}  R44={b1,b3}   member-disjoint-aligned matching
C: use A in one C113-A name and B in the other
```

Coordinate decoration independently makes any layout state-only or boundary-
only. C's construction is a feasibility witness, not a selected correlation.

`b0--b3` retains exact family member `abd` but moves it from Souls `V,W` to
Souls `F,S` while every Soul changes map. `b0--b2` changes family
`{abc,abd,cde}` to `{abe,acd,bcd}` with no surviving member, while Relic
definition, persistent artifact, sole core, exact union `{a,b,c,d,e}`, atomic
vocabulary, and one coordinate continue.

Every branch retains:

| Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | ---: | ---: | ---: | ---: |
| `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |

C105-C already forces four keys per locked definition and C113-A already
forces two locked name classes above the five-definition/seven-key outside
base. Both displayed matchings cover all four keys, so A/B need nothing extra;
one of each realizes C. Independent read-only construction checks retained the
same edge classification for profiles `(2,2,2)`, `(3,3,3)`, `(5,3,2)`,
`(4,4,4)`, `(7,5,2)`, and `(5,5,5)`.

## Ideal ratings and exclusions

At one fixed exact-key/Soul relationship, inherited fit is **direct**. An
overlap-bearing aligned edge is **partial** at the same-artifact/fixed-
coordinate/across-snapshot boundary and **partial** at surviving-member
continuity because a motif persists but moves to a disjoint Soul block. A
member-disjoint aligned edge is **partial** at the broader artifact/core/union/
vocabulary/fixed-coordinate boundary and **neutral** at exact-family-member
continuity. Aligned-identity exclusion or catalog coexistence is
**aggregate**.

C122 selects no both-identities-per-definition, fiber, key, endpoint, owned
artifact, or C113 type; no state/boundary cross-product or correlation; no
partner count, degree, matching, connectedness, every-pair rule, overlap
cardinality, or common anchor; and no semantic cause, transition, path, common
access, live switching, disclosure, agency, frequency, value, power,
non-dominance, persistence, release, or implementation. A/B exclude an
identity only from aligned edges, not from the inherited common-edge graph.

If C is selected, the next coarser descendant is the occupied joint support of
`{state,boundary} x {overlap,member-disjoint}`. Its breadth can be two, three,
or four occupied cells before choosing diagonal orientation, a missing cell,
definition-level coexistence, or per-key dual alignment.

## Process-doc warning

`docs/overnight-agent-plan.md` lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. A correction is
now in `HANDOFF.md`'s living head. Treat the direct `AGENTS.md` rules as
governing and do not use the overnight plan as current guidance until its
live/frozen boundary is repaired.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C122 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
