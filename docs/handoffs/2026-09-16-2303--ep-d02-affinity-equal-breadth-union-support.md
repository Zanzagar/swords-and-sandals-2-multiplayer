# Handoff — every capable affinity breathes; fixed-width union support next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-17 03:03:18
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.84: whether none, some but not all, or every
**forced-breadth** affinity definition also structurally supports rotating the
exact noncore singleton union while holding its breadth fixed. The owner
selected C3c.83-C. Preserve the accepted EP-D02 wording until a complete
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle remain in their SS2 lane.

## Selected direction

EP-D02-C3c.83-C makes
`D^u_{var,v}=D^u_{support,v}`. Every support-positive or “breathing” affinity
definition has at least two completed exact operative keys with unequal roster-
wide noncore breadth `u`. C82-B's support-negative affinities remain forced to
one breadth outside the denominator. This requires two realized values, not
every value in the definition's structural support set.

At six Souls, Tide can realize the documented compact `u=3` and broad `u=4`
simulacra while Ashen and Veil remain forced at `u=2`. C83 guarantees that
structural contrast, not that broad is stronger or that either form is fun.
Later predicate, owner-Soul relevance, disclosure, agency, and sidegrade rules
must keep compact recurrence and broad coverage genuinely non-dominated.

Every fixed exact key retains inherited **direct** ideal fit. The same-
definition/cross-key breadth contrast is only **partial** because continuity of
name, palette, core, singleton count, and group count accompanies difference
only across separate simulacra. C's every-eligible-name mandate is
**aggregate**. Soul counts remain `{6,8,9,10,12,14,15}` with tight floors of
five definitions, seven exact keys, and forty-two Soul/key rows at six Souls;
at least one affinity has degree at least five.

## Active owner choice — EP-D02-C3c.84

For definition `d`, let `L_v(d)` be its ambient noncore label palette,
`N_v(d)=|L_v(d)|`, and `UnionSets_v(d)` contain the exact noncore unions of all
legal map families with its stable singleton count and group count. Define

`D^{U=}_{support,v}={d : exists U!=U' in UnionSets_v(d), |U|=|U'|}`.

Then

`d in D^{U=}_{support,v} iff min W_v(d)<N_v(d)`.

If a legal family leaves an ambient noncore label unused, replace one used
label with it throughout the family; map distinctness, map count, per-map
width, empty total intersection, sole core, and union cardinality remain fixed
while exact union identity changes. If the union exhausts the palette, it is
the unique subset of that size. Every breathing affinity therefore already
supports equal-breadth rotation. C84 can classify only the forced-breadth
residual left by C82-B:

- **A — only breathing affinities support rotation:** no forced-breadth name
  supports it. This preserves the cleanest anchored teaching lane but discards
  fixed-quantity motif substitution.
- **B, recommended — some but not all forced-breadth affinities support
  rotation:** one fixed-width name can exchange motif identity while another
  remains union-locked. This keeps an approachable anchor and opens a
  qualitative sidegrade lane, but creates three derived support classes.
- **C — every affinity supports rotation:** every forced-breadth name leaves
  ambient slack. This maximizes future variants but removes the genuinely
  union-anchored lane and gives every affinity latent comparison burden.

The six-Soul examples make the distinction concrete:

```text
locked:   palette {h,a,b},   core h, k=2, rho=2
          {h,a}|{h,b} -> U={a,b}, u=2=N

capable:  palette {h,a,b,c}, core h, k=2, rho=2
          {h,a}|{h,b} -> U={a,b}, u=2
          {h,a}|{h,c} -> U={a,c}, u=2
```

The two capable families prove structural support only; C84 does not author two
keys. Under B, Tide is breathing and has derived support, Ashen may use the
degree-four capable shape, and Veil may retain the degree-three locked shape.
Neither forced-breadth name is yet required to realize an alternate union.

C84 support itself is **neutral** under the standing ideal because it authors
no alternate simulacrum. Every fixed key retains **direct** fit. B's coexistence
across definitions is **aggregate**, not direct. A later same-definition/cross-
key equal-breadth rotation would be **partial**: name, palette, core, singleton
count, group count, and `u` continue while exact `U` differs, but the forms
remain separate simulacra with no selected coexistence or transformation.

All three directions retain Soul counts `{6,8,9,10,12,14,15}` and tight floors
`5/7/42`; support adds no exact key. B requires at least two forced-breadth
definitions and at least one forced-breadth affinity of degree at least four.
C requires every forced-breadth affinity to have degree at least four. Every
direction inherits at least one affinity of degree at least five from C82-B and
C83-C. With no more than six causal labels, the residual capable tuples are
`(degree,k,rho)=(4,2,2)`, `(5,2,2..3)`, `(6,2,2..4)`, and `(6,3,2)`;
`(3,2,2)` is the tight locked boundary.

C83-C's required pair has unequal breadths, so it cannot witness C84 rotation.
A later actual-rotation card must decide behavior prevalence inside
`D^{U=}_{support,v}`; requiring actual equal-breadth rotation from a breathing
definition would need at least a third key. Then follow complete map-family
variation at fixed union and block assignment at fixed family. Instance/state/
evaluation-boundary causation remains a later orthogonal attribution choice.
C84 selects no exact labels, actual pair, predicates, owner-Soul relevance,
disclosure, agency, sidegrade quality, power, acquisition, persistence,
release, or implementation. It does not satisfy C39's separate distinct-
instance and material shared-cell predicate-difference requirement.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C83-C
as selected and C84 as the sole active owner choice. The authoritative decision
record is unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04
directions remain unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain
selected, and R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Read-only frontier-order,
finite-mathematics, and design-lens audits independently checked the support/
behavior ordering, exact criterion, finite tuple enumeration, construction
floors, and recommendation. A separate read-only adversarial record audit
verified C83-C selection, sole-active C84 state, residual A/B/C exhaustiveness,
the support-only boundary, exact tuple/floor consequences, and ideal ratings.

`git diff --check`, no-index whitespace checks for current untracked
deliverables, the sole-active-heading assertion, stale-state scans, cross-
document C83-C/C84 assertions, the authoritative-record no-diff check, SVG XML
parsing, Unicode-format-control scanning, newest-handoff ordering, and an
independent Node enumeration of all twenty-nine legal tuples passed. The
repository test suite was not run because this was a documentation-only
decision round.
