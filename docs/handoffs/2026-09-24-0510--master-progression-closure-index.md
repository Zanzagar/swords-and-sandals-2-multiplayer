# Handoff — Master progression closure index; Relic frontier bounded

**Session ID:** `7cb88e20-0adc-4a67-b7f2-e23e75e55af9`
**UTC:** 2026-09-24 05:10:07
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, the living head, and `$ss2-progression-design`. The
owner asked for a master closure index before returning to Soul Relic design.
That index now exists at
`docs/design/endless-progression-master-closure-index.md`.

## Outcome

The index fixes three different destinations: owner-framework closure,
normative implementation-readiness, and the later standalone **Souls and
Simulacra** expansion/audit/adaptation pass. It separates `OWNER`, `DERIVE`,
`SPEC`, `AUTHOR`, `TUNE`, `EVALUATE`, and `BUILD` work so direction-selected
worksheet nodes cannot be mistaken for accepted systems or implementation
permission.

It defines:

- twenty fixed whole-system closure gates (`SC-01`–`SC-20`);
- the authoritative owner ledger, still 3/7 product decisions closed and 0/3
  architecture dispositions closed;
- twelve Soul Relic/Charm exit gates (`SR-01`–`SR-12`);
- a finite Relic proof packet;
- twenty frozen remaining Relic candidate slots (`RCS-01`–`RCS-20`), of which
  RCS-16 is `SPEC` and RCS-18 is `EVALUATE`, so `Phi_SR = 18` initially;
- card admission, anti-recursion, reclassification, and reopening rules;
- bounded D02, D07, R9.4, R10, R11, readiness, and authoring views;
- a dependency sequence through replay, standalone adaptation, readiness,
  separate implementation authorization, headless proof, and playable proof;
  and
- a policy-evaluation ladder in which RL/self-play helps map robust policy
  surfaces after the production-rule headless simulator exists but does not
  choose product semantics or replace invariants/human playtests.

An ordinary Relic answer must reduce `Phi_SR` and may not mint descendants.
A genuinely missed independent product choice requires an owner-visible
charter amendment naming the material consequence and new bound.

## C171 correction

Do not present or resolve EP-D02-C3c.171. Its A/B/C alternatives are preserved,
but the card is now `AUTHOR/EVALUATE`, not `OWNER`: its own scope changes no
player action, authoritative transition, information surface, local mechanic,
power/release result, or implementation obligation. C170-A is sufficient for
the framework's existential canonical-account continuity promise. The proposed
three-opportunity and graph descendants are pruned unless later evidence passes
the explicit reopening test. None of C171-A/B/C was selected.

That correction is placed at the active instructions in the owner packet,
rigor audit, and living head rather than only in this handoff.

## Next Relic frontier

Return to Relic design at `RCS-03` / `SR-04`: what may materially transform a
persistent Relic and who or what authorizes the transformation. First map its
prerequisites and confirm that the alternatives change a player-facing action
or authoritative state transition; then present exactly one bounded A/B/C card.
Precedence and stable artifact/definition/lineage identity are the separate
registered RCS-04 slot and must not be bundled into RCS-03.

Continue applying the **Souls and Simulacra** identity and *Achintya Bheda
Abheda* ideal only at the named operative relationship/boundary, with direct,
partial, neutral/protective, or aggregate fit and the gameplay tradeoff stated.
The ideal does not admit an otherwise immaterial graph choice.

## Files changed this session

- added `docs/design/endless-progression-master-closure-index.md`;
- updated `docs/design/endless-progression-owner-packet.md` at the C171 active
  instruction and session protocol;
- updated `docs/design/endless-progression-decision-rigor-audit.md` at its C171
  current-state instructions and finite-scope explanation;
- updated `README.md` to link the master index;
- updated `HANDOFF.md` living head; and
- added this handoff.

The authoritative decision record remains unchanged at SHA-256
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
Implementation remains blocked. The pre-existing user-modified
`.agents/skills/ss2-progression-design/SKILL.md` was not changed.

## Validation

- `git diff --check`: pass.
- Index counts re-derived from the document: 20 `SC-*`, 12 `SR-*`, and 20
  `RCS-*` rows; `Phi_SR = 18` follows from two pre-routed slots.
- Current C171 instruction search finds only the explicit “reclassified; not
  active” headings in the owner packet and rigor audit.
- Authoritative decision-record checksum: unchanged as above.
- No Node test suite was run because this was documentation/process work only;
  no runtime, schema, wrapper, capture, installation, save, or snapshot changed.

