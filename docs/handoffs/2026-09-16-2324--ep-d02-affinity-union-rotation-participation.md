# Handoff — selective fixed-width support; actual rotation classes next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-17 03:24:05
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.85: whether neither, exactly one, or both of the
breathing and fixed-width-capable affinity classes actually realize a same-
definition equal-breadth exact-union rotation. The owner selected C3c.84-B.
Preserve the accepted EP-D02 wording until a complete replacement is replayed
and explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle remain in their SS2 lane.

## Selected direction

EP-D02-C3c.84-B makes `E^{U=}_v` a nonempty proper subset of the forced-
breadth affinity domain. Three nonempty structural classes now coexist:

- every breathing affinity has derived equal-breadth union support;
- some forced-breadth affinities are fixed-width-capable; and
- the remaining forced-breadth affinities are union-locked.

At six Souls, Tide can use the degree-five breathing shape, morphic Ashen the
degree-four fixed-width-capable shape, and stable Veil the degree-three locked
shape. This selects support only. No definition is yet required to realize an
equal-breadth alternate exact key.

The supported-Soul set remains `{6,8,9,10,12,14,15}` with tight floors of five
definitions, seven exact keys, and forty-two Soul/key rows at six Souls. Every
fixed key retains inherited **direct** ideal fit; C84 support itself is
**neutral**, and its catalog coexistence is **aggregate**. A later actual
same-definition rotation would be **partial** because its continuous and
different features occur across separate simulacra.

## Active owner choice — EP-D02-C3c.85

Let `D^{U=}_{var,v}` contain equal-union-support-positive definitions with two
same-definition exact keys satisfying `u(b)=u(b')` and `U(b)!=U(b')`. Split it
across the breathing and fixed-width-capable structural classes, and let
`K^{U=}_v` contain each class with at least one actual witness.

A naive empty/proper/full card over definitions would bundle breathing-only,
fixed-width-only, and both-class participation with within-class prevalence.
C85 therefore asks only whether `K^{U=}_v` contains zero, one, or both eligible
classes:

- **A — neither class realizes rotation:** every breathing name still realizes
  C83-C's unequal-breadth contrast, but no equal-`u` pair changes `U`; the C84-B
  fixed-width-capable lane remains behaviorally latent.
- **B, recommended — exactly one class realizes rotation:** equal-breadth
  rotation becomes real while participation is limited to one eligible class
  rather than required in both. The immediate next A/B card chooses class
  identity. Only fixed-width-capable-only would separate quantitative breathing
  from qualitative fixed-width substitution; that is the likely next
  recommendation, but C85 does not select it.
- **C — both classes realize rotation:** at least one breathing name and one
  fixed-width-capable name rotate. This maximizes raw variant space but makes a
  breathing name carry both breadth change and same-width motif substitution.

The concrete six-Soul witnesses are:

```text
BREATHING TIDE
T1  U={a,b,c},   u=3
T2  U={a,b,c,d}, u=4    T1/T2 satisfy C83-C
T3  U={a,b,d},   u=3    T1/T3 realize equal-breadth rotation

FIXED-WIDTH-CAPABLE ASHEN
A1  U={a,b},     u=2
A2  U={a,c},     u=2    A1/A2 realize equal-breadth rotation

UNION-LOCKED VEIL
V1  U={a,b},     u=2=N  no alternate size-two union exists
```

C83-C already requires every breathing definition to have an unequal-`u`
pair. One pair cannot be both equal and unequal in breadth, so a participating
breathing definition needs at least three keys. A forced-breadth-capable
definition has one possible `u`, so Ashen's existing two morphic keys can
rotate `U` without adding a key. C85 does not make union identity their sole
difference; their named-Soul size roles may change too.

The exact tight floors are:

| C85 result | Definitions | Exact keys | Six-Soul rows |
| --- | ---: | ---: | ---: |
| A | 5 | 7 | 42 |
| B, fixed-width-capable only | 5 | 7 | 42 |
| B, breathing only | 5 | 8 | 48 |
| C | 5 | 8 | 48 |

B therefore has identity-conditional floors; do not report `5/7/42` without
saying it is attained only by fixed-width-capable-only participation. All
branches retain Soul counts `{6,8,9,10,12,14,15}` and C84-B's palette-degree
consequences. Actual behavior adds no new palette degree.

At the fixed-key/Soul-roster boundary, ideal fit remains **direct**. Each actual
same-definition/cross-key rotation is **partial**: definition, palette, core,
singleton count, map-group count, and breadth continue while exact union
identity differs, but the forms remain separate simulacra. A's marginal effect
is **neutral/protective**; B's one-class and C's two-class participation are
**aggregate**, not direct.

Actual rotation is not automatically fun or balanced. A new motif may be
strictly stronger, irrelevant, unreachable for the owner Soul, or hidden from
the player. Later predicate quality, owner-Soul relevance, disclosure, agency,
and non-dominance rules must make rotation a different puzzle rather than a
hidden quality tier. Geometry alone also does not satisfy C39-C: reuse requires
distinct same-definition instances and a separately material final-predicate
difference on a shared fixed cell, plus another invariant definition.

After C85-B, choose the sole participating class with an A/B card. Then resolve
some-versus-every prevalence inside each participating class, rotated-union
overlap, complete family variation at fixed union, and block-to-map assignment
at fixed family. Instance/state/evaluation-boundary causation remains a later
orthogonal attribution choice. C85 selects none of those, nor transformation,
coexistence, live switching, predicates, disclosure, power, acquisition,
persistence, release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C84-B
as selected and C85 as the sole active owner choice. The authoritative decision
record is unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04
directions remain unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain
selected, and R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Read-only frontier-order,
finite-mathematics, and product-design audits independently confirmed the
class-participation factorization, conditional floors, and B recommendation.
A separate adversarial record audit caught one conditionality error in my first
C85-B rationale: B itself limits participation to one class but does not
separate the two variation axes if breathing-only is later selected. I
corrected that wording at the operative instruction in every current record;
the audit's follow-up passed.

`git diff --check`, no-index whitespace checks for current untracked
deliverables, the sole-active-heading assertion, stale-state scans, cross-
document C84-B/C85 assertions, the authoritative-record no-diff check, SVG XML
parsing, Unicode-format-control scanning, newest-handoff ordering, exhaustive
subset-family checks, explicit A/B/C witness accounting, and supported-roster
profile checks passed. The repository test suite was not run because this was a
documentation-only decision round.
