# Handoff — fully coupled Soul Relic evidence excluded

**Session ID:** `72534467-4493-4e32-9856-6f79ae638153`
**UTC:** 2026-09-24 15:34
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`; present only `RCS-03C3B1A0` next.

## Owner answer recorded

The owner's latest `A` selected `RCS-03C3B0-A`: no counted position may contain
an additional fully inseparable H+S material path with zero projection-
isolation witnesses. Every position retains its required independently
attributable H or S form. Distinct history/state positions may coexist in one
entry, and ordinary causation remains legal.

At one position the selected form and predicate participate **directly**; H/S
difference remains mechanically real; excluding a fully inseparable supplement
is **neutral/protective**; and universal exclusion is **aggregate**. The choice
trades bespoke fused-moment clauses for local causal legibility. It changes no
authoritative decision wording and authorizes no implementation.

## Seventh register amendment

Post-answer screening found that old `RCS-03C3B1A` compressed three independent
boundaries:

1. residual material use of a position's opposite temporal form;
2. minimum declaration/execution/outcome phase attribution; and
3. additional irreducible multi-phase-H supplements among positions that
   already have a phase-isolatable H path.

It is now a non-counting parent for `RCS-03C3B1A0`, `RCS-03C3B1A1`, and
conditional `RCS-03C3B1A2`; `RCS-03C3B1B` follows with catalog phase support.

**Agent corrections:** one review incorrectly assumed B0-A made every material
H read independently H-sensitive. The three-row asymmetric H+S counterexample
disproved that premise. The first repair then classified only H+S packages
while claiming temporal projection-purity across every effective path. A
`T_H` predicate with an independently established material `{S,X}` package
disproved that scope, so B1A0 now covers every material opposite-form path,
regardless of its other package members. A separate review proved that one
phase-isolatable path and an additional irreducibly multi-phase path vary
independently. All gaps were corrected before presenting the next choice.

The register has 47 rows: 31 `SCREEN`, 1 `OWNER-OPEN`, 7 `PRUNED`, 6
`DIR-SELECTED`, 1 `SPEC`, and 1 `EVALUATE`; therefore `Phi_SR = 32`.

## Next card

`RCS-03C3B1A0` asks whether a position that already has one independently
isolatable temporal form may materially use the opposite form on another
effective path. That opposite form may join the selected temporal form or some
other legal predicate input.

- A: no position has an opposite-form material path. Every history position is
  temporally H-pure and every state position temporally S-pure. Entries may
  still combine distinct history and state positions.
- B: every position has at least one opposite-form material path.
- C: temporally projection-pure positions and opposite-form positions coexist.

Recommend A. It keeps one truthful temporal clock per position while locating
history/state unity in the larger multi-position Relic relationship. B and C
permit richer provenance-conditioned clauses, but introduce a second temporal
language that cannot be independently manipulated at that position. Exact
facts, package operators, phase identity/support, roots/atoms, fan-out, reuse,
payoff, UI, persistence, release, and implementation remain separate.

## Validation

- Structural target: 47 register rows and `Phi_SR = 32`, with only
  `RCS-03C3B1A0` marked `OWNER-OPEN`.
- Full suite: 584 tests, 583 passed, 0 failed, 1 expected raw-trace archive skip.
- `git diff --check` passed.
- `docs/design/endless-progression-decisions.md` remains byte-identical at
  SHA-256 `e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
- No runtime, Ruffle, capture, installation, save, snapshot, fixture,
  candidate, observation, manifest, or golden work was performed.
