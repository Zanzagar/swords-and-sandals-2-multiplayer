# Handoff — automatic Soul Relic invocation selected

**Session ID:** `49440671-6bb3-4302-b4a9-c43f70f6aadf`
**UTC:** 2026-09-24 07:21
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`; present only `RCS-03C3A` next.

## Owner answer recorded

The owner selected `RCS-03C2A-A`: every legal eligible opportunity invokes
exactly one cut-atomic evaluation automatically once its authoritative inputs
are fixed. No still-operative participant authority may separately bank,
withhold, veto, grant, or release invocation. Upstream participant actions may
still create relationship facts or recurrence permission, but a bundled
invocation role, standing invocation instruction, or default-with-material-
veto is incompatible with A.

This selects invocation rather than successful output. The evaluation may
still yield a provisional candidate or no candidate; C134 sealing and C135
same-tag recurrence protection remain unchanged. `RCS-03C2B` is pruned because
participant-authorization timing has no object. `RCS-02` and `RCS-13` cannot
reintroduce invocation consent, veto, or precommitment without explicitly
reopening C2A-A. The authoritative decision record remains unchanged, and no
implementation is authorized.

## C3 prerequisite screen and next card

The acquisition part of `RCS-03C3` has one derived answer. Cut atomicity,
immediate eligibility, automatic invocation, current-`S` semantics, and the
complete pre-adjudication projection require one coherent authoritative read
at the actual invocation cut: current `S_e` plus the within-battle `H_e` facts
still semantically admissible there. Earlier state is not current `S`; torn
revisions, readiness latches, and post-cut rereads are invalid/specification
defects, not owner directions.

The sole active player-material C3A boundary is occurrence-history semantic
freshness. Its options classify the nonempty set of every materially H-reading
position, including any permitted coupled `H`+`S` addition, by whether open-
retention-capable positions are absent, universal, or a nonempty proper subset:

- A: every H-reading position has a disclosed finite nonterminal semantic-age
  horizon;
- B: every H-reading position permits at least one materially qualifying
  occurrence with no nonterminal age expiry; or
- C: freshness-bounded and open-retention-capable positions coexist.

Recommend A. It preserves meaningful sequence memory while preventing turn-one
priming, stale-history laundering, and very late opaque automatic procs. Exact
horizon units/lengths, occurrence phase, clearing, consumption/reuse, current-
state fields, payoff, and UI realization remain separate AUTHOR/SPEC/TUNE or
registered work.

A fifth prerequisite audit found that the old C3 row still compressed canonical
atom identity, same-cut cross-result/tag fan-out across one or multiple
coincident invocations, use-dependent claim/
consumption and no-result disposition, later same-tag reuse, and later
different-tag same-root reuse. `RCS-03C3` is now a non-counting parent replaced
by `RCS-03C3A`–`RCS-03C3F`; existing `RCS-08` explicitly owns cross-combatant/
cross-root evidence fan-out. One atom cannot occupy two positions of one
relationship result, while C3B separately selects the semantic atom boundary
before exact per-family catalogs become AUTHOR/SPEC.

The register now has 39 slots: 25 `SCREEN`, 1 `OWNER-OPEN`, 7 `PRUNED`,
4 `DIR-SELECTED`, 1 `SPEC`, and 1 `EVALUATE`, so `Phi_SR = 26`.

## Validation

- Full suite: 584 tests, 583 passed, 0 failed, 1 skipped. The skip is the
  expected raw-trace archive check for this worktree profile.
- Structural audit: 39 Relic register rows, `Phi_SR = 26`, and exactly one
  `OWNER-OPEN` Relic row.
- `git diff --check` passed.
- `docs/design/endless-progression-decisions.md` remained byte-identical at
  SHA-256 `e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
- No runtime, Ruffle, capture, installation, save, snapshot, fixture,
  candidate, observation, manifest, or golden work occurred.
