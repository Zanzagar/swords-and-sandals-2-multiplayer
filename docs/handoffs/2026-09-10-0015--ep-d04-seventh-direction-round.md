# Handoff — EP-D04 seventh direction round pending

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-10 04:15:02
**Branch:** `design/endless-progression-owner-packet`

Resume the two EP-D04 R7 distribution choices in the owner packet. Preserve
accepted EP-D01, EP-D02, and EP-D07 and every selected-but-unaccepted EP-D04
direction. Do not edit the authoritative record or implement anything.

## Session outcome

The owner's `go with your recommendation` selected `R6.1-A`; it did not accept
a final EP-D04 replacement.

One R6 semantic cell is the complete versioned offer menu. Its fingerprint
contains cardinality; every offer's committed descriptor, authorized
disclosure, and either latent R5 leaf or deterministic preselection commitment
that resolves to it; actionable order/position; and the accepted selection,
consent, tie/deadline, hunt-menu, eligibility, and commitment grammar. Candidate
families receive the same paired exogenous context, though legal `pi_offer`
policies may choose differently. A commitment resolves identically regardless
of candidate policy. Non-actionable permutations canonicalize, actionable
duplicates remain one context property, and roots/seeds are replicates.

## Recomputed frontier

Three measures are now distinct:

- `U_route`: family-neutral structural weighting over paired R5 route leaves;
- `G_offer`: the pinned generator's raw probability over joint R6 contexts;
- `E_pi`: the legally experienced distribution induced by applying the complete
  disclosed-information player policy to `G_offer`, with no post-result
  reweighting.

The two current owner choices are independent:

1. **R7.1-A, recommended — structural gate plus one-way experience veto.** Use
   `U_route` for the primary paired causal ceiling/niche gate. Let
   `G_offer -> E_pi` reject a structurally valid catalog if required counters
   are practically absent, colour choice becomes generic, or offer steering
   collapses diversity. Experience can never rescue a failed structural gate.
   B makes experience primary; C removes its veto authority.
2. **R7.2-A, recommended — recursive fixed parent mass.** Within each separately
   gated format, freeze a finite versioned mechanics hierarchy before scoring,
   give canonical parents fixed equal mass, recursively divide their mass among
   semantic children, and average roots only inside exact leaves. A parent with
   100 descendants cannot swamp one with five. B weights every exact leaf
   equally in a flat list; C uses bespoke weights vulnerable to post-result
   tuning.

`G_offer` comes from the declared generator root/cursor measure, exactly or by
an estimator whose confidence requirements are selected later. Live telemetry
is diagnostic, not normative. Authored file count and captured sample count
never define semantic or generator probability.

After R7, set numeric mixed coverage, the permitted Legendary lead and
concentration, experience-veto floors, ties, solver sufficiency, and unresolved
cell treatment. Exact taxonomy/support authoring remains required before final
EP-D04 replay.

## Verification

`git diff --check` passed after this handoff. The authoritative decision record
has no diff. No runtime code, tests, vanilla evidence, licensed assets,
installation, save, snapshot, capture, candidate, fixture, observation,
manifest, or golden changed. Tests were not run for this docs-only owner round.
No commit or push was made. Implementation remains blocked.
