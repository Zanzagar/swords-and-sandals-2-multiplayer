---
handoff:      2026-09-26-0518--relic-universal-routing-configuration-selected
written:      2026-09-26 05:18 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    three bounded read-only B3B audits plus one read-only axis adjudication
branch:       design/endless-progression-owner-packet
commits:      9e35979..HEAD
suite:        proportional serial checks 10/10 passed; register reparses 79/79 with Phi_SR 38; decision-record hash unchanged
supersedes:   2026-09-26-0501--relic-joint-responsive-routing-selected
---

# Handoff — universal participant-configurable Relic routing selected

Start with `HANDOFF.md` above its archive line and
`$ss2-progression-design`. The sole active Relic frontier is
`RCS-03C3C3B3B1`; present that one card only.

## Where things stand

The owner selected `RCS-03C3C3B3A-B`, requiring every routing-capable versioned
Relic contract to provide at least two materially different prospectively
bindable participant routing settings. A twenty-second prerequisite amendment
replaced old B3B with constructive-language prevalence B3B1 and conditional
context-branching prevalence B3B2. The register has 79 rows and
`Phi_SR = 38`; the authoritative decision record remains unchanged.

The prior term “attunement” was corrected to **routing stance** because
remembered boundary attunement already names a distinct RCS-03A persistent
coordinate. No coupling between them has been selected.

## Highest-value next work

Record the owner's B3B1 answer, then audit the resulting next row before
presenting it. B3B1 classifies legal participant construction:

- A: all contracts expose only indivisible authored whole-policy routing
  stances; recommend this direction;
- B: every contract supports a bounded typed builder, with presets allowed; or
- C: closed-only and constructive contracts coexist.

Under A, prune B3B2 and open B4A. Under B/C, open B3B2. Do not answer exact
stance count, primitives, context predicates, actor, information, timing,
defaults, randomness, permission, or results inside B3B1.

## Hard rules and traps

- Every lawful setting plus immutable versioned Relic semantics must denote one
  total automatic selector law over every reachable nonempty ledger product.
  A separately described fixed tie/completion rule is not another owner axis.
- Classify legal participant controls, not implementation syntax: compiling a
  builder into generated IDs does not make it a closed stance, while an
  intrinsically complex authored stance remains closed when selected whole.
- Presets do not remove a builder contract from the constructive class.
  Multiple humans selecting whole stances do not make it constructive merely
  because RCS-02 later composes their inputs.
- B3B2, not B3B1, owns whether constructive contracts permit
  participant-authored input-dependent branches.
- “Routing stance” and “remembered boundary attunement” are distinct terms and
  coordinates unless a later explicit replacement couples them.
- No live prompt, veto, `none`, partial tuple, retry, or downstream-aware switch
  is legal. B4A/B4B still own randomness and draw coupling.
- Unrestricted scripts, arbitrary lookup tables, or a guaranteed universal
  cross-Relic language are explicit replacement paths, not hidden B.
- Do not edit the authoritative decision record without an explicit accepted,
  rejected, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Verification and Git

Confirm the register reparses to 79 rows with 37 `SCREEN`, 1 `OWNER-OPEN`, 8
`PRUNED`, 29 `DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`;
`Phi_SR = 38`. Confirm the decision-record SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
Commit the answer atomically with a `Co-Authored-By` trailer, push only the
feature branch, verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
