# Handoff — affinity breadth support mixed; actual eligible-name variation next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-17 02:42:56
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.83: whether no, some but not all, or every
**support-positive** affinity definition actually realizes at least two values
of roster-wide singleton-realized noncore vocabulary breadth `u`. The owner
selected C3c.82-B. Preserve the accepted EP-D02 wording until a complete
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle remain in their SS2 lane.

## Selected direction

EP-D02-C3c.82-B makes `D^u_{support,v}` a nonempty proper subset of the
affinity-definition domain. At least one affinity name's stable palette degree,
singleton count, map-group count, and sole core admit two noncore union
breadths, while at least one other name's tuple forces one. This selects
structural eligibility only, not actual unequal-breadth keys.

At six Souls, Tide may be support-positive at profile `(2,2,2)` with
`(degree,k,rho)=(5,3,3)`, while Ashen and Veil remain support-negative at
profile `(4,2)` with `(3,2,2)`. Fixed-key ideal fit remains direct; structural
support is neutral and coexistence across definitions merely aggregate. The
supported-Soul set stays `{6,8,9,10,12,14,15}` with tight C82-B floors of five
definitions, seven keys, and forty-two Soul/key rows; at least one affinity has
degree at least five.

## Active owner choice — EP-D02-C3c.83

For affinity definition `d`, define

`Breadths_v(d)={u_v(b): b in R_{affinity,v} and def_v(b)=d}`

and

`D^u_{var,v}={d in D^u_{support,v}: |Breadths_v(d)|>=2}`.

The denominator must be the support-positive domain. Support-negative names
are forced to one breadth and cannot serve as authored-stable witnesses.

- **A — no support-positive affinity realizes unequal breadths:**
  `D^u_{var,v}` is empty. C82's breadth-changing capacity remains unrealized;
  fixed-breadth map, partition, and predicate differences may still matter.
- **B — some but not all support-positive affinities realize unequal
  breadths:** `D^u_{var,v}` is a nonempty proper subset of
  `D^u_{support,v}`. This necessarily requires at least two support-positive
  definitions and creates eligible-variable, eligible-stable, and forced-
  stable catalog classes.
- **C, recommended — every support-positive affinity realizes unequal
  breadths:** `D^u_{var,v}=D^u_{support,v}`. Every capable name has at least
  two exact keys with different `u`; C82-B's support-negative names remain
  forced-breadth. This does not require every attainable value in `W_v(d)`.

C is recommended because it makes every deliberately eligible affinity realize
its compact-versus-broad breadth contrast without B's eligible-but-unused
subtype. It is universal only inside `D^u_{support,v}`, not across all
affinities. It guarantees structural contrast, not fun, sidegrade quality, or
power.

The six-Soul C witness uses stable palette `{h,a,b,c,d}`, core `h`, `k=3`, and
`rho=3`. Tide key `FS|VW|TR` uses maps
`{h,a,b}|{h,a,c}|{h,b,c}`, giving `U={a,b,c}` and `u=3`. Its C79-required
partner-fluid key `FV|ST|WR` uses
`{h,a,b}|{h,c,d}|{h,a,c}`, giving `U={a,b,c,d}` and `u=4`. The compact form
has noncore incidence `a2/b2/c2/d0`; the broad form has `a2/b1/c2/d1`.
Later predicates can make that recurrence-versus-coverage contrast a
sidegrade, but C83 does not yet do so.

Every fixed exact key retains inherited **direct** ideal fit: one core
participates across distinct Soul maps. The same-definition/cross-key contrast
is only **partial**: name, palette, core, `k`, and `rho` continue while `u`
differs, but the forms are separate exact simulacra. C's every-eligible-name
mandate is **aggregate**, not direct.

All C83 branches retain Soul counts `{6,8,9,10,12,14,15}`. A and C have tight
floors `5/7/42` at six Souls. B has no single construction attaining all of
its component minima; its exact Pareto frontier is `(definitions,keys,rows)`
equal to `(6,9,54)` at six Souls or `(5,7,63)` at nine. B also requires at
least two affinities of degree at least five. At six/eight Souls only equal-
size names can be support-positive under the selected no-repeat mixed-profile
rules, so B must add a second two-key equal-size definition. At nine, existing
Tide `(3,3,3)` and Ashen `(4,3,2)` can be support-positive while Veil `(7,2)` stays
support-negative.

If breadth is monotonically rewarded, the broad key becomes a hidden god roll;
if its extra label is unreachable, off-owner, or predicate-dead, the change is
token. Later owner-Soul relevance, predicate quality, disclosure, agency, and
sidegrade rules must make compact recurrence and broad coverage genuinely
non-dominated. C83 alone does not satisfy C39's separate distinct-instance,
fixed-cell, material shared-predicate witness.

Unequal `u` already forces unequal exact `U` label sets and complete map
families. C83 does not pick those identities. The next structural projection
must isolate **equal-breadth exact-union rotation**, then family variation at
fixed union, then block assignment at fixed family. Instance/state/evaluation-
boundary cause is a later orthogonal attribution choice. Transformation,
coexistence, live switching, predicates, disclosure, owner-Soul relevance,
agency, sidegrade quality, power, acquisition, persistence, release, and
implementation all remain open.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C82-B
as selected and C83 as the sole active owner choice. The authoritative decision
record is unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04
directions remain unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain
selected, and R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Three read-only audits
independently checked frontier order, finite mathematics/construction floors,
and gameplay/thematic recommendation. A fourth adversarial record audit caught
and drove correction of a stale C82 roadmap sentence, an overbroad “permitted
key” quantifier, exact-degree wording that should have been a lower bound, and
language that could have implied all eligible geometry was behaviorally latent
rather than only its breadth-changing capacity; its follow-up passed.

`git diff --check`, no-index whitespace checks for all current untracked
deliverables, the sole-active-heading assertion, stale-state scans, cross-
document C82-B/C83 assertions, the authoritative-record no-diff check, SVG XML
parsing, Unicode-format-control scanning, newest-handoff ordering, and
independent Node set-family/construction/Pareto assertions all passed. The
repository test suite was not run because this was a documentation-only
decision round.
