# Handoff — lived foil required per name; represented-copy coverage next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-21 02:10:48
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.126: whether every represented persistent instance
has at least one same-history aligned edge in no locked definitions, some but
not all locked definitions, or every locked definition. The owner selected
C3c.125-C. Preserve accepted EP-D02 wording until a complete replacement is
replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the
current full-assurance pass before the separate standalone expansion, audit,
and adaptation pass. Vanilla evidence and the byte-identical oracle remain in
their own lane.

## Selected direction — EP-D02-C3c.125-C

Every locked Relic name has at least one aligned edge materially realized by
the same owner and persistent artifact in one canonical nonforked fixed-version
legal history:

`D^{hist-X}_{locked,v}=D^{U=}_{locked,v}`.

Different names and edges may use different histories, and endpoints need not
be adjacent. This turns all four C124-A classes—`S-O`, `S-R`, `B-O`, and
`B-R`—into at least one lived relationship without selecting a direct
transition, cause, direction, reversibility, choice, disclosure, or practical
frequency. Tight floors remain nine definitions, twenty-three exact keys, and
138 Soul/key rows at six Souls. Soul counts remain `{6,9,10,12,14,15}` and
tight maximum degree remains six. Each lived edge is partial at the same-owner/
same-artifact/across-history boundary; universal name prevalence is aggregate.
The authoritative decision record remains unchanged.

## Active owner choice — EP-D02-C3c.126

Define the represented persistent IDs with a lived aligned edge by

`Ids^{hist-X}_v(d)`

`  ={r in Ids_v(d)`

`      : there exists {b,b'} in X^{hist}_v(d)`

`        with inst_v(b)=inst_v(b')=r}`,

then define

`D^{all-Id-hist-X}_{locked,v}`

`  ={d in D^{U=}_{locked,v}:Ids^{hist-X}_v(d)=Ids_v(d)}`.

C125-C makes `Ids^{hist-X}_v(d)` nonempty for every name. It does not make it
equal to all represented IDs. A history-negative ID still has static aligned
edges covering every completed key under C119-C/C121-C; its aligned endpoints
simply occur only in mutually exclusive histories.

### A — no name is positive in every represented instance

`D^{all-Id-hist-X}_{locked,v}=empty`. Every locked name contains at least one
history-positive and one history-negative represented instance. This is the
strongest incarnation-individuality countercase: a name deliberately includes
a morphic artifact and an aligned-fate-bound artifact. Under one-copy
ownership, duplicate substitution, no transfer, and permanence, however, it
mandates a same-name copy-quality lottery unless both are disclosed and valuable
sidegrades.

### B — fully positive and represented-instance-hybrid names coexist

`D^{all-Id-hist-X}_{locked,v}` is a nonempty proper subset. At least one name
gives every represented instance a lived edge, while another name contains
both positive and negative instances. This can distinguish systematic from
incarnation-sensitive lineages, but layers hidden name and copy taxonomies and
has the largest premium-name/premium-copy risk.

### C — every represented instance has a lived aligned foil — recommended

`D^{all-Id-hist-X}_{locked,v}=D^{U=}_{locked,v}`. Every persistent ID
represented by completed locked keys has at least one internal aligned pair
that its same owner and artifact materially realize in one qualifying history.
The copy a player acquires therefore cannot decide whether the player ever
experiences the name's lived relationship.

## Concrete separator and tight floors

```text
S-O NAME

R17={b0,b3}    one lawful R17 history contains b0 ... b3
R44={b1,b2}    both snapshots are valid, but occur only in mutually
               exclusive R44 histories
```

This name satisfies C125-C through R17 but fails C126-C. C requires R44 to
have its own lawful `b1 ... b2` history; the two IDs need not coexist, share an
owner, or share a history.

| Branch | Definitions | Keys | Rows at six Souls | Minimum represented locked IDs | Minimum lived edges |
| --- | ---: | ---: | ---: | ---: | ---: |
| A | 9 | 23 | 138 | 8 | 4 |
| B | 9 | 23 | 138 | 5 | 4 |
| C | 9 | 23 | 138 | 4 | 4 |

A uses two two-key IDs per name, one lived and one counterfactual. B needs at
least one two-ID hybrid and can use one ID for each other name. C can use one
four-key ID per name. In a one-ID O-only name, equal-coordinate blocks
`{b0,b3}/{b1,b2}` retain only the O matching as aligned; an R-only name uses
`{b0,b2}/{b1,b3}`. Swap state and boundary for the other comparison row. One
dyad may be lived while the other remains counterfactual. Therefore all
branches need only one lived edge per name: `4/5/8` would be conditional on
retaining the illustrative two-ID layout, not a global tight floor.

Every lived edge is **partial** at the same-owner/same-artifact/across-history
boundary. A history-negative fiber is **neutral/protective** only there; its
static aligned relationships retain their partial fit. Instance and definition
prevalence are **aggregate**.

C's remaining exploit is one token lived dyad per represented artifact. Other
keys may still have only counterfactual aligned foils, each fiber may use a
different history, and unrepresented inventory possibilities remain outside
the completed fixed-version quantifier.

C126 selects no endpoint coverage, complete carrier, common history, direct
transition, direction, reversibility, cause, agency, exact state/boundary
semantics, frequency, value, power, persistence format, release, or
implementation.

## Frontier discipline

Do not jump directly to global endpoint coverage. The selected static ladder
was C118 definition existence, C119 represented-instance incidence, C120
complete-carrier existence, then C121 all-fiber/global-key coverage. The lived
ladder must preserve the same unbundled order:

1. C125 definition-level lived-edge existence — selected C.
2. C126 represented-instance lived-edge incidence — active.
3. If C, ask whether every name has at least one lived-key-complete represented
   fiber.
4. Then ask whether every represented fiber is lived-key-complete, equivalently
   whether every completed endpoint has a lived aligned partner.

Exact state/boundary semantics precede causal transition and agency. A state-
only comparison might describe artifact mutation, while a boundary-only
comparison might describe one unchanged artifact interpreted at two contexts;
calling both transformations now would silently conflate them.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling: follow direct `AGENTS.md` rules
and do not treat the overnight plan as current guidance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C126 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
