# Session handoffs

A handoff is the **brief for one session**: what to do next, in what order, and
what not to do. Starting a session should cost one sentence —

> read the latest handoff in `docs/handoffs/` and proceed

— rather than a pasted wall of context that goes stale the moment it is sent.

## Naming

```
docs/handoffs/YYYY-MM-DD-HHMM--short-slug.md
              2026-08-31-1238--corpus-audit-provenance-repair.md
```

Local date and time **when the handoff was written** (which is the end of its
session), then a slug naming what the session was about. Local rather than UTC
because a human reads it; the offset is recorded in the frontmatter, so nothing
is ambiguous. No colons — Windows forbids them in filenames.

The names sort chronologically, so `ls docs/handoffs/` puts the newest last and
"the latest handoff" is unambiguous without an index to consult.

Sequential `H-NNN` numbering was tried first and dropped: a number tells you
nothing about when it was written or whether it is still current, and a stale
brief that looks authoritative is worse than no brief.

## Frontmatter

Every handoff opens with it. This is the part that makes a handoff traceable
back to the work rather than just readable:

```yaml
---
handoff:      2026-08-31-1238--corpus-audit-provenance-repair
written:      2026-08-31 12:38 -0400
sessionStart: 2026-08-31 09:39 -0400
sessionId:    4a79fa7f-176b-455c-aa91-372038a141af
agentRuns:    wf_bc86037b-0f9 (13 auditors), wf_d2aa6793-7cb (1 writer + 3 verifiers)
branch:       arena/champion-capture
commits:      73f7fba..2585952
suite:        617 passed / 0 failed / 0 skipped
supersedes:   none
---
```

`sessionId` and `agentRuns` are the load-bearing ones. Every subagent's full
transcript, its brief included, is recoverable at:

**Derive this path, do not copy it — it is a function of the working directory,
and the working directory has moved twice.** Claude Code names the project
directory after the absolute path of the tree it was launched in, with the
separators flattened. So:

```
# WSL, measured 2026-09-01 — this is where the runs of the 2026-08-31 sessions are
~/.claude/projects/-home-corey-projects-swords-and-sandals-2-multiplayer/
  <sessionId>/subagents/workflows/<runId>/

# Windows: same rule, derived from whichever tree was used at the time.
%USERPROFILE%\.claude\projects\<flattened absolute path>\<sessionId>\subagents\workflows\<runId>\
```

The literal path this file used to give —
`C--Users-corey-OneDrive-Documents-ChatGPT-SS2-Multiplayer-Mod` — names the
RETIRED OneDrive tree. It was already stale when the Windows tree moved to
`C:\ss2-capture` on 2026-08-31, and it never described WSL at all. A path
written out literally in a document that calls it load-bearing is exactly the
thing that rots; the rule survives a relocation and a literal does not.

So a later session can read exactly what an auditor was asked and what it
answered, instead of trusting a summary of it. `docs/overnight-agent-plan.md`
already depends on that path, and every number in it was derived that way.

`suite` must state the exact count. A session that measures a different number on
its first minute has found something — and that only works if the number was
written down. A **skip** in particular means a path derivation broke.

Record hashes as a `commits:` RANGE, not a tip. The tip is written before the
last commit of the session exists, so a tip hash in the body is wrong by
construction; tell the reader to run `git log --oneline -1` instead.

## The two documents are not the same thing, and must not drift

| | `HANDOFF.md` (repo root) | `docs/handoffs/<stamp>--<slug>.md` |
| --- | --- | --- |
| [2026-09-20 21:30 — the bolts are built, and a `null` was a scheduled divergence](2026-09-20-2130--the-bolts-are-built-and-a-null-was-a-scheduled-divergence.md) | `f1f6ade5` | **THE FIRST SPELL VERBS ARE BUILT — `cast_lightning_bolt` AND `cast_frightning_bolt` — BUT THE THING TO CARRY FORWARD IS THAT THE MAP'S TABLE HAD THE WRONG SHAPE AND THE SHAPE SILENTLY PRODUCED THREE WRONG VALUES IN CODE A LIVE CAPTURE WAS SCHEDULED TO DIVERGE ON.** `SS2_DIRECT_DAMAGE_SPELLS` carried `damageMethod: null` for hell fireball, dire fireball and frightning bolt for three weeks, because the map listed one row per SPELL while the bytes push `damage_method` and `bonus_frame` **once per ARM** — three spell call sites serve eleven spells, so all three fireballs are `"burning"`/4 (`+0x91c1`) and both bolts `"lightning"`/8 (`+0x85af`). **A row-per-spell table structurally cannot record a per-arm argument**, so the values read as map silences. **And the measurement was already in the tree**: `ss2-capture-wrapper.as` has named these literals at these offsets since `7601888`, five hours after `8c3fc0a` wrote the nulls, and it says **group**. `observation.js` deep-compares `/events` against the wrapper's real `arguments[4]`, and the one fixture carrying a null is an ACTIVE staged capture target. **Only the bolts are a turn** — the fireball family lands damage from a per-frame handler after a ballistic flight and molten death spawns 10-20 boulders (41-81 samples); **sample count is NOT the discriminator, timing is**, and the fireball's `_currentframe == 4` test is an IDEMPOTENCE GUARD (doubled `Not`) rather than an impact trigger. **Also corrected:** the ladder has SEVEN unconditional arms not six (id 45 sits immediately before gale and pre-empts it); `inventory_maxslots` DOES gate the hero's in-battle buttons and `randomise_gladiator`'s spell fill, and **there is no default of 6** — six needs `herolevel` 40 and levels 6-14 give 2; `CANONICAL_RESOURCE_SOURCES` must NOT gain the six slots, which the adapter contract had already decided. **My own errors:** I destroyed ~500 lines with `git checkout -- <file>` while undoing a mutation test, and **my fix for Codex's first finding caused its second** — `boltOnOffer` invalidated the construction the psyche range check had been retired against, leaving a charged caster at separation 1960 burning 57 stamina a turn for ever. **My 21 focused tests all passed and none asked the AI anything.** 4 verifiers, 4 returned, 2 HOLDS 2 PARTIALLY-BROKEN (both breaks mine). Codex `gpt-6-astra` ×3: needs-attention, needs-attention, approve. 23/23 golden hashes identical, `BATTLE_STATE_VERSION` unmoved. Suite 2126 / fail 0 / skipped 1, exit 0. |
| [2026-09-20 02:00 — the six slots are declared, and three of yesterday's sentences were wrong](2026-09-20-0200--the-slots-are-declared-and-three-sentences-were-wrong.md) | `2fa3286b` | **`inventory1`–`inventory6` ARE DECLARED RESOURCES AND ALL 23 GOLDEN REPLAY HASHES ARE BYTE-IDENTICAL, BUT THE THING TO CARRY FORWARD IS THAT ONE WAVE AIMED AT THE BYTES BROKE THREE SENTENCES PUBLISHED THE DAY BEFORE.** **"The gate on casting a gale is carrying id 38 in one of six slots" is wrong by four conditions** — it is a 90% top-of-function roll whose failure chooses NOTHING, 23 preceding arms of a strict 28-arm fixed-order ladder, `check_inventory(38)`, `fightdistance < 400`, and `armourclass < armourclass_max / 2`; gale is arm 24 of 28. **"A character-initialisation block writes 1 to every slot, beside `shield = 0`" is wrong in every clause but the offset** — it is OPPONENT generation (the whole body is inside `if (whichcharacter != game.hero)`, so "a fresh gladiator carries nothing" is FALSE for the player) and `shield = 0` is the last statement of the `herolevel == 1` arm whose branch TARGET is the first inventory write. **"The empty marker differs by column" is wrong twice** — `_root.inventory0` and `_root.inventory1` are byte-identical rows (`0x3FF6C8`/`0x3FF6E9`), so 0 is inventory's own second nothing-row, which is what reconciles the champion DNA's trailing zeroes. **The sharper replacement: 1 is the marker the CODE uses (fourteen emptiness tests, every one `== 1`; every literal slot write is 1; zero writes of 0), 0 is the marker the authored DATA mostly uses.** **Shipped:** the six names with NO defaults (the `psyche_up` shape), the demo roster 0 → 1, `check_inventory` documented for the first time, and four more facts the map never carried — the offer gate differs by side, `inventory_maxslots` gates nothing in combat, there is no affordability check on a spell's stamina cost, and `is_that_virtuous` holds a dead third `> 0` predicate. **Also corrected: `test/seeded-play-pins.test.js` explained the goldens' safety with "`derive: false` stops the default fill", which is FALSE** — the bag loop is ungated and 31 of a golden hero's 38 keys come from the default table; the real protection is a name having no default, and `ss2-team-rules.test.js` had said so for four days. **Two priced costs did not exist:** `CANONICAL_RESOURCE_SOURCES` stays out until a verb writes a slot, and the campaign record needs nothing. **My own errors:** a wave brief that both forbade and granted reading the installation in one paragraph, and a `git stash` run underneath a live Codex review. Wave `wf_1ee83aec-a10` VERIFIED, 9 started 9 returned 0 dead. Codex `gpt-6-astra`: approve, no material findings. Suite 2098 / fail 0 / skipped 1, exit 0. |
| [2026-09-19 21:30 — four defects, a campaign, and a blocker that moved](2026-09-19-2130--four-defects-a-campaign-and-a-blocker-that-moved.md) | `164a0c3b` | **THE CAMPAIGN LAYER GOT THE HOST IT NEVER HAD AND `shove` GOT BUILT, BUT THE THING TO CARRY FORWARD IS THAT A CODEX REVIEW OF THIRTEEN ALREADY-PUSHED COMMITS RETURNED FOUR REAL DEFECTS, THREE HIGH.** **The CLI fought gladiators that were not the demo roster's** — `demoSide().members` straight into `createTeamBattle` gave red-1 strength 5/5/5 and 140 max health against the roster's 9/7/8 and 46, so every persisted bout described the wrong fighters; the repo already names that shape as a past mistake twice. **`shove` was offered per FRAME not per FOE**, so one nearby enemy unlocked a shove against every enemy in any lane — the cross-lane defect the owner found by watching on 2026-09-18, reintroduced in a new verb. **The campaign could silently roll back** (an unreadable record sorted BEFORE every real timestamp and the guard read only the tail). **Two writers shared one `.writing` file** — a contract nothing checks is a comment. **Shipped:** a campaign that survives the process (`file-backend.js` + `arena-campaign.mjs`, state IS the records), `shove` (zero RNG samples), `aiTaunts` and `rankJoinSurplus: 0`, the archive manifest at 8,325 files from 1,588, and three shell decisions out of the DOM set. **`use_item` is derived** — 1 is the empty inventory marker, confirmed three ways, and the marker DIFFERS BY COLUMN — but `cast_gale`'s blocker MOVED rather than cleared: `inventory1`-`6` are not declared resources, and declaring them is a schema change that can move all 23 golden replay hashes. **Left unstarted at 75% context on purpose.** Suite 2098 / fail 0 / skipped 1, exit 0. |
| [2026-09-19 15:00 — four ranked items, and three premises that did not survive](2026-09-19-1500--four-ranked-items-and-three-broken-premises.md) | `164a0c3b` | **FOUR RANKED ITEMS CLOSED AND TWO VERBS SHIPPED, BUT THE DURABLE RESULT IS THAT THREE PUBLISHED CLAIMS BROKE — one of them mine, eight hours old — AND ALL THREE BROKE THE SAME WAY**: a measurement with one arm, taken at the wrong layer, or never taken. **The AI taunts at range** (priced in hitpoints; 288-112, 8.8σ, winning on BOTH arms) and **joins its ally's fight** (owner's call on a sweep; crossings 20.0%→25.9%, turns with 2+ fights 30→196, at ~2.3σ of win rate — the `aiCharges` trade). **`shove` is built and takes NO RNG sample**, phase derived from the oracle because the map carried two rows and not the phase — which is why it shipped in an afternoon where `taunt` took a month. **The archive manifest attested 19% of the archive** for eighteen days; regenerated, old attestation verified first and proved a strict superset. **The broken claims**: "taunt-always wins 257 of 400, ~4σ" is the BLUE arm of a two-arm test (red LOSES 189-208; control 200-200; alternating 2.65σ); "with lanes nobody can gang up" is backwards (one lane gives 0 of 2,851); and **"the shell is 4,110 lines and not one is executed by a test" is FALSE** — 13 of 70 are, via `liftFromShell`, and `src/render/arena-shell.js` has held shell decisions since 2026-09-12. **Six of my own errors, five caught by a measurement or a mutation rather than review**: an amortised approach that built a taunt-bot, a strike arm discounted once where the build rolls twice, a shove priced like a flee, a dial defaulted ON while its docstring said nothing moved, an id that spelled `-join-always` and took the OFF path, and two test stagings that passed while exercising the wrong thing. Suite 2079 / fail 0 / skipped 1, exit 0. |
| [2026-09-19 04:00 — the taunt was never ranked](2026-09-19-0400--the-taunt-was-never-ranked.md) | `164a0c3b` | **THE AI TAUNTS AT RANGE NOW, AND TWO OF THE PREVIOUS HANDOFF'S PUBLISHED NUMBERS BROKE THE SAME WAY — a measurement with one arm, or taken at the wrong layer.** `chooseAiAction` prices a taunt in HITPOINTS (the recovery, the direction-20 strike, and one turn of the target's `max_damage` when it can force a FLEE) and takes it at range when it beats the swing the gladiator is walking toward; the attack rows are untouched, so the 560-combination band sweep still holds. **`aiTaunts` ships ON and names itself in the id only when OFF** — the opposite spelling from `aiCharges`, because charging LOSES on the arithmetic while taunting replaces a walk worth nothing. New policy against old, sides alternating: **288-112, 72.0%, 8.8σ, winning on BOTH arms**. **The verb was failing in two places, not one**: taunt was legal on 914 of 2,064 decisions and on 664 of them (72.6%) no attack was legal at all, so the walk arm returned before any table existed — **it was not ranked last, it was never ranked**, because the taunt is a LONG-RANGE verb the build wires on the two longrange controller frames and on neither close-range warrior frame. **And the two claims that broke**: "taunt whenever legal wins 257 of 400, ~4σ" is the BLUE arm of a two-arm experiment — the same policy on red LOSES 189-208, the AI-vs-AI control is 200-200, alternating gives 226 of 399 at 2.65σ, and the gap between arms is four times the effect; and **"nobody can gang up" is backwards** — with the lane gate applied as the offer applies it, a 2-on-1 happens on 3.0% of turns with the shipped lanes and **0 of 2,851 turns in one lane**, so the remedy ranked item 2 proposed is the disease it describes and the layout fork is CLOSED rather than answered. **The demo roster gains a duellist** (`charisma: 16` on slot 3, swept not picked) because taunt was offered 926 times and taken 5 before it — a correct policy with nobody to express it is the `aiCharges` failure again. **Four of my own errors, each caught by a measurement rather than by review**: amortising the approach over the walks built a taunt-bot (81.6%, bouts 84% longer); the effect-1 strike rolls the chance a SECOND time so the arm is quadratic in it; a SHOVE priced like a FLEE made an archer taunt a foe it could shoot; and one clause added to `provenance.note` blew a 512-character cap and turned 24 campaign tests red. Codex `gpt-6-astra`: approve, no material findings. Suite 2048 / fail 0 / skipped 1, exit 0. |
| [2026-09-18 01:30 — the picture was lying, and the canvas was a postage stamp](2026-09-18-0130--the-picture-was-lying-and-the-canvas-was-a-postage-stamp.md) | `cbee9926` | **THE OWNER FOUND FOUR DEFECTS BY WATCHING A BOUT AND THE SUITE HAD CAUGHT NONE OF THEM**, because all four live in the two places this project does not test: a seven-line MIME table in the dev server, and a 4,110-line browser shell not one line of which is executed by a test. **Sound**: every mp3 served 200 as `application/octet-stream`, which browsers will sniff for an image and will not decode for `<audio>` — the whole soundtrack, mute, for as long as the extractor has existed. **Lanes**: reach was Euclidean because `getfightdistance` is, so **4,440 of 7,845 melee swings (57%) were cross-rank**; after `ss2SameLane`, 0 cross-lane offers in 35,523. **The victory loop**: frame 1426 loops `celebrate1a`, not the run, and a previous session had written the whole-run loop down as an acceptable approximation — **a stated approximation is still a wrong picture**. **And the best report of the four**: "is AI attacking its own teammate with ranged?" — the AI was innocent (0 ally-targeted in 250 shots) and **85 of 120 arrows ENDED inside a living teammate's body**, because the walk clamp parks a front-liner at `target.x ∓ physical_size` and the arrow's stop-short ends the flight at the same point, by construction. The arc was not the cause and its published clearance (`>= 1.055` figure heights) is wrong anyway: 0.789 on its own terms, 0.636 with the real `targetSize`. **Separately: a gladiator who had not moved was facing nobody**, so a missing `facing-left` read as "faces right" and **40 of 40 opening ranged attacks scored as back attacks at +50% damage**. **The knockback displaces at last**, after being deferred twice for a cost that did not exist. **The canvas had never been sized** — `<canvas>` with no width or height is 300×150, so every pixel number this project has published went through an unrecorded bilinear upscale. **A TWELVE-AGENT FUNCTIONALITY AUDIT** (6 inventories + 6 adversarial checks, 12/12, 0 dead, 32 defects, all six headlines corrected) then retracted four numbers this repo publishes — `aiCharges` is inert on the path anybody plays, `rankStride` defaults to 97 and not 0, "bouts settle without the crowd" was measured on the one roster tuned to avoid it, and "17 of 42 verbs" counts assignment SITES carrying 48 labels — and concluded that **the bottleneck is the AI, not the rules**: nineteen verbs resolve, `quick-attack` is chosen on 100% of 3,498 legal turns, six legal verbs 0 times in 6,160 decisions, and the rig can reach ~62 animations while the shipped game draws 19. **One correction to the audit's own framing, measured before handing off**: the band ranking is NOT broken — swept over 560 stat combinations it chooses quick 400 / normal 139 / power 21 — so the melee monoculture is a property of the DEMO ROSTER, not of the policy, and only the non-damage verbs (`taunt`, absent from the preference table entirely) are a real AI gap. |
| [2026-09-16 23:56 — the stance, the glow, and an AI that winds up](2026-09-16-2356--the-stance-the-glow-and-an-ai-that-winds-up.md) | `cbee9926` | **THE CHARGED STANCE IS BUILT AND IT GLOWS.** `changeCombatants` resets both fighters to `Standing` and then overrides whichever holds a charge — `gotoAndStop("psyche_charging")` at counter 2, `psyche_charging2` at 3 (`+0x281e`, `+0x284d`, `+0x287c`, `+0x28ab`) — so a gladiator holding a psych-up charge STANDS charged. `src/render/stance.js` owns the whole idle decision; the shell had `timelineFor("Standing")` and a clock inline, so "which pose does a gladiator rest in" was a decision the suite could not reach. **It is the first figure effect group anything here draws**, which closed a condition `figureRouteFor()` had set for itself and could not meet — `filtersScaled` is `true`, radii linear at 4.2742/8.5483/17.0967px. **SIX VERIFIERS, TWO REFUTED ME**: "the only held fighter stance in combat" is false (86 of the clip's frame scripts end in `Stop` and only 7 self-loop, so the figure parks on the terminal frame of nearly every action — the true claim is *the only one that survives a turn boundary*), and "reproduces the build rather than approximating it" is false — **action-end hold affects EVERY action and this engine has no concept of it**, now ranked first. Two real defects fixed: no `alive` guard, and a negative `now` leaking a negative phase. **A MAP SILENCE RETIRED BY THE BYTES**: sprite 2249 frame 1 is `initbattle` and sets both fighters' `psyche_up = 1`, the only initialisation site in the SWF. **AND THE AI WINDS UP NOW, BEHIND `aiCharges`, OFF BY DEFAULT AND IN THE RULE-SET ID WHEN ON** — owner's call against the measurement that the verb was chosen 0 times in 6,000 AI actions, **but the arithmetic refutes the criterion it was taken on**: charge 11.53 damage/turn against quick 17.82, so the flag buys a CHARACTER and says so. 40 seeded 3v3 bouts: 0 charges off, 2,433 on, 40/40 resolving either way. A Codex review then found the arm charging at the WRONG FOE — `legalActions` emits one option per foe and I took the first, having decided a self-targeted action's target could not matter; it sets both the range gate and the damage roll. **And I shipped a dead guard, deleted it, restored it on the review's advice, then measured 0 of 60 attackable foes outside the discharge gate and deleted it again.** **EXTENDED 03:40/04:40 — THE TAUNTED FLEE IS BUILT AND THE RUN IS NOT A BIG WALK**: `movement_speed * 40`, **no** boot bonus, stop gap **10** not 20, and the movement table's closed form `40 * ms - 10` is +1 low at 23 of 57 reachable speeds because the tween is a do/while. **A FLEEING GLADIATOR RUNS THROUGH THE MAN WHO TAUNTED HIM, DERIVED**: both run arms carry a body test (`+0x3fbd`, `+0x4146`) — live `_x` every frame, abort rather than clip, the ATTACKER's `physical_size` — and it is guarded on the facing, which row 3 inverts, so the arm that runs is always the arm whose guard is false. A second Codex review returned four: **three real** (stale facing after a flee, since `changeCombatants` re-derives both at every phase advance; one flee spending one source-qualified `taunted1` where the build has a boolean; a forced swap consuming NOTHING and a forced rest four flags of five, because rows 1-7 are statements and row 3 clears before its silenced `getphase` — now `SS2_CHAIN_CLEAR_FLAGS`, the same list `forcedStatusFlag` ranks off) and **one rejected on the bytes**, whose recommendation would have diverged the engine from the oracle. Three of the four were the same defect: **a rule already written in this repo that the new branch did not call** — and a fourth thing made it easy, a comment saying *"exactly two verbs move anybody"*, **a census true when written and read as a licence once it was false.** `[-2100, 2100]` is byte-verified too (`+0x31c2` … `+0x325b`); the caveat saying it lived in prose only was the DECODER's silence, not the build's. **EXTENDED 08:00 — A TWELVE-AGENT WAVE BROKE THAT COMMIT AND FOUND THE MONTH'S LARGEST LIVE DEFECT**: `ss2FacingEffects` had ONE call site, the movement branches, so a gladiator who had not yet walked carried no `facing-left` — and `ss2IsBackAttack` reads a MISSING token as "faces right", so **40 of 40 opening ranged attacks were scored as back attacks at +50% damage**, invisible to melee because at 500 apart nothing melee is in reach until somebody walks. Fixed with an `openingEffects` hook (STATUS only, refused by name otherwise), which is where `changeCombatants` derives it too; three of four seeded pins moved and the fourth is the evidence. **`damagecharacter`'s KNOCKBACK DISPLACES NOW, and the reason it had not was FALSE**: "it re-datums every pinned hash and every golden that carries one" was written in three places and dropped its hedge in a `next:` field — no golden carries a position or a hash and none structurally can, and the displacement moves ZERO pins (every seeded driver swings directions 1-4 and the gate needs 5-12 or 30, which is luck, not coverage). **AND THE ARENA CLAMP IS CITED TO DEAD CODE, WRITTEN FOUR HOURS EARLIER**: `nextphase`'s ±2100 block acts on `_root.game.hero`/`.villain`, plain `new Object()`s, against 50 reads of `attacker._x`; the live clamp is in `attacker.onEnterFrame` and hitting it ENDS the phase. **Finding the literal proved the literal, not the effect — a retraction is a new claim.** `knockback()` decoded at last (`_x + force`, one real second, no clamp in its 155 bytes) with **four** call sites, not two — `shove` and `cast_gale` are player verbs this engine does not model at all. 12 started, 12 returned, 0 dead, 2 of 6 headline claims refuted by their own refuters. |
| [2026-09-16 22:28 — the build plays seven runs](2026-09-16-2228--the-build-plays-seven-runs.md) | `cbee9926` | **THE 05:00 HANDOFF RANKED "ANIMATION SEQUENCES" THIRD, AS ART COMPLETENESS. IT WAS A CORRECTNESS GAP IN FIVE DISPATCHED ANIMATIONS**, because the premise underneath it — *"this engine dispatches one animation per action and never a sequence"*, in three handoffs and four source files — was true of the ENGINE and had never been checked against the BUILD. In AVM1 a `gotoAndPlay("x")` runs FORWARD until an action stops it, and **seven of export 1241's 101 labels carry no terminating action inside their own span**: `Hurt8`->`Hurt9` (stop 1283), `knockback`->`knockback_mov` (1446), `psyche_up`->`psyche_charging` (1626), `psyche_up2`->`psyche_charging2` (1643), `burning`->`flame_repeat` twice (1963), `celebrate1`->`celebrate1a`, `initialize`->`Standing`. So direction 8 plays 34 frames where direction 9 plays 18, a knockback 19 and not 13, a first charge 18 and a second 17 — and **`burning` plays 32 where this engine played 2**, 6% of the build, invisible to every test because the pack's entry really is two poses long. **THE DISCRIMINATOR THIS PROJECT HAD BEEN USING WAS WRONG**: "no `StartSound` binding" is true of 21 of 101 labels and of only four continuations, while `hurt8`, `knockback` and `celebrate1` are silent ENTRY points whose sound fires on the continuation — **which also answers a puzzle the living head recorded twice, that `hurt8` is "the one silent hurt"**. **`supersededBySibling: ["knockback"]` WAS BACKWARDS**: `damagecharacter` dispatches `"knockback"` and the 13-frame clip is its continuation, so this engine drew the second half of a knockback and never the first. Landed `src/render/clip-sequences.js`, read-only `tools/clip-sequences.mjs` that re-derives it from the oracle, pose concatenation with the effect-group indices REBASED, six authored beat counts, 22 tests, four mutation checks. **A SIX-VERIFIER WAVE REFUTED TWO OF MY CLAIMS AND ONE WAS THE SAFETY ARGUMENT**: clip labels are NOT strictly downstream of `toTeamWireState` — the resolver puts them ON events, 19 of 23 goldens state `combatwon`, and renaming one moves 3 of 5 literal hash pins. The conclusion holds for a different reason (this diff is `src/render/`), and the wrong reason would have licensed a resolver-side rename. **AND `psyche_charging*` ARE A HELD STANCE THE ENGINE HAS NOT BUILT** — `changeCombatants` poses both fighters from the counter every turn with `gotoAndStop` (`+0x281e`, `+0x284d`, `+0x287c`, `+0x28ab`), so a gladiator holding a charge STANDS charged instead of `Standing`. Found, named, ranked first, deliberately not attempted. **A PINNED CODEX REVIEW THEN RETURNED needs-attention WITH TWO, BOTH REAL**: the knockback correction had made knockbacks SILENT, and re-deriving it found the same hole already open on `hurt8` — sound follows the RUN now, never the family; and the new decoder read ActionPush doubles without swapping their two 32-bit words, which the fighter clip's int32 argcs hid. |
| [2026-09-16 05:00 — `psyche_up` is built](2026-09-16-0500--psyche-up-is-built.md) | `79ae298f` | **THE LAST VANILLA ACTION THAT WAS NEITHER BUILT NOR BLOCKED NOW RESOLVES**, and the twelve figure-pack effect groups three handoffs recorded as reaching nobody reach a real gladiator (10 of 12 — the other 2 sit on continuations this engine cannot sequence). Three presses: the counter is read at press time and advanced on report-back, so 1 plays `psyche_up`, 2 plays `psyche_up2`, 3 plays `psyche_up3` and fires a range-gated grievous. **THE DISCHARGE NEEDED NO ARITHMETIC** — `directionProfile`'s `direction === 30` arm already resolved it, `ceil(max_damage * 1.5)` falling back to `character_level * 10`, **which also closes the brief's first named hole: the map's unexpanded "level-based fallback" IS expanded**, in the module the goldens replay against. **IT IS NOT AN `ATTACK_BAND` AND THAT IS THE LOAD-BEARING DECISION**: membership means "always attacks" and two of three presses draw NOTHING, so a band entry would put samples on the ordered channel the build never takes and desynchronise every peer replaying the same tape — pinned by a test counting the RNG journal across a charge, with an ordinary attack as the control. **The reset lives in `phaseTransitionEffects` because that IS this engine's `nextphase`**, which is where the build puts it, so the rule has one copy rather than eight. **Out of range it decides nothing** — no roll, no damage, no death — unlike every melee attack, and the charge is KEPT. **Where the counter lands is a STATIC CANDIDATE, marked**, and the map's own gloss on it is wrong by one press: the readings differ as a CADENCE (3 then 2 per discharge, against 3 every time), not as a re-discharge. **Six pinned lists turned red and every one was right to**, including the test written to fail when this was built and an adapter test that used `psyche_up` as its example of a field that is NOT a resource. **Nothing moved**: suite 1931, fail 0, every golden included — the counter is a resource with NO default, which is what bought that. |
| [2026-09-16 01:30 — `psyche_up` was never the owner's](2026-09-16-0130--psyche-up-was-never-the-owners.md) | `79ae298f` | **THREE HANDOFFS CARRIED `psyche_up` AS AN OWNER DECISION ABOUT "WHAT THOSE SPELLS ARE". IT IS A VANILLA SS2 ACTION** — a `getphase` label, a button on all four controller frames, 31 mentions in the battle map, a fully decoded discharge chain — and the twelve figure-pack effect groups are its ART. AGENTS.md says new systems are the agent's to build and this one does not even need designing. The derivation is `docs/handoffs/PSYCHE-UP-BRIEF.md`, checked by a wave of 10 (5 questions, 5 verifiers, 10 started, 10 returned, 0 dead): **five verdicts, ALL PARTIALLY-BROKEN, none confirmed — six premises broke, four of them mine.** **I published `round(strength)` at `+0x653f` as psyche_up's DAMAGE; it is its STAMINA COST**, a row of the staminacost-by-phase table beside `power_attack -> round(strength*3)` and `rest -> 0 - round(stamina*15)` — a table whose `rest` row is negative can only be a cost table — while the damage is `ceil(max_damage * 1.5)` in the attack-roll dispatcher. **And the agent that corrected me OVERSHOT and its verifier caught that too.** Two live defects fixed on the way: **every grievous blow bound its actor to `attack30` and there is no such clip** (the fighter carries `attack1`..`attack12`; the same defect as the `direction === 23` branch directly above it, one number later, surviving because the suite pinned 23 and not the CLASS — the guard is the RANGE now, mutation-checked), and **`ss2-rules.js`'s deferral paragraph was wrong about three of its four entries for two days**, still deferring the ranged trio that shipped 2026-09-13. Also recorded: a defaulted resource moves all 23 golden replay hashes (measured), this repo's reassurance to the contrary is right for the wrong reason, and **`ss2-capture-staging.md` prescribes a capture the committed wrapper cannot take** — its recording window is exactly `checkattackroll`'s duration and both counter writes happen after it returns. |
| [2026-09-15 23:30 — two clip phenomena, one per rasteriser](2026-09-15-2330--two-clip-phenomena-one-per-rasteriser.md) | `79ae298f` | **THERE ARE TWO CLIP EFFECTS AND EACH EXISTS UNDER EXACTLY ONE RASTERISER.** `tools/clip-probe/index.html` gained a GRADIENT backdrop and a RESAMPLED BITMAP — both built from the page's own arithmetic, a 64x64 offscreen filled by a formula and drawn at a non-integer scale, so still no SS2 asset — and with three null controls at 0 and a positive control at 18,440 the centre box (~200px clear of every rectangle) reads: every whole-pixel clip **0/0**; every FRACTIONAL rectangle **2,548 at delta 6 under `cpu` and 0 under `gpu`**; GRADIENT + snapped clip 0/**3 at delta 1**; **BITMAP + a WHOLE-pixel clip 0/**50 at delta 1**. So the fractional-rectangle effect perturbs PATH ANTIALIASING and is CPU-ONLY — it is what the snap removes — while **a clip over a RESAMPLED BITMAP perturbs the sampler at delta 1, is GPU-ONLY, and does not care whether the rectangle is whole.** That is the arena's 654-pixel band (rows 177..434 are the sky and the crowd, which are raster), so it is NOT an open residual of the clip and no rectangle fixes it; the 21:30 entry saying otherwise is corrected. **THE SNAP IS STILL RIGHT AND BETTER ARGUED**: 644 interior movers removed under `cpu`, nothing cost under `gpu`. **AND I CORRECTED MYSELF A SECOND TIME**: 21:30 said the probe gives every number identical under both rasterisers — true only of a page with ONE canvas, because allocating a second changes the answer, and the run with gradient trials but no offscreen still read 2,548, so it is the ALLOCATION not the drawing. **THE PAGE CAUGHT A THIRD DEFECT IN ITSELF, THE FIRST ABOUT ITS OWN INSTRUMENT**: adding the bitmap trials took the NULL CONTROL from 0 to 8,196 at delta 45 because the offscreen was allocated AFTER three baselines, so every trial was compared against a baseline from the other regime — and it printed "THE CONTROLS FAILED, so every row above is noise" instead of a number. **A lazily-created resource is a regime change with a timestamp.** `tools/shot.sh` takes the rasteriser as its sixth argument now — it hardcoded `--disable-gpu` for a session after its sibling stopped, so two shots taken with the two tools differed by 15.9% before anything under test changed — and a test READS the shell script to pin the contract (default, both branches, the refusal, the printed line, and that the hardcoded flag is gone), mutation-checked. |
| [2026-09-15 21:30 — the rasteriser moves a sixth of the arena](2026-09-15-2130--the-rasteriser-moves-a-sixth-of-the-arena.md) | `79ae298f` | **EVERY PIXEL COUNT THIS PROJECT HAS PUBLISHED WAS MEASURED WITH `--disable-gpu` AND NOTHING RECORDED IT.** `tools/shot-live.mjs` passed the flag as a constant; it is the EIGHTH argument now, `cpu` (the default and the old behaviour) or `gpu`, **refused by name** if it is neither — a typo silently selecting software rasterisation is how the weapon glow's "3.9x frame cost" came to be published as a property of the feature. `chromeFlagsFor` is exported so the suite reaches it, and it asserts the two flag lists **differ by `--disable-gpu` and nothing else**, because two shots are a measurement only if that holds. **VARYING ONLY THAT FLAG MOVES 152,530 PIXELS — 15.9% of the arena frame, max delta 105** — of which 133,568 are inside the canvas (20.3% of it, bbox exactly the stage) and 16,607 are the sidebar, while each rasteriser against itself is byte-identical. **So two shots under different rasterisers are not comparable at all.** It is a DIFFERENT phenomenon from the clip residual and the signature says so: 15.5% of the movers sit on a strong gradient against 99.8% for the clip, over a third of the stage — bitmap resampling and gradient dither, not antialiasing. **AND THE FRACTIONAL-CLIP FINDING SURVIVES THE CHECK**, which is what the previous handoff's ranked item 3 asked: `tools/clip-probe/index.html` shot under both gives every number identical (2,548 centre pixels for every fractional rectangle, 0 for every whole one) and its own canvas differences to 0 between them, so the residual the snap removed was never an artefact of the measuring configuration. **THE ARENA REPORTS ITS OWN RECTANGLE NOW, AS A VALUE RATHER THAN A LOG LINE**: `stageFitReportFor` returns the canvas, the CSS rect, the ratio, the stage in DEVICE and PAGE pixels and all four bars, **calling `stageFitFor` and `stageClipRectFor` rather than re-typing them** (which is what its test asserts); `render` hangs it on `window.__stageFit` every frame and `shot-live` prints it beside every shot. **IT CORRECTED ITS AUTHOR ON ITS FIRST RUN** — the canvas is 870x800 and starts at page y 43, not the 870x688 at y 98 derived from the letterbox bars; **two errors that nearly cancelled**, leaving a derived stage top of 156.5 against a true 158. **OPEN AND NAMED**: under `gpu` the snapped clip still moves 654 pixels, EVERY ONE AT DELTA 1, in the sky and crowd band (rows 177..434) where under `cpu` the same pair is 0 — all-delta-1 over a gradient is a dither difference, not the mechanism just closed. |
| [2026-09-15 19:00 — the residual was the rectangle, and a warning was wrong](2026-09-15-1900--the-residual-was-the-rectangle-and-a-warning-was-wrong.md) | `79ae298f` | **THE 392-PIXEL CLIP RESIDUAL IS CLOSED AND IT WAS THE RECTANGLE'S OWN FRACTIONAL EDGES.** `stageFitFor` halves and multiplies by a float, so `stageClipRectFor` landed on a whole pixel only by accident — at the arena's 870x688 canvas it was `0, 58.531, 870 x 570.938`. A fractional clip makes Chrome clip through an ANTIALIASED MASK instead of a whole-pixel scissor, and that perturbs antialiased edges anywhere on the surface, hundreds of pixels from any boundary. It snaps to whole device pixels now, each edge moving by at most half of one. **`tools/clip-probe/index.html` is the instrument and the reusable part** — the canvas2d counterpart of `tools/swf-probe.mjs`, no SS2 asset, no pack, same content and same canvas with ONLY the rectangle varying, reading its own pixels back so nothing depends on differencing a screenshot afterwards. Null control 0, null-with-overhang 0, positive control 19,911. At 870x688 in a 160x160 box ~200px clear of every edge: every WHOLE-pixel rectangle gives centre 0 **even when it cuts 81,393 pixels at the edges**, while a FRACTIONAL one gives centre 2,548 at delta <= 6 **even when it cuts nothing** — so it is the fraction and not the cutting. The sweep: one fractional edge is as bad as four (centre 2,548 in every case, identically), 0.25 does it and 0.001 does not — a blitter switching ONCE for the surface, not error accumulating. **AND IT HOLDS IN THE ARENA**: `seed=7`, frame 120, 1200x800 against `?clip=0`, movers 20px or more inside every clip edge go **644 BEFORE to 0 AFTER**, leaving 870 — one full canvas-width row AT the boundary, which is the clip's own edge. **CONTROL (a) IN THE PREVIOUS HANDOFF TESTED A CLIP THAT WAS NOT THERE**: an inflated rectangle intersected with the device bounds is a solid rectangle and Skia collapses that back to a B/W region clip, so *"a clip inflated by 10,000px is pixel-identical to no clip"* exercises an ELIDED clip and cannot speak to one that is present — that inference is why the question pointed at "what is being cut" for a session. **CONTROL (b) WAS REAL AND NEARLY POWERLESS**: `?groups=0` is byte-identical to the default at this dressing by md5. **`tools/arena/main.js` HAS NO `?filters=0`** — it MENTIONS one in a comment beside the clip, and the living head said both shells carried it; only the screens page does. **AND THE PREVIOUS HANDOFF'S WARNING AGAINST WRITING AN ARENA PROBE IS WITHDRAWN**: "the call site executes and the logging does not" is an invalid inference, because the stage clip is COMMITTED code and a working clip cannot distinguish "my edit was loaded" from "a build without the probe was loaded". Four agents broke it independently and a 1200x2600 shot settles it — the panel holds `figure groups:` and `seam: painter ops=100` lines, both logged from inside `renderStage`. The panel is simply BELOW THE FOLD at every size the clip work was shot at. **Two of my own explanations died on this probe** (a clip anywhere perturbs AA — no; a clip that CUTS perturbs elsewhere — no), and a verifier was right that "99.8% of the movers are on antialiased edges" is CORROBORATING, NOT DIAGNOSTIC: at a maximum delta of 8 a difference of any cause can only surface on antialiased coverage. **The probe's own positive control failed TWICE and said so both times.** Wave: 10 started, 10 returned, 0 dead. |
| [2026-09-15 15:35 — a synthetic SWF is an oracle, and two glows were doubled](2026-09-15-1535--a-synthetic-swf-is-an-oracle-and-two-glows-were-doubled.md) | `470865d7` | **THIS REPOSITORY CAN ASK A FLASH PLAYER A QUESTION WITH A KNOWN ANSWER NOW.** `tools/swf-probe.mjs` writes a few-hundred-byte SWF carrying a chosen filter and `tools/ruffle-shot.ps1` renders it under Ruffle — no game state, no save, no SS2 bytes. Two questions each ranked open and each called "a session's work to set up" were both answered in one afternoon, because a synthetic movie ISOLATES THE VARIABLE where one frame of the real build offers one sample and no control. **THE GREEN BAND IS CLOSED AND IT WAS NEVER A BAND**: the sky leaves the stage and how far depends on the frame — top edge -12.0 at frame 1 but **-164.8 at frame 60**, 1 operation above the stage against 17 — and `HANDOFF.md`'s extents table, the thing that made this look like a 15-pixel disagreement for five handoffs, was computed AT FRAME 1 on a surface with 200 of them. **A table computed at one frame is not a table.** Varying only Ruffle's `--letterbox` over a probe with one rectangle inside the stage and four outside it: every outside edge reads `#000000` with it on and its own colour with it off, inside control `#ffffff` in both — so **a player rasterises out-of-stage content and MASKS it, and this renderer had neither behaviour**, computing a letterbox in `stageFitFor` then painting through the bars it made. `stageClipRectFor` is the other half, `?clip=0` is the kill switch, and the difference is 43,985 px (6.98% of the page) against a null control of 0. **EVERY GLOW AND SHADOW IN THE BUILD WAS DRAWN AT TWICE ITS BLUR, BEHIND A CORRECT CITATION OF THE CSS SPEC**: `drop-shadow`'s third length is documented as twice a standard deviation and Chrome treats it as one — `blur(S)` and `drop-shadow(0 0 S)` render pixel-identical (reach 5), `drop-shadow(0 0 2S)` reaches 10. The oracle agrees: at blurX 8 it draws `63 39 26 2 0` where we drew `63 52 41 31 23 16 11 7 4 2 1`. **The peak was never wrong — 63 against 63. Only the width was.** Seven pinned literals across five test files moved, each corrected at its assertion. **`blur()` IS DELIBERATELY UNCHANGED** and that is the half nearly got wrong: `blur(sigma)` beats `blur(sigma/2)` against the oracle at all six widths, so a correction that fits one branch is not a correction to its sibling. **STILL OPEN AND NOW SPECIFIED**: strength is discarded above the clamp (the oracle scales alpha linearly then widens a saturated plateau; the enchantment pack carries strength 2 and 2.796875, so the weapon glow ships at a half to a third of its intended strength) — the exact canvas sequence is written at the `approximated:` marker in `filters.js` and **do not stack `drop-shadow`s, they composite `source-over` which is `1-(1-a)^k` not `k*a`**. **AND THE STRENGTH HALF OF EVERY GLOW IS DRAWN NOW — ranked first in this same handoff and closed in the same session.** `glowAmplificationFor` describes a sequence and `amplifyGlows` runs it: silhouette the group buffer under a WHITE drop-shadow, draw it `ceil(strength)` times with `lighter` (last at `globalAlpha = strength % 1`), colourise with `source-in`, draw under. **Additive compositing sums premultiplied channels and clamps at 1, so the alpha out is exactly `min(1, blurredAlpha * strength)` — an identity, not a fit**, which is the only reason it is allowed; white is load-bearing because it is the one colour whose premultiplied channels equal its alpha. Verified against the oracle BEFORE the compositor was touched — oracle `63 39 26 2` / `126 78 52 4` / `252 156 104 8` against ours `62 38 19 8` / `124 76 38 16` / `248 152 76 32`, peaks within 1.6% and the doubling exact, with strength 1 as the null control rendering identically on both paths. In the arena against its own `?amplify=0`: **0 differing pixels unenchanted, 3,015 with an enchantment**, bounded to the weapon, mean shift B +28.6. It DECLINES a mixed list by design (44 of them, still counted); **every one of the 12 enchantment art frames carries a PAIR of saturating glows, so a single-glow shortcut would have dropped the second colour.** **THE MUTATION AUDIT CAME BACK OPPOSITE TO ITS OWN PREMISE**: batch 1 is 27 mutations, 24 KILLED, 3 SURVIVED, against the 2026-09-07 engine audit's 37 of 48 surviving. Three facts in my own brief were wrong and agents broke all three — `groupRunsOf` is in `tools/arena/main.js` not `src/render/screen.js`, and `prefixesOf` was DELETED on 2026-09-15 while the living head still cited it as current. |
| [2026-09-15 10:15 — the glow is drawn, and I measured the instrument twice](2026-09-15-1015--the-glow-is-drawn-and-i-measured-the-instrument-twice.md) | `3b712373` | **THE WEAPON ENCHANTMENT RENDERS AND THE COLOURS AGREE WITH THE BYTES** — Flame R +64.6, Frost B +45.4, Poison G +37.9, Wraith neutral, each dominant channel matching its own glow record, against a null control of 0 differing pixels and two byte-identical files. The ladder came from `itemglow`'s AVM1 bytes, the colour from the render, and the two never met until that table. **TWO RETRACTIONS, BOTH MINE, BOTH THE SAME SHAPE**: "it hangs a headless render" was `--virtual-time-budget`, which `tools/shot.sh` uses and which does not advance while a filtered composite is outstanding; "so it is a 3.9x frame cost" was `--disable-gpu`, and with GPU rasterisation it is ~1ms per enchanted fighter, free at two. **First I measured the screenshot MODE and called it a property of the page, then the RASTERISER and called it a property of the feature — both numbers real, both subjects wrong. Measure the instrument before the world when the defect is in something you just built.** New: `tools/shot-live.mjs`/`.sh`, real-time CDP screenshots that kill their own browser and **pin `performance.now`/`Date.now` to the FRAME NUMBER** — a frame count alone is not determinism on a page that animates on elapsed time (9,900 pixels at B +5.1 without the clock, 2,822 at B +45.4 with it; the rest was the weapon moving). **`tools/shot.sh` LEAKS A CHROME PROCESS PER INVOCATION — 73 were alive**, which makes working URLs fail and reads as a page defect. Open and NEITHER MINE TO DECIDE: the twelve figure-pack groups sit on four DECLARED-UNPLAYED spell labels (dispatching them means deciding what the spell is), and the strength half of every glow is carried and not drawn (closing it honestly needs a Ruffle render of `weapon0` frame 5, not repeating `drop-shadow` until it looks right). |
| [2026-09-15 07:18 — the glow is drawn, and it hangs the page](2026-09-15-0718--the-glow-is-drawn-and-it-hangs-the-page.md) | `3b712373` | **THE FIGURE AND ICON EXTRACTORS CARRY THE FILTERS, A NEW TOOL DERIVES THE WEAPON-ENCHANTMENT LADDER FROM THE BYTES, THE RENDERER COMPOSITES IT — AND THE ONE GROUP THAT REACHES A GLADIATOR IS ON SCREEN IN ALL FOUR ENCHANTMENT COLOURS** — Flame R +64.6, Frost B +45.4, Poison G +37.9, Wraith neutral, each dominant channel matching its own glow record, null control 0 pixels. ~~**HANGS A HEADLESS RENDER, CAUSE NOT FOUND**~~ **— WRONG AND CORRECTED IN THE SAME SESSION: what stalls is `--virtual-time-budget`, which `tools/shot.sh` uses, and a CPU profile puts 95.8% of samples in `(program)`, native rasterisation. A FAILED from `shot.sh` is evidence about the instrument.** **AND THE FRAME COST IS NOT 3.9x EITHER — CORRECTED TWICE:** that was `--disable-gpu` (software); with GPU rasterisation it is 6.1ms/frame either way at 2 fighters and 11.8 against 6.1 at 6 — about **1ms per enchanted fighter**, so the ranked-first "cost worth reducing" is RETIRED. **First I measured the screenshot MODE and called it a property of the page, then the RASTERISER and called it a property of the feature; both numbers were real and both subjects were wrong.** `tools/shot-live.sh` shoots it in real time, kills its own browser, and pins `performance.now`/`Date.now` to the FRAME NUMBER — **a frame count alone is not determinism on a page that animates on elapsed time** (9,900 pixels at B +5.1 without the clock, 2,822 at B +45.4 with it; the rest was the weapon moving). **THE FRAME-1 FREEZE HID 24 GLOWS BEHIND A CLEAN BILL OF HEALTH** — `--report` said "DROPPED 0 filters ... 0 skipped drawables" while sprite 703 sat at depth 39 of the same clip; every drop counter was honest, because `flattenFrame` never READ those frames. **Count what the instrument never looked at, not only what it dropped.** **THE TWELVE GROUPS IN THE FIGURE PACK REACH NO GLADIATOR** — all four effect-bearing labels are declared unplayed; 44,880 and 73,440 combinations swept, reached 0 times. **THE STRENGTH HALF OF EVERY GLOW IS CARRIED AND NOT DRAWN**: alpha clamps at 1, so 17 of 18 strengths could be set to anything with the suite green; `shadowStrengthSaturated` is named apart now and reads 23 saturated / 1 scaled. **THE BATTLE MAP HAD `itemglow`'S ARGUMENTS INVERTED SINCE 2026-09-13** and wrote the contradiction that follows as a question "only a capture settles" — seven witnesses say otherwise, the first being the build's own `weaponenchantments = ["","","Flame","Frost","Poison","Wraith"]` at `0x3fe79c`. **An inconsistency between two readings of one build is evidence a reading is wrong before it is evidence about the build.** **TWO WAVES, 16 AGENTS, 0 DEAD, 0 OWNERSHIP VIOLATIONS; 10 VERDICTS: 3 CONFIRMED, 5 PARTIALLY-BROKEN, 2 BROKEN — and SIX of my own premises broke**, including one I briefed that an agent implemented faithfully (the glow covers the whole weapon limb, 19 ops not 8) and whose test could not catch the correction because its fixture lacked the discriminating input. **`tools/shot.sh` LEAKS A CHROME PROCESS PER INVOCATION — 73 were alive**, which makes working URLs fail and reads as a page defect. |
| [2026-09-15 04:00 — the filters land, and the sky has a clock](2026-09-15-0400--the-filters-land-and-the-sky-has-a-clock.md) | `b0bd5c51` | **THE FILTERS ARE APPLIED**: `screen.js` emits 248 filter GROUPS and both canvas shells composite each through ONE offscreen with ONE `ctx.filter`, never per leaf — a group's filter is a filter of the COMPOSITE, and stamping it on `townsquare`'s 1523 paths is a different picture, not an approximation. Nested groups COMPOSE (40% of ops sit under exactly two filtered ancestors, so the rule is reachable and tested). **FOLDING THE SKY'S ENCLOSING ColorMatrix GAVE IT A DAY/NIGHT CYCLE IT HAS NEVER HAD** — frame 1 `#440037`→`#79689f` dawn, 60 `#2d2dfd`→`#5fbefe` midday, 200 `#000030`→`#3f5f75` night, where before the fold every frame was the midday blue while `time_of_day` climbed 1..200. **AND THE LIVING HEAD WAS RIGHT WHILE THE CODE WAS WRONG**: yesterday's commit wrote "not a ColorMatrix" into `props.js`, I paraphrased THE CODE into an agent brief, and the agent measured and broke it — quoting a source file into a brief is not re-measuring; the living head's own "208" was the BLUR count (150 matrices, 148 non-identity) and is corrected. **A MATRIX-ONLY GROUP IS NOT COMPOSITED AT ALL** — 31 groups, 8210 of 13638 ops (60%), 5 with a descendant that DOES composite, which is the innermost-wins picture `screen.js` argues against; counted, not guessed at. **THE FIGURE AND ICON EXTRACTORS READ NO FILTER FIELDS AT ALL AND ARE DROPPING THE ELEMENTAL WEAPON GLOW** — sprite 703 `weapon0` carries 24 glows, two on every frame 2..13, and the enchantments are flame/frost/poison/wraith at frames 2/5/8/11. **NO BUILD-LEVEL DENOMINATOR**: 1,894 filter records, a reader reaches 848 (44.8%), and the two pack numbers are not on the same unit. **THREE WAVES, 34 AGENTS, 0 DEAD, 0 OWNERSHIP VIOLATIONS; 18 VERDICTS: 1 CONFIRMED, 15 PARTIALLY-BROKEN, 2 BROKEN.** **I RETRACTED A FINDING OF MY OWN WITHIN THE HOUR**: I computed the sky's stage position from the op matrices alone, said it was drawn 244px too high and was the green band — withdrawn; `layerPlacementFor` applies (315.7, 216.85) and the sky spans -12.0..207.7, the upper half, occluded by the crowd. **A partial composition is not a position** — the pixels-versus-placement seam, made a fourth time by the person who wrote the warning four hours earlier. |
| [2026-09-15 00:46 — the tint landed, and the trail vanished](2026-09-15-0046--the-tint-landed-and-the-trail-vanished.md) | `b0bd5c51` | **THE COLOUR TRANSFORM IS IN `src/render/props.js` AND THE SHELL'S COPY IS GONE.** `propOpsFor` carries each placement's transform onto fill, stroke, both opacities and every gradient stop, through `filters.js`; the arena's UI bar plate measures `#30160c` where it measured `#ffffff`. The two cases the fold cannot express are counted in a new `propInvoiceFor` **with denominators, because both are DEAD on the real pack**. **APPLYING IT CORRECTLY REVEALED THAT FOURTEEN OF THE TWENTY BOWS DREW NO ARROW TRAIL** — `arrowTrailOpsFor` was indexing `bullet_trail`'s seven-frame TIME fade with the WEAPON's art index, and bows 67..80 clamped onto frame 7 (`alphaMultiplier` 0). Wrong before, and invisible, because the alpha was being dropped anyway. The build settles it: `trail.bullet.gotoAndStop(secondary_weapon - 60)` puts the weapon index on a CHILD inside the puff, and sprite 48's seven frames are one drawing at seven alphas ending in `this.removeMovieClip()` — **the puff's whole life, not a lookup with fourteen dark entries** — and `extract-props.mjs` had quoted those exact bytes and written the wrong conclusion beside them. **THE FILTERS LEAVE THE EXTRACTORS NOW AND NOTHING READS THEM**: 363 effect groups over 3209 placements and 276 typed filters are carried, the picture is unchanged, and a verifier proved the obvious op-count check could not have varied (`prefixesOf` reads only `entry.path`); the evidence that CAN vary is `canvasFilterFor`, **0 applied before and 214 now**. **TWO WAVES, 24 AGENTS, 0 DEAD, 0 OWNERSHIP VIOLATIONS, 12 VERIFIER VERDICTS: 1 CONFIRMED, 10 PARTIALLY-BROKEN, 1 BROKEN — 22 of 23 here.** They broke: a number that COULD NOT VARY ("0 own filters" swept downstream of the discard that drops the only two own-filtered placements); **thirteen real-pack tests passing on a broken path** (measured both ways — unanchored 41 pass / 0 fail, anchored a named failure); **a test that measured a COUNTERFACTUAL** and reported 15 dark bows where the shipped defect was 14. **CORRECTING THE POINTER IS NOT CORRECTING THE POINTEE** — `text.js` struck its own copy of the alignment claim and NAMED `tools/screens/main.js` as stale without changing it, so that file still carried a live bug report for a fixed bug: ninth instance here, and the first created by a previous fix's own note. Seven pointers cited line numbers that no longer held the code — **cite SYMBOLS**. One verifier finding is recorded as WRONG rather than obeyed: two `approximated` shapes coexist but exactly one documented reader takes both. |
| [2026-09-14 17:12 — the screens are drawn, and five are black on purpose](2026-09-14-1712--the-screens-are-drawn-and-five-are-black-on-purpose.md) | `628dbabf` | **ALL 26 ROOT SCREENS DRAW AND THE ARENA'S UI BAR READS ITS OWN WORDS.** `tools/screens/index.html` walks them; `help` renders WHOLE — parchment, shield, mace, helm, the return button and **656 glyph operations of readable English in the build's own Goudy Handtooled** — and the arena bar reads `TOOLTIPS:OFF   SOUND:ON`. **Both verified by PIXELS**: the bar's rows went `#000000 x1270` to `#ffffff x51` and `#e5e1e0 x69` glyph pixels. `src/render/screen-text.js` is the join and takes `screen.js`'s `textNotDrawn: 187` down to 28. **`?probe=1` MAKES A PAGE READ ITS OWN CANVAS BACK** and is the only reason "1698 operations painted" vs "the canvas is black" got settled rather than argued — `townsquare` has 142 distinct colours and 439,266 opaque pixels, of which `#000000` is 435,374, because **`flattenFrame` freezes a nested sprite at ITS frame 1 and on five screens frame 1 is a closed black curtain** (same root cause as the sky's 8 unreachable gradients). `tools/shot.sh` now takes a PAGE PATH. **THE BAR WAS WHITE FOR THE SAME REASON IT WAS WORDLESS**: `propOpsFor` drops each placement's colour transform, the plate is `rgb x0, alpha x0.5`, and the fix is composed in the SHELL today and says so in its own log — ranked first is moving it to `src/render/props.js`. **THE FILTERS ARE BLOCKED IN THE EXTRACTORS, not the renderer** — the data never leaves `extract-props.mjs`/`extract-screens.mjs`, and the page reports `NO filter data in the pack` rather than inventing one; 1525 of `townsquare`'s ops (93%) sit under a FILTERLIST nothing applies. **TWO BUILD WAVES, 20 AGENTS, 20 RETURNED, 0 DEAD, 0 OWNERSHIP VIOLATIONS, ALL TEN VERIFIER VERDICTS PARTIALLY-BROKEN — 16 of 16 here.** The transferable finding: **ASK WHAT YOUR EVIDENCE COULD POSSIBLY VARY OVER** — a fold justified by a byte-identical op digest survived deleting the colour transform from gradient stops entirely, because all 107 transformed stops are under ALPHA-ONLY transforms. Siblings: an `assert.equal(X, X)` on the only number reporting the bar's two dead readouts, and a headline extractor fix deletable green because **no test called `extractFigure` at all**. One track changed NO source file — all eight of its defects were in the test. **I briefed four wrong premises and agents broke all four**, because the briefs numbered them and invited it. **A verifier disclosed that its own `cp -al` shadow trees left repo files sharing inodes — use `cp -r`.** |
| What it is | the accumulated **state** of the project | the **brief** for one session |
| Answers | "what is true, and what was found" | "what should I do now" |
| Grows | yes, and it is ~800 lines | no — one screen, ideally |
| Lifetime | permanent, corrected in place | frozen when the session ends |

**`HANDOFF.md` is itself split (2026-08-31).** Above its `## THE ARCHIVE LINE`
heading is the LIVING HEAD — state, rules, next steps, open items — which is
appended to and corrected in place; below it is frozen evidence. So the
correction path is: the living head, plus the next handoff. That split exists
because a handoff freezes when its session ends, and a correction with nowhere
live to land never reaches the next reader.

A handoff **points at** `HANDOFF.md` for state. It must not restate it. The
moment a handoff starts explaining findings it has become a second state
document, and the two will disagree — which is worse than having one. If they
ever do disagree, `HANDOFF.md` is right and the handoff was frozen earlier.

If a fact belongs to the project, it goes in `HANDOFF.md`. If it belongs to "what
the next person should do about it", it goes in a handoff.

## Writing one

**Never edit a handoff after its session ends.** It is a record of what was
believed at that moment, and later sessions cite it. Corrections go in the next
handoff (name it in `supersedes:`) and in `HANDOFF.md`.

Keep this shape; it is what worked:

1. **Where things stand** — branch, remote, test count. Two or three lines.
2. **Read first, in order** — usually `HANDOFF.md`'s corrections block, then this.
3. **Highest-value work**, ranked, *with the reason each is ranked there*. The
   ranking is the actual deliverable; a flat list makes the next session
   re-derive the priorities.
4. **Hard rules** — the non-negotiables, verbatim. They are short, and assuming
   them instead of repeating them has cost whole tracks.
5. **Traps from this session** — concrete, specific, ideally the author's own
   mistakes. This section has consistently been the most valuable one.

## Index — newest first

| Handoff | Session | One line |
| --- | --- | --- |
| [2026-09-14 14:00 — five modules exist and two are not drawn yet](2026-09-14-1400--five-modules-exist-and-two-are-not-drawn-yet.md) | `d9f27b67` | **THE ARENA RENDERS THE BUILD'S OWN ART AND THE LAST WAVE BUILT THREE MODULES NOTHING CALLS.** Six DISTINCT arenas (measured: 96-100% different over 2,728 sampled points, and the six sand colours in the render match the six read off the bytes), a 200-frame day/night sky, real gradients, and **the gladiator has a FACE** keyed to the animation he is playing. **RANKED FIRST: draw the text and the screens — both modules are finished, tested and exported, and connected to nothing.** 1027 glyphs round-trip to readable English; all 26 root screens are extracted; the arena's UI bar is still blank boxes and 25 screens have never been on a screen. **TWO TOOLS MADE THIS SESSION WORK**: `tools/shot.sh` drives the Windows Chrome headless and `tools/sample-png.mjs` reads the pixels back. Three defects were invisible to a green suite and obvious in a render — and **two confident wrong conclusions came from renders that were TOO SMALL**, including a sword called missing through four screenshots that was there all along. **THE WEAPON WAS NEVER MISSING**: the rig has always drawn `weapon0`'s art at that limb and the attached one lands within 1% of it. **89 of 387 wardrobe pieces were indexed by nothing** until `weapon`/`secondary_weapon` became declared resources — no golden moved, measured over 138 tests. **MY OWN MISTAKES, ALL IN THE RECORD**: I committed five agents' work mid-flight under a pixel-sampler message (`git add -A` during a fan-out, the trap AGENTS.md already names) and four of five agents caught it independently; my briefs carried two wrong byte-facts (the moon's blur is on `cloud_patterns`; a clip-scoped 4,544 presented as a build-wide census) which agents broke; and **the colour transform must FLOOR, not round** — `(channel * multTerm) >> 8` is an arithmetic shift, two of three copies rounded, 69 of 1023 fills were a unit out, and no test caught it. **OPEN AND THE OWNER'S**: the 9 embedded typefaces are commercially licensed (Bitstream, Monotype), extracted only to gitignored `assets/text/`. **MEASURED AND UNEXPLAINED**: a 3.4-stage-pixel neon-green band at the top of the stage whose arithmetic and render disagree by 15px. |
| [2026-09-14 02:55 — the backgrounds were JPEGs and I drew none of them](2026-09-14-0255--the-backgrounds-were-jpegs-and-i-drew-none-of-them.md) | `d9f27b67` | **THE OWNER ASKED "where are the backgrounds that are actually in game? Did you make these?" AND THE ANSWER WAS THAT I HAD INVENTED NOTHING AND WAS STILL NOT SHOWING HIM THE GAME.** SS2's arena walls are RASTER; `shapeToPaths` rendered a bitmap fill as `fill: "none"`, the note died at the next seam, and the extractor reported ZERO failures. **An approximation that is not counted is indistinguishable from a correct read** — and once it had a name the SAME omission turned up four more times. `test/extraction-honesty.test.js` now recomputes every manifest's tally from its own data. **14 bitmaps (0 failures, 6 with alpha), 97 gradients, SIX arenas, a 200-frame sky CLOCK, weather.** **AND I CAN SEE IT NOW**: Chrome is on the Windows side, so `tools/shot.sh` screenshots the arena headlessly — three of this session's defects were invisible to the suite and obvious in a PNG. **FIVE READING FAILURES, ALL MINE**: `combatCamera` is a `return;` and `combatscale` is the live camera (my own disassembly printed `Push undefined; Return` as its FIRST TWO LINES and I wrote a reason for them not to count; `--references` said five hits and I read two); my alpha masks were GREYSCALE and canvas reads the alpha channel, not luminance; the 3v3 camera framed the formation instead of the fight because I generalised `midwaypoint` to the spread when the build's is the separation of the two who are FIGHTING; `time_of_day` is a CLOCK to 200, not `1+random(23)`, which nearly got the night sky's masks written off as unreachable; and `ATTACHMENTS.length === 16` certified a table four rows short of the build's twenty attach calls. **THREE AGENT RUNS, 0 DEAD**: one capped wave (VERIFIED) plus a 6-agent asset census and a 4-agent build wave on disjoint files. Delivered **9 fonts / 1027 glyphs / 4061 glyph entries round-tripping to readable English**, **all 26 screens** (136 characters, 4915 paths, 0 failures, everything unresolved counted by kind), **faces and icons** with the 228 expression calls that drive them from the fighter's own labels, and opt-in buttons and filters — **whose inertness I PROVED by re-extracting at HEAD and comparing content hashes**, not by accepting the claim. **THE TYPEFACES ARE COMMERCIALLY LICENSED** (Bitstream, Monotype, named in the build's own bytes) so the tools parse and report and write no font file; that boundary is the owner's and is OPEN. **NAMED AND NOT CLAIMED**: the weapon is declared, reaches the wire on the roster's exact path, and produces 8 ops spanning 66% of the figure in node — and does not appear on screen. The break is between the wire and the draw call in `tools/arena/main.js`. |
| [2026-09-14 02:00 — I read the corpse, and the arena is drawn](2026-09-14-0200--i-read-the-corpse-and-the-arena-is-drawn.md) | `d9f27b67` | **THE ARENA SCREEN DRAWS — and getting there cost a full retraction of this session's own central conclusion.** I concluded the shipped arena has a FIXED camera: that `combatCamera` was live and `combatscale` was dead code. **Backwards in every part.** `combatCamera`'s body is `96 01 00 03 3e` — `Push undefined; Return` — at file `0x6e46c2`; its other 470 bytes are unreachable, and it is the ONLY function body in the build that begins that way. `combatscale` is called from `gladiators.onEnterFrame` (+0x0e98) guarded on `_global.phasecomplete != false`, in the scope where it is defined. **THE ARENA PANS AND ZOOMS; the committed record was right and I was about to delete it.** **TWO CHEAP FAILURES PRODUCED IT**: my own disassembly printed `Push undefined / Return` as its first two lines and I wrote a reason for them not to count (the docstring I then wrote cites the body starting one instruction AFTER the Return); and `--references` told me there were five hits and I read two. **Eighth instance of the signature failure and the first where the refuted claim was mine and hours old.** Caught by ONE capped wave — 5 questions + 6 write-nothing verifiers, 0 dead, VERIFIED — where two investigators aimed at DIFFERENT questions broke it by different routes in twenty minutes. **THE SIZE TABLE WAS OUT BY 20x IN THREE OF FIVE ROWS** (twips translations composed with pixel bounds; the two right rows are exactly the two whose contents sit at the origin, so the bug could not reach them). **CHAR 1729 IS `sky`, NOT THE CROWD** — the real crowd is 2112 — and that misnaming had reached the extractor, where the sky shipped under the key `crowd`: the renderer looks layers up BY KEY, so it would have painted a sky where the stands belong and no stands at all, silently. Also `maxscale`/`zoomscale` are `_global` not `arena`; `_global.zoomscale = 5` so **a bout OPENS at a twentieth of full size and rushes in**; `sky` is scaled 1.04 so "every transform is 1.00" is false; and the crowd's HORIZONTAL parallax is inert because `camPoint` is written nowhere in 7.5MB. **BUILT**: seven layers in the build's nesting order with the fighters painting BETWEEN the crowd and the rain, the `combatscale` camera reproduced (pan dead zone 300..340 easing /16, seven declared zoom bands resolving to FIVE because the fourth eats three of them, pan lagging zoom by one frame — all pinned so "corrections" fail), **SIX ARENAS AND 23 HOURS** where the repo had one of each (`?arena=`, `?sky=`, `?rain=`), and **the depth factor moved 1.7 -> 1 because the build's sand is not a rectangle**: at 1.7 the back rank stands in the crowd. **NOBODY HAS LOOKED AT IT.** |
| [2026-09-14 01:30 — the arena is 1:1, and three readings were mine](2026-09-14-0130--the-arena-is-1to1-and-three-readings-were-mine.md) | `b4ca2b15` | **THE ARENA IS 1:1 AND NOTHING DRAWS IT YET.** Root frame 221 places six objects and then runs 488 instructions that fill one of them; every transform is 1.00, so **one arena unit is one stage pixel**. Stage 640x420, arena origin (320, 167), **ground line stage y 367**, fighters at `_x ±250` landing at stage x 70 and 570, rocks at ±2160 far off stage because the arena PANS. **char 643 is the backdrop and is the stage size to the pixel**; 1729 is the animated crowd. Both extracted, neither drawn — that is ranked item 1 and it is now a rendering job rather than a derivation. **THREE WRONG READINGS, ALL MINE**: "the display list is EMPTY" (`resolveTimeline` returns one entry per frame OF THE TIMELINE, so `frames[0]` was frame 1, not 221 — committed to the map and built on); "the build never assigns `bullet._rotation`" (it does, at three sites — a bombard TUMBLES ~4 degrees a frame clamped at 170, a snipe holds ±90, and the ±90 exists because the ART IS VERTICAL); and "snipe is never AI-chosen and needs an owner decision" (the policy was right — snipe's chance is clamped at 99 and overtakes at foe defence ~20, and the demo roster has defence 5). **One sample is not a measurement: one frame index, one roster, one read.** Also blood counts 3/6/9/15 are DEPTH OFFSETS, not counts — every hit sprays five, and `hurt8`'s blood anomaly was that misreading. **BUILT AND PLAYING**: blood and sparks from `bounceitem` — a real bouncing particle system, gravity 2 (the arrow's own 2), 25-frame life, and **ARMOUR STRIKES SPARKS WHERE FLESH BLEEDS**, which this engine already had the `armourclass` for; and the arena's two edges, `rockMC` at ±2160 against a clamp of ±2100 **derived from `nextphase` weeks earlier** — two independent readings agreeing about where the arena stops. **A TRAP WORTH KEEPING: the rocks need NO coordinate conversion and the blood DOES**, because `attachMovie` is called on `arena.gladiators` for one and the fighter CLIP for the other — one register apart. **THE ENCHANTMENT SELECTOR IS FOUND** after four sessions: `itemglow(weapon, type, potency)`. ~~`frame = (potency - 2) * 3 + type + 1` — and the standing note read its four labels as four TYPES when they are four POTENCIES … **only a capture of a known enchantment settles which is right**.~~ **INVERTED, AND STRUCK 2026-09-15: the frame is `3 * (type - 2) + potency + 1`, the four labels ARE the four types, and no capture is needed — the bytes settle it seven ways over. There was never a tension.** Left visible rather than edited away because this row is what a reader meets first; see the 07:18 handoff. |
| [2026-09-13 21:30 — ranged is built and the guard had a hole](2026-09-13-2130--ranged-is-built-and-the-guard-had-a-hole.md) | `b4ca2b15` | **RANGED IS BUILT AND THE ARROW FLIES.** See the row below for the ranged half; this line records what the SECOND half of the session found. **The build models the projectile and none of it was written down** — 125 `bullet` references in one block, `maxscale` nowhere else in the corpus. **The build has TWO coordinates and this engine has THREE**: vanilla's `bullet._y` carries the arc because both its gladiators stand at `_y = 200`, while here arena `y` is DEPTH and height is the renderer's `lift` — and that third axis is where the owner's lane-scaling question lands. **A BOMBARD GOES OVER A BODY AND A SNIPE DOES NOT, derived rather than chosen**: measured off the reproduced ballistic, a snipe flies flat at 0.674 figure heights and a bombard is never below 1.055 anywhere a body could stand, against a gladiator exactly 1.000 tall — so the lob is offered against every foe in every lane and the flat shot needs a clean one. **THE BUILD HAS FIVE ARROWS FOR TWENTY BOWS** (50 frames, frames 6-50 all repeating the fifth) **and bows 61-64 are SLINGS that throw stones** — the owner saw a rock and the rock was correct. **THREE THINGS I GOT WRONG AND THE OWNER OR A TEST CAUGHT**: I said the build never rotates the bullet (it does, at three sites — a bombard TUMBLES ~4 degrees a frame clamped at 170, a snipe holds ±90, and the art is VERTICAL which is why ±90 exists); I said snipe is never AI-chosen and needed an owner decision (the policy was already right — snipe's chance is clamped at 99, it overtakes at foe defence ~20, and the demo roster has defence 5); and I twice asked for an arena BACKDROP when **root frame 221's display list is EMPTY and the arena is 488 instructions of `attachMovie`**. **AN AVM1 INTERPRETER IS TRACTABLE — 58 opcodes build-wide — AND IS NOT NEEDED**: every effect call and every arena attach pushes literals, so `extract-clip-effects.mjs` reads them out instead. **BLOOD VOLUME TRACKS THE ATTACK BAND** — 3/6/9 drops by hurt label, 15 on every death. Also: a third extractor for PROPS (a prop is neither a labelled animation nor a static item), the phase now WAITS for the arrow as `bullet_in_air` does, and the projectile's drawing decisions left `tools/arena/main.js`. |
| [2026-09-13 21:30 — ranged is built and the guard had a hole](2026-09-13-2130--ranged-is-built-and-the-guard-had-a-hole.md) | `b4ca2b15` | **RANGED IS BUILT — the bow, `bombard`, `snipe`, `bash_attack` and the `swap_weapons` turn that arms one** — and `RANGED-BRIEF.md` is CLOSED. **Almost all of it was already in the repository and nobody had asked**: the swing arithmetic for directions 21 and 22 has been in `ss2-attack-candidate.js` the whole time, `bombard`/`snipe` were extracted and bound to sound in September, and `ss2BattleValues` already carried the bow override — what was missing was the VOCABULARY, which is exactly what the construction refusal said it was waiting for. **The owner decided all four open questions and took the BUILD'S OWN answer to three**: the swap costs a turn and one stamina, an archer closed on inside `100 + physical_size` loses the bow entirely and bashes (`closerange_archer` wires no shot at all), and ammunition is finite and tiered 5/10/15/20/25/30 by herolevel with a FORCED swap at zero. The fourth is the only authored rule in the feature: a body between you and your target blocks the shot (`ss2ShotBlocked`, shaped after `ss2BodyBlocks`, structurally inert with the second axis off) — **and he corrected it the same evening by looking at the arena: ONLY AN ENEMY SCREENS.** The archer had ONE legal target, blocked by its own rank-0 ally sitting 76.1 units off the lane against a `physical_size` of 86 — structural, not a seed, because allies stagger diagonally. **Deliberately the opposite of `ss2WalkDestination`**, which blocks on every body because a walk is a body moving through space and cannot pass through anyone; a shot passes OVER a formation cooperating with the shooter. `ss2ShotBlocked` still knows no sides — it is pure geometry, the policy is the caller's. Bombards 237 -> 240 and swaps 92 -> 96, both arithmetic maxima. **And a test would have gone quiet over it**: the axis-off pin staged its blocker as an ALLY, so it would have passed whatever the axis was doing. **An archer has a MINIMUM range and no maximum**, and the controller frame is chosen ONCE per turn by the nearest foe — per-foe would let it bash the fighter on top of it and shoot across the arena in the same turn, making closing on it worth nothing. **THE LESSON, told THREE TIMES IN ONE SESSION: a guard keyed on a CONSEQUENCE has an exception nobody counted.** The refusal holding ranged shut fired on `weapon_range > arena width`, because a type-4 row's range multiplier is 100 — **ids 65 and 75 carry 4** (item tables `:472`/`:482`), reach ~262, straight past it; reproduced, all three melee verbs at 262 units with a bow drawn. **I then made the same mistake one revision later**, refusing `equipped_weapon == 2` outright because root frame 221 starts everyone in melee — true, and still the wrong key, because a state the build reaches on turn two is one a capture can observe. **And then `/adversarial-review` found the THIRD**: I left the reach backstop keyed on that same criterion, so `ss2Combatant(..., { battleStarted: true })` with `using_bow` true poured the bow's numbers into the melee slots and **they never came back** — a restored archer that swapped to melee fought at 17-73 damage and reach 262 where its sword says 21-27 and 130. Fixed at the root (derive a second time with the bow put away), not with a fourth guard. **Writing down why a consequence-keyed guard is wrong does not stop you leaving one in place.** **THE SOUND HAD BEEN PICKING ITS OWN CLIP SINCE SOUND LANDED — the SEVENTH instance of the signature failure.** `animationFor` has always preferred the engine's own label over the family's first, with a comment saying why; `chooseSound` spread across the whole family by a counter, so the figure played `attack3` while the speaker played whichever of `1092`-`1095` the counter reached. Two halves of one join disagreeing, with the correct rule written out in full on the other side of it. Ranged made it undeniable (`bombard` and `snipe` share a family and have different sounds) and it was caught before shipping rather than after the owner heard it. **Re-read off the build rather than from prose**: ammo tiers `+0x3634`-`+0x378d`, ranged cost `round(strength*3)` `+0x6bb5`, the unguarded decrement `+0x6bf5`, `gotoAndPlay("bombard")`/`("snipe")`, **`bash_attack` plays `Attack2`** (`+0x64ce` — and `attack23` IS NOT A CLIP, the presentation was about to emit one), and **`swap_weapons` plays `Block`** (`+0x4d65`), which is silent because the build binds it no sound. **`whichweapon` is NEVER ASSIGNED ANYWHERE IN THE BUILD** — `battlevalues` reads `attack_type` and `attack_speed` off it and those are the only four references in the SWF, so the build's own `attack_speed` is `undefined` in both weapon modes and this engine's derivation of it is authored in both. **Measured 24 seeds a size: 24/24 settle at every size, 237 bombards bounded by AMMUNITION not team size, pile-up tell absent.** **`snipe` chosen ZERO times** — the build's arithmetic, not a defect: bombard's expected damage beats it against this roster, and a 1,600-combination sweep says snipe wins 10.7% of stat space. Recorded, not tuned. **Five pinned hashes moved, all named; NO GOLDEN MOVED, for the sixth time.** A defect this session introduced and a TEST (not the sweep) found: a position-blind archer was left mute. |
| [2026-09-13 16:00 — everything but ranged is closed](2026-09-13-1600--everything-but-ranged-is-closed.md) | `cc273a6b` | **The presentation work is finished and RANGED is the only thing left** — the owner has asked for a full sweep of it and answered the question that gated it: **range WILL interact with the second axis**, which is cheap because `getfightdistance` is already Euclidean over both axes, so a foe two ranks back is already further away; what it does not settle is whether a body BETWEEN blocks the shot, which vanilla cannot answer and so is authored. **THE LAST OPEN DECISION CLOSED after four sessions: `BATTLE_STATE_VERSION` is DERIVED** from a hash of the sorted projection fields (1 -> 573176825), an identity not an ordering, with a test asserting the declared list matches what the projection returns — so it cannot drift again. **Eleven pinned hashes moved, all named; NO GOLDEN MOVED.** Two stings on the way: `src/engine.js` projected `battle.version`, leaking the derived number into the FROZEN compatibility façade and moving all six legacy hashes (caught by the test literally called "the pinned hashes do not move"; it carries `LEGACY_WIRE_VERSION = 1` now), and **90 tests failed on ONE schema line** because `fnv1a` returns hex where a sealed campaign record is contracted to hold a positive integer. Also: **four more decisions out of `tools/arena/main.js`** (`perSideFrom`, `rankOfDepth`, `viewportFor`, `rosterOrderOf` — every one has had a live defect, every one found by screenshotting) **and the extraction changed behaviour by accident**: `Number(null)` is 0 not NaN, so an absent `?teams` clamped to 1 and the default arena silently became a 1v1 — checked against the original expression across ten inputs, agreeing on all but `?teams=2.7`, which used to hand a FRACTIONAL team size to the roster builder. And two reporting findings: **`ids 1..24` is a min and a max, not a range** — five absent `features` ids became a ranked open question that way when the build only ever exported nineteen (the real signal it hid: `shield` is missing exactly id 13) — and **the weapon enchantment selector has NOT been found**, recorded as a gap in the search rather than guessed: `weapon0` is char 703 with flame/frost/poison/wraith at 2/5/8/11, the resources exist, and `updatecharacter` contains no `gotoAndStop` anywhere in its 11,225 bytes. |
| [2026-09-13 11:45 — the gladiator is dressed, and the shell has a seam](2026-09-13-1145--the-gladiator-is-dressed-and-the-shell-has-a-seam.md) | `cc273a6b` | **The arena draws the build's own gladiator WEARING HIS OWN ARMOUR** — 387 wardrobe pieces attached by the build's own table (disassembled from `updatecharacter`: sixteen slots, fifteen at the limb origin, the shield alone offset, and helmet/hair sharing depth 5 so a helmet REPLACES hair), all 44 morph shapes rendering as 290 baked frames which turn out to be **the blood in the death animations**, and **a 2v1 that PINCERS**. **THE LESSON: A GREEN SUITE IS NOT COVERAGE.** A 20-agent mutation audit broke one line at a time — 15 of 17 applied mutations went red and **2 SURVIVED, both in the dressing path, and not from under-assertion but because the code had NEVER BEEN EXECUTED**: `paintExtractedFigure` ran nine times suite-wide, always with no wardrobe. **And the test that looked like coverage asserted `helmet.depth === hair.depth` on the DATA TABLE**, the fact the rule is derived from rather than the rule, so it passed with the rule deleted. **FLANKING**: the owner watched a 2v1 pile up behind itself; measured 208 outnumbered turns, 100% same-side, 0 crossings in 3,424 turns. Never geometry — `ss2BodyBlocks` is `|dy| < physical_size`, so a foe one rank away never blocked. Pincered 0% -> 17.8%, simultaneous fights 273 -> 413 turns, 24/24 settle, pile-up tell absent, no golden moved. **Codex then found three dressing defects**: the attach offset is ActionScript PIXELS and I added it to TWIPS (the shield twenty times too close — settled by the build's own `head.eyes._y = -14` on a 55px head), a malformed wardrobe threw into the render loop where the shell's retry made it abort EVERY frame, and attachments ignored the rig's paint order entirely. Also: `src/render/arena-shell.js` is a test seam for the browser shell **and building it broke the shell while the suite stayed green at 1085** — which is the seam's own argument. Plus two process failures worth more than the code: **`grep` is not a gate** (committed on `fail 2`, because grep succeeds when it matches) and **`git add -A` staged seventeen agent worktrees** and pushed them. **NEXT IS RANGED and it has its own brief, `RANGED-BRIEF.md` — everything presentational is already built, so the work is the resolver's alone.** |
| [2026-09-13 02:35 — the wardrobe was in the linkage table all along](2026-09-13-0235--the-wardrobe-was-in-the-linkage-table-all-along.md) | `cc273a6b` | **Stage 3 shipped — the arena draws the build's own gladiator — and a 12-agent wave (6 questions + 6 verifiers, 12 started, 12 returned, 0 dead, 4 HOLDS + 2 PARTIALLY-BROKEN, 31 of 37 claims UNVERIFIED) found that the fighter clip does NOT dress itself.** Clip 1241's 236 DoAction tags are a blood emitter, facial expressions and stop/struck/fired; `shield` occurs ZERO times in its 14,907 action bytes. **The mechanism is at the ROOT** — `DoAction` at `0x40bf76`, body `0x40bf7c`, one 149-entry constant pool carrying every slot, every limb, and `<limb>.bareskin` for each. **`attachMovie` onto a named limb, `Color` onto its `bareskin` child.** **THE WARDROBE IS THE LINKAGE TABLE: 387 pieces, 12 slots, 401 shapes, 0 failures** — weapon 89 (0..220), helmet 40 (1..120), hair 40, six body slots 25 each (2..26), shield 25, facehair 24, features 19. **89 weapon symbols against ~90 weapon rows: the item row's ID IS the art column**, which retires `MAP_SILENCE`-shaped doubt about appearance. **AN EMPTY SLOT MAY MEAN YOU ARE LOOKING AT THE WRONG SLOT** — sprite 704 at depth 35 named `shield` holds nothing and I ranked that as work; the shield attaches to the RIGHT FOREARM at depth 3. **`helmet` and `hair` share depth 5, so a helmet replaces the hair.** **WINDOWS CANNOT REACH WSL'S LOOPBACK HERE** — Windows times out where curl-in-WSL gets 200, so three sessions called this server healthy from the one place it was always going to work and the owner's browser could never open the arena; `--host 0.0.0.0` added, default unchanged. **The owner found four defects by playing it**: sounds cut off (one Audio element per file, and the build SHARES files — `706.mp3` serves five labels), sounds not starting (autoplay blocked, spectate never interacts, the rejection swallowed), a blank preview (it fetched its data), and **a 2v1 that will not flank** — not a bug, but two faithful constraints plus a missing objective, 0 crossings in 3,424 turns. Also: Codex broke three things in stage 3 (a malformed pack FROZE the arena — the same scar `cursor.js` carries; partial packs drew a limb short; every outline at 67.4% width **with my own test asserting the bug**), `?rank=0` silently gave the 97 engine and a bare census gave the 1-D one, and all 44 morph shapes now parse. **Ranked first: dress the gladiator — the one gap is the per-piece attach offsets.** |
| [2026-09-13 00:40 — the fighter is a rig, and Codex broke four things in it](2026-09-13-0040--the-fighter-is-a-rig-and-codex-broke-four-things.md) | `cc273a6b` | **Stage 2 of asset extraction is DONE: the fighter clip resolves.** 101 animations, 2,222 poses, **61 shapes, 0 parse failures**, out of the player's own install into gitignored `assets/figure/`. **The gladiator is a thirteen-depth RIG and his whole body is ELEVEN shapes** — 22,783 `PlaceObject2` tags of which **22,683 are MOVE-ONLY**, so a hundred placements set up named limbs once and every frame after is matrix tweening. **The ranked item said to scope by FRAME and that is the wrong axis**: name the rig's depths and all 2,222 frames come free. **The previous brief's "gift" omits 34 MORPH SHAPES** — the closure from 1241 is 148 characters, not 114 — all of them on effect depths (blood, charge guard, potions, heart) and none on the rig, so a flattener that assumed shape would have lost them silently. `weapon` (703) is 13 frames labelled flame/frost/poison/wraith behind a frame-1 `stop()`; **`shield` (704) is EMPTY and is attached at runtime by a mechanism nobody has found** — the first unknown in stage 3. **THE LESSON, sixth instance: two things that share an error confirm each other, and this time both were mine.** I wrote `PlaceObject2`'s replace rule backwards and then wrote a test asserting the backwards version; the suite was green and the code was wrong. **`/adversarial-review` returned five findings and four were real** — a replace KEEPS the instance (Ruffle: `replace_with` then `apply_place_object`; **144 of the build's 201 replaces carry no matrix**, and correcting it moves 55 placements across 7 sprites, **clip 1241 not among them**, measured by diffing every sprite both ways rather than inferred — I reasoned about that twice and was wrong both times); **FILTERLIST precedes BlendMode** so I read the blend mode out of the filter count (**1,507 of 1,522 PO3 tags carry filters, 3 carry both**); **`clipDepth` 0 is not a mask**; and masked content was exported unclipped as "correctly resolved geometry". Plus write guards I had not thought about at all: `--out` could escape `assets/`, and `writeFileSync` follows a symlink — which could have truncated the ORACLE through a read-only tool. The build's sha256 is now re-read AFTER writing. **The suite's own meta-test then caught me writing three assertion-free tests.** Also: **the previous handoff was mis-stamped 92 minutes after its own commit, which broke the `ls` sort so this brief sorted BEFORE the one it supersedes** — renamed and restamped. **THEN THE OWNER OPENED THE PREVIEW AND GOT A BLANK PAGE**, because it `fetch`ed its data and a `file://` page may not — a check with a prerequisite is not a check, so it is now self-contained. **And once it rendered, the gladiator had black-and-white WEDGES across his chest**: `shapeToPaths` emitted each edge run as its own OPEN subpath, and a renderer closes a filled open subpath with a straight chord. **Torso 127 open runs -> 10 closed regions.** Every number called it correct (824 of 824 shapes "survived", `d` strings well-formed) and the two tests pinning path data asserted the OPEN strings — **the sixth-instance lesson again, one commit later.** Fixed by stitching: reverse a `fillStyle0` edge, chain on EXACT integer twips, one element per fill under `fill-rule: evenodd`; **an edge with the same fill on both sides is INTERIOR**, which style-state persistence makes common. **The preview now SOUNDS too** — the two extractions join on the build's own frame labels, 80 of 101 animations carry sound and the other 21 say so — **and applies COLOUR TRANSFORMS, which are the condition tints: 4,544 placements** (`death_poisoned` 735, `cast1` 732, `frozen` 560). **The base body art is a GREY CANVAS built to be tinted**, which answers stage 3's open question: team colours are the art's own mechanism. **Ranked first: stage 3, the renderer preferring extracted art — what is still open is the eight armour SLOTS the base clip does not dress.** |
| [2026-09-12 23:58 — the arena has two axes, and the build has its own voice](2026-09-12-2358--the-arena-has-two-axes-and-the-build-has-its-own-voice.md) | `cea2ec2e` | **The second axis is SHIPPED at the owner's chosen stride of 97, three lanes** — not a flag. Facing is now DERIVED from position as `changeCombatants` derives it (49 references to `gladiator_dir`, six writes, none in a button handler: **turning was never an action in the build**), a blow from behind costs extra (AUTHORED — `attack_direction` is a clip-name suffix, not a bearing), and **the build's own sounds play in the arena**, extracted from the player's install and never committed. **THE LESSON, fifth instance: a SUMMARY in a transcription is not a catalogue.** The owner heard a jump sound on a walk; the cause was bucketing by the map's PROSE ranges, where "movement and charge (33-104)" is four clips including a leaping attack and "Block (118/179)" swallowed both jumps — while `Block` carries no sound at all. 101 frame labels against 17 prose ranges. **Nothing in the suite could catch it because BOTH SIDES bucketed the same coarse way and agreed.** Also: the overlap clamp iterated FOES only (83% of turns had allies inside each other, closest gap 0); refusing a rank change into an occupied lane blocked 56% of them, and arriving BESIDE fixed it; a lane change was a 186px vertical teleport, **2.7x the travel of the biggest jump the build can make**; and the census LIED for one commit, reporting the default engine when asked for the 1-D one. **Two owner corrections that overturned my conclusions**: flanking needs no fourth lane (a fast gladiator overruns its lane and steps sideways already behind — 586 crossings where I had measured 0), and core stat mechanics need no redesign (at EQUAL point budgets fast+regen is 13-11, and my 22-2 had unequal budgets). **Ranked first: stage 2 of asset extraction, the display list.** |
| [2026-09-12 20:30 — the build had a second axis all along](2026-09-12-2030--the-build-had-a-second-axis-all-along.md) | `cea2ec2e` | **`getfightdistance` computes `ydist` from `_y` and returns `round(sqrt(xdist^2 + ydist^2))` — the build's own distance is EUCLIDEAN, and this repository recorded it as "the rounded x-separation" in FOUR places.** Both offsets the docstring always cited are real; `ydist` sits BETWEEN them at `+0x0338` / `+0x03de`. **Fifth instance of the signature failure, and the first found by reading the ORACLE rather than a transcription.** So the second axis is half DERIVED, not wholly invented: the METRIC is the build's, while where a gladiator STANDS stays authored under `MAP_SILENCE.multi-slot-arena-geometry`. **Ranked item 1 of the 16:00 brief is DONE and playable at `?rank=N`**, and so is item 2. Measured, 24 seeds a size: stride 0 / 97 / 150 gives most-fights-at-once **1 / 2 / 3**, blows-through-a-body **44.7% / 20.3% / 3.7%**, and **24/24 settled at every stride** — two or three simultaneous fights where one was geometrically impossible. `rankStride: 0` is the default and is off STRUCTURALLY (`y: null` reads as 0), so there is no second code path. **The trap, and it cost a commit: "move toward the nearest foe's rank" is a PILE-UP MACHINE** that rebuilds the single interface in two dimensions — the census reported it unasked, because strides 97 and 150 returned IDENTICAL numbers. The rule that works is *fight who is in front of you*. **Three defects the axis created, all mine, all found by measuring and none by reading**: the overlap clamp parked fighters permanently ONE UNIT out of reach (the 1-D "parks a walker on its own gate threshold" failure, arriving on the new axis); the census's through-a-body test could not see the axis and reported 91.4% where the truth was 0.0%; and the back rank was drawn STANDING IN THE CROWD, which fixing drove the horizon off the top of the canvas. Also: the census now COMPUTES the two numbers the 16:00 brief quoted at it and it has its first test; `positionBuild` in `seeded-play-pins.test.js` was declared and NEVER CALLED while the file claimed it pinned the approach. **`figure.js` said "there is no second axis to be faithful to" — a confident negative stops the check that would refute it.** **Ranked first and it is the OWNER'S: play it at `?rank=97` vs `150` vs `0`** |
| [2026-09-12 16:00 — one interface, so the second axis is a prerequisite](2026-09-12-1600--one-interface-and-the-second-axis-is-a-prerequisite.md) | `9e416f58` | **The owner looked at a 3v3 and called it "super limited"** — everyone jumbles into one melee, no breakoff fight, no choice between duelling and brawling, nowhere to stand off and shoot. **Measured, that is not a tuning problem: a walk may never cross a FOE (`+0x3de6`), so the two teams meet at exactly ONE interface and every blow in the bout is struck there.** 0 turns in 1,682 where any red stood right of any blue; never more than 1 simultaneous fight. **A second engagement is geometrically impossible, so no AI or targeting change can produce one** — which retires the plan of the previous hour. **Two guesses of mine died first, both made before measuring**: that the left-to-right order is frozen (false — a walk may cross an ALLY, order reshuffles 24/24) and that better targeting would spread them (a no-op — target picks a DIRECTION, the clamp picks the STOP). `tools/engagement-census.mjs` is committed as the instrument, because every number in the preceding design discussion was a scratch script that died with its session: **a 3v3 uses 99 units of a 4,200-unit arena (2.4%), can-hit-every-foe on 41.0% of turns, 44.7% of blows through a living body.** Also landed: **a live AI defect** — `chooseAiAction` picked the globally weakest foe then RESTED if it was out of reach, 78 of 1,688 3v3 actions, 0 at 1v1/2v2 which is why it survived; **the back rank painted OVER the front one** (`withDrawOrder` sorted by clip depth, which runs backwards for painting) and **nothing read the `xscale`/`yscale` the stream always carried**, so every gladiator drew one size whatever its `physical_size`; **the roster panel had gone back-to-front with the teams interleaved** because it borrowed `drawOrder`, caught by screenshotting; and the depth stagger was flattened so the drawing stops claiming a separation the model does not have (worst drawn/model 1.658 -> 1.069). **Stills of the arena CAN be captured** (headless Windows Chrome) even though animation still cannot — three of this session's defects were found by looking, and one of them no test could have caught. **Ranked first: build the second axis, discrete nodes, playable behind a flag — and the question that outranks the geometry is game feel and is the owner's** |
| [2026-09-12 11:30 — Codex found in one pass what twelve agents missed](2026-09-12-1130--codex-found-what-twelve-agents-missed.md) | `9e416f58` | **`/codex:adversarial-review` was NEVER unrunnable by a session, and the instruction saying so had been copied forward through five handoffs — parking the top-ranked item in the owner's lane for four sessions.** `disable-model-invocation: true` blocks Claude AUTO-INVOKING a slash command; it never blocked running the command's body, which is one `node scripts/codex-companion.mjs` call. **Run, it returned two HIGH defects that a 12-agent wave had missed an hour earlier on the same diff**, both reproduced by hand before being believed. **(1) A walk could carry a gladiator PAST a foe** — the one thing `ss2WalkDestination` is load-bearing for, and this session introduced it: the build's clamp sets the destination to `defender._x - physical_size` unconditionally so it can land BEHIND the walker, which crosses nobody with vanilla's single defender and crosses somebody with two. An actor at 0 with foes at -10 and +20 was offered `walk-right`, the FAR foe clamped to `20 - 86 = -66`, and it travelled LEFT through the foe at -10; the forward pass had skipped that foe because it filters on the REQUESTED direction while the realised travel reversed. **(2) A bow was offered melee at 500 units, also caused by this session** — `weapon_range` 4,486 against a 4,200-wide arena, so the gate could never shut. Refused at the RULE SET's construction gate on a criterion it owns (a reach wider than its own arena) rather than by sniffing `using_bow`, which the resolver never sees. **(3) A stated weapon with no reach was silently disarmed** under `derive: false`, and the check caught its own live instance on the first run — `tools/arena/roster.js`, hidden for a day because the shop gate at its speeds sells only mult-1 weapons so the bare-hands fallback happened to be right: **luck, not a contract**, so the refusal fires even when the numbers coincide. **(4) The projection version, left to the owner**: `x` and `weapon_range` changed the format twice while `BATTLE_STATE_VERSION` stayed 1 — the 2026-09-07 "pin the shape" decision arriving as a cost. Also fixed: **the spectated arena never threw a punch** (`options[turnNumber % options.length]` cycles walk-left/walk-right/rest for net-zero displacement forever; 20,712 actions and 0 attacks became 1,066 and 781) **and the test had re-implemented the same line**, so shell and test agreed while both were wrong — the SECOND instance of that hazard in a file whose header already records the first. The model was already `gpt-6-astra` via `~/.codex/config.toml`, but ambiently; it is now pinned in a user-level command, because the plugin is vendored under a version directory and edits there vanish on update. **The lesson: a wave audits the RECORD (all six of its findings were about whether the repo's claims were true), a review audits the CODE — they answer different questions, and ADR 0001's ordering is right for a reason it does not give.** **Ranked first and it is the owner's: LOOK AT THE ARENA** — three things changed under it today that no agent here can see |
| [2026-09-12 09:30 — weapon_range is projected, and twelve verifiers broke six things](2026-09-12-0930--weapon-range-is-projected-and-the-wave-broke-six.md) | `9e416f58` | **Ranked item 1 closed: `weapon_range` is a projected resource, the build's own overlap clamp is restored, the `foe.x` narrowing is deleted.** The reach it replaced was `physical_size` — **the reach of no gladiator the build can make** — on the strength of a docstring citing `ss2-item-tables.md:58`, which is HALF of a wrapped line whose line 59 continues `+ _root["weapon" + c.weapon][5] * 44`. There is no unarmed branch in `battlevalues`; the smallest `[5]` in ninety rows is 1. **Fourth instance here of a quoted offset's neighbouring line holding the answer, and the first that was a DOCSTRING rather than a `MAP_SILENCE` entry.** The clamp is the DEFENDER's `physical_size` and the gate the ATTACKER's `weapon_range`; one function for both is what deadlocked the faithful clamp. Byte-identical walk ratios to the narrowing it deletes (19.0/24.6/26.8%), 8/8 settling in all 24 sweep configurations, and the weapon finally MATTERS. Two fixtures had stated `weapon_range: 1` — the `[5]` multiplier pasted into the field it multiplies into. **Then 12 agents (6 questions / 6 write-nothing verifiers, 0 dead, 5 HOLDS / 1 PARTIALLY-BROKEN) broke six things, five of them mine, and the HOLDS verdicts carried more damage than the broken one.** **(1) THE SUB-100 NUDGE SEPARATES**, and I wrote "drives the two together" into two source docstrings, two test comments and a commit message. The nudge's own bytes DO NOT settle it — `hero._x += 1` is toward or away depending on what `gladiator_dir` MEANS, and the rival convention fits the same opcodes; the TURNAROUND 3.5 KB away settles it (`if (hero._x < villain._x) hero.gladiator_dir = "right"`, `+0x28f3` → `+0x290e`), so the word is FACING and both arms move the pair APART. **And the claim it supported broke a second time, independently: the guard is `< 100` while the parked case needs ≥ 124, so it cannot fire there at all — the build has no escape from that case, it simply has the case.** **(2)** `physical_size` IS a live reach gate, just not `weapon_range`: the frame-4 selector's bow arm is `fightdistance < 100 + hero.physical_size`. **(3) A THIRD reach gate exists and this repo did not hold it** — the VILLAIN's own AI gate at `@0x23f835` `+0x0356` is `(equipped_weapon == 1 && fightdistance < weapon_range) || (equipped_weapon == 2 && fightdistance < 200)`; **the two sides do not share a gate in vanilla.** **(4)** `[5]` runs 1/2/3/**4**/**100** — 18 ranged rows at 100, so the reach scale is 44 to 4,400 and 21 of 90 rows have never been in a modelled bout. **(5) The archive claim is mostly WITHDRAWN**: 3,004 resolve not 3,091, 432 are ambiguous and EVERY one spans the range column, and "not one implies a `weapon_range` below `physical_size + 44`" is **unfalsifiable by construction** because min `[5]` is 1 over every row. **(6) I made the exact reporting error my own docstring convicts an earlier session of** — claiming the paste meant "no bout ever settled" when 24/24 settled either way and the real diagnostic is 0 attacks and 7× the actions; the crowd kills them. **The lesson outranks all six: a check that confirms a PATTERN cannot catch a wrong INTERPRETATION** — the tool now DERIVES the nudge's direction and prints SEPARATES or CLOSES. **Unfixed and ranked first: the browser arena's SPECTATE mode never reaches a swing** (`main.js:572` cycles walk-left/walk-right/rest for net-zero displacement forever — 24/24 bouts, 20,712 actions, 0 attacks), which predates this session |
| [2026-09-12 01:40 — the last authored number is derived](2026-09-12-0140--the-last-authored-number-is-derived.md) | `7acfa329` | **The walk displacement was in the BUILD all along, two instructions below the stamina cost `MAP_SILENCE.movement-displacement` was quoting** — `attacker_x_walk = movement_speed * 16` (`+0x3d78`), scaled by `100 + 2*boot` percent, set as a DESTINATION and eased by `ceil(gap/8)` a frame until the gap is 20 or less. At the `movement_speed` clamp floor of 4 that realises `64 - 20 = 44` exactly, so **the uncited "one walk is 44 px" was right and the reader who called it a conflation with `weapon_range`'s 44 was wrong — but it is the FLOOR case**: `movement_speed` 12 walks 172. The entry is REMOVED (nine to eight), `walkDistance` is renamed `walkDistanceAtSpeedFloor`, and a walk is per-actor. **The lesson outranks it: a gap in that catalogue is a gap in a TRANSCRIPTION and says nothing about the build**, and three sessions running read one as if it did — so an entry whose `settledBy` reaches for a capture must now say why the bytes cannot answer it first. All eight movement phases read (run `*40`, charge `*20`, jump `round(ms*0.6)` per frame) and held to the SWF by a new tool, which **caught me reading a charge's COST as its destination on its first run**. Two owner items moved: **3v3 walking fell 52.2% → 26.8%**, so half of the "53% of a 3v3" pacing decision was a wrong number; and the crowd's "no honest bout reaches patience" assertion genuinely broke — 3 of 36 now run past 200, and with the toll off **2 never terminate at all**, so that test's own remedy ("raise patience past the tail") is unfollowable. **Then a 12-agent wave (6 questions / 6 verifiers, 0 dead, 4 HOLDS / 2 PARTIALLY-BROKEN) broke four things in the derivation**, one of them behaviour: the build's `get_percentage` round trip is lossy and `add_percentage` divides before multiplying, so the collapsed formula is +1 out at six reachable `(movement_speed, boot)` pairs; and the tween is a DO/WHILE, so my `while` returned 0 where the build moves 2. **And it retracted `440dae9` in full: `phase_action` records the HERO only** — the 66 "villain" rests all sit at hero `staminaleft == 0`, they are frame 1's forced rest (`+0x0d2e`), the wrapper's own comment said so, and the living head had said so 2,600 lines down since `d39fb8b`. A living head long enough to disagree with itself needs a second reader. The wave also found two defects in ITSELF: three agents had scratch files clobbered by another agent's same-named file, and my brief taught all six a parse that drops 69% of the archive's lines |
| [2026-09-11 23:40 — two "owner's lane" items were a session away](2026-09-11-2340--two-owner-items-were-a-session-away.md) | `1a8d94aa` | **A SESSION CAN READ THE INSTALLED SWF AND THE CAPTURE ARCHIVE, and both were being treated as out of reach** — the build is at the path `item-table-transcription.mjs` already defaults to, and the populated archive is a second checkout at `/mnt/c/ss2-capture/captures` (1,650 rufflelogs). Neither needs Ruffle, staging or a capture window; `AGENTS.md` reserves only LAUNCHING Ruffle and touching the install/save/snapshots. **Two ranked owner items closed on that basis.** (1) The `weaponweights` direction: index 1 IS the heavy end, so `ss2WeaponMass` is right and the swing cost is NOT backwards — and `ss2-item-tables.md` had said so since 2026-08-30, TWO LINES under the offset `MAP_SILENCE.swing-cost` cites for the array's location while declaring its values unheld. **Third instance of declaring the map silent without reading the surrounding paragraph.** The array holds STRINGS, so `attack_speed` is a weight-CLASS index, never a numeric speed. (2) The approach measured from the archive by the controller flip — which IS `fightdistance < weapon_range` first holding — **n=1,512, median 5, mode 5**, corroborating the five-walk figure the authored 44 rests on, though deriving no displacement. **AND THE MISTAKE THAT MATTERS MOST: a four-way design fork went to the owner on GLADIATORS THAT CANNOT EXIST** — five invented archetypes declaring `herolevel: 3` (budget 25) while spending 40-53 points, each with `magicka: 0` below the floor of 1, when `stat-vector-reachability.mjs` had implemented `13 + 4L` for days. "684 turns, patience no longer clears the tail" is WITHDRAWN; re-measured over reachable builds, 200 clears every self-terminating bout with 70% headroom. **What survived is larger: `SS2_CROWD` is not a backstop but what makes 52-63% of reachable CROSS pairings terminate at all**, because attacking heals the attacker and a build that spent its points away from strength deals single digits — and unlike D1 and D2 that corner is reachable by ordinary progression. The owner caught it by asking whether these were decisions or facts |
| [2026-09-11 21:15 — both halves of position are built](2026-09-11-2115--both-halves-of-position-are-built.md) | `1a8d94aa` | **Gladiators now stand somewhere and must walk to reach each other.** The resolver half landed on the presentation half from the same session: `x` on the combatant and inside `combatStateHash`, `EffectKind.POSITION` (absolute, like `RESOURCE`), `SS2_ARENA`, a `startingPosition` hook, two walk verbs, the build's own controller gate (`fightdistance < weapon_range`, STRICT `<`, frame 4 `DoAction@0x238bbf` `+0x00f6`) and an AI that closes. **Everything re-derived from the map**; the preserved patch is SUPERSEDED and differs in three deliberate places — `movement_speed` is NOT a resource (a `battlevalues` output, and `agility` is already projected, so ONE vocabulary change not two); the walks order LEFT then RIGHT because all eight rows of the controller table put `walkleft` at `optionB` in BOTH facings, so the patch's away-then-toward was invented; and the walk event carries `vanillaLabel`, the field presentation cannot derive. **THE GOLDENS DID NOT MOVE** — `fixtureReplay` returns null from `startingPosition`, and a mutation giving fixtures a position fails 15 tests including the whole replay suite — while every other hash did, and the asymmetry is the check: six team-projection pins moved, six `engine.js` legacy pins byte-identical. **THE NUMBER THAT NEEDS AN OWNER DECISION: 53% of a 3v3 is now walking** (1v1 11 actions to first contact / 10 walks / 27 total; 3v3 35 / 48 / 91; all 120 seeds settle). Five walks a side at 1v1 independently reproduces the archive's own figure, so the fidelity is good and the PACING is the question; the levers are `SS2_ARENA.frontX` and `walkDistance`, and `weapon_range` is NOT one. Two consequences found by tests rather than reading: read-back now DROPS position (the build re-places everyone at battle entry, and a carried survivor kept its blueprint's `x` while a fresh challenger did not, so bout 2 of a circuit never reached a swing), and `tools/hotseat.mjs` opens ENGAGED with a new `--approach` flag. **9 mutations, one SURVIVED and is now pinned** — removing the read-back drop passed everything, because the hotseat fix had made the CLI independent of it and took the only coverage with it |
| [2026-09-11 18:30 — a walking gladiator has a clip to play](2026-09-11-1830--a-walking-gladiator-has-a-clip-to-play.md) | `1a8d94aa` | **Ranked item 3 is closed: presentation before position.** The stream has `CommandKind.MOVE_CLIP` carrying `from` and `to` and nothing else — **not** a partial `place-clip`, because `scene.js` folds that one by overwriting all seven geometry fields, so a partial command sets `y` to `undefined` and `toY(undefined)` is NaN and the figure VANISHES. `SS2_STATIC_MAP_BINDINGS` has a movement case at `ASSUMED` provenance, detected by the event's own geometry rather than by parsing the type string; `src/render/timeline.js` has four gait schedules and `travelAt`, where `timelineFor()` returned `unknown` for all eight movement labels before. **Nothing emits a `move-clip` yet, and that is the point** — the resolver half is the preserved patch, and the one thing it must ADD is `vanillaLabel` on the movement event: `to < from` gives the direction and nothing separates a walk from a charge, so an event without it is REPORTED rather than given a guessed gait. **Geometry is not a label decision**: an event the bindings cannot name still moves, so the scene never draws a figure standing where the resolver says it is not. Two decisions left the browser shell — `figureXAt` and `timelinesForStep` — for the reason `animationCursor` left it; `render-arena-host.test.js` had RE-IMPLEMENTED the second, so test and shell could agree while both being wrong. **12 mutations applied one at a time; TWO SURVIVED the first version of the tests** — a smoothstep passed because 0.5 is a fixed point of every symmetric ease, and "travel also lunges" passed because the travel branch was only sampled where `advance` is already 0. Both fixed and re-verified. A third error was caught by reading the diff: hoisting `layout.placementFor` turned an unmapped record into a thrown `SlotLayoutError`. **`/codex:adversarial-review` on this diff is UNSPENT and is the owner's to type** — the command is `disable-model-invocation: true` |
| [2026-09-10 22:44 — the economy was the cheese](2026-09-10-2244--the-economy-was-the-cheese.md) | `661a9a6f` | **A 13-agent panel was asked to design 3v3 so it could not be cheesed; all four designs were BROKEN**, by two properties of the shipped 1v1 engine that 3v3 only multiplies. **D1: a bout need not terminate** — 4,000 consecutive mutual `rest` actions left `battle.result` null, because rest is stamina-positive AND heals. **D2: strength was a trap and stamina the only stat** — strength 7 beat strength 30 over 39 actions without losing a point of stamina or health, because a swing was priced on the wielder and paid out of the weapon. Both closed by owner decision: an AUTHORED crowd-impatience toll (standoff now ends at turn 234; 0 of 120 honest bouts pay anything) and a swing priced on the weapon's `attack_speed` with strength in the denominator, plus the shop's own measured purchase gate. **Both gated behind `fixtureReplay`, so all 23 goldens still reproduce their MEASURED `staminaleft`** — repricing that path would have been a corpus break, not a balance change. Three seeded-play pins re-derived with the reason at the pins. **Two self-inflicted errors recorded where they happened**: a fork put to the owner on a false premise (`crowd_interest` is a gold multiplier, the `taunttimer` abandons an ANIMATION), and `patience` tuned twice against a distribution the toll itself shaped. **The owner's next task is small and decides a lot**: read the six `weaponweights` values at `+0x3dd4` — if index 1 is not the heavy end, every swing cost is backwards |
| [2026-09-10 17:30 — the map was not silent, twice](2026-09-10-1730--the-map-was-not-silent-twice.md) | `661a9a6f` | **Ranked item 2 was blocked by a FALSE "the map is silent" claim** — `ss2-rules.js` said `_root.arena.fightdistance` had "no writer at all", and the corpus names it twice with offsets (`getfightdistance`, champion-dna:710-712 and adapter-contract:1232). `git blame` puts the claim at `831bcdc` (2026-09-01), **two days AFTER** `5d3d777` recorded the writer, so it was never a stale note: a sentence written without opening the neighbouring document. Second such claim in two days. The gap that IS real is now `MAP_SILENCE.movement-displacement` (six → seven, the day after seven → six — the symmetry is why the id LIST is pinned and not the count), careful about the one uncited figure the repo holds ("one walk is 44 px") and settleable with **no new capture**. `assertDistinctPlacements` promised a distinct screen position and never checked x or y; closed, with a test that bites. `seeded-play-pins.test.js` claimed it had re-derived "exactly one" literal hash pin — there are **ten**, six predating it by eleven days; the conclusion survived the miscount and the reasoning did not. **A whole position implementation was built and driven in a scratch copy and works** — ±250, five walks a side (matching the archive from an independent direction), ±30, combat; all 23 goldens green via `fixtureReplay`. **Not landed, and the measurement is why**: with the gate live the suite goes to 72 failures and two hanging files, because a walk emits `clip-goto Standing` — the idle clip — the identical defect the owner found watching the arena that morning, caught in the afternoon by the sweep guard written for it. **Presentation first, resolver second.** Wave: VERIFIED, 6 questions / 6 verifiers, 0 dead, **31 claims unverified** |
| [2026-09-10 13:23 — the arena is drawn](2026-09-10-1323--the-arena-is-drawn.md) | `fc951c81` | **The RENDERED ARENA landed** — `src/render/` (pure, under the suite) plus `tools/arena/` (a thin canvas shell) plus a zero-dependency loopback static server, so a browser fights through the same resolver and adapter the tests run, imported directly as ES modules with no build step. It is the presentation stream's first consumer outside `test/`, which is the shape the campaign record was in before 2026-09-07 — and like that one, reading it back found defects. **A shipped clip label contradicted the battle map**: `hurtLabel` emitted `hurt21/22/23` marked `assumed` because a `MAP_SILENCE` entry said the map gave no adjustment, when the map gives it one sentence later with byte offsets (`hurt1/2/3`, map:1471-1472) — the silence entry, the code and the test all agreed with each other and none agreed with the map. **Two comments claimed the action boundary is never derivable from the wire**; it is exactly `wire.events.length + 1` before `applyAction` (0 mismatches over 193 actions), and the real reason it is not projected is hash stability. **Nothing enforced "ship no SS2 asset"** until now. First host anywhere here with `awaitAnimations: true`, so the per-action gate ENFORCES and part 4 of the acknowledgement seam — the animation-timeout policy — exists for the first time. Drawing the authored ally band exposed that it sits further back but draws in front, pinned rather than corrected. **The canvas is verified only from static screenshots**: headless Chrome gives an rAF loop 2 frames whatever the virtual-time budget says |
| [2026-09-10 12:12 — everything is pushed, and the Workflow gate is gone](2026-09-10-1212--everything-is-pushed-and-the-workflow-gate-is-gone.md) | `ff2653b2` | No code changed. Nine commits that three sessions had left unpushed finally went, and the branch is fully up to date for the first time. **The `Workflow` permission gate is GONE** — the owner noticed he was still getting a per-workflow dialog under bypass permissions and dropped the entry, which moves an obligation from the harness onto the session: say what a wave will spawn before launching it, because the 6/6 hard cap in the committed script is now the only deterministic guard. `Bash(git push *)` stays in ask and every deny rule is untouched. **A claim the fan-out cost argument rested on is untested**: the settings file asserted bypass mode skips ASK rules, and the owner's experience contradicts it — flagged in place, measure it rather than cite it. Also: the 12:42 handoff's own push count was wrong the same way the two before it were wrong, so the fix is now arithmetic rather than care — count unpushed commits AFTER committing the handoff, into the living head. Ranked list unchanged; `settlement.arm()` on an unfought battle is still item 1 |
| [2026-09-07 12:42 — the 78 are done, and the suite does not bite](2026-09-07-1242--the-78-are-done-and-the-suite-does-not-bite.md) | `ff2653b2` | The 78 stale living-head rows are re-derived and corrected at their sentences by a 6-question / 6-verifier wave (VERIFIED, 6/6 and 6/6). Three of the session's defects were its OWN and agents found all three: the previous commit minted six new stale line numbers while repairing one; the wave was launched on a wrong offset (+65 when the sweep's own numbers were already ~27 low, so the truth is ~92) and had to be killed and relaunched off text-matched anchors; and the row lists carried two proposals the sweep's own refuters had already broken. **Then the headline: a first systematic mutation audit found 37 of 48 one-line breakages SURVIVE the suite**, eight adversarially CONFIRMED — but six of the eleven kills are the corpus-integrity gates, so the code protecting the corpus is the best-tested code here. `src/team/controllers.js` has zero negative tests; every literal pinned hash is taken before any action is applied. **And Stage 7 is not the next thing**: EP-D07 lives on an unmerged branch, is invisible to a grep, and forbids the AI takeover all five designs were built on; `settlement.arm()` is accepted on a battle nobody fought |
| [2026-09-07 11:40 — the sweep reached the untouched six](2026-09-07-1140--the-sweep-reached-the-untouched-six.md) | `b60442bf` | The six documents the previous sweep left untouched are swept: 79 rows, 73 applied and 6 rejected, every one re-derived by hand. **Four of the six rejections are numbers that do not reproduce at all**, and one — `capture-staging`'s "22 promoted goldens" — was CORRECT, so a blanket 22→23 would have broken it. **The headline is that 78 stale rows sit in `HANDOFF.md`'s LIVING HEAD, lines 801–3132, not in the archive**: the sweep chunked the file as though the archive line were at 800 when `grep -n` puts it at 3133. Two verifiers, two survivals, both finding more than asked: the Steam build id is BOTH a compatibility key (262 files, four throwing gates) and an install identity, and the CONTROLLERS autopilot gate lets through exactly the labels the battle map's hazard list names. Its own commit left three handoff-navigation tests red |
| [2026-09-07 09:55 — the last adapter gap, and the sweep](2026-09-07-0955--the-last-adapter-gap-and-the-sweep.md) | `cb4573bf` | The per-action animation acknowledgement is BUILT, and my own contract told me to build it wrong: `event.sequence` is stamped per EVENT and one action emits up to four (measured over 5,708 actions), so the token comes from `lastResolvedAction().firstEventSequence` — which is deliberately unprojected because `combatStateHash` covers everything that is. Advisory by default; no timeout anywhere, because nothing has captured vanilla's completion signal. Then the document-integrity sweep: 17 surveyors, 34 refuters, 17/17 and 34/34 returned, 3,146 claims, 264 stale or wrong. **The refuters broke 7 of 34, and one would have BROKEN THE CORPUS** — the Steam-build row is a compatibility key carried by 262 files, not an install-identity field. The OneDrive tree is not gone: it is a third copy of the archive, 1,589 files |
| [2026-09-07 08:25 — the accepted decisions do not authorize anything](2026-09-07-0825--the-accepted-decisions-do-not-authorize-anything.md) | `8c4c0cbf` | Ranked item 2 answered: three EP decisions accepted, forty normative clauses, and every one ends "This decision does not authorize implementation" — so the deliverable is a FORK for the owner, not a branch. The accepted text is not on this branch; it is on the unmerged `design/endless-progression-owner-packet`, and this tree's copy is an older design where all six read `pending`. Meanwhile the campaign read-back got its first consumer (`--circuit n`), and `playable` turned out to be a flag that CANNOT be true — measured over 240 rosters. Codex found four more defects, two in the tests I had just written; two of my fixes survived mutation and now have tests that kill them |
| [2026-09-07 07:24 — the ranked list ran out](2026-09-07-0724--the-ranked-list-ran-out.md) | `0251f418` | Every ranked item closed, including two that were not what the brief thought: item 3 was REAL (the map carried both status rules and the hero's is first-match-wins) and item 4 was already done, with the sync running the other way. Owner decided and I built the status phase; team fights are playable (`--teams 2v2`); the campaign record can finally be READ BACK. The lesson: when the ranked list emptied, `docs/roadmap.md`'s "not started" column was the better map. Three Codex reviews found nine defects the new tests missed, three of them universals I had written as facts. Wire format moved — all 23 golden replay hashes — and only a test guards it |
| [2026-09-07 03:30 — the armoured golden reaches the resolver](2026-09-07-0330--the-armoured-golden-reaches-the-resolver.md) | `0251f418` | The 23:13 brief's one task was an email ALREADY SENT five days earlier and nothing in the repo knew — the reusable failure, recorded first. Ranked item 1 landed WITHOUT a wave: REPLAY_UNDRIVABLE is empty, all 23 goldens drive a real battle, the damage pair is required of whoever ATTACKS. A declared defender pair MOVES the battle hash while reaching no arithmetic, so completing a fixture is a protocol change. An independent Codex review found two real defects the tests missed, one a regression this session introduced (a refused attack consumed RNG and moved the hash). AGENTS.md's observation count was wrong the day it was written: 69, not 70. **Owner DECIDED enchantments: option (a), status phase as a forced legal action** — blocked on byte verification, which this WSL tree cannot do |
| [2026-09-02 23:13 — reach for the stars: the email to the developer](2026-09-02-2313--reach-for-the-stars-the-email-to-the-developer.md) | `426439d1` | First task: draft (never send) a reverent, honest email from the owner to the game's developer asking about collaboration or source access — solo, no wave; the owner's research and the recipient must be asked/verified, never invented. Records the enforced verification precedence (harness ADR 0001) and that ultracode stays ON |
| [2026-09-02 16:59 — three waves, cut at the usage limit](2026-09-02-1659--three-waves-cut-at-the-usage-limit.md) | `426439d1` | Three capped fan-out waves on ranked items 1–3 stopped at 92% of the session limit (30% of weekly usage in 20 min); VERIFIED: nothing on the hero's one-action path reads the defender's damage pair; REPLAY_UNDRIVABLE's "both directions" assertion was a self-comparison, now a positive check; ranked item 4 closed, item 3's premise broken; two stale catalogue instructions corrected at their sites |
| [2026-09-02 13:40 — the first armoured golden](2026-09-02-1340--the-first-armoured-golden.md) | `0ffa2c73` | **`golden-armoured-deflection-threshold-cleared` is promoted** — the armoured family's first, from two independent captures 17 minutes apart, and the first runtime evidence here carrying armour (`armourclass 79`) or `fightMode: "tournament"`. 1,353 unattended rounds at ~8s each; the gate refused it first, correctly, until `provenance.staged` existed. Ranked item 3 CLOSED (weapon table in code, verified against the build, `weapon` derives the damage pair). Item 2's arithmetic landed; APPLYING it replaces a turn and is yours. Capture reframed: not a scarce window, 14s a round, and the binding constraint is villain stamina, which staging cannot set. **Two rule defects of our own**: the uncapped verifier fan-out (1,069 verifiers, five-hour limit) and `overnight-agent-plan.md` promoted to doctrine by one line in AGENTS.md. Git hygiene now exists in the harness and is enforced |
| [2026-09-02 00:07 — the wave refuted more than it confirmed](2026-09-02-0007--the-wave-refuted-more-than-it-confirmed.md) | `d8905702` | **Newer than the row below it, which is stamped in UTC under a `-0400` label — `ls` misorders them.** An 8-question wave with 24 write-nothing verifiers, VERIFIED, broke **17 of 24** claims. Ranked item 2 REFUTED from the bytes: `activeEnchantment`'s primary-potency pairing is byte-faithful and must NOT be changed (the proc gate reads `weapon_enchantment_potency` unconditionally at `+0x1c0f`, hoisted ahead of every `equipped_weapon` test); the real defect was its fallback, which applied the primary status for any `equipped_weapon` outside {1,2}. `localeCompare` was FIVE files, not two, and one of them orders the capture manifest whose digest 22 goldens cite — measured free (0 of 3,655 pairs move under en-US; az-AZ moves 682), 22 of 22 digests reproduced. Item 5's premise false: the adapter CAN feed the rule set via `aiFill.resources`. Item 4 answered and inverted — a remainder digest leaks undrawn rolls; the leak-free maximum is the consumed prefix. Left to the owner |
| [2026-09-02 01:30 — the corpus got a consumer, and the wave broke ten of twelve claims](2026-09-02-0130--ss2-rules-and-the-wave-that-broke-it.md) | `16532b60` | **`src/team/ss2-rules.js` exists**: SS2's own attack arithmetic runs inside the shared resolver, all 22 goldens replay through `createTeamBattle`/`applyAction`, and hot-seat plays it by default. Ranked items 1 and 2 of the previous brief, done. A 12-agent write-nothing wave broke **10 of 12** claims: the `rest` branch has its own `hitpoints += 3 + ceil(stamina)` at `+0x51d5` and this session had OVERRULED the map to say otherwise; and `death()` removes the phase transition, so a killing blow costs the attacker nothing — the replay test had been asserting nineteen times that the engine must disagree with the fixtures. Both fixed, both pinned, and two errors corrected in the battle map itself |
| [2026-09-01 21:12 — the project became playable](2026-09-01-2112--the-project-became-playable.md) | `6c06a03b` | **`node tools/hotseat.mjs` plays a fight** — the first playable thing here, after the owner asked whether the approach would let the project survive. Answer: the verification machinery had become the project; 22 goldens fed nothing. Adds the `map-derived` tier whose absence (not effort) blocked wiring SS2's real arithmetic in; retracts 2026-09-01-1550's ranked items 1 and 3 by measurement; makes the raw archive verifiable and mirrored. Three waves VERIFIED, 28 of 48 verdicts BROKEN |
| [2026-09-01 15:50 — Codex independence, and what the corpus actually proves](2026-09-01-1550--codex-independence-and-the-corpus-archetype.md) | `515e2223` | Supersedes 2026-09-01-1250. Both waves VERIFIED (209/209, 230/230); an outside Codex review found 67 wrongly committed traces that 758 agents missed; all 22 goldens fight ONE zero-stat opponent and 0 of 82 fixtures pin `speed`; the armoured fix is an arming-gate change already proven on another route |
| [2026-09-01 12:50 — WSL capture pipeline, and the armoured fixture defect](2026-09-01-1250--wsl-capture-pipeline-and-armoured-fixture-defect.md) | `515e2223` | Capture pipeline driven from WSL for the first time since the relocation; the armoured/tournament families are blocked by their own fixtures, a 110 derivation raised and retracted the same day (neither value is scenario-determined); both agent waves UNVERIFIED-PARTIAL with 109 dead verifiers |
| [2026-08-31 22:44 — the four self-citing goldens are re-promoted](2026-08-31-2244--self-citing-goldens-repromoted.md) | `18794878` | All four re-promoted pipeline-only from independent records; five verifier BROKENs exposed settle writing manifests before the gate, no manifest→golden guard, and a test whose prescribed deletion rewarded a self-citing golden |
| [2026-09-01 00:21 — corpus repair and doc-integrity guards](2026-09-01-0021--corpus-repair-and-doc-integrity-guards.md) | `18794878` | Supersedes 2026-08-31-2244. Four self-citing goldens re-promoted; settle no longer writes attestations before the gate; a test that rewarded the defect it named; three contradicting instructions in the head corrected; doc-pointer guards added |
| [2026-09-01 00:30 — migration close-out, and what is untested](2026-09-01-0030--migration-closeout-and-what-is-untested.md) | `a87c4347` | WSL is primary and Windows is episodic; an honest list of what was configured but never exercised, incl. the capture pipeline since relocation; save+snapshots backed up; the pinned build auto-updates |
| [2026-08-31 18:20 — the pairwise gate, measured](2026-08-31-1820--pairwise-gate-measured.md) | `a87c4347` | The gate HAS teeth (407 leaves at callSite) and on committed evidence refuses nothing, because the nonce check fires forty lines earlier; "162" reproduces exactly; the fresh-nonce hole also unlocks the authored-from gate |
| [2026-08-31 14:43 — nonce gate, stat arithmetic, toolchain](2026-08-31-1443--nonce-gate-stat-arithmetic-toolchain.md) | `e386f047` | A record’s own copy stopped counting as the second session; the stat-vector arithmetic settled the 22 written-off fixtures; one shared AGENTS.md for both agents |
| [2026-08-31 12:38 — corpus audit and provenance repair](2026-08-31-1238--corpus-audit-provenance-repair.md) | `4a79fa7f` | 5 transcriptions found (not 23), provenance made honest, pairwise gate decoupled from the matcher; levelling reopens 22 fixtures |
