# Handoff — Chosen arm required; decisive-input attribution next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-22 19:01:38
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.141: whether every legal same-tag recommit path
must contain an actually selected lever whose decisive effect can be isolated
to predicate-read relationship facts or recurrence control, rather than relying
only on attribution-bundled effects. The owner selected C3c.140-A. Preserve
accepted EP-D02 wording until a complete replacement is replayed and explicitly
accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Finish the current
full-assurance pass before the standalone expansion/audit/adaptation pass.
Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.140-A

`F^{menu-only}_v` is empty. Every legal adjacent same-tag recommit path has at
least one live/false opportunity whose actual selected continuation belongs to
a C139-style context-held isolation pair. The actual arm exactly reproduces the
witnessed suffix through `q2`; the counterfactual changes only that action and
holds every later participant choice and exogenous/random input fixed wherever
legally alignable. Substitution must change whether or when `q2` occurs.

The selected continuation need not solely, immediately, beneficially, or
completely rebuild the relationship. It may create, preserve, accelerate,
delay, or inhibit a material fact; maintain or forbearance can qualify when the
substitution changes `q2`. A fused predicate-plus-recurrence contrast counts
once and proves neither axis independently.

At the action-anchored distinguishing cut, continuing operative context and a
materially different outcome coexist **directly**. The complete recurrence is
**partial**, menu-only exclusion **neutral/protective**, and universal
prevalence **aggregate**. The branch retains `9/23/138`, four represented IDs,
eight lived edges, four complete IDs, no incomplete represented ID, Souls
`{6,9,10,12,14,15}`, and degree six. The authoritative decision record remains
unchanged.

## Why decisive-input attribution is next

C140-A closes unused-menu laundering, but an actual selected action can still
leave no pure predicate or recurrence difference at the decisive boundary. It
may bind those two axes together, bind either to context or evaluation control,
or leave only another control carrying the decisive effect. A Rekindle-the-
Echo rite whose Heat and still-governing authorization both differ at `chi` is
the clearest separator. The action truly changes `q2`, yet no legal matched
pair holds every non-target input fixed. Counting it as independent predicate
or recurrence support would manufacture a clean lever.

Independent attribution must therefore precede the later support-set choice.
Continuous context cannot repair the issue because R17 can remain operative
from `c1` through `q2` while the only chosen-arm contrast stays bundled.

## Active owner choice — EP-D02-C3c.141

For witness `w` and opportunity `d`, `Pi^{chosen}(w,d)` contains all C140-
qualified isolation pairs anchored to the action actually selected at `d` and
to `w`'s realized suffix.

For pair `pi`, `chi(pi)` is the earliest aligned canonical semantic evaluation
boundary after `d` at which applying the complete fixed-tag occurrence contract
makes the arms differ in whether the next same-tag occurrence becomes
provisionally complete. When both eventually reach `q2` at different
boundaries, `chi` is the earlier completion boundary; when only one does, it is
that arm's completion boundary. `X^-(chi)` is each arm's authoritative input
projection immediately before adjudication there. Classification uses
`X^-(chi)`, never a frame, poll, callback, storage update, arbitrary earlier
fact divergence, post-`q2` mutation, or arbitrary later state.

The complete `q2` input projection at `chi` includes:

- every predicate-read `H`/`S` fact;
- complete tag-specific recurrence-control semantics, including any semantic
  queued, scheduled, consumed, cooldown, retry-suppression, or latent
  authorization that later exists and still governs this occurrence;
- fixed-context identity and operative availability; and
- evaluation authority, phase eligibility, and every other semantic input
  capable of deciding completion.

Clearing, renaming, caching, duplicating, or moving an implementation field
cannot remove a semantic input or manufacture independence. An occurrence-
specific permission, reservation, queue, consumption, suppression, or other
authorization lineage remains recurrence control exactly when changing it,
with predicate facts and all other `X^-(chi)` inputs fixed, can change whether
this occurrence is complete. A coordinate can belong to both predicate and
recurrence projections when it carries both meanings; changing it then proves
neither isolation. A genuine relationship fact whose authorization force is
exhausted is predicate-side even if an earlier recurrence difference helped
produce it. Earlier causal histories may differ beyond C140's anchored-suffix
constraints: C141 classifies the extensional marginal decisive input, not
mediator-path purity or sole historical causation.

`I_P(w,d)` exists when some chosen-arm pair has a nonempty predicate-read fact
difference at `chi` while recurrence control and every other complete-`q2`
input are equal. `I_R(w,d)` exists symmetrically when recurrence control differs
while every predicate-read fact and other input are equal.

No isolation among coordinates inside the varied projection is required. One
opportunity may prove both only through two distinct valid isolation pairs; a
single pair changing both proves neither. Additional bundled pairs do not erase
a valid independent pair. Incomplete analysis is unresolved, never proof of
bundled-only behavior.

“Attribution-bundled” means no qualifying pair proves either `I_P` or `I_R`.
The bundle need not be predicate plus recurrence: it may combine either target
projection with context, evaluation/phase authority, or another semantic
control, or leave only another decisive control different after an earlier
target-axis divergence. A fused predicate-plus-recurrence rite is the clearest
separator, not the whole class.

Let `F^{no-axis-iso}_v` contain tags with at least one legal adjacent-commit
witness whose live/false opportunities have neither `I_P` nor `I_R`. C140-A
guarantees chosen-arm leverage on such a witness; all of it is attribution-
bundled. Negative membership requires complete reachable-domain analysis.

### A — every path contains independently attributable chosen-arm leverage — recommended

`F^{no-axis-iso}_v` is empty. Every tag and every legal recommit witness has at
least one actual selected opportunity with `I_P` or `I_R`. One form suffices;
A does not require both, and additional bundled rituals remain legal.

A gives every path one decisive-input explanation that can be surfaced,
audited, balanced, and regression-tested without double-counting a joint
transition. At decisive boundary `chi`, the same operative context and
identical non-target inputs coexist with one materially different relationship
or permission projection, so the local fit is **direct**. The whole recurrence
remains **partial**, exclusion **neutral/protective**, and universal prevalence
**aggregate**. Cost: an intentionally composite ritual may need another clean
comparison that feels token or artificial.

### B — every tag permits some attribution-bundled-only path

`F^{no-axis-iso}_v=K^{candidate}_v`. Every tag has at least one legal witness
whose chosen-arm leverage has no predicate-only or recurrence-only isolation
against the complete `q2` input projection. The bundle may be predicate-plus-
recurrence, either axis plus context/phase/other control, or another decisive-
control residue after an earlier target-axis divergence. B chooses no bundle
type. Other paths may still be independently attributable.

B is the strongest coherent-composite countercase. At `chi`, Rekindle the Echo
can leave the renewed bond and its still-governing permission to manifest as
one composite decisive input, without an artificial two-switch decomposition.
The same context and material chosen-arm transformation still coexist
**directly** at inherited
C140 cut `tau`; at decisive boundary `chi`, independent-axis attribution is
**neutral/unsupported** and no direct relationship is guaranteed. The whole
recurrence is **partial**, and universal bundled-path permission **aggregate**.
Costs are entangled tooltips, replay diagnosis, balance knobs, and tests; the
rite cannot truthfully count as a pure predicate or recurrence lever.

### C — independent-required and bundled-path-permitting tags coexist

`F^{no-axis-iso}_v` is a nonempty proper subset. At least one tag permits an
attribution-bundled-only witness, while at least one other tag has `I_P` or
`I_R` on every legal recommit witness.

C supports cleanly attributable Vows beside deliberately composite paths.
Local witnesses inherit A or B's rating; catalog coexistence is **aggregate**.
Costs are two decisive-input explanation and validation standards plus ease or
premium asymmetry.

## Concrete separators

- **`I_P`:** the witness selects Rekindle rather than Vent. At `chi`, R17 is
  operative and every `q2` input except predicate-read Heat is identical; only
  Rekindle's arm is complete.
- **`I_R`:** the witness selects Invoke rather than Bank. At `chi`, every
  predicate-read Guard/Heat fact and other input is identical, but only Invoke
  supplies P's recurrence control.
- **Bundled-only:** Rekindle the Echo necessarily restores Heat and authorizes
  P together, with both semantic roles still material at `chi`; no actual-arm
  pair isolates either projection.
- **Control-bundled:** Rekindle changes Heat, but at `chi` its arm is also the
  only one with operative R17 or eligible evaluation authority; no pair
  isolates Heat from every non-target input.

A forbids a bundled-only path for every tag. B requires one such path per tag.
C permits it for some but not all tags.

## Compatibility, floors, and deferrals

A/B/C are exhaustive because `F^{no-axis-iso}_v` is empty, the whole nonempty
tag domain, or a nonempty proper subset. C45-C supplies at least three tags, so
C remains feasible. B does not say every path is bundled-only. Every branch
retains `9/23/138`, four represented IDs, eight lived edges, four complete IDs,
no incomplete represented ID, Souls `{6,9,10,12,14,15}`, and degree six.

C141 chooses no catalog independent-support set
`K_ind subseteq {predicate,recurrence}`. A or C makes it nonempty; under B it
may be empty. After A or C, the next dependent attribution atom may choose
`{predicate}`, `{recurrence}`, or both. After B, recompute the frontier and
first decide whether any independent support is required. C141 chooses no
distribution by tag, path, or opportunity; no requirement that one opportunity
prove both through distinct pairs; and no requirement for additional bundled
transitions. It selects no continuous `c1`-to-`q2` context, mediator-path
purity, retained strand, source identity/reuse, `H` retention, relationship
operator, actual failed evaluation, opportunity count/duration,
actor/authority, sole/immediate/beneficial/complete causation, recurrence
phase, cadence, cooldown, cap, cross-encounter recurrence, consumption, payoff,
power, persistence, release, or implementation. It does not choose which non-
target coordinate participates in a bundle or that coordinate's behavior.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling: follow direct `AGENTS.md` rules
and do not treat the overnight plan as current guidance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C141 only. Record
the selected branch and its consequences, recompute the frontier, and present
exactly one next prerequisite-ready choice. Do not infer acceptance of the
whole EP-D02 replacement or start implementation.
