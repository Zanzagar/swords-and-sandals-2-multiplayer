# Handoff — one rotation class selected; class identity next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-17 03:47:59
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.86: whether the fixed-width-capable class or the
breathing class is the sole affinity class that realizes a same-definition
equal-breadth exact-noncore-union rotation. The owner selected C3c.85-B.
Preserve the accepted EP-D02 wording until a complete replacement is replayed
and explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow; use A/B when a selected parent leaves exactly two real
branches.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle remain in their SS2 lane.

## Selected direction

EP-D02-C3c.85-B makes

`K^{U=}_v subseteq {breath,fixed}`

a singleton. Exactly one of the breathing and fixed-width-capable affinity
classes contains an actual same-definition pair with equal breadth `u` and
unequal exact noncore unions `U`; the other class contains none. Union-locked
definitions remain ineligible.

The owner selected B because it is thematically closest among C85's options to
the standing ideal. Preserve the precise rating: each actual same-definition,
cross-key rotation has **partial** fit because the definition, ambient palette,
sole core, singleton count, map-group count, and breadth continue while exact
union identity differs, but the forms remain separate simulacra. C85's
one-class participation pattern is only **aggregate**. Every fixed exact-key/
Soul-roster relationship retains inherited **direct** fit.

C85-B does not choose class identity. Its floor is conditional: fixed-width-
capable-only can attain five definitions, seven exact keys, and forty-two six-
Soul rows, while breathing-only needs five definitions, eight keys, and forty-
eight rows. Both retain supported-Soul counts `{6,8,9,10,12,14,15}` and add no
palette-degree requirement beyond C84-B.

## Active owner choice — EP-D02-C3c.86

Only two substantive identities remain:

### A — fixed-width-capable affinities only — recommended

`K^{U=}_v={fixed}`. At least one fixed-width-capable definition realizes an
equal-breadth union rotation; no breathing definition does. The six-Soul
teaching construction is:

```text
TIDE BREATHES
T1  U={a,b,c},   u=3
T2  U={a,b,c,d}, u=4

ASHEN ROTATES AT FIXED SCOPE
A1  U={a,b},     u=2
A2  U={a,c},     u=2

VEIL REMAINS ANCHORED
V1  U={a,b},     u=2=N
```

A gives three readable gameplay verbs: quantitative compact-versus-broad Tide,
qualitative same-width motif substitution for Ashen, and a stable covenant for
Veil. Ashen's existing two morphic keys can carry the rotation without adding
a key, though union identity is not necessarily their only difference. The
tight floor is `5/7/42`.

The actual Ashen relation is **partial** and C86's placement is **aggregate**.
A is recommended because fixed-quantity substitution makes what continues and
what differs easier to perceive, not because distributing axes across separate
definitions directly satisfies the ideal. The risk is that rotatable Ashen
reads as a hidden upgrade over anchored Veil; later quality, disclosure,
agency, owner-Soul relevance, and non-dominance rules must prevent that.

### B — breathing affinities only

`K^{U=}_v={breath}`. At least one breathing definition carries both the C83-C
unequal-breadth obligation and a separate equal-breadth rotation; every fixed-
width-capable definition keeps one exact `U`:

```text
TIDE CARRIES BOTH AXES
T1  U={a,b,c},   u=3
T2  U={a,b,c,d}, u=4
T3  U={a,b,d},   u=3

ASHEN KEEPS ONE EXACT UNION
A1  U={a,b},     u=2
A2  U={a,b},     u=2

VEIL REMAINS ANCHORED
V1  U={a,b},     u=2=N
```

B creates a deeper three-form Tide-like simulacrum family, which is a real
thematic countercase. It also puts quantitative and qualitative comparison
axes under one name, leaves C84-B's fixed-width-capable lane behaviorally
latent on this projection, and requires a third breathing key because one pair
cannot have both equal and unequal `u`. Its tight floor is `5/8/48`.

B's actual rotation has the same **partial** ideal rating as A, and its class
allocation is likewise **aggregate**. Choose it if a deliberately complex
chameleon/Tide identity matters more than the three-dialect catalog grammar.

There is no ordinary C option. “Neither” would reopen C85 and choose its A;
“both” would reopen C85 and choose its C. An owner-written replacement remains
available and may explicitly reopen that parent.

Neither branch satisfies C39-C through union geometry alone. Reuse requires
distinct same-definition instances, a separate material final-predicate
difference on a shared fixed cell, and another invariant definition. C86 also
chooses only class identity. Some-versus-every prevalence inside the selected
class follows immediately, then rotated-union overlap, complete family
variation at fixed union, and block-to-map assignment at fixed family.
Instance/state/evaluation-boundary causation remains orthogonal and later.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C85-B
as selected and C86 as the sole active owner choice. The authoritative decision
record is unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04
directions remain unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain
selected, and R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. One bounded read-only review
wave independently verified that C86 has exactly two identities, re-derived
the `5/7/42` versus `5/8/48` floors and supported Soul set, and challenged the
recommendation under the standing ideal. The challenge confirmed A for
readability and content economy while preserving B's stronger deep-Tide
countercase and the equal formal ideal ratings.

`git diff --check`, no-index whitespace checks for current untracked
deliverables, the sole-active-heading assertion, stale-state scans, cross-
document C85-B/C86 assertions, the authoritative-record no-diff check, SVG XML
parsing, Unicode-format-control scanning, newest-handoff ordering, explicit
A/B witness accounting, and supported-roster/floor checks passed. The
repository test suite was not run because this was a documentation-only
decision round.
