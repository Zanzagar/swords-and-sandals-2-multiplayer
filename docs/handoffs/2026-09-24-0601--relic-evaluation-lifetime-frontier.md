# Handoff — Relic evaluation lifetime isolated before binding

**Session ID:** `16cc13a0-2974-4bc1-ac0d-ff506f4a78a1`
**UTC:** 2026-09-24 06:01:29
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, the living head, and `$ss2-progression-design`.

## Owner decision and current position

The owner selected **RCS-03A-C, complete two-dialect transformation**.
State-supporting Relics may materially transform remembered condition, and
every boundary-supporting Relic may materially transform remembered evaluation-
boundary attunement. C113-A definition purity remains fixed: no definition
receives a state-by-boundary transformation cross-product.

That selection is recorded only in the worksheet and master register. The
authoritative decision record remains unchanged.

The sole active owner choice is now `RCS-03B1` under `SR-04`: pre-completion
semantic evaluation-lifetime topology. Present only this card and wait for the
owner's A/B/C answer.

## Second prerequisite correction

The first 2026-09-24 register amendment correctly separated transformation
domain, binding, combat-evidence sampling, cause, agency, collision, and output
identity. It still treated “binding” as one question and thereby assumed an
undefined cross-cut pending evaluation.

The source-boundary audit proved seven independent consequence boundaries:

1. whether any player-semantic evaluation may remain authoritatively pending
   across pre-completion semantic cuts;
2. if so, what opens it and whose authority or precommitment is required;
3. which Relic-side revision binds to that pending evaluation;
4. if a later legal in-flight Relic-context change exists, whether the pending
   evaluation preserves, rebinds, or terminates; and
5. whether a participant may cancel or disrupt it, with what consent and
   no-result disposition;
6. what automatic expiry and maximum player-semantic horizon govern it; and
7. whether pending evaluations may coexist or overlap.

The old `RCS-03B` parent alias is therefore replaced by `RCS-03B1`–`RCS-03B7`.
One 25-slot row became seven, so the frozen register now contains 31 slots.
Before the RCS-03A answer the twice-amended potential would have been 29; after
RCS-03A-C, current `Phi_SR = 28`.

**Agent correction:** the first prerequisite repair still overcompressed this
boundary. Its draft next card compared opening snapshot, later adjudication,
and invalidate-on-change as though those were parallel alternatives. Opening
snapshot and invalidation share a bind cut; invalidation adds a separate
survival rule. The draft also conflicted with C133-A/C134-A if read after
provisional completion. It was discarded before presentation. An interim four-
row correction then still cited opening, cancellation, and expiry without
routing them; the three missing conditional rows were restored before owner
presentation. The seven independent rows above replace it at every operative
instruction.

Tuple atomicity is derived rather than selectable. One evaluation must read the
exact persistent Relic instance, definition/version, definition-appropriate
mutable coordinate, Bound Soul resonance, and input/payoff Charm presence from
one co-temporal authoritative revision. Contingent combatant `H`/`S` evidence
remains separate for `RCS-03C`.

## Active RCS-03B1 card

The card classifies the subset `K^{pending}_v` of supported relationship tags
that have at least one real cross-cut pending-evaluation occurrence before
provisional completion `q`:

- **A — cut-atomic evaluations only (recommended):** the subset is empty. A
  relationship may be constructed over time through `H`/`S`, but no separate
  evaluation object remains pending before the decisive cut.
- **B — every supported tag is pending-capable:** the subset is the whole tag
  domain. Every tag has at least one legal ritual-like occurrence that opens,
  survives a later meaningful cut or decision, and then completes or ends.
- **C — atomic and pending-capable tags coexist:** the subset is nonempty and
  proper. Some tags support Vow-like pending attempts; others always listen and
  resolve atomically.

Recommend A. The selected system already has temporal texture through history,
live state, break/renew/rearm, transformation, and distinct recommit
occurrences. A preserves deliberate setup but avoids a second queue of half-open
rituals, expiry/cancellation grief, stale-attunement questions, and concurrency
UI. B is the strongest honest countercase if visible ritual-thread play is
intended to become central.

C133-A and C134-A still govern at and after provisional completion: no branch
creates a post-completion response or revocation window. C135 recommits are
fresh occurrences, not one evaluator continuing through multiple commits.

If A is selected, RCS-03B2–B7 close by derivation or pruning and RCS-03C becomes
next. If B or C is selected, RCS-03B2 initiation becomes next, followed by
RCS-03B3 binding before evidence sampling; B4–B7 remain separate conditional
slots.

The complete definitions, examples, exhaustive subset proof, ideal-fit
classification, costs, proof burdens, and deferrals are immediately before
“Session protocol and evidence” in
`docs/design/endless-progression-owner-packet.md`.

## Files changed in this session

- `docs/design/endless-progression-master-closure-index.md` — recorded
  RCS-03A-C, replaced RCS-03B with seven atomic rows, and corrected register and
  potential arithmetic.
- `docs/design/endless-progression-decision-rigor-audit.md` — recorded the
  second owner-visible register amendment and derived tuple atomicity.
- `docs/design/endless-progression-owner-packet.md` — recorded RCS-03A-C,
  corrected the frontier pointer, and added the complete RCS-03B1 card.
- `HANDOFF.md` — corrected each operative current continuation and count in the
  living head.
- this handoff.

After the owner requested durable Git hygiene, the pre-existing one-at-a-time
edit in `.agents/skills/ss2-progression-design/SKILL.md` was preserved and the
skill gained one narrow persistence rule: validate and make an atomic local
commit after each changed owner round; pushing still requires explicit owner
authority.

## Authority and validation

The authoritative decision record was not edited. Its expected SHA-256 is
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
No implementation, runtime, schema, wrapper, capture, installation, save, or
snapshot work was performed. Runtime tests are unnecessary for this
documentation/process-only round.

Validation passed:

- structural/index validator — 20 ordered `SC-*` cards with all seven
  mandatory fields, 12 ordered `SR-*` gates, 31 registered `RCS-*` slots,
  computed `Phi_SR = 28`, and exactly one active Relic card, `RCS-03B1`;
- relative-link validator — all 47 relative links in the master index resolve;
- authoritative decision-record SHA — unchanged at the value above;
- changed-document trailing-whitespace scan — no findings; and
- `git diff --check` — pass.
