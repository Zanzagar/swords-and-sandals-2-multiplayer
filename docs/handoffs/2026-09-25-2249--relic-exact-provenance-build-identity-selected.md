---
handoff:      2026-09-25-2249--relic-exact-provenance-build-identity-selected
written:      2026-09-25 22:49 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    /root/b2b_atomicity; /root/b2b_dependencies; /root/b2b2_gameplay_clean
branch:       design/endless-progression-owner-packet
commits:      4c39c76..HEAD
suite:        3,642 tests / 3,627 passed / 0 failed / 15 expected fresh-tree skips; handoff-navigation 7/7; design-record and register checks passed
supersedes:   2026-09-25-1902--relic-repeated-provenance-support-selected
---

# Handoff — exact-provenance Relic build identity selected

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`. The sole active Relic frontier is
`RCS-03C3B3A`; present that one card only.

## Where things stand

The owner selected `RCS-03C3B2B2-B`. Exact configured provenance `Pi` now
fully determines pure-build identity: every distinct currently admissible
equal-`Pi` candidate belongs to one canonical class, including candidates with
different semantic source forms. The class replaces its member identities in
the pure-build projection, carries their deduplicated complete union lineage,
and is not a new additive causal root. Member origin, authorization, payment,
accounting, semantic content, outcome, history, and lineage remain distinct.

Membership is prospective, reflexive, symmetric, and transitive. Joins are
atomic and idempotent; same-cut cohorts settle before the coherent evidence
read, and late members cannot rewrite settled results. Coarsening refreshes no
history, prolongs no state, resurrects no evidence, and opens no readiness.
SR-07 owns source-revision boundaries; RCS-15/SR-11 owns class lifetime.

A fifteenth prerequisite audit then split old B3 into non-counting parent B3,
`RCS-03C3B3A` for global addressable atom-layer support, and conditional
`RCS-03C3B3B` for same-lineage parent/child co-addressability. Supporting both
layers somewhere in the catalog does not determine whether one lineage can be
addressed at both scales. B4 owns the partition among semantic children and
cannot answer that separate gameplay question.

The amended fifty-eight-slot register has 26 `SCREEN`, 1 `OWNER-OPEN`, 7
`PRUNED`, 21 `DIR-SELECTED`, 1 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`;
`Phi_SR = 27`. The authoritative decision record remains unchanged because a
bare option label selects direction but does not complete acceptance.

## Highest-value next work

Present B3A's three global addressability-layer policies:

- A: parent representatives only. Canonical causal roots and exact-`Pi`
  classes may furnish positions; meaningful child applications remain
  identifiable proof/content but are not separate atoms. B3B and B4 prune.
- B: semantic child applications only. Concrete authored target/effect/
  application manifestations furnish positions; their parent roots/classes
  remain operative lineage and grouping identities but cannot furnish
  positions themselves. B3B prunes and B4 follows.
- C: both layers have reachable functional support somewhere in the catalog.
  This does not imply same-lineage overlap; B3B decides that next, then B4.

Recommend B. It makes every furnishing atom a meaningful application while
the parent or exact-`Pi` class remains mechanically operative through complete
lineage and independence at the same position. That is a direct local
Achintya Bheda Abheda fit and avoids a second addressability language. State
the real costs: B4 must author a complete meaningful-child frontier, candidate
and explanation burden rises, sources with no meaningful child are excluded,
and direct whole-root/class recipes and root-level nesting disappear.

Use the held examples. Under A, the Bastion class may pair once with unrelated
Heat while its qualifying Guard remains content/proof of that class atom.
Under B, a later B4-valid Defiant Guard child or Twinstrike returning-cut child
may pair with Heat while its parent cannot. If several children qualify, B3A
does not choose the proof or UI. Under C, a Bastion parent witness and an
unrelated Twinstrike-child witness establish both layers without proving that
one lineage exposes both.

## Hard rules

- Resolve exactly one prerequisite-ready owner choice, then recompute, commit,
  and push before presenting another.
- A parent representative is only the upstream canonical identity: one B2A
  causal root or one B2B2 exact-`Pi` class in pure-build evidence. It is never
  additive and exposes no member roots alongside the class.
- Every semantic child must be an authored, player-meaningful realized
  application, never an alias, field, packet, callback, animation marker,
  phase/build view, storage write, or resolver microstep.
- Every child retains complete applicable proof lineage. A class child inherits
  the class's full union lineage, not just the visible member's lineage.
- One authoritative A4 active support basis settles prospectively before Relic
  matching and cannot vary by Relic, recipe, result, payoff, or claim. A fact
  or child carries continuity lineage plus every member lineage of that basis.
- Intersecting lineages and one shared nonmultiplying authorization/accounting
  parent cannot independently furnish two positions of one relationship
  result.
- B3A creates, splits, merges, succeeds, or promotes no root and grants no
  fan-out, claim, reuse, payoff, persistence, release, or implementation
  authority.
- Do not edit the authoritative decision record without an explicit
  `accepted`, `rejected`, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Traps from this round

- Old B3 was not atomic. “Both layers supported” hid globally disjoint support
  versus at least one same-lineage dual-scale source; those permit different
  recipes and counterplay even though lineage rules block double occupancy.
- B3A-A has the narrowest layer vocabulary, not necessarily the fewest atoms.
  A parent predicate may still inspect a meaningful application without making
  that application the atom.
- B3A-B does not promise one uniquely selected child or one UI presentation.
  Proof selection and display retain downstream owners.
- B3A-C has the widest layer vocabulary, not necessarily the most total atoms
  or builds, and same-lineage ambiguity exists only if later B3B selects it.
- A nonempty union only proves the all-empty branch infeasible. Selecting A or
  B still obliges the completed catalog to realize every accepted temporal/
  basis source cell; B relies on B4 to demonstrate that frontier.
- An A4 fact cannot choose whichever minimal support basis best matches the
  current Relic. The authoritative basis is source state, selected before
  matching, and every basis member stays in its proof lineage.

## Verification and Git

Three clean read-only rechecks report no remaining atomicity, dependency,
lineage, or gameplay flags after the corrections above. The register reparses
to the stated 58-row distribution and `Phi_SR = 27`; the authoritative
decision record retains SHA-256
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
`git diff --check` passes, the targeted handoff-navigation suite passes 7/7,
and the full serial suite passes with 3,642 tests: 3,627 passed, 0 failed, and
15 expected fresh-tree skips. Commit this round atomically, push the feature
branch, verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
