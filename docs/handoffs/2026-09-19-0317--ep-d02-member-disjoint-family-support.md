# Handoff — overlap coverage selected; member-disjoint family support next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-19 03:17:25
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.102: whether none, some, or every union-locked
definition structurally supports two member-disjoint complete noncore map
families. The owner selected C3c.101-C. Preserve the accepted EP-D02 wording
until a complete replacement is replayed and explicitly accepted. Do not
implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting there. Separate states,
keys, definitions, catalog entries, labels, or cosmetics that respectively
supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate standalone-game expansion, audit, and adaptation
pass. Vanilla evidence and the byte-identical measurement oracle remain in
their SS2 lane.

## Selected direction — EP-D02-C3c.101-C

Every completed exact key in every union-locked definition has at least one
same-name partner that changes every supported Soul's noncore map while
preserving and reassigning at least one exact complete-family member. Other
common edges may still use member-disjoint families.

Tight floors remain six definitions, nine exact keys, and fifty-four six-Soul
rows. Supported-Soul counts remain `{6,8,9,10,12,14,15}` and tight maximum
degree remains six. At each qualifying edge, the same-Soul and shared-member
relations are partial; universal exact-key coverage is aggregate. The answer
does not require every common edge to overlap or connect the overlap-edge
graph. The authoritative decision record remains unchanged.

## Frontier audit

Structural support for member-disjoint complete families precedes actual
behavior. Without it, a later absence of completed member-disjoint common edges
would conflate deliberate authoring with mathematical incapability—the same
support/behavior error corrected earlier in this design tree.

For each locked definition `d`, let

`F^{adm}_v(d)={M(x):x in A^{adm}_v(d)}`

and

`D^{cap-disj-M}_{locked,v}={d in D^{U=}_{locked,v}: exists F,G in F^{adm}_v(d) with F intersect G empty}`.

Disjoint families are automatically unequal, and no Soul can retain an exact
map between assignments using them. They are hypothetical families, not
completed keys or edges of `W^S_v(d)`. “Complete-family replacement” is only
structural shorthand: stable Relic identity, core, exact union, and all atomic
cell labels remain, while predicate and receipt semantics are undecided.

Connectedness remains lateral because graph paths are static comparisons, not
transitions. Predicate/receipt continuity and controlled substitution remain
gameplay-material siblings, but require exact-key cause, comparability, and an
authorized comparison boundary before they can be stated honestly.

## Active owner choice — EP-D02-C3c.102

```text
SUPPORT-POSITIVE SIX-SOUL GEOMETRY — degree 6, profile (2,2,2)
core h omitted; U={a,b,c,d,e}

M0={abc,abd,cde}
M1={abc,abe,cde}   M0/M1 share abc,cde
Mx={abe,acd,bcd}   M0/Mx are member-disjoint

The authored C101 pair may remain M0/M1. Mx stays hypothetical.

SUPPORT-NEGATIVE EIGHT-SOUL GEOMETRY — degree 5, profile (2,2,2,2)
Every family chooses four of the six two-label maps over U={a,b,c,d}.
Any two families share at least 4+4-6=2 members.
```

### A — no locked definition supports complete-family replacement

`D^{cap-disj-M}_{locked,v}` is empty. Every two admissible unequal complete
families in every locked name share an exact map member. Any possible all-Soul-
changing family comparison therefore retains an exact relational motif.

A creates the cleanest motif-survival covenant and best protects the standing
ideal at the exact-family-member boundary. It preserves compact eight-Soul
geometry and prunes a latent branch. Its costs are excluding six- and nine-Soul
catalogs and forbidding a potentially dramatic total-family-reweave contrast.
Tight floors are `6/9/72` at eight Souls; maximum degree is five. Supported
Souls are `{8,10,12,14,15}`.

### B — support-positive and support-negative definitions coexist

`D^{cap-disj-M}_{locked,v}` is a nonempty proper subset. Some names can
legally replace every complete-family member while others cannot.

B could reserve total-reweave potential for an advanced metamorphic archetype,
but creates a hidden geometry subtype before any behavior exists. It first
coattains at ten Souls, needs distinct positive and negative locked names, and
excludes six, eight, and nine Souls. Tight floors are `7/11/110`; maximum
degree is six. Supported Souls are `{10,12,14,15}`.

### C — every locked definition supports complete-family replacement — recommended

`D^{cap-disj-M}_{locked,v}=D^{U=}_{locked,v}`. Every locked name's geometry
admits both C99's overlap-bearing pair and a pair of member-disjoint complete
families.

C101-C already gives every authored simulacrum an actual continuity-bearing
foil. C therefore adds room for an optional total-reweave comparison without
creating motif-migration orphans. It preserves the six-Soul floor, avoids a
support-only subtype, and gives later content the widest contrast vocabulary.
Costs are universal degree-six geometry, exclusion of eight Souls, a larger
proof surface, and latent-content risk unless a later actual-behavior rule
earns the capacity. Tight floors remain `6/9/54`; supported Souls are
`{6,9,10,12,14,15}`.

## Exhaustive boundary

The complete degree-at-most-six classification is:

| Souls | C99-positive locked `(degree,k,rho)` geometries | Support signs |
| ---: | --- | --- |
| 6 | `(6,4,3)` | positive |
| 8 | `(5,3,4)` | negative |
| 9 | `(6,4,3)` | positive |
| 10 | `(5,3,5)`; `(6,4,3)`; `(6,4,5)` | negative; positive; positive |
| 12 | `(5,3,4)`; `(6,4,3)`; `(6,4,6)` | negative; positive; negative |
| 14 | `(5,3,4)`; `(6,3,7)`; `(6,4,3)`; `(6,4,7)` | negative; negative; positive; negative |
| 15 | `(5,3,4)`; `(5,3,5)`; `(6,4,3)`; `(6,4,5)` | negative; negative; positive; positive |

Only `(6,4,3)` and `(6,4,5)` admit disjoint families. They have respectively
30 legal families with 270 overlapping/165 disjoint pairs and 222 legal
families with 24,435 overlapping/96 disjoint pairs. All other displayed tuples
have zero disjoint pairs. Six and nine Souls force positive support, eight
Souls forces negative support, and both signs first coexist at ten.

Support adds no completed key. A and C use one two-key locked name at their
respective roster floors. B uses a positive and negative two-key locked name,
adding one definition and two keys to the inherited outside-locked base.
C101-C forces the sole pair in a two-key name to be overlap-bearing even when
its geometry supports disjoint families.

Fixed-key fit remains direct. C102 support is neutral and its prevalence
aggregate. A later actual member-disjoint comparison would be partial at the
broad stable-definition/core/union boundary because atomic vocabulary
continues while every Soul map and complete family member changes. It is
neutral at exact-family-member continuity because no member survives. C is
recommended for optional gameplay contrast and the six-Soul floor, not because
latent capacity better satisfies the ideal. A is the strongest thematic
countercase.

C102 selects no completed member-disjoint edge, actual definition prevalence,
every-common-edge overlap, every-pair rule, graph connectedness, overlap count,
family or assignment, semantic continuity, controlled substitution,
comparability, C39 witness, transformation, disclosure, agency, value,
sidegrade quality, power, persistence, release, or implementation. If support
survives, actual participation must be asked inside the support-positive domain
so incapability is not confused with deliberate nonuse.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C101-C
as selected and C102 as the sole active owner choice. The authoritative
decision record is unchanged. EP-D01, EP-D02, and EP-D07 remain accepted;
EP-D04 directions remain unaccepted, R9.4 remains reopened, R10.1-A and
R10.2-A remain selected, and R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Three bounded read-only
checks independently confirmed frontier order, terminology, tuple signs,
floors, roster exclusions, and support-versus-actual factorization. One caught
that hypothetical families must not be called `W` edges and that “rupture”
would overclaim unresolved semantics.

Validation passed: whitespace checks passed for tracked and current untracked
design artifacts; the authoritative decision record has no diff; C102 is the
sole active owner-packet heading; all three floor rows match the supported Soul
sets; family enumeration and boundary witnesses passed; the SVG parses as XML;
no Unicode format controls were found; and this is the newest handoff. Tests
were not run because this was a documentation-only design round.
