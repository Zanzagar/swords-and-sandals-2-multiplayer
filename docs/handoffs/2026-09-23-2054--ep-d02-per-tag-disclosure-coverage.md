# Handoff — Both forms exist globally; per-tag disclosure coverage next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-23 20:54:55
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.157: any-form per-dual-tag pre-choice disclosure
coverage. The owner's latest `A` selected C3c.156-A and nothing in C157. This
remains Soul Relic design, specifically the player-information surface of the
selected same-opportunity, one-hinge result-rearm grammar. Preserve accepted
EP-D02 wording until a complete normative replacement is replayed and
explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready bounded choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Use A/B when the
selected prerequisites leave only two genuine directions. Do not invent a
third option or batch participation, overlap, history/path breadth, opportunity
saturation, chooser authority, and value.

## Selected direction — EP-D02-C3c.156-A

Retain the actual-command-holder observation contract `O^-_u(w,d)` and the
form-legible pair sets `Lambda_P(w,d)` and `Lambda_R(w,d)` from C156. Private
AI policy/model/script/evaluator state, post-choice-only feedback, external
guides, empirical suspicion, generic labels, and another seat's uncommunicated
information do not count. A qualifying role must be lawfully human-
controllable.

The selected A makes

`K^{pre-form}_v={P,R}`.

The completed dual-tag catalog has at least one qualifying predicate pair and
one qualifying recurrence pair whose legal arms and form-specific
interventions are truthfully distinguishable to their actual command-holders
before commitment. The two witnesses may use different tags, histories,
opportunities, pairs, or human-controllable roles.

At each qualifying pre-choice boundary, continuing operative context and one
real causal distinction coexist **directly**. Its later relation to provisional
completion at `q2` and the singleton hinge remains **partial**; excluding total
catalog opacity is **neutral/protective**; and two-form catalog support is
**aggregate** because the witnesses may be separate.

C156-A adds no tag, history, opportunity, pair, continuation, cut, receipt-
width, menu, or visible-button floor. The tight candidate catalog remains one
dual tag plus both mechanical specialists. Every fixed catalog and width floor
remains unchanged.

## Frontier correction

The earlier provisional statement that two-form per-tag overlap was next
skipped a coarser atom. C143 could ask mechanical overlap directly because
C141-A had already made every tag's mechanical form set nonempty. C156-A fixes
only the disclosure catalog union `{P,R}`. One mechanically dual tag may still
expose `{P,R}`, `{P}`, `{R}`, or nothing.

Asking “both versus not both” immediately would collapse a partially explained
Relic and a wholly opaque Relic into one negative class. C157 therefore first
resolves whether every dual tag participates in disclosure at all.

## Active owner choice — EP-D02-C3c.157

For each `k in D^{dual}_v`, define

`K^{pre-form}_v(k)={F in {P,R} : there exist w in W_v(k) and
d in D^{live}(w) with J^{both-op}(w,d) and Lambda_F(w,d) nonempty}`

and

`E^{pre+}_v={k in D^{dual}_v : K^{pre-form}_v(k) is nonempty}`.

C156-A makes `E^{pre+}_v` nonempty and makes the union of these per-tag
support sets `{P,R}`. Only two branches remain.

### A — every dual tag exposes at least one form — recommended

`E^{pre+}_v=D^{dual}_v`. Every mechanically dual tag has at least one legal
joint opportunity with a qualifying P or R pre-choice account. A tag may
expose only one form; A does not yet require both.

At each qualifying `O^-` boundary, continuing operative context and one real
causal distinction coexist **directly**. Its later relation to `q2` and the
hinge remains **partial**; excluding zero-form tags is **neutral/protective**;
and universal tag coverage is **aggregate**.

A ensures no mechanically dual Soul Relic is wholly inscrutable. Its costs are
one explanation/localization/validation obligation per dual tag and less
occult mystery. Its explicit remaining exploit is token support: one obscure
account of only P or only R can satisfy a tag while its other form remains
hidden.

### B — disclosed and wholly opaque dual tags coexist

`E^{pre+}_v` is a nonempty proper subset of `D^{dual}_v`. At least one dual
tag exposes P or R, while another exposes neither after complete information
analysis. The positive class collectively still exposes both forms under
C156-A.

B's strongest case is an occult-archetype vocabulary: teachable covenants
coexist with simulacra known only through outcomes and experimentation.
Positive witnesses retain A's **direct** local fit. An opaque tag's mechanical
P/R isolations remain **direct** at the hidden hinge, but player-facing causal
legibility is **neutral/unsupported**; class coexistence is **aggregate**.

Costs are a hidden information taxonomy, guide/data-mining advantage, blind
first-use decisions, likely clarity or power premiums, and direct tension with
the standing ideal at each opaque player-information boundary.

## Concrete separator

```text
A — ANY-FORM COVERAGE
Ashen:      K_pre = {P}
Dreamglass: K_pre = {R}
every other mechanically dual tag has at least P or R

B — MIXED WITH A WHOLLY OPAQUE TAG
Ashen:      K_pre = {P,R}
Dreamglass: K_pre = empty
Dreamglass still has both mechanical forms at its one hinge,
but no lawful human command-holder receives an authorized P/R account.
```

## Proof, floors, anti-laundering, and deferrals

A/B are exhaustive because C156-A makes `E^{pre+}_v` a nonempty subset of
the nonempty dual domain: it is the whole domain or a nonempty proper subset.
An empty C branch contradicts C156-A.

A needs one explicit same-tag `Lambda_P` or `Lambda_R` witness for every dual
tag. B needs C156-A's global P/R witnesses plus one exact tag whose every legal
history, joint opportunity, qualifying pair, lawfully human-controllable
observation contract, and reachable authorized pre-choice reference has been
exhaustively analyzed and supplies neither form. One opaque menu or history,
failure to find a tooltip, an AI-only route, or incomplete analysis does not
prove opacity.

Cross-tag information counts only when a stable generic rule truthfully
applies to the target tag's actual legal arms. Relic names, Souls, visual
families, shared icons, post-choice effects, empirical inference, private AI
state, external guides, and another seat's uncommunicated information cannot
replace the exact tag witness. All C156 anti-laundering rules remain.

A retains the tight three-tag floor: one dual plus the predicate-only and
recurrence-only mechanical specialists. B needs one disclosure-positive and
one opaque dual plus both specialists, deriving a tight four-tag floor. Neither
branch adds a history, opportunity, continuation, pair, cut, receipt-width,
menu, or visible-button floor. An opaque tag retains the inherited mechanical
three-continuation minimum without a qualifying information account.

Both branches retain nine definitions, twenty-three exact keys, 138 Soul/key
rows at six Souls, four represented locked IDs, eight lived aligned edges,
four one-history-complete IDs, no incomplete represented ID, Souls
`{6,9,10,12,14,15}`, degree six, and C46-A's same-boundary width two.

C157 does not choose whether a participating tag exposes P, R, or both; tag
identities or prevalence; two-form overlap; same-history, every-history, same-
opportunity, or every-opportunity disclosure; shared explanation or command-
holder role; hinge legibility; authority or communication; modality,
accessibility, discoverability, feedback, preview, value, non-dominance,
payoff, power, fun, UI, release, or implementation. Under A, per-dual-tag two-
form disclosure overlap is next.

## Repository state

The owner packet, rigor audit, living head, and systems map carry C156-A and
make C157 the sole active choice. The authoritative decision record was not
edited and its SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.

This was a documentation-only design update. Validate `git diff --check`, SVG
XML parsing, one active frontier, and the authoritative-record hash. Do not run
the Node suite because no code or executable contract changed.
