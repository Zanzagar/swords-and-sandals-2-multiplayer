# Handoff — all six cells reach native dual play; per-pair breadth next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-14 04:29:29
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.52, per-pair six-cell grounding breadth for input-
Charm-free dual results. The owner selected C3c.51-A. Preserve the accepted
EP-D02 wording until a complete replacement is replayed and explicitly
accepted. Do not implement.

## Selected direction

C3c.51-A sets `G_v=C6`. Every one of the six occurrence-history/current-state/
cross-form by action-semantic/build-provenance cells must belong to at least one
valid local grounding package for a genuine receipt in some reachable empty-
input two-receipt result.

A singleton package proves independently isolatable cell grounding. A larger
inclusion-minimal package counts every genuinely necessary coupled cell without
pretending that any member works alone. Mere co-presence, correlation, raw
evaluator read sets, labels, dormant rows, duplicate predicates, and untraced
declarations count as none. Reachability remains relational rather than
deterministic, nonpredicate resolver coordinates stay fixed inside a positive
witness, and receipt identity remains local to one `J_h`.

This is catalog-wide coverage. It does not require every supported Soul/exact
operative-Relic-realization pair to support every cell or even yet require every
pair's empty-input dual to have any valid six-cell package. It selects no exact
mapping, number of contexts or receipts, package arity, cross-context receipt
identity, ease, frequency, power, payoff, access, acquisition, or
implementation. C3c.51-C's global non-six-cell-cause obligation is not
selected. This is a direction, not a complete accepted EP-D02 replacement, and
changes no authoritative wording.

## Frontier result

C3c.51-A fixes the cell projection of the native-dual grounding relation to all
six cells. C3c.50-A separately gives every exact Soul/operative-realization pair
an empty-input dual route, but does not connect every pair to the six-cell
grounding relation. One pair could carry all six witnesses while another has a
real dual whose receipt transition has no valid six-cell package.

Per-pair breadth is therefore the preferred next card. It fixes the native
footprint object before later Soul-substitution and exact-realization-
substitution stability choices. Package arity and a safely defined active-
input-Charm contribution card are independent ready siblings, but come after
this native baseline by priority rather than hard dependency.

## Sole active choice

Define

`L_v={(p,c) in P_v x C6 : some legal empty-input dual context anchored to exact p has a valid C3c.51 local package T containing c}`.

For exact pair `p=(b,s)`, let `C_v(p)` be its supported cell footprint. A
positive edge is one same-context capability witness; different edges may use
different contexts, sources, payoff-Charm states or absence, receipts, and
coupled package partners. A cell that occurs only inside a genuinely coupled
package still creates an edge; it is participation, not independent access.
C3c.51-A gives the full cell projection of `L_v`, while C3c.50-A does not give
its pair projection.

- **A — every pair reaches every cell:** `L_v=P_v x C6`. Every supported Soul
  with every exact operative Relic realization has some empty-input dual
  receipt grounded through each of the six cells. This maximizes freedom and
  makes cell-support sets substitution-stable, but imposes the full Soul ×
  exact-realization × six authoring matrix and risks homogenized or token
  routes.
- **B, recommended — every pair is grounded, with selective footprints:**
  `L_v` is proper but its pair projection is all of `P_v`. Every pair supports
  at least one cell, every cell appears globally, and at least one pair/cell
  edge is absent. This prevents hollow native duals while preserving pair
  identity. It necessarily creates at least two distinct footprints, and its
  serious cost is unequal flexibility plus later disclosure, size, stability,
  and premium-breadth safeguards.
- **C — at least one pair has no six-cell-grounded native dual:** the pair
  projection is proper. At least one exact pair has an empty footprint even
  though C3c.50-A still gives it an empty-input dual; other pairs collectively
  cover all six. This permits alien or cross-layer resolution but creates an
  opaque, potentially hollow class. C selects neither `E0` nor another resolver
  mechanism and does not imply lower power or a Charm requirement.

For B's illustration, Firebound/Ashen `R17` may support
`{H/action,X/build}`, Stonebound/Ashen `R17` `{S/build}`,
Firebound/Dreamglass `R44` `{S/action,X/action}`, and Stonebound/Dreamglass
`R44` `{H/build}`. Every pair has a foothold and the union is all six, while no
listed pair is omnilingual. Names and sets are illustrative.

A/B/C exhaust a full product, a proper relation with every pair row nonempty,
or a relation with at least one empty row. A derives degree six for every pair
and, from at least two supported Souls and one operative realization, at least
twelve pair/cell edges. B derives minimum row degree one, all six columns, at
least one missing edge, and at least two distinct footprints. C derives at
least one empty and one positive footprint. One coupled package may create
several edges, so no context or receipt count follows.

A missing edge requires complete analysis of every legal empty-input context
for that exact pair at its realization's fixed evaluation boundary, across all
fixed-payoff-state-or-absence and source alternatives, reachable `Z_h` pairs,
dual receipts, and valid packages. Failed search or incomplete dependency
analysis is unresolved. Cross-context receipt comparison is neither required
nor permitted. An owner-written replacement remains available.

This card selects no exact B/C footprint, B degree or stronger floor beyond its
minimum one, extra C minimum, variation-owning Soul/definition/realization/state
axis, substitution stability, singleton/coupled distribution or package cap,
stable receipt identity, active-Charm contribution, `E0`, resolver mechanism,
transformations, precedence, base ownership, exact content/mapping, overlap/
reuse, arbitration, power/payoff, access/acquisition, rebinding, horizons, UI,
persistence/migration, validation, release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record
C3c.51-A and identify C3c.52 as the sole active queue item. The authoritative
decision record and normative system design remain unchanged. EP-D01, EP-D02,
and EP-D07 remain accepted; EP-D04 directions remain unaccepted, R9.4 remains
reopened, R10.1-A and R10.2-A remain selected, and R10.3-R10.8 remain pending.
Continue to present exactly one A/B/C choice per turn. No code tests were run
because this was documentation and vector work only. No runtime, capture,
evidence, installation, save, or snapshot was touched. Preserve the user's
existing change to `.agents/skills/ss2-progression-design/SKILL.md`.

Three independent read-only audits passed the frontier choice, formal relation,
exhaustive topology, B recommendation, player-facing examples, coupled-package
handling, context-local receipt rule, and explicit deferrals after repairing
two wording defects: the negative search now keeps the exact realization's
evaluation boundary fixed, and the degree deferral preserves A's degree six
and B's inherited minimum one.

`git diff --check` passed. No-index whitespace checks for the untracked rigor
audit, SVG, and this handoff returned the expected different-from-`/dev/null`
status with no diagnostics. The SVG parsed with Python's standard XML library,
the newest handoff resolves to this file, and the authoritative decision and
system documents are clean.
