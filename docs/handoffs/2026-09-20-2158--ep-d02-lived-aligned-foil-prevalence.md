# Handoff — four pure Relic archetypes selected; lived aligned foils next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-21 01:58:29
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.125: whether no locked Relic name, some but not
all locked names, or every locked name has at least one aligned foil pair that
the same owner and persistent artifact can materially realize in one canonical
nonforked fixed-version legal history. The owner selected C3c.124-A. Preserve
accepted EP-D02 wording until a complete replacement is replayed and explicitly
accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the
current full-assurance pass before the separate standalone expansion, audit,
and adaptation pass. Vanilla evidence and the byte-identical oracle remain in
their own lane.

## Selected direction — EP-D02-C3c.124-A

No locked definition contains both aligned family identities:

`D^{both-Xfam}_{locked,v}=empty`.

Every locked name is overlap-only or member-disjoint-only in the aligned
projection. C123-C independently requires both identities in both C113-A
comparison rows, so the completed locked catalog has four nonempty compound
definition classes:

```text
S-O — state-only / motif migration
S-R — state-only / complete reweave
B-O — boundary-only / motif migration
B-R — boundary-only / complete reweave
```

This does not remove either identity from a key's broader common graph:
C101-C/C105-C still give every completed key an overlap-bearing and a distinct
member-disjoint common partner. The tight floor is now nine definitions,
twenty-three exact keys, and 138 Soul/key rows at six Souls. Soul counts remain
`{6,9,10,12,14,15}` and tight maximum degree remains six. Name-level exclusion
is neutral/protective at this projection; actual edges retain their inherited
partial fit. The authoritative decision record remains unchanged.

## Active owner choice — EP-D02-C3c.125

For each locked definition `d`, define

`X^{hist}_v(d)={e in X^{1cW}_v(d):there exists gamma_e satisfying the rule below}`.

`gamma_e` is one canonical nonforked fixed-version legal history in which the
same `combatantId`, `ownerCombatantId`, and persistent `soulRelicInstanceId`
have materially operative occurrences of both exact endpoints of aligned edge
`e`, in either order. Merely previewing or storing an endpoint does not count.
Rollback, copied saves, alternate reward/settlement branches, administrative
repair, cross-version migration, reacquisition, and different instance IDs do
not join endpoints. Each edge may use a different history, and endpoints need
not be adjacent.

Define

`D^{hist-X}_{locked,v}`

`  ={d in D^{U=}_{locked,v}:X^{hist}_v(d) is nonempty}`.

### A — no locked definition has a lived aligned foil

`D^{hist-X}_{locked,v}=empty`. Every aligned comparison is split across
mutually exclusive legal histories, although each endpoint is independently
legal and reachable. This is the strongest fate-bound-artifact countercase: an
owned Relic has one lineage, and alternate forms exist only as codex, planning,
or balance counterfactuals. It avoids a temporal realization obligation, but
leaves C107-C124's same-instance geometry outside lived player experience.

### B — lived and counterfactual definitions coexist

`D^{hist-X}_{locked,v}` is nonempty proper. At least one name has a lived
aligned pair and at least one has none. This can create explicit morphic and
fated artifact families, but risks a hidden premium-name taxonomy unless both
are disclosed, accessible, and strategically non-dominated. It does not choose
which of `S-O`, `S-R`, `B-O`, or `B-R` is lived.

### C — every locked definition has a lived aligned foil — recommended

`D^{hist-X}_{locked,v}=D^{U=}_{locked,v}`. Every named Relic has at least one
aligned pair encountered materially by the same owner and persistent artifact
in one lawful history. This converts each of C124-A's four archetypes from a
static comparison sheet into at least one relationship the player's Relic can
actually live through. The token-history exploit remains: one obscure, forced,
rare, one-way, or dominated history can satisfy C.

## Concrete witness, floors, and ideal ratings

Reuse C124-A's four four-key names:

```text
O-only IDs: {b0,b3} and {b1,b2}
R-only IDs: {b0,b2} and {b1,b3}
```

A gives no aligned dyad a qualifying history. B gives at least one dyad in one
name a history while keeping every dyad of another name mutually exclusive. C
gives at least one dyad in each of `S-O`, `S-R`, `B-O`, and `B-R` a qualifying
history. These are temporal classifications of existing edges, so all branches
retain `9/23/138`, the same Soul set, and degree six. At the tight floor the
minimum lived-edge counts are zero, one, and four respectively. C is only
definition incidence: in a four-key name, two keys may still lack a lived foil.

The fixed exact-key/Soul relationship remains **direct**. Every aligned edge
remains **partial** at the same-artifact/fixed-coordinate/across-snapshot
boundary. A same-history edge is also **partial** at the same-owner/same-
artifact/across-lived-history boundary: continuity and distinction meet on one
artifact but at different moments, with cause and agency still open. Empty,
mixed, or universal name prevalence is **aggregate**. A is **neutral** at the
lived boundary even though its static edges retain partial fit.

C125 selects no direct transition, adjacency, direction, reversibility,
repeatability, cause, player choice, disclosure, practical frequency, endpoint
coverage, common history across multiple edges, exact state/boundary semantics,
sidegrade value, power, persistence format, release, or implementation. Source-
evidence declaration/resolution/outcome phase and the exact key's grammar-
evaluation boundary are separate deferred axes; the former is not a
prerequisite for this existential history classification.

## Frontier discipline

Do not ask aligned partner degree next merely because it is graph-theoretically
available. C106 explicitly deferred second-partner support until an actual
second foil has a gameplay purpose; R-pure geometry can otherwise be silently
constrained. Same-history existence gives the first required foil a lived
purpose before any branching question.

After C, the natural descendant is whether every completed endpoint has a
same-history aligned partner; definition incidence deliberately comes first.
After B, first resolve which compound definition classes admit lived names.
After A, lived coverage and branching collapse; decide whether counterfactual
forms have an explicit player-facing codex/planning role or leave the lane.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling: follow direct `AGENTS.md` rules
and do not treat the overnight plan as current guidance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C125 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
