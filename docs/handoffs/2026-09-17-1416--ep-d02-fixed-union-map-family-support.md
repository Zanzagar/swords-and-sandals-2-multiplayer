# Handoff — overlapping union rotation selected; fixed-union family support next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-17 18:16:35
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.89: whether neither, exactly one, or both of the
fixed-width-capable and union-locked forced-breadth affinity classes support
changing the complete unordered singleton-map family while the exact noncore
union remains fixed. The owner selected C3c.88-A. Preserve the accepted EP-D02
wording until a complete replacement is replayed and explicitly accepted. Do
not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate standalone-game expansion, audit, and adaptation
pass. Vanilla evidence and the byte-identical measurement oracle remain in
their SS2 lane.

## Selected direction

EP-D02-C3c.88-A fixes `H^{U-disj}_v=empty`. No actually rotating fixed-width
definition supports two disjoint equal-breadth exact noncore unions. Every
actual union rotation must retain at least one noncore motif while changing
another, though the retained motif may differ by comparison pair. Capable-but-
nonrotating definitions remain outside that denominator. Tight floors stay six
definitions, eight exact keys, and forty-eight six-Soul rows. Supported-Soul
counts remain `{6,8,9,10,12,14,15}`. Fixed-key ideal fit is direct, the support
restriction neutral/protective, and every actual cross-key rotation partial.
The authoritative decision record remains unchanged.

## Active owner choice — EP-D02-C3c.89

For exact affinity key `b`, remove the stable sole core from every Soul's
singleton map and collapse duplicate rows. The resulting unordered `rho`-set
is the complete noncore map family `M_v(b)`; its union is `U_v(b)`. Family
support means some exact `U` admits at least two different legal complete
families. This forgets which Soul block receives which map, and it authors no
exact key.

Every breathing definition has family support by derivation. Let
`K^{M|U}_v` contain `fixed` and/or `locked` when that whole forced-breadth
class contains at least one support-positive definition. The fixed denominator
is the complete fixed-width-capable class, not only C87's actual rotators.

```text
SAME CORE h • SAME U={a,b,c,d} • k=3 • rho=2

family 1  {h,a,b} | {h,c,d}
family 2  {h,a,c} | {h,b,d}
```

The exact vocabulary is unchanged; what changes is which noncore motifs coexist
inside one singleton map. The pair is a structural witness only.

### A — neither forced-breadth class participates

`K^{M|U}_v` is empty. Breathing affinities retain derived support, while every
fixed-width-capable and locked name has one family per exact union. This is the
leanest taxonomy, but Veil can never express “same motifs, different
relationships.” Tight floors remain `6/8/48` and the tight maximum degree five.

### B — exactly one forced-breadth class participates — recommended

`|K^{M|U}_v|=1`. Fixed-width-capable-only versus union-locked-only is the next
A/B identity choice; C89 does not choose it. The likely later recommendation is
locked-only: Ashen already owns exact-vocabulary substitution, while Veil could
keep its vocabulary and reweave how its motifs coexist. B opens one relational
dialect without giving the same hidden axis to both forced-breadth classes.
Its cost is another follow-up and latent support that must later become visible,
selectable, material, and non-dominated. Floors remain `6/8/48`; fixed-only
requires degree six, while locked-only requires degree five.

### C — both forced-breadth classes participate

`K^{M|U}_v={fixed,locked}`. At least one definition in each class has family
support. This maximizes structural design space, but blurs Ashen and Veil:
Ashen may both replace and re-pair vocabulary, while Veil can re-pair it too.
Floors remain `6/8/48`; a degree-six fixed witness and degree-five locked
witness are required.

All branches retain Soul counts `{6,8,9,10,12,14,15}`. Every breathing tuple
is support-positive. Fixed-width support exists only at
`(q,k,rho)=(6,3,2)`. Union-locked support has both signs, with tight positive
`(5,3,2)` and negative `(3,2,2)`. The fixed witness has `N=5,u=4`, so C88-A's
`2u>N` still holds: disjoint component maps inside one family are not disjoint
exact unions across keys. Support is existential for some `U`, not every union
or breadth.

At the operative boundaries, fixed-key fit remains direct, structural support
neutral, and class allocation aggregate. A later same-definition, same-`U`,
unequal-family pair would be partial because its genuine relational difference
still occurs across separate simulacra. B is recommended for gameplay taxonomy,
not because a one-class allocation more directly realizes the ideal. A is the
strongest countercase if the project is unwilling to commit later to actual,
legible Veil reweaving.

A/B/C are exhaustive because `K` is a subset of `{fixed,locked}`. Under B,
class identity and then within-class definition prevalence come next. Under C,
prevalence is resolved separately in both classes. Only after the support
domain is complete does actual family-variation participation follow. Block-
to-map assignment remains finer, positive rotated-union overlap magnitude is
an independent sibling, and cause remains orthogonal.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C88-A
as selected and C89 as the sole active owner choice. The authoritative decision
record is unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04
directions remain unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain
selected, and R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Three bounded read-only checks
verified the support-first frontier, finite tuple classification, construction
floors, and B recommendation. The authoritative record was not edited.

Validation passed: `git diff --check`; no-index whitespace checks for current
untracked deliverables; sole-active-heading and stale-state scans; cross-document
C88-A/C89 assertions; authoritative-record no-diff; SVG XML parsing; Unicode
format-control scanning; newest-handoff ordering; and construction-floor
assertions. Tests were not run because this was a documentation-only design
round.
