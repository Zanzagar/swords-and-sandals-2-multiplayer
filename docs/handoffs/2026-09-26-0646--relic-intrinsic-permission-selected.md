---
handoff:      2026-09-26-0646--relic-intrinsic-permission-selected
written:      2026-09-26 06:46 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    one bounded read-only dependency audit with one focused follow-up, plus one strict read-only final diff verification
branch:       design/endless-progression-owner-packet
commits:      b89e7fe..HEAD
suite:        proportional serial checks 10/10 passed; register reparses 87/87 with Phi_SR 37; decision-record hash unchanged
supersedes:   2026-09-26-0623--relic-exact-route-selected
---

# Handoff — intrinsic exact-overlap permission selected

Start with `HANDOFF.md` above its archive line and
`$ss2-progression-design`. Present C2D only; D1 is independently ready but
queued by the one-card protocol.

## Where things stand

The owner selected `RCS-03C3C3C2A-A`: exact-overlap permission is intrinsic to
every applicable versioned Relic/relationship contract. No participant setting
may change the complete law for the same held-fixed selected exact pair.
C2B/C2C1/C2C2 are pruned. This answer does not decide stochasticity or grant
joint survival.

A bounded read-only audit upheld those prunings and broke two dependencies in
the planned next step. D1 distinct-related exposure is already decidable from
the selected tuple before permission, so it is ready but queued rather than
dependent on C2E. Conditional D2 requires D1-B and its own permission-law
screen, not C2E; exact-first traversal is only a normalization choice.

C2D probability must also condition on all earlier committed authoritative
state regardless of random origin. A persisted random acquisition choice among
deterministic variants cannot be averaged into a permission draw. Measure
`pi_w(o)` immediately before one designated permission realization after every
binding, fact, tuple/pair, and prior committed state is fixed.

The register remains 87 rows: 36 `SCREEN`, 1 `OWNER-OPEN`, 13 `PRUNED`, 33
`DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`; `Phi_SR = 37`. C2D is
the current presented card. The authoritative decision record remains
unchanged.

## Highest-value next work

Record the owner's C2D answer, then give C2E its required fresh prerequisite
audit before writing incidence options:

- A: every canonical complete intrinsic exact-permission law is deterministic
  across its reachable domain. It may still map different factual contexts to
  different certain bits. Recommend A for causal Relic/relationship identity,
  team planning, replay trust, and policy comparison without removing combat
  or acquisition RNG.
- B: every such law is stochastic-capable on at least one reachable fixed exact
  opportunity. Not every opportunity must roll.
- C: deterministic-only and stochastic-capable complete laws coexist.

For A, Ashen may always allow shared Heat when both covenant marks remain
intact and always deny it otherwise. For B, every applicable law has some fixed
case like a committed 70/30 Ashen judgment. For C, deterministic Stone Witness
and stochastic-capable Ashen Oracle law classes coexist.

## Hard rules and traps

- C2D classifies complete laws, not catalog averages, whole-contract mixtures,
  or individual fixed opportunities.
- Earlier random state is fixed input. A fresh designated allow/deny
  realization is C2D randomness. Hidden information and opaque deterministic
  hashes do not create stochasticity.
- A law random in one context and deterministic elsewhere is
  stochastic-capable. A law mapping different contexts deterministically to
  zero and one remains deterministic.
- A hybrid exact-and-related pair has one opportunity, probability, draw at
  most, and bit. Denial cannot redraw, retry, or reroute.
- G2 retains joint draw coupling across several distinct simultaneous pair
  opportunities; C2D settles one-pair law marginals only.
- C2E's fresh audit must decide whether constant versus context-responsive
  chance is owner-facing topology or AUTHOR/SPEC/TUNE work.
- D1 is ready but queued. Do not call C2E its prerequisite. D2 is conditional
  on D1-B and its own fresh screen, not on C2E.
- Do not edit the authoritative decision record without an explicit accepted,
  rejected, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Verification and Git

Confirm the register reparses to 87 rows with 36 `SCREEN`, 1 `OWNER-OPEN`, 13
`PRUNED`, 33 `DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`;
`Phi_SR = 37`. Confirm the decision-record SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
Commit the answer atomically with a `Co-Authored-By` trailer, push only the
feature branch, verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
