# Handoff — Dual paths saturated; specialist polarity next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-23 00:48:47
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.146: whether every specialist fixed tag is
predicate-only, every specialist is recurrence-only, or both focused
polarities coexist. The owner selected C3c.145-A. Preserve accepted EP-D02
wording until a complete replacement is replayed and explicitly accepted. Do
not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Finish the current
full-assurance pass before the standalone expansion/audit/adaptation pass.

## Selected direction — EP-D02-C3c.145-A

For each dual tag `k`, let `B^{both}_v(k)` be its legal histories containing
both C141-qualified independent forms. C145-A makes
`B^{both}_v(k)=W_v(k)` for every dual tag: every legal adjacent same-tag
recommit history contains at least one `I_P` and at least one `I_R`
opportunity.

The two forms may use different actions, ordering, opportunities, and `chi`
boundaries. C145-A does not require one joint opportunity or same-cut effect.
Each `chi` is **direct**; two forms in one fixed-tag history are **partial**;
excluding single-form dual paths is **neutral/protective**; universal tag-and-
path saturation is **aggregate**.

Every branch retains `9/23/138`, four represented IDs, eight lived edges, four
complete IDs, no incomplete represented ID, Souls `{6,9,10,12,14,15}`, degree
six, and C46-A's same-boundary maximum of two. The authoritative decision
record remains unchanged.

## Why specialist polarity is next

C143-C left the nonempty specialist complement's class composition unresolved.
This is a coarser tag-taxonomy question than same-opportunity co-location,
which the packet expressly identifies as a finer rule. Continuous-context
breadth is orthogonal. Both remain ready siblings, but neither should bury the
older specialist-class choice.

## Active owner choice — EP-D02-C3c.146

Define

`S^{spec}_v=K^{candidate}_v setminus D^{dual}_v`,

which is nonempty under C143-C, and partition it into

- `S^P_v={k in S^{spec}_v : K^{ind}_v(k)={Pred}}`; and
- `S^R_v={k in S^{spec}_v : K^{ind}_v(k)={Rec}}`.

C141-A makes every tag's form set nonempty, and nonduality excludes
`{Pred,Rec}`, so these classes are disjoint and exhaustive. Every path of a
predicate specialist is predicate-only; every path of a recurrence specialist
is recurrence-only.

Here `predicate-only` and `recurrence-only` refer only to C141's independently
attributable forms. The other projection may remain fixed background state or
participate in an attribution-bundled interaction; bundled comparisons remain
legal for either specialist.

Positive membership needs a supported-form witness plus complete proof that
the other form is absent from that tag. Proving an entire polarity class absent
requires complete classification of every specialist tag. Failed search is
unresolved.

### A — predicate specialists only

`S^P_v=S^{spec}_v`; `S^R_v` is empty. Every specialist path is predicate-only.
Recurrence still occurs on every C145-A dual path, but no recurrence-only path
or specialist exists.

A is the strongest simplicity countercase. Focused play teaches one intuitive,
independently attributable bond-rebuilding grammar, while independent
manifestation authorization belongs to the richer dual relationship and
cannot become a standalone permission-button specialist. Recurrence may still
be fixed background state or participate in a bundled specialist interaction.
Each specialist `I_P` is **direct** at `chi`; excluding recurrence specialists
is **neutral/protective**; dual histories are **partial**; specialist
uniformity is **aggregate**. Costs are no focused independently attributable
invocation grammar and a risk that the Simulacra half feels secondary.

Example: Q and every specialist are Rekindle/Vent predicate-only on every
path. Every dual P path still contains both independent forms.

### B — recurrence specialists only

`S^R_v=S^{spec}_v`; `S^P_v` is empty. Every specialist path is recurrence-only.
Predicate isolation still occurs on every dual path, but no predicate-only
path or specialist exists.

B is the invocation-first mirror. Focused play independently attributes
whether a simulacrum may manifest again, while independently attributable bond
rebuilding belongs to the dual grammar. Predicate facts may still be fixed
background state or participate in a bundled specialist interaction. Each
specialist `I_R` is **direct** at `chi`; excluding predicate specialists is
**neutral/protective**; dual histories are **partial**; specialist uniformity
is **aggregate**. Costs are no focused independently attributable relationship-
repair grammar and a risk that Soul bonds feel like background state.

Example: R and every specialist are Invoke/Bank recurrence-only on every path.
Every dual P path still contains both independent forms.

### C — both specialist polarities coexist — recommended

`S^P_v` and `S^R_v` are both nonempty. At least one predicate specialist and
one recurrence specialist coexist with the nonempty saturated-dual class.

C gives **Souls and Simulacra** a clean three-part independent-attribution
vocabulary: bond rebuilding without `I_R`, manifestation authorization without
`I_P`, and both forms participating within every dual history. Bundled
interactions may still involve both projections on any class. Separate tags do
not themselves realize the standing ideal. Each actual `chi` is **direct**;
specialist exclusion is **neutral/protective**; C145-A's within-history dual
participation is **partial**; class coexistence is **aggregate**. Costs are two
teaching languages, disclosure/testing and polarity-balance burden, false
progression-tier risk, and token class support through one obscure tag of
either polarity.

Example: every P path is both-form; Q is predicate-only on every path; R is
recurrence-only on every path. These need not be three Relic definitions.

## Compatibility and deferrals

A/B/C exhaust the recurrence-specialist subset being empty, the whole
nonempty specialist domain, or a nonempty proper subset. A produces only
predicate-only and both-form paths in the candidate catalog. B produces only
recurrence-only and both-form paths. C guarantees all three path types.

C requires two distinct specialist tags plus one dual tag and is feasible
exactly at the inherited three-tag floor. A/B need only the already-derived
nonempty specialist class; the third tag can be another dual or same-polarity
specialist. A later dual-subtype coexistence choice may raise the combined tag
minimum and must recompute it then. No branch changes the fixed catalog floors
or adds a receipt, history, opportunity, result, same-opportunity, or same-
`chi` requirement.

C146 selects no specialist identities, counts or ratios, distinct-definition
or distinct-Soul representation, mapping to Relics, Souls, Charms, sources,
rarity, or power, accessibility, visibility, control, frequency, anti-token
thresholds, same-opportunity co-location, `chi` equality/order, deliberate mode
choice, continuous context, predicate internals, retained sources, evaluator
schedule, actor, causation strength, cadence, payoff, persistence, UI, release,
or implementation. Same-opportunity participation and continuous-context
breadth remain independent ready siblings.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C146 only. Record
the selected branch, recompute the frontier, and present exactly one next
prerequisite-ready choice. Do not infer acceptance of the whole EP-D02
replacement or start implementation.
