---
handoff:      2026-09-26-0501--relic-joint-responsive-routing-selected
written:      2026-09-26 05:01 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    three bounded read-only B3 audits plus one read-only diff verifier
branch:       design/endless-progression-owner-packet
commits:      82756fa..HEAD
suite:        proportional serial checks 10/10 passed; register reparses 78/78 with Phi_SR 38; decision-record hash unchanged
supersedes:   2026-09-26-0449--relic-single-ledger-tuple-selected
---

# Handoff — joint-responsive Relic routing selected

Start with `HANDOFF.md` above its archive line and
`$ss2-progression-design`. The sole active Relic frontier is
`RCS-03C3C3B3A`; present that one card only.

## Where things stand

The owner selected `RCS-03C3C3B2-B`, requiring at least one reachable
held-fixed witness where another contender changes a tag's selected proof
route or marginal law. A twenty-first prerequisite amendment replaced old B3
with material participant-configurability prevalence B3A and conditional
policy-language/completion B3B. The register has 78 rows and `Phi_SR = 38`;
the authoritative decision record remains unchanged.

## Highest-value next work

Record the owner's B3A answer, then audit the resulting next row before
presenting it. B3A classifies routing-capable versioned Relic selector
contracts:

- A: no contract exposes a material participant routing setting;
- B: every contract does; recommend this only with the later small,
  Relic-specific closed-attunement direction; or
- C: system-fixed and participant-configurable contracts coexist.

Under A, prune B3B and open B4A. Under B/C, open B3B. Do not answer actor,
consent, information, binding horizon, defaults, policy vocabulary,
randomness, permission, or results inside B3A.

## Hard rules and traps

- Material configurability requires two lawful prospectively bindable settings
  whose tuple laws differ on one reachable held-fixed upstream product.
  Equipping the Relic, changing upstream evidence, accepting a sole default,
  or choosing a cosmetic setting does not count.
- A system composer, canonical tie-break, or total completion rule does not
  erase a genuinely material participant coordinate.
- The complete selector remains prospective, automatic, and total. No live
  prompt, veto, `none`, partial tuple, retry, or downstream-aware switch is
  legal.
- B2's cross-tag response is input scope, not participant authority. B4A still
  owns stochastic support and B4B owns draw coupling.
- The recommendation for B3A-B is conditional, not a hidden B3B answer. If the
  later policy language becomes broadly programmable, recommend B3A-A instead.
- Do not edit the authoritative decision record without an explicit accepted,
  rejected, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Verification and Git

Confirm the register reparses to 78 rows with 37 `SCREEN`, 1 `OWNER-OPEN`, 8
`PRUNED`, 28 `DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`;
`Phi_SR = 38`. Confirm the decision-record SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
Commit the answer atomically with a `Co-Authored-By` trailer, push only the
feature branch, verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
