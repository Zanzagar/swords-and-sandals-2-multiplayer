# Handoff — one key-complete instance per name; universal represented-instance coverage next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`  
**UTC:** 2026-09-20 01:40:39  
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.111: whether no, some, or every union-locked Soul
Relic definition gives every represented persistent instance-ID fiber—and thus
every completed exact key—a same-ID one-coordinate partner. The owner selected
C3c.110-C. Preserve the accepted EP-D02 wording until a complete replacement
is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy, not
theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the current
full-assurance pass before the separate standalone expansion/audit/adaptation
pass. Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.110-C

For every union-locked definition `d`,

`D^{exists-IdK}_{locked,v}=D^{U=}_{locked,v}`.

Every locked Relic name contains at least one represented persistent instance
ID `r` whose complete key fiber satisfies
`K^{1c}_v(d,r)=B_{v,r}(d)`: every completed key carried by that ID has some
family-distinct same-ID partner holding exactly one of input-affecting state and
grammar-evaluation boundary fixed. Other represented fibers may remain
positive-but-incomplete.

Tight floors remain six definitions, eleven exact keys, and sixty-six six-Soul
rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree remains
six. A one-coordinate relationship is partial at the same-artifact/one-
coordinate-continuous boundary; complete-fiber incidence and universal
definition prevalence are aggregate. C110-C covers neither every represented
instance nor every key and grants no cause, path, access, disclosure, or
agency.

## Independent frontier and construction checks

Three write-nothing reviews independently checked the next card:

- the frontier audit found no missing intermediate quantifier or support card;
- the math audit proved that all represented fibers being key-complete is
  **exactly equivalent** to global completed-key one-coordinate coverage,
  because fibers partition the key set and qualifying pairs cannot cross IDs;
- the gameplay audit recommended C and identified B as the strongest
  countercase when systematic and compound-specialist name languages are
  explicit and valuable.

C107-C gives every represented fiber at least two keys. Any finite fiber of
size at least two can be covered: use dyads when even, or one three-key path
plus dyads when odd. The inherited family geometry therefore needs no separate
support decision.

The tight A/B/C floors were independently re-derived as `6/12/72`, `7/16/96`,
and `6/11/66`. A fiber-hybrid definition needs a complete two-key fiber and a
positive-but-incomplete fiber of at least three keys. The previously verified
fifth family `M4={abc,bcd,ade}` preserves both C101 overlap-bearing and C105
member-disjoint partner coverage, so the five-key witness has no inherited
geometry collision.

## Active owner choice — EP-D02-C3c.111

Define

`D^{all-IdK}_{locked,v}={d in D^{U=}_{locked,v} : Ids^{K1c}_v(d)=Ids_v(d)}`.

Membership means every represented instance fiber is key-complete,
equivalently every completed locked exact key has some same-ID one-coordinate
partner. “Global” refers only to the fixed-version completed locked domain, not
unrepresented inventory records.

```text
FIBER-HYBRID — satisfies C110-C, but not global key coverage
R17:
  b0 = state0 / boundaryA
  b1 = state0 / boundaryB   <- R17 complete

R44:
  b2 = state1 / boundaryC
  b3 = state1 / boundaryD   <- qualifying pair
  b4 = state2 / boundaryE   <- uncovered

ALL-FIBER-COMPLETE — four-key floor
R17:
  b0 = state0 / boundaryA
  b1 = state0 / boundaryB

R44:
  b2 = state1 / boundaryC
  b3 = state1 / boundaryD   <- both dyads complete
```

### A — no locked name is globally key-covered

`D^{all-IdK}_{locked,v}` is empty. C110-C still gives every name a complete
fiber, so every name also contains a positive-but-incomplete fiber. This
preserves one compound specialist snapshot in every name, but forces per-copy
taxonomy and premium/dud-form risk. A minimum `[2,3]` hybrid gives tight floors
`6/12/72`.

### B — globally covered and fiber-hybrid names coexist

`D^{all-IdK}_{locked,v}` is nonempty and proper. B is the strongest gameplay
countercase if systematic teaching names and compound-specialist names are
disclosed, accessible, strategically valuable, and non-dominated. Without
those later rules it creates hidden name taxonomy plus same-name instance
uncertainty. One full four-key name and one hybrid five-key name give tight
floors `7/16/96`.

### C — every locked name is globally key-covered — recommended

`D^{all-IdK}_{locked,v}=D^{U=}_{locked,v}`. Every represented persistent
artifact and completed simulacrum gets some same-ID one-coordinate foil. C
closes C110's token-exemplar and per-copy incidence-lottery loopholes while
allowing diagonal pairs. Its costs are universal partner authoring and
validation, possible artificial foil forms, loss of compound-only specialist
keys, and a remaining one-obscure-partner-per-key loophole. Complete `[2,2]`
attains tight floors `6/11/66`.

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 6 | 12 | 72 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 16 | 96 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 6 | 11 | 66 at six Souls | 6 |

At one fixed exact-key/Soul relationship, inherited fit remains **direct**.
Same-ID cross-snapshot contrasts remain **partial** at the broad persistent-
artifact boundary. One-coordinate pairs remain **partial** at the narrower
same-artifact/one-coordinate-continuous boundary. An uncovered key is
**neutral/protective** there. Every-fiber/global coverage and catalog prevalence
are **aggregate**. C is recommended for systematic participation, not a
stronger direct doctrinal rating.

C111 selects no state-only versus boundary-only support, both-type requirement,
exact fiber topology, partner degree, every-pair rule, graph connectedness,
edge alignment, cause, path, access, disclosure, agency, value, power,
persistence format, migration, release, or implementation.

## Preserved state and verification

The owner packet, living head, rigor audit, SVG, and this handoff record C110-C
as selected and C111 as the sole active owner choice. The authoritative
`docs/design/endless-progression-decisions.md` remains unchanged. EP-D01,
EP-D02, and EP-D07 remain accepted; EP-D04 directions remain unaccepted, R9.4
remains reopened, R10.1-A and R10.2-A remain selected, and R10.3-R10.8 remain
pending. Preserve the owner's existing uncommitted skill edit. No
implementation, commit, or push occurred. This docs-only turn ran no test
suite; structural checks covered patch whitespace, sole-active-card status,
SVG XML parsing, authoritative-record immutability, and control characters.
