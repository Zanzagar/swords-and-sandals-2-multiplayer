# Handoff — Every recommit path continuous; same opportunity next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`  
**UTC:** 2026-09-23 04:13:12  
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.149: same-opportunity dual-form participation. The
owner selected C3c.148-A. Preserve accepted EP-D02 wording until a complete
normative replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Finish the current
full-assurance pass before the standalone expansion/audit/adaptation pass.

## Selected direction — EP-D02-C3c.148-A

`U^{all-cont}_v=K^{candidate}_v`: every fully specified legal adjacent same-tag
recommit history keeps its fixed context operatively available in every
authoritative semantic state from post-`c1` through pre-`q2`. Relationship
truth may change, and context outages before or after the measured slice remain
open.

Each C141 `chi` remains **direct**; the same context across the changing
temporal relationship is **partial**; in-slice outage exclusion is **neutral/
protective**; and universal tag/path prevalence is **aggregate**. No history-
count or total-tag floor is added. All fixed catalog floors remain unchanged.

C148-A creates no same-opportunity witness, but every later positive same-
opportunity actual history is automatically continuous inside the measured
slice. It says nothing about a counterfactual arm's continuity through `q2`.

## Why same-opportunity participation is next

C145-A puts both independent forms on every legal path of every dual tag but
allows them to occur at distinct decisions. Tag-level same-opportunity
participation precedes per-path prevalence, matching C144 participation before
C145 saturation. Same-`chi` and simultaneous-effect questions remain finer.

## Active owner choice — EP-D02-C3c.149

For dual tag `k`, legal history `w`, and live/false opportunity `d`, let
`Pi_P(w,d)` and `Pi_R(w,d)` contain C141-qualified chosen-arm pairs proving
`I_P` and `I_R`, respectively, at their own `chi`. Define

`J^{both-op}(w,d) iff Pi_P(w,d) is nonempty and Pi_R(w,d) is nonempty`

and

`E^{both-op}_v={k in D^{dual}_v : some legal w,d has J^{both-op}(w,d)}`.

One semantic opportunity means one authoritative pre-choice snapshot and one
atomic selection. UI grouping cannot merge separately committed choices, and
cosmetic UI steps cannot split one atomic choice. A fused pair changing both
predicate facts and recurrence control proves neither form. A positive
opportunity needs two distinct pairs sharing the actual continuation and using
two distinct counterfactual continuations; duplicate aliases count once.

```text
SAME OPPORTUNITY
actual Converge = Rekindle + Invoke
vs Invoke Only:   recurrence equal; Heat differs -> I_P at chi-P
vs Rekindle Only: Heat equal; authorization differs -> I_R at chi-R

SEPARATED OPPORTUNITIES
d-P: Rekindle vs Vent -> I_P
later d-R: Invoke vs Bank -> I_R
```

### A — every dual tag has a same-opportunity witness — recommended

`E^{both-op}_v=D^{dual}_v`. Every dual tag has at least one actual legal
history with a semantic opportunity supporting both forms through distinct
pairs. Other paths may separate them.

At shared `d`, continuing context and two distinct marginal forms co-
participate **directly**. Each `chi` is **direct**; unequal decisive/effect cuts
are **partial**; separated paths are **neutral**; and universal dual-tag
participation is **aggregate**. Costs are one dense three-continuation
opportunity per dual tag, artificial or dominated-alternative risk, and a token
loophole if the only joint opportunity is obscure.

### B — every dual tag's independent forms are opportunity-separated

`E^{both-op}_v` is empty. No opportunity on any legal dual history supplies
both independent forms. C145-A therefore forces `I_P` and `I_R` onto at least
two distinct opportunities on every dual path. Attribution-bundled decisions
may still change both projections while proving neither form.

This permits the strongest staged-ritual countercase: repair the bond, then
authorize manifestation through two clearer, potentially interruptible binary
decisions. It does not require every other decision to be binary or forbid a
fused bundled ritual. Each `chi` is **direct**; independent-form participation
across distinct opportunities is **partial**; co-location exclusion is
**neutral/protective**; and universal independent-form separation is
**aggregate**. Content using the staged realization may add latency/menu
friction, and no single decision supplies both independently attributable
forms.

### C — same-opportunity and independent-form-separated dual tags coexist

`E^{both-op}_v` is nonempty proper. At least one dual tag has a joint
independent-form opportunity; complete analysis proves another has none and
always separates its `I_P` and `I_R` forms. Attribution-bundled decisions remain
legal in either class.

Local witnesses retain A/B's ratings; class coexistence is **aggregate**.
Costs are another dual subtype, disclosure/testing burden, flexibility or
power-premium risk, and token positive membership through one obscure joint
opportunity. Specialists remain outside C149's domain.

## Proof, floors, and deferrals

A/B/C exhaust the nonempty dual domain. Positive membership needs one legal
history, one semantic opportunity, and two distinct complete-projection pairs.
Negative membership requires exhaustive proof over every history, opportunity,
and qualifying pair; failure to find the second pair remains unresolved.

A positive opportunity derives at least three semantic continuations—the
actual arm and two distinct counterfactuals—but not necessarily three visible
buttons. A/B retain the tight three-tag floor: one dual plus both specialists.
C requires two duals plus both specialists and therefore derives four candidate
tags. No branch forces multiple histories per tag. B forces at least two
qualifying opportunities on every dual path.

All branches retain nine definitions, twenty-three exact keys, 138 Soul/key
rows at six Souls, four represented locked IDs, eight lived aligned edges, four
one-history-complete IDs, no incomplete represented ID, Souls
`{6,9,10,12,14,15}`, degree six, and C46-A's same-boundary width two.

C149 chooses no identities, per-path joint prevalence, same-`chi`, simultaneous
effects, effect order, deliberate mode choice, additional attribution-bundled
choices, exact counts beyond derived floors, actor, access, strategic value,
cadence, payoff, persistence, UI, release, or implementation. Under A/C, same-
opportunity path prevalence follows;
under B, recompute the frontier.

The authoritative decision record hash remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
