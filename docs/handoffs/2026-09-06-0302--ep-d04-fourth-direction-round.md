# Handoff — EP-D04 fourth direction round pending

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-06 07:02:01
**Branch:** `design/endless-progression-owner-packet`

Resume both EP-D04 R4 questions in the owner packet. Preserve accepted EP-D01,
EP-D02, and EP-D07 and every selected-but-unaccepted EP-D04 direction. Do not
edit the authoritative decision record or implement anything.

## Session outcome

The owner's `I agree with your rec` selected the three R3 recommendations; it
did not accept a final EP-D04 replacement:

1. one unassisted paid Circuit with one immutable attempt per encounter is the
   primary combat-ceiling episode; defeat or Concede fails it, while Rematch,
   Assistance, Recovery, and learning remain separate robustness views;
2. the optimized all-Legendary higher endpoint ceiling must hold independently
   in 1v1, 2v2, and 3v3; and
3. every format needs real mixed-rarity strict wins, while Tempered and
   Inscribed keep separate causal guarantees across the combined endpoint grid
   rather than each receiving a forced niche in every format.

## Recomputed frontier

A read-only adversarial check tested the next dependency before it was shown to
the owner. It confirmed that route admission must precede cell identity and
found one additional independent ambiguity: in 2v2/3v3, an all-Legendary claim
could name one focal gladiator or the entire allied composition. The same team
could otherwise change rarity classification solely by rotating the focal
seat.

R4 therefore contains two choices:

1. **Route-admission boundary:** `A` (recommended) gives both families the same
   family-neutral precommitted route for the primary combat gate and retains a
   separate versioned offer-set/`pi_offer` player-experience view that cannot
   rescue a failed paired gate; `B` lets each policy select a possibly different
   route in the primary gate; `C` omits offer-policy evaluation. The paired
   route must be selected by a preregistered rule that cannot inspect either
   candidate policy.
2. **Team rarity claimant:** `A` (recommended) classifies the whole allied
   composition; `B` classifies one focal gladiator against frozen/rotated
   reference allies; `C` requires both gates independently. Under A, every one
   of each ally's sixteen endpoint hosts must be Legendary for an
   all-Legendary team, while a strict mixed claimant contains a causally active
   Tempered or Inscribed payload. Team Tactic remains outside rarity. This
   requires compatible all-Legendary 2v2/3v3 compositions under team budgets,
   not merely one legal personal all-sixteen loadout.

Recommendations are `1A, 2A`. After these answers, define paired ordered-Circuit
and offer-set fingerprints with nested encounter cells and seeds as replicates.
Only after cell identity can weighting authority be chosen. Numeric coverage,
Legendary lead/concentration bounds, ties, solver sufficiency, unresolved-cell
treatment, migration/release authority, and final replay remain later.

## Verification

`git diff --check` passed after this handoff. The authoritative decision record
has no diff. No runtime code, tests, vanilla evidence, licensed assets,
installation, save, snapshot, capture, candidate, fixture, observation,
manifest, or golden changed. Tests were not run for this docs-only owner round.
No commit or push was made. Implementation remains blocked.
