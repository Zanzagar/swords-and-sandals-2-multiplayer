# Handoff — Complete cut sets match; common-cut multiplicity next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`  
**UTC:** 2026-09-23 15:41:17  
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.155: equal-multicut joint-opportunity support. The
owner selected C3c.154-A. This remains Soul Relic design, specifically same-tag
result-rearm topology. Preserve accepted EP-D02 wording until a complete
normative replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Ground abstract
terms in the Soul Relic player loop whenever needed. Finish the current full-
assurance pass before the standalone expansion/audit/adaptation pass.

## Selected direction — EP-D02-C3c.154-A

Retain

`J^{equal-chi}(w,d) iff Chi_P(w,d)=Chi_R(w,d)`

and let `K^{asym-chi}_v` contain the dual tags with any live joint opportunity
that fails `J^{equal-chi}`.

The selected A makes `K^{asym-chi}_v` empty. At every live joint opportunity
on every legal history of every dual tag, `Chi_P=Chi_R`. Every exact actual-
anchored canonical decisive cut supported by one independently isolated form
is supported by the other. Equality requires neither equal pair multiplicity
nor a bijection and does not force a singleton set.

At every supported boundary, continuing relationship context and both
independent forms coexist **directly**; each isolation remains **direct**;
multicut structure, cross-cut pair combinations, downstream effects, and the
larger recurrence remain **partial**; excluding one-form-only cuts is
**neutral/protective**; and universal opportunity/path/tag equality is
**aggregate**.

C154 adds no tag, history, opportunity, pair, continuation, receipt-width,
menu, or cut floor. The tight candidate catalog remains one dual tag plus the
predicate-only and recurrence-only specialists. Every fixed catalog and width
floor remains unchanged.

## Why common-set cardinality is next

C154-A gives every joint opportunity one nonempty common cut set. The set may
still contain several boundaries. Because both pair sets are nonempty, every
predicate/recurrence pair combination aligns exactly when that common set is a
singleton. Singleton support and all-pairs alignment are therefore one owner
choice, not two.

This support question precedes multicut distance, visibility, value, or non-
dominance because C155-A would leave their multicut domain empty. Generic
materiality is already supplied by C137/C140/C141 for each qualifying pair;
C155 itself remains evaluator topology and proves no visibility or fun.

## Active owner choice — EP-D02-C3c.155

For every joint opportunity define

`Chi_PR(w,d)=Chi_P(w,d)=Chi_R(w,d)`,

`X^{multi-chi}_v(k)={(w,d) : w in W_v(k), d in D^{live}(w),
J^{both-op}(w,d), and |Chi_PR(w,d)|>=2}`,

and

`K^{multi-chi}_v={k in D^{dual}_v : X^{multi-chi}_v(k) is nonempty}`.

### A — one common decisive cut everywhere — recommended

`K^{multi-chi}_v` is empty. Every live joint opportunity has a singleton
common set. Equivalently, every qualifying predicate/recurrence pair
combination at that opportunity maps to the same exact actual-history
canonical boundary. Different opportunities may use different cuts.

At that hinge, continuing context and every independently predicate- and
recurrence-isolating alternative coexist **directly**; each isolation remains
**direct**; downstream effects and the larger recurrence remain **partial**;
excluding multicut scheduling is **neutral/protective**; and universal
opportunity/path/tag singleton coverage is **aggregate**. Costs are exhaustive
singleton proof, prohibition of deliberately staged Ignition/Seal comparison
timing within one opportunity, burst-like semantic pressure, and token one-
hinge compliance. This does not guarantee visibility, value, or fun.

### B — every dual tag supports an equal-multicut opportunity

`K^{multi-chi}_v=D^{dual}_v`. Every dual tag has at least one live joint
opportunity whose equal common set contains two or more exact cuts. Other
opportunities may remain singleton.

B's strongest case is temporal resonance: both independent forms can be
isolated at Ignition and both again at Seal around one actual choice. At each
cut, continuing context and both forms coexist **directly**; their temporal
relationship across cuts is **partial**; and universal per-tag multicut support
is **aggregate**. Costs are a five-continuation witness, dense or artificial
foils, timing opacity, earlier-completion pressure, and token stages unless
later player-meaning rules justify them. Multiple cuts are not multiple player
decisions or effect times.

### C — singleton-only and multicut-capable tags coexist

`K^{multi-chi}_v` is a nonempty proper subset of `D^{dual}_v`. At least one
dual tag has a singleton common set at every joint opportunity, while another
has an equal-multicut witness.

C creates single-hinge and temporally resonant Soul Relic archetypes. The
singleton class inherits A's **direct** hinge fit; each multicut boundary
inherits B's **direct** local fit while cross-cut participation remains
**partial**; class coexistence is only **aggregate**. Costs are another timing
subtype, likely clarity or tempo premiums, universal singleton proof for one
class, witness proof for the other, and balance pressure between simpler and
denser opportunity grammars.

## Concrete separator

```text
ONE HINGE
actual Deliberate Convergence
    vs Invoke Only   -> predicate isolation at Seal
    vs Rekindle Only -> recurrence isolation at Seal
Chi_P = Chi_R = {Seal}

TWO HINGES
actual Deliberate Convergence
    early Invoke/Rekindle foils  -> both forms at Ignition
    sealed Invoke/Rekindle foils -> both forms at Seal
Chi_P = Chi_R = {Ignition, Seal}
```

The multicut case has five semantic continuations at one opportunity: the
actual arm plus four distinct foils. It does not imply five visible buttons,
two player decisions, or composed counterfactual worlds.

## Proof, floors, anti-laundering, and deferrals

A/B/C are exhaustive because every common set is nonempty and therefore
singleton or multicut. Exact cardinality above two is downstream, not a fourth
branch.

A needs complete `Pi_P` and `Pi_R` enumeration at every live joint opportunity
on every legal dual history and universal singleton proof. One aligned pair is
insufficient. B needs one fixed-field witness per dual tag with two distinct
exact cuts, each supported by an explicit predicate pair and recurrence pair.
C combines exhaustive singleton-only proof for one class with a multicut
witness for the other. One pair cannot count at two later boundaries because
its `chi` is its unique earliest decisive cut. Incomplete analysis remains
unresolved.

Delayed evaluation, phase labels, ordinals, frames, timestamps, callbacks, or
UI events cannot manufacture exact boundary identity. Pairs cannot be stitched
across opportunities or histories; aliases and duplicate traces count once;
a fused pair proves neither form; analytical cross-product pairing does not
compose two action substitutions into one world.

Keep combatant, Bound Soul, exact Relic realization, Charms, source
configuration, occurrence contract, evaluation boundary, and version fixed.
Changing a field cannot establish B/C support.

A adds no floor: one history, one opportunity, one cut, and the inherited
three semantic continuations remain feasible. B retains the three-tag minimum,
but every multicut witness derives at least two cuts, four distinct qualifying
pairs, and five semantic continuations. C requires two dual tags plus both
specialists and therefore a tight four-tag minimum; its multicut witness has
the same local floor. No branch adds a history or opportunity minimum.

Every branch retains nine definitions, twenty-three exact keys, 138 Soul/key
rows at six Souls, four represented locked IDs, eight lived aligned edges, four
one-history-complete IDs, no incomplete represented ID, Souls
`{6,9,10,12,14,15}`, degree six, and C46-A's same-boundary width two.

C155 does not choose one global cut identity, pair multiplicity, exact
cardinality above two, multicut prevalence, distance/actionable intervals,
visibility, chooser authority, value, non-dominance, fun, effect timing/order,
response, UI, payoff, release, or implementation. Under A, multicut-specific
questions are moot and the cut-topology ladder can yield to the remaining
player-semantic frontier. Under B/C, multicut player meaning precedes finer
prevalence.

## Repository state

The owner packet, rigor audit, living head, and systems map carry C154-A and
make C155 the sole active choice. The authoritative decision record was not
edited and its SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.

This was a documentation-only design update. Validate `git diff --check`, SVG
XML parsing, a single active frontier, and the authoritative-record hash. Do
not run the Node suite because no code or executable contract changed.
