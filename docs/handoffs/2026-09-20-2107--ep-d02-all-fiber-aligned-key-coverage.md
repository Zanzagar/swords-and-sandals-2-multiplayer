# Handoff — one complete carrier selected; every artifact/form next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-21 01:07:27
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.121: whether no, some but not all, or every union-
locked Soul Relic definition gives every represented persistent artifact—and
therefore every completed exact key—an aligned partner carrying the
definition's sole one-coordinate comparison type and changing every supported
Soul's map. The owner selected C3c.120-C. Preserve accepted EP-D02 wording
until a complete replacement is replayed and explicitly accepted. Do not
implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the
current full-assurance pass before the separate standalone expansion, audit,
and adaptation pass. Vanilla evidence and the byte-identical oracle remain in
their own lane.

## Selected direction — EP-D02-C3c.120-C

For represented ID `r` of locked definition `d`, let `K^X_v(d,r)` contain the
completed keys incident to an internal edge in

`X^{1cW}_v(d)=A^{1coord}_v(d) intersection W^S_v(d)`,

and let `Ids^{KX}_v(d)` contain the IDs for which that endpoint set is the
whole fiber. The owner selected

`D^{exists-IdKX}_{locked,v}=D^{U=}_{locked,v}`.

Every locked Relic name now has at least one represented persistent artifact
whose every completed form has an aligned partner. Other represented
artifacts may remain aligned-positive but incomplete. This closes definition-
level absence, not per-artifact or global completed-key coverage.

Tight floors remain seven definitions, fifteen exact keys, and ninety six-Soul
rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree remains
six. Each actual aligned edge is partial at the same-persistent-artifact/fixed-
coordinate/across-snapshot boundary; complete-fiber incidence and universal
definition prevalence are aggregate. The authoritative decision record
remains unchanged.

## Active owner choice — EP-D02-C3c.121

Define

`D^{all-IdKX}_{locked,v}={d in D^{U=}_{locked,v}:Ids^{KX}_v(d)=Ids_v(d)}`.

The represented fibers partition `B_v(d)`, and every aligned edge is same-ID
because `A^{1coord}_v(d)` is. Therefore class membership is equivalent to

`for every b in B_v(d), there exists b' in B_v(d) with {b,b'} in X^{1cW}_v(d)`.

C120-C makes `Ids^{KX}_v(d)` nonempty in every locked definition but does not
make it equal to `Ids_v(d)`. Counts or shares of complete fibers are finer
descendants; no coarser incidence card is missing.

### A — no locked definition is globally aligned-key-covered

`D^{all-IdKX}_{locked,v}` is empty. C120-C still gives every name at least one
complete carrier, so every name is fiber-hybrid: it also has an aligned-
positive but key-incomplete represented artifact.

This is the strongest principled countercase if every name deliberately
combines a self-contained teaching carrier with a visible, accessible,
agentic, non-dominated compound artifact. Otherwise it mandates per-copy
comparison asymmetry and a premium-carrier/dud-artifact lottery under
permanent one-copy-per-name ownership.

### B — globally covered and fiber-hybrid names coexist

`D^{all-IdKX}_{locked,v}` is nonempty and proper. At least one name gives every
represented artifact full aligned-key coverage, while another retains a
complete carrier plus an incomplete artifact. This can support systematic and
specialist name languages, but otherwise adds hidden taxonomy, can correlate
with the state/boundary name split, and risks a premium-name caste.

### C — every represented fiber is aligned-key-complete — recommended

`D^{all-IdKX}_{locked,v}=D^{U=}_{locked,v}`. Equivalently, every completed
locked exact key has a same-persistent-artifact partner carrying the
definition's sole one-coordinate type and changing every supported Soul's
assigned map.

C closes C120's token-carrier, premium-copy, and orphan-form loopholes. Every
represented artifact and form participates in the clean comparison language,
while diagonal and other nonaligned comparisons remain legal. Costs are
universal aligned-foil authoring and validation, possible artificial or padded
snapshots, less incarnation-level asymmetry, tiny-fiber laundering, and a
remaining one-obscure-partner-per-key loophole. C supplies no common access or
path between forms.

## Verified witnesses and floors

The tight fiber-hybrid name is C120's `[2,4]` separator:

```text
R17={b3,u1}             aligned and key-complete
R44={b0,u0,u2,u3}       typed b0--u0 is common
                         typed u2--u3 is not common
```

R44 is aligned-positive through `b0--u0`, but `u2,u3` have no aligned partner.
Their same-coordinate foil retains a Soul map, while their whole-roster foils
are diagonal. Internal common edges still cover every key, and all six keys
retain inherited overlap-bearing and member-disjoint common partners.

The tight full name reuses C105's aligned `[2,2]`:

```text
R17={b0,b2}             aligned member-disjoint dyad
R44={b1,b3}             aligned member-disjoint dyad
```

The overlap-bearing partition `{b0,b3}/{b1,b2}` also works. Coordinate
decoration supplies either the state-only or boundary-only definition class
without manufacturing the excluded comparison type.

A complete fiber needs at least two keys. C120 proved that an aligned-positive
but incomplete fiber needs at least four: C113-A makes its sole-type graph a
disjoint union of fixed-coordinate cliques, C111-C excludes singleton blocks,
and three keys therefore force a triangle in which C117-C's common cover also
covers every aligned endpoint. Consequently the tight hybrid is `[2,4]`.

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 7 | 19 | 114 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 17 | 102 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |

The inherited outside base is five definitions and seven keys. C113-A requires
distinct state-supporting-only and boundary-supporting-only locked definitions.
A uses two six-key hybrids, B one six-key hybrid and one four-key full name,
and C two four-key full names. The verified witnesses attain the bounds at
every retained Soul profile without changing the family palette or requiring
degree above six.

Independent read-only frontier, construction, and gameplay audits confirmed
that C121 is the correct next atom, the equivalence with global completed-key
coverage is exact, the `[2,4]` and `[2,2]` witnesses preserve all inherited
geometry, and C is recommended while A is the strongest principled
countercase.

## Ideal ratings and exclusions

At one fixed exact-key/Soul relationship, inherited fit remains **direct**. An
actual aligned edge is **partial** at the same-persistent-artifact/fixed-
coordinate/across-snapshot boundary. An aligned-uncovered form is **neutral/
protective** only at the combined intersection: its typed edge remains
**partial** at the narrow fixed-coordinate boundary, while its diagonal whole-
roster edge is **partial** at the broader artifact boundary and **neutral** at
the omitted fixed-coordinate projection. Fiber completeness, completed-key
incidence, and definition prevalence are **aggregate**. C improves systematic
participation; it does not upgrade the relationship to direct.

C121 selects no partner count or degree, endpoint multiplicity, every-pair
rule, matching, connectedness, common anchor, or exact fiber count/size; no
requirement that overlap-bearing and member-disjoint partners are both aligned
and no selection of which family relation supplies an aligned partner; no
exact state/boundary semantics, cause, path, transition, access, disclosure,
agency, ease, value, power, non-dominance, persistence, release, or
implementation. It quantifies only over represented completed-domain IDs, not
every inventory instance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C121 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
