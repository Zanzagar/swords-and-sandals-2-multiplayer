# Handoff — full joint matrix selected; definition coexistence next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-21 01:41:57
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.124: whether both actual aligned family identities
coexist under no locked definition, some but not all locked definitions, or
every locked definition. The owner selected C3c.123-C. Preserve accepted EP-
D02 wording until a complete replacement is replayed and explicitly accepted.
Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the
current full-assurance pass before the separate standalone expansion, audit,
and adaptation pass. Vanilla evidence and the byte-identical oracle remain in
their own lane.

## Selected direction — EP-D02-C3c.123-C

The owner selected the full occupied joint product:

`J^X_v={state,boundary} x {overlap,member-disjoint}`.

Both overlap-bearing motif migration and member-disjoint complete-family
reweaving occur somewhere under state-only comparison, and both occur
somewhere under boundary-only comparison. Comparison axis therefore does not
dictate family-change depth at catalog scope.

This does not require both identities under one definition, artifact, fiber,
key, endpoint, or owned instance. Tight floors remain seven definitions,
fifteen exact keys, and ninety Soul/key rows at six Souls. Soul counts remain
`{6,9,10,12,14,15}` and tight maximum degree remains six. Four-cell breadth is
aggregate; individual relationships keep their inherited direct, partial, or
neutral ratings. The authoritative decision record remains unchanged.

## Active owner choice — EP-D02-C3c.124

Define

`D^{both-Xfam}_{locked,v}`

`  ={d in D^{U=}_{locked,v}`

`      : X^O_v(d) is nonempty and X^R_v(d) is nonempty}`.

“Both” means at least two distinct aligned edges. One edge cannot be both
overlap-bearing and member-disjoint. Because C121-C makes every definition
aligned-positive, each definition outside this set is O-only or R-only in the
aligned projection.

```text
                         MOTIF MIGRATION       COMPLETE REWEAVE
A — NO NAME HAS BOTH     S-O name              S-R name
                         B-O name              B-R name

B — SOME NAMES HAVE BOTH S-OR name             same S-OR name
                         B-O name              B-R name

C — EVERY NAME HAS BOTH  S-OR name             same S-OR name
                         B-OR name             same B-OR name
```

### A — no locked definition has both — recommended

`D^{both-Xfam}_{locked,v}` is empty. Every locked name has exactly one aligned
family identity, and C123-C forces all four compound classes `S-O`, `S-R`,
`B-O`, and `B-R` to be nonempty.

A turns the already-complete global matrix into four stable, learnable Relic
archetypes. It continues C113-A's definition-pure language and avoids making
every name satisfy the same versatility checklist. Costs are the largest
content floor, name-level segregation, and potentially having to change Relic
names to change family-change grammar. Its purity applies only to aligned
edges; C101-C/C105-C still give every key both common-edge identities in the
broader graph.

### B — some but not all locked definitions have both

`D^{both-Xfam}_{locked,v}` is a nonempty proper subset. At least one name is
dual and at least one is single-identity. B supports specialist and versatile
Relics together, but has the strongest hidden-premium risk: without later
access and value controls, dual names may read as complete and single names as
lesser. It does not choose which comparison row contains either class.

### C — every locked definition has both

`D^{both-Xfam}_{locked,v}=D^{U=}_{locked,v}`. Every name has at least one O
edge and one distinct R edge along its sole comparison type.

C is the strongest countercase if every Relic should be an internally rich
microcosm, able to host both surgical motif migration and radical reweaving.
Costs are per-name complexity, homogenization, and a token-minority-edge
loophole. It still provides no access or player choice between the modes.

## Concrete witnesses and tight floors

C105's four-key geometry has O edges `01,03,12`, R edges `02,13`, and one
noncommon edge `23`. Use:

```text
O only: IDs {b0,b3} and {b1,b2}
R only: IDs {b0,b2} and {b1,b3}
OR:      one ID containing b0,b1,b2,b3
```

For an `S-OR` name, hold boundary fixed and use four distinct states. For a
`B-OR` name, hold state fixed and use four distinct boundaries. The typed
graph is `K4`; its five common edges contain all three O edges and both R
edges, and cover every key.

| Branch | Tight witness | Definitions | Keys | Rows at six Souls |
| --- | --- | ---: | ---: | ---: |
| A | `S-O + S-R + B-O + B-R` | 9 | 23 | 138 |
| B | `S-OR + B-O + B-R` | 8 | 19 | 114 |
| C | `S-OR + B-OR` | 7 | 15 | 90 |

The totals include the inherited five-definition/seven-key outside base. Every
locked definition needs four keys. B cannot use only two locked definitions:
C113-A requires one in each comparison row, and C123-C would make both dual,
contradicting a proper subset. All branches retain Soul counts
`{6,9,10,12,14,15}` and tight maximum degree six.

## Ideal ratings, exclusions, and next dependency

The fixed exact-key/Soul relationship remains **direct**. Each aligned edge is
**partial** at the same-artifact/fixed-coordinate/across-snapshot boundary. An
O edge is additionally **partial** at exact surviving-member continuity; an R
edge is **neutral** at exact-member continuity and **partial** at the broader
artifact/core/union/vocabulary boundary. Definition-level exclusion or
coexistence is **neutral/protective** or **aggregate**, never direct. A is
recommended for gameplay legibility, not because separating names is a closer
doctrinal analogue.

C124 selects no per-artifact, per-fiber, per-key, per-owned-instance, or same-
edge coexistence; no counts, shares, frequency, access, path, choice, semantics,
value, power, persistence, release, or implementation. “Migration” and
“reweaving” describe static comparisons and do not select causal operations.

After A, dual-definition local descendants are impossible; move to another
prevalence/topology/access axis. After B, choose which comparison-type rows
contain dual definitions before row-local prevalence. After C, the next local
descendant is empty/proper/full prevalence of definitions having at least one
artifact fiber that internally contains both O and R aligned edges; universal
per-artifact and per-key dual coverage remain finer.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling: follow direct `AGENTS.md`
rules and do not treat the overnight plan as current guidance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C124 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
