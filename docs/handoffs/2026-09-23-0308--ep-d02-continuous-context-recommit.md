# Handoff — Both specialist polarities selected; continuous context next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-23 03:08:54
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.147: continuous operative-context recommit
support. The owner selected C3c.146-C. Preserve accepted EP-D02 wording until a
complete replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Finish the current
full-assurance pass before the standalone expansion/audit/adaptation pass.

## Selected direction — EP-D02-C3c.146-C

Let `S^{spec}_v=K^{candidate}_v setminus D^{dual}_v` and partition it into
predicate-only `S^P_v` and recurrence-only `S^R_v` specialists. Selected C
makes both classes nonempty beside the nonempty C145-A saturated-dual class.
The completed catalog therefore contains predicate-only, recurrence-only, and
both-form legal paths in C141's independent-attribution sense.

`-only` does not forbid the other projection from remaining fixed background
state or participating in an attribution-bundled interaction. Each qualifying
`chi` is **direct**; specialist exclusion is **neutral/protective**; two forms
within a dual history are **partial**; and three-class coexistence is
**aggregate**, not a direct local realization of the ideal. Two specialists
plus one dual attain the inherited three-tag floor. The fixed catalog floors
and authoritative record remain unchanged.

## Why continuous context is next

Continuous-context breadth and same-opportunity co-location commute and are
both ready. Continuous context goes first under coarse-before-fine discipline:
it is the older all-tag, whole-trajectory question left open by C138-A and
C140-A. Same-opportunity co-location is a finer dual-only comparison. It
remains ready after C147.

Specialist anti-token prevalence does not go next because tag count, definition
or Soul representation, player access, frequency, and strategic value are
still separate unselected authorities. A raw prevalence threshold would bundle
them.

## Active owner choice — EP-D02-C3c.147

For every candidate tag `k=(h,j)`, let `W_v(k)` be its nonempty set of fully
specified legal adjacent same-tag recommit histories satisfying C136 through
C141. For `w in W_v(k)`, let `S_w[c1,q2]` contain the authoritative semantic
post-state produced by `c1` and every later authoritative semantic state
through the actual history's input state immediately before adjudication that
produces `q2`.

Frames, animations, callbacks, polling, storage, logging, and resolver
microsteps do not enter this slice. C138's operative-availability definition
controls: `h` must remain legally instantiated and able to evaluate without
equip, summon, unsuppress, rebind, recreate, or equivalent restoration.

Define

`W^{cont}_v(k)={w in W_v(k) : h is operatively available in every state of S_w[c1,q2]}`

and

`K^{cont}_v={k in K^{candidate}_v : W^{cont}_v(k) is nonempty}`.

Continuity applies only to operative availability. It does not freeze
relationship truth, sources, `H`/`S` facts, evaluator phase, or semantic state.
Positive membership needs one complete continuous history. Nonmembership needs
complete reachable-domain proof that every legal history contains a real
outage; incomplete search is unresolved.

### A — every tag supports a continuous-context witness — recommended

`K^{cont}_v=K^{candidate}_v`. Every tag has at least one legal history in which
its fixed context remains operative from the post-`c1` state through the input
state for `q2`. Other histories may contain outages.

This permits every relationship to be lost, rebuilt, and reauthorized in
place. Each C141 isolation is **direct** at `chi`; same operative context across
the temporal loss-and-renewal trajectory is **partial**; universal support is
**aggregate**; and other outage paths are **neutral** at this projection.
Costs are an uninterrupted-path authoring/proof obligation per tag, continuous-
loop pressure, and token support through one obscure continuous history.

Example: R17 stays operative after `c1`; Heat becomes false; selected Rekindle
versus Vent isolates Heat; renewed sufficiency and authorization produce `q2`.

### B — every recommit history contains a context outage

`K^{cont}_v` is empty. Every legal history of every tag contains an
authoritative state after `c1` and before `q2` in which its fixed context is
unavailable.

B is the strongest remanifestation countercase: disappearance and return become
constitutive parts of every recommit ritual. The local `chi` remains
**direct**; persistent tag lineage across disappearance and return is
**partial**; full-trajectory operative-context continuity is **neutral/
unsupported**; and universal outage topology is **aggregate**. Costs are a
compulsory outage-plus-live-lever sequence, power-cycle pressure, and
restoration friction. C138-A still requires a separate material opportunity
while the context is live and false; the outage or restoration alone cannot
qualify.

Example: R17 becomes Silenced after `c1`, returns while Guard plus Heat is
still false, and only then does selected Invoke versus Bank supply the live
independent lever that reaches `q2`.

### C — continuous-capable and outage-required tags coexist

`K^{cont}_v` is a nonempty proper subset of `K^{candidate}_v`. At least one tag
has a continuous history; complete analysis proves another tag must suffer an
outage on every history.

C supports in-place Vows beside remanifestation-bound rites. Local isolations
remain **direct**, temporal trajectories retain their applicable **partial**
rating, and catalog coexistence is **aggregate**. Costs are another timing
taxonomy, disclosure/testing burden, likely ease or premium pressure, and a
token continuous class carried by one obscure path.

Example: P can rebuild and rearm while its context stays operative; every Q
history loses and restores its context before a separate live/false lever
reaches `q2`.

## Compatibility and deferrals

A/B/C exhaust `K^{cont}_v` being the whole candidate domain, empty, or a
nonempty proper subset. C146-C already supplies at least three tags, so C needs
no higher tag floor. All branches retain `9/23/138`, four represented IDs,
eight lived edges, four one-history-complete IDs, no incomplete represented
ID, Souls `{6,9,10,12,14,15}`, degree six, and C46-A's result width two.

C147 selects no tag identities or mapping to dual/specialist classes,
definitions, Souls, Charms, sources, rarity, or power; all-versus-some
continuous paths within a participating tag; a joint continuous-and-same-
opportunity witness; outage count, duration, placement, cause, or restoration
authority; retained sources or semantic facts; failed-evaluation schedule;
same-opportunity participation, `chi` equality/order, or deliberate mode
choice; opportunity count, actor, causation strength, cadence, cap, payoff,
persistence, UI, release, or implementation.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C147 only. Record
the selected branch, recompute the frontier, and present exactly one next
prerequisite-ready choice. Do not infer acceptance of the whole EP-D02
replacement or start implementation.
