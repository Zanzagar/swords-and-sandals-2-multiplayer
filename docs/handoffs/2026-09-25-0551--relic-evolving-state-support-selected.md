---
handoff:      2026-09-25-0551--relic-evolving-state-support-selected
written:      2026-09-25 05:51 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    /root/b2a2b2_consequences; /root/b2a2b2_consistency; /root/b2a3_card
branch:       design/endless-progression-owner-packet
commits:      9d0ba82..HEAD
suite:        7 passed / 0 failed / 0 skipped (handoff-navigation target); design-record checks passed
supersedes:   2026-09-25-0531--relic-relation-roots-selected
---

# Handoff — evolving current-state support selected

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`. The sole active Relic frontier is
`RCS-03C3B2A3B`; present that one card only.

## Where things stand

The owner selected `RCS-03C3B2A3A-B`. `E^{state-rev}_v` is nonempty: at
least one prospectively versioned form has a reachable committed revision that
leaves one current-state continuity episode uninterrupted, materially changes
player-semantic state under a held-fixed comparison, can independently change
an effective `S` predicate, legal continuation, or authoritative outcome, and
remains material with root/Relic/payoff/claim bookkeeping projected out.

This is an existential support floor. Exact forms, counts, distribution, and
prevalence remain AUTHOR/SPEC. No-op writes, representation churn, and genuine
termination followed by re-establishment do not qualify. A3A-B selects no
root-succession, multi-root-support, addressability, claim/reuse, payoff,
persistence, release, or implementation rule.

The 56-row register is now 28 `SCREEN`, 1 `OWNER-OPEN`, 7 `PRUNED`, 17
`DIR-SELECTED`, 1 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`; `Phi_SR = 29`. The
authoritative decision record remains unchanged because a bare option label
selects direction but does not complete the acceptance protocol.

## Highest-value next work

Present `RCS-03C3B2A3B`: does an evolving state preserve or succeed its
continuity root?

For a committed eligible revision `x`, let `r^-_x` and `r^+_x` be the
episode's one operative continuity root immediately before and after the cut.
A preserving form leaves them equal. A succeeding form creates exactly one
fresh replacement root atomically at the cut without terminating the episode.
The predecessor remains ancestry-only under A3B, not automatically a second
current support candidate.

At every revision cut, both policies append to the post-cut root's cut-indexed
lineage the complete pre-cut ancestry plus every indispensable revision-cause
lineage. Earlier snapshots and settled results remain unchanged. A4 later owns
the current fact's exact causal-support topology.

Present exactly three exhaustive stable form policies:

- A: every eligible revision preserves its root; the successor subset is
  empty, and the founder continuity root remains operative throughout the
  uninterrupted episode.
- B: every eligible revision succeeds its root; the successor subset equals
  nonempty `E^{state-rev}_v`, and every committed revision creates one fresh
  replacement continuity root.
- C: preserving and succeeding forms coexist; the successor subset is
  nonempty and proper, requiring at least one reachable form of each kind.

Recommend A. A3A-B already guarantees materially evolving states. A gives one
uninterrupted episode one teachable continuity identity while its real
predicates, counters, outcomes, and later semantic children can change. It
prevents stack gains, material refreshes, thresholds, or mode cycling from
becoming hidden root currency. `Kindle` may move from `Heat I` through
`Heat II` to `White-Hot` under one `r_K`. The root as continuing “soul” and
its materially different manifestations as “simulacra” give the strongest
direct local Achintya Bheda Abheda fit.

B gives every revision a distinct root-level milestone, not an automatic
response window: `Heat I/r_K0 -> Heat II/r_K1 -> White-Hot/r_K2`. Each root
retains full prior lineage and cannot count independently with an ancestor. A
return to Heat II creates fresh `r_K3`, never resurrects `r_K1`. Later access
and payoff rules would have to expose any outward use; until then B creates
latent root-density, UI, persistence, and cadence/oscillation pressure.

C can distinguish ordinary development from named metamorphosis. For example,
within one continuously present Heat state-form, thresholds preserve while its
`Heat III -> Living Flame` manifestation succeeds. This adds an authored
development/metamorphosis vocabulary but also a premium root-topology taxonomy.

## Hard rules

- Resolve exactly one prerequisite-ready owner choice; recompute the frontier
  after the answer and commit the durable round before presenting another.
- Do not edit the authoritative decision record without an explicit
  `accepted`, `rejected`, or complete accepted replacement disposition.
- Do not treat preserve-plus-additive-root as a fourth A3B policy. A3B creates
  only a replacement root under succession; A4 owns extra current support.
- Do not alter the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots. No implementation is authorized.

## Traps from this round

- Policy is prospective by revision form, never chosen from target, Relic,
  payoff, favorable history, claim state, or present recipe completion.
- State, operative-root pointer, and post-cut ancestry commit or roll back
  atomically. Retry/replay of one canonical occurrence returns the same root
  identity; restore, logs, callbacks, aliases, and reads cannot duplicate it.
- All same-episode changes committed at one authoritative cut canonicalize into
  one pre-to-post revision. Distinct revisions require distinct cuts; no
  recursive same-cut root cascade is allowed.
- Reversal never resurrects a value-keyed old root. Under succession it creates
  a fresh successor.
- Succession alone creates no H fact, refreshes no H age, breaks/rearms no
  readiness episode, satisfies no tag, and exposes neither retired predecessor
  nor successor for downstream use.
- No A3B option grants addressability, nested use, fan-out, cross-combatant
  access, claim, consumption, reuse, payoff, persistence, or release authority.
  A4 follows every answer.
