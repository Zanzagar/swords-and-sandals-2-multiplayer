---
handoff:      2026-09-26-0623--relic-exact-route-selected
written:      2026-09-26 06:23 +0000
sessionStart: 2026-09-05 23:04 -0400
sessionId:    01a074ac-c9e7-7303-8538-c8e392199ac2
agentRuns:    one bounded read-only C2 prerequisite audit with a focused countermodel follow-up, one bounded master-index writer, one strict read-only review, and one replacement strict verification
branch:       design/endless-progression-owner-packet
commits:      df3ce3c..HEAD
suite:        proportional serial checks 10/10 passed; register reparses 87/87 with Phi_SR 41; decision-record hash unchanged
supersedes:   2026-09-26-0602--relic-deterministic-routing-selected
---

# Handoff — authoritative exact-overlap route selected

Start with `HANDOFF.md` above its archive line and
`$ss2-progression-design`. The sole active Relic frontier is
`RCS-03C3C3C2A`; present that one card only.

## Where things stand

The owner selected `RCS-03C3C3C1-B`: at least one reachable bound routing
stance selects an authoritative exact-overlap pair. A twenty-fifth prerequisite
correction separates participant permission configurability, coordinate
independence, permission-policy language and branching, stochastic support,
and exact permission incidence. Adversarial review also separated joint
stochastic coupling across distinct simultaneous pair opportunities from
single-pair marginals. The amended register has 87 rows and `Phi_SR = 41`;
the authoritative decision record remains unchanged.

## Highest-value next work

Record the owner's C2A answer, then audit the resulting next row before
presenting it:

- A: participant permission-configurability is empty. Once an exact selected
  pair and its factual inputs are fixed, no participant setting changes its
  permission law. Recommend A: whole routing stances already provide agency
  over exposure, while an intrinsic covenant preserves Relic identity and
  avoids a second generic approval optimizer.
- B: every exact-exposure-capable contract offers at least two prospectively
  bindable participant settings whose permission laws differ on one matched
  exact pair. C2B then asks whether that force is coupled or independent.
- C: intrinsic and participant-configurable contract classes coexist.

For example, A makes the shared-Heat pair's law an authored Ashen/relationship
property even if another Relic has a different law. B permits Concord versus
Severance settings to change the law for that same tuple. C permits configurable
Ashen beside an intrinsic Stone Witness.

## Hard rules and traps

- C2A decides whether material participant permission control exists, not who
  holds it, how long it is locked, whether it is random, or which pairs pass.
- A setting counts only if the same exact selected tuple and tag pair remain
  fixed while its authored permission-control force changes the complete law.
  Merely choosing another ledger or exposing/avoiding overlap does not count.
- A bundled permission role inside a routing stance counts as configuration;
  C2B later asks whether permission can vary independently while routing stays
  fixed. UI factoring cannot manufacture independence.
- Coupled permission does not imply a closed permission language: a constructed
  permission component may be paired one-to-one with a closed routing stance.
- Distinct simultaneous pair opportunities may have correlated draws even
  when their marginals match. Conditional G2 owns that question; one hybrid
  exact-and-related pair still supplies only one draw.
- A does not mean every contract shares one law or that permission is
  deterministic. Relic/context variation remains legal and C2D still owns
  stochastic support.
- A hybrid exact-and-related pair gets one permission law and bit. No draw or
  denial may retry the selector, switch ledgers, or redraw on replay.
- The replacement verifier first used an unsafe double-quoted search pattern;
  Bash attempted one nonexistent command. It wrote nothing, was rerun with safe
  quoting, and its corrected no-write result is the one recorded here.
- Do not edit the authoritative decision record without an explicit accepted,
  rejected, or complete accepted-replacement disposition.
- Do not touch the installed oracle, captures, fixtures, observations,
  manifests, goldens, saves, or snapshots.

## Verification and Git

Confirm the register reparses to 87 rows with 40 `SCREEN`, 1 `OWNER-OPEN`, 10
`PRUNED`, 32 `DIR-SELECTED`, 2 `DERIVED`, 1 `SPEC`, and 1 `EVALUATE`;
`Phi_SR = 41`. Confirm the decision-record SHA-256 remains
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
Commit the answer atomically with a `Co-Authored-By` trailer, push only the
feature branch, verify it is clean and synchronized with
`github/design/endless-progression-owner-packet`, and do not merge PR #3.
