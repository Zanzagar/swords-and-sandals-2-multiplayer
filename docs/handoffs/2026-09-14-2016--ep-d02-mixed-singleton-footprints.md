# Handoff — mixed singleton footprints; Bound-Soul total-footprint stability next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-14 20:16:32
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.57, total native-dual footprint stability under
Bound-Soul substitution. The owner selected C3c.56-B. Preserve the accepted
EP-D02 wording until a complete replacement is replayed and explicitly
accepted. Do not implement.

## Selected direction

Reuse each exact pair's singleton-backed cell set `I_v(p)` and define

`Q_v={p in P_v : |I_v(p)|>=2}`.

C3c.56-B makes `Q_v` a nonempty proper subset of `P_v`. At least one exact
supported Soul/operative-Relic-realization pair has two or more distinct
singleton-backed native-dual cells, while at least one other exact pair has
exactly one. C3c.55-B still gives the one-anchor pair at least two total cell
edges, so it necessarily has at least one coupled-only edge.

The exact singleton minimum is one and singleton degree is nonregular.
Total-degree regularity remains open. This is potential raw material for a
later-teachable contrast between multi-anchor and relational one-anchor causal
structures; it does not itself provide stable membership, disclosure, trigger
quality, player control, ease, frequency, power, or value. It selects no exact
member, footprint, cell, package, context, receipt, or route. This is a
direction, not a complete accepted EP-D02 replacement, and authoritative
wording remains unchanged.

## Frontier result

The next clean atom is the stability of the **total** native-dual cell
footprint under Bound-Soul substitution while holding one exact tagged
operative Relic realization fixed. It precedes the nested stability of
`Q_v` membership or the exact singleton set `I_v(p)`. It also avoids bundling
the compound Relic coordinate, which must later separate definition,
same-definition instance/state snapshot, and possibly state/boundary axes.

For fixed version `v`, write `C_v(b,s)=C_v((b,s))`, where `b` fixes the exact
definition-tagged operative Relic realization and `s` is a supported Soul.
C3c.42-A gives at least two supported Souls. C3c.3-B supplies the actor-ID-
preserving counterfactual configuration product; C3c.49-A/C3c.50-A give every
pair dual and empty-input-dual capability. No live Soul rebinding is implied.

Define

`V^C_{S,v}={b in R_v* : some s_1,s_2 in S_v have C_v(b,s_1) != C_v(b,s_2)}`.

A difference needs one cell in the symmetric difference. The positive side
needs a valid `L_v` package edge; the absent side needs C3c.52's complete
negative analysis. Different predicates, contexts, packages, receipts, or
singleton/coupled status inside the same total cell set are not a total-
footprint difference. Equal cell sets do not join any of those lower-level
objects across contexts.

## Sole active choice

- **A, recommended — every exact realization has a Soul-stable cell palette:**
  `V^C_{S,v}` is empty. For each exact `b`, all supported Souls have the same
  total cell set. C3c.43-A still makes every `b` materially Soul-responsive at
  the predicate layer. This creates a simpler stable outer vocabulary for
  later teaching while containing another combinatorial axis after C3c.56-B's
  mixed causal structure. It prevents a Soul from opening or closing an entire
  native-dual cell at fixed `b`, and it forces C3c.52-B's required total-
  footprint variation onto changing exact `b`. C3c.55-B's exact-two witness
  repeats across that `b`'s whole Soul fiber, producing at least `|S_v|`
  exact-two rows, each with four absent cell edges.
- **B — mix Soul-stable and Soul-variable exact realizations:**
  `V^C_{S,v}` is a nonempty proper subset of `R_v*`. At least one exact
  snapshot has the same cell set under every Soul, while another changes at
  least one cell for some Soul pair. This provides both Soul-axis and exact-
  `b`-axis footprint variation, but adds an exact-instance/state/boundary
  taxonomy that is not automatically a teachable named-Relic archetype.
- **C — every exact realization has a Soul-variable cell palette:**
  `V^C_{S,v}=R_v*`. Every exact snapshot changes at least one total cell for
  some supported Soul pair. This makes Soul identity reshape the outer recipe
  vocabulary everywhere, but imposes the universal Soul-by-realization and
  absent-edge burden for an intrinsic-Soul comparison that supplies no live
  Soul swap. It does not require every Soul pair to differ, every Soul to be
  unique, or footprint degree to change.

For A, fixed Ashen `R17` could give both Firebound and Stonebound the total
footprint `{H/action,X/build}` while their `H/action` predicates differ. The
singleton sets may still differ: Firebound can have both cells singleton-
backed, while Stonebound has only `H/action` singleton-backed and reaches
`X/build` through a coupled package. A distinct exact realization supplies the
other global cells. Thus A stabilizes only the total cell labels, not exact
recipes or C3c.56 membership.

For B, Ashen `R17` could be stable at `{H/action,X/build}` for every Soul,
while Dreamglass `R44` gives Firebound `{H/action,X/build}` and Stonebound
`{S/action,X/build}`. Both Dreamglass rows remain degree two, so the set change
is not automatically broader, easier, or stronger.

For C, fixed Ashen `R17` could give Firebound `{H/action,X/build}` and
Stonebound `{S/build,X/build}`. Every other exact realization needs some
analogous Soul-pair set difference, but not the same changed cell or a unique
set for every Soul.

A stable `b` needs complete six-cell equality across every supported Soul. A
variable `b` needs a positive edge under one Soul and a complete absent-edge
proof under another. A applies the stable burden to every `b`; B needs one
stable `b` and one variable witness; C needs a variable witness for every `b`.
Failed search is unresolved.

The strongest token A catalog can keep the labels equal while using unrelated,
obscure contexts for different Souls and place C3c.43-A's material predicate
difference where players barely notice it. B can vary only one showcase
snapshot; C can vary one rare edge per snapshot. Mapping, distribution,
context/trigger quality, and disclosure remain later safeguards.

An owner-written replacement may provide another explicit exact-realization-
scoped Soul-stability rule. It must state any required stable and variable
witnesses and retain the same positive-edge and complete-absence proof
standard. This card selects no singleton stability, definition/state
ownership, live rebinding, route or receipt identity,
transformation, precedence, disclosure, control, ease, power, payoff,
acquisition, persistence, release, or implementation.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record
C3c.56-B and identify C3c.57 as the sole active one-at-a-time owner card, with
A recommended. The authoritative decision record and normative system design
remain unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04 directions
remain unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain selected,
and R10.3-R10.8 remain pending. Continue exactly one A/B/C choice per turn.

No code tests were run because this was documentation and vector work only. No
runtime, capture, evidence, installation, save, or snapshot was touched.
Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`.

Three independent read-only audits re-derived the formal topology, frontier
ordering, gameplay examples, recommendation, proof burdens, and hidden
cardinality/degree consequences. They confirmed that total `C_v` stability is
the clean outer atom; singleton stability is nested; the Relic coordinate must
later be split; and C3c.42/C3c.43 Soul responsiveness does not force a total
cell-set change. Post-edit review caught and repaired two wording defects: a
replacement clause incorrectly required a nonempty complement, which would
have contradicted C, and B called an undisclosed exact-snapshot property
“predictable.” All three final read-only checks passed after repair.

`git diff --check` passed. No-index whitespace checks for the untracked rigor
audit, SVG, and this handoff returned the expected different-from-`/dev/null`
status with no diagnostics. The SVG parsed with Python's standard XML library,
this file resolved as the newest handoff, the stale-C3c.56-active-pointer and
premature-C3c.57-selection scans found none, and the authoritative decision and
system documents are clean.
