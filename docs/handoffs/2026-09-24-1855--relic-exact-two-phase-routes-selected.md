# Handoff — exact-two Soul Relic phase routes

**Session ID:** `72534467-4493-4e32-9856-6f79ae638153`
**UTC:** 2026-09-24 18:55
**Branch:** `design/endless-progression-owner-packet`

Start with this handoff, `HANDOFF.md` above its archive line, and
`$ss2-progression-design`; present only `RCS-03C3B1A2A` next.

## Owner answer recorded

The owner's latest `A` selected `RCS-03C3B1A1B2-A`. Every phase-plural
history position has exactly two independently isolatable phases, so
`T^{tri}_v` is empty. Inherited B1-C still requires at least one
phase-singular position and at least one distinct exact-two position. Exact
phase-pair identities remain catalog authoring.

At an exact-two position both phase-to-predicate links participate
**directly**, their continuity through one source role across possibly
different histories is **partial**, excluding a third independent route is
**neutral/protective**, and universal exact-two prevalence within the plural
domain is **aggregate**. This constrains independent isolation only: a later
coupled package may materially read an uncounted third phase. The answer
changes no authoritative decision wording and authorizes no implementation.

## Tenth prerequisite correction

Old `RCS-03C3B1A2` compressed coupled-supplement position prevalence with
coupled-package phase width. The recorded X2/X3 countermodel holds
singular/exact-two topology, coupled-position prevalence, and catalog phase
union `{D,E,O}` fixed while making one indispensable package ready at `110`
through `{D,E}` and the other ready only at `111` through `{D,E,O}`. Under
immediate automatic invocation, an interruption between those cuts changes
counterplay. Package width is therefore player-material rather than merely an
authoring label.

The register now treats B1A2 as a non-counting parent for
`RCS-03C3B1A2A` and conditional `RCS-03C3B1A2B`. Exact pair identities,
operators, package counts, singular/exact-two mapping, incidence, and quotas
remain AUTHOR/SPEC. The first post-answer screen had classified width as
authoring; that Agent error is named and corrected in every working record.

The register has 50 rows: 30 `SCREEN`, 1 `OWNER-OPEN`, 7 `PRUNED`, 10
`DIR-SELECTED`, 1 `SPEC`, and 1 `EVALUATE`; therefore `Phi_SR = 31`.
`RCS-03C3B1A2A` is the sole active card.

## Next card

Let `J^{multi}_v` contain every history position with at least one reachable,
materially effective, C3c.33-valid H-only satisfaction transition whose
inclusion-minimal indispensable claim package spans at least two of
`{D,E,O}`. The package is supplemental: the position retains its already
required independent phase anchor or anchors. It must be effective in a
readiness episode where an earlier independent route has not already invoked
and settled the position.

Present only:

- A: `J^{multi}_v` is empty; no history position has a coupled supplement.
- B: `J^{multi}_v=T_H`; every history position has one.
- C: `J^{multi}_v` is a nonempty proper subset; coupled-supplement and clean
  positions coexist.

Recommend C. Every position keeps its independent anchor or anchors, at least
one clean position preserves timing education and counterplay, and selected
Vow/Confluence positions let distinct phases participate jointly in one
operative cause. That joint path is the strongest direct Achintya Bheda
Abheda expression in this phase route without requiring a compound route at
every history position. Costs are a visible complexity taxonomy, exhaustive
negative proof for clean positions, tooltip/replay load, and a possible
clarity or power premium.

Positive membership needs a reachable, non-preempted, inclusion-minimal
package witness. Raw co-variation, a shared record/cut, or an authored AND
label is not proof. Negative membership needs complete reachable-domain
dependency analysis. Under A, B1A2B is pruned and B1B follows; under B or C,
B1A2B becomes next.

## Validation

- Two independent read-only reviews passed the B2-A recording, countermodel,
  A/B/C partition, conditional-width split, dependencies, arithmetic, and
  deferrals. One review found two position/source wording overclaims in C;
  both were corrected before commit and no mechanical finding remained.
- Structural target: 50 register rows and `Phi_SR = 31`, with only
  `RCS-03C3B1A2A` marked `OWNER-OPEN`.
- Full suite: 584 tests, 583 passed, 0 failed, 1 expected raw-trace archive
  skip.
- `git diff --check` passed.
- `docs/design/endless-progression-decisions.md` remains byte-identical at
  SHA-256 `e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.
- No runtime, Ruffle, capture, installation, save, snapshot, fixture,
  candidate, observation, manifest, or golden work was performed.
