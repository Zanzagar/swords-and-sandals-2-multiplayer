# Handoff — C75 fully peer-covered affinities selected; size support next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-16 03:39:13
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.76: whether no, some but not all, or every affinity
Relic definition has a C74-stable, C75-fully-peer-covered profile containing
at least two different Soul-group sizes. The owner selected C3c.75-A. Preserve
the accepted EP-D02 wording until a complete replacement is replayed and
explicitly accepted. Do not implement.

## Standing north star

The intended standalone name is **Souls and Simulacra**. Every later choice
must remain consistent with that identity and always use *Achintya Bheda
Abheda* (*acintya-bhedabheda*) as the enduring design ideal. At any represented
operative relationship, prefer meaningful continuity or participation and
real, mechanically legible distinction coexisting at that same boundary.
Separate states, keys, definitions, catalog entries, labels, or cosmetics that
respectively supply “unity” and “difference” do not satisfy the ideal.

This is an inspirational game-design analogy, not theological equivalence, a
silent invariant, or an automatic answer. Every recommendation must name the
relationship and boundary, rate its fit as direct, partial, neutral, or
aggregate, and state the gameplay tradeoff. Continue the exact one-at-a-time
A/B/C owner workflow.

Finish the current full-assurance design-framework pass against the SS2-derived
skeleton before the separate expansion, audit, and adaptation pass. Vanilla
evidence and the byte-identical measurement oracle remain in their SS2 lane.

## Selected direction

`D^solo_v` is empty. Every part of every affinity definition's C74-stable
profile is at least two, so every supported Soul has an equal-map peer on every
affinity exact key. Each affinity still has at least two unequal maps and one
all-Soul core. This gives the fixed affinity-key/Soul-roster relationship
direct peer participation, distinction, and continuity.

C75-A gives `4<=|S_v|<=15` and requires
`2<=rho<=floor(|S_v|/2)` for every affinity definition. Tight local and C39-
witness key floors are three and four; minimum rows are twelve and sixteen.
Every named-Soul peer-presence map is derived all-one, so its planned variation
card collapses. C75-A selects no exact group size, named-Soul size assignment,
partners, labels, predicates, power, or implementation and changes no
authoritative wording.

## Frontier audit

The exact group-size-role draft withdrawn before C75 bundled no-peer/has-peer
status with pair-versus-trio size. C75-A now resolves the former uniformly, but
the latter still has an eligibility prerequisite. A uniform stable profile such
as `(3,3)` forces every Soul's size role to three; it is not a genuine stable
witness against a mixed profile's possible role variation.

For affinity definition `d`, define

`Sizes_v(d)={r>=2:r occurs in lambda_v(d)}`

and

`D^mixed_v={d in D_affinity,v:|Sizes_v(d)|>=2}`.

Independent formal and adversarial checks found no still-coarser live
projection. Zero-versus-positive profile-size heterogeneity is the minimal
support test before named-Soul size-role assignment. Core-label continuity is
an independent ready sibling.

## Active frontier

- **A — every affinity definition has equal-size groups:** `D^mixed_v` is
  empty. Every profile is `(q,...,q)`, although `q` and `rho` may differ by
  definition. Local ideal fit is direct; the marginal effect is protective
  symmetry. It maximizes readability and collapses the later size-role card,
  but every `rho` must divide the roster size and prime roster sizes are
  impossible.
- **B, recommended — equal-size and mixed-size affinities coexist:**
  `D^mixed_v` is nonempty proper. Name-stable “equipoised circle” and
  “major/minor circle” geometries provide a readable baseline plus exceptional
  relational puzzles without a hidden same-name profile roll. B's added
  catalog coexistence is aggregate, while each fixed key retains direct core,
  peer-participation, and map-difference fit. Costs are at least six Souls, a
  composite roster, another taxonomy, and disguised-tier risk.
- **C — every affinity definition has mixed-size groups:**
  `D^mixed_v=D_affinity,v`. Every affinity supports later majority/minority
  size-role puzzles and roster sizes five through fifteen remain possible.
  Local fit is direct because peer participation, shared core, unequal maps,
  and unequal cohort cardinality coexist. The universal mandate is aggregate
  and risks making structural hierarchy or one repeated `(n-2,2)` template
  ubiquitous.

At six Souls, `FSV|WTR` has equal profile `(3,3)`, while `FSVW|TR` has mixed
profile `(4,2)`. Every Soul has a peer in both. C76 chooses only which
definition profiles support multiple sizes; it does not make a named Soul move
between them.

For `n=|S_v|`, a uniform `rho`-part profile exists exactly when `rho` divides
`n` and `n/rho>=2`. A mixed fully peer-covered profile exists exactly when
`n>=2*rho+1`. Exact feasible A/B/C Soul sets are
`{4,6,8,9,10,12,14,15}` / `{6,8,9,10,12,14,15}` / `5..15`.

Tight A/B/C definition and local-key floors are `3/4/3`; C39-witness key
floors are `4/5/4`; local row floors are `12/24/15`; C39-witness row floors are
`16/30/20`. Tight affinity witnesses remain degree three. At the whole-catalog
minimum, the required coreless pair-complete prism needs degree four under A
and degree five under B/C because their roster minima are six and five.

Tight all-six-label witnesses are recorded in the owner packet. In shorthand:
A uses four-Soul prism/profile `(2,2)`; B uses a six-Soul degree-five prism plus
distinct `(3,3)` and `(4,2)` affinity definitions; C uses a five-Soul degree-
five prism plus `(3,2)`. Echo supplies the one-only definition. C39 adds one
same-definition key preserving profile class and separately changing a shared-
cell predicate materially.

C76 selects no exact sizes or multiplicities, `rho` values, named-Soul size
assignments or variation, peers, partitions, labels, predicates, variation
cause, transformation, disclosure, power, persistence, release, or
implementation.

Under A every Soul's exact size role is derived stable, so proceed to partner/
partition support on this branch. Under B/C only `D^mixed_v` enters a later
named-Soul size-role frontier; uniform definitions are forced stable rather
than stable witnesses. One eligible definition gives a binary stable/variable
choice; at least two admit empty/proper/full over the eligible subset. Exact
partner identity remains finer.

## Preserved state

The owner packet, living head, rigor audit, SVG, and this handoff record C75-A
and C76 as the sole active owner choice. The authoritative decision record is
unchanged. EP-D01, EP-D02, and EP-D07 remain accepted; EP-D04 directions remain
unaccepted, R9.4 remains reopened, R10.1-A and R10.2-A remain selected, and
R10.3-R10.8 remain pending.

No runtime, capture, evidence, installation, save, snapshot, or implementation
was touched. Preserve the user's existing change to
`.agents/skills/ss2-progression-design/SKILL.md`. Partition feasibility,
all-six-label constructions, prism-capacity bounds, definition/key/row floors,
active-frontier pointers, tracked and relevant-untracked whitespace,
authoritative-record nonchange, and SVG XML parsing all passed. This
documentation-only turn did not run the repository test suite.
