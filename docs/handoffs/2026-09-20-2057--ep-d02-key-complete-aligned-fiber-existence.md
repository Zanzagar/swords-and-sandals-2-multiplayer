# Handoff — every artifact aligns; one key-complete carrier per name next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-21 00:57:58
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.120: whether no, some but not all, or every union-
locked Soul Relic definition contains at least one represented persistent-
instance fiber whose every completed exact key participates in an edge that is
both the definition's sole one-coordinate comparison type and an all-Soul-
changing common relation. The owner selected C3c.119-C. Preserve the accepted
EP-D02 wording until a complete replacement is replayed and explicitly
accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

For every recommendation, name the operative relationship and boundary, rate
the fit as direct, partial, neutral, or aggregate, and expose the gameplay
tradeoff. Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, and concrete example. Finish the
current full-assurance pass before the separate standalone expansion, audit,
and adaptation pass. Vanilla evidence and the byte-identical oracle remain in
their own lane.

## Selected direction — EP-D02-C3c.119-C

For locked definition `d`, let `Ids^X_v(d)` contain the represented persistent
instance IDs whose completed fiber contains an edge in

`X^{1cW}_v(d)=A^{1coord}_v(d) intersection W^S_v(d)`.

The owner selected

`Ids^X_v(d)=Ids_v(d)` for every locked `d`,

and therefore

`D^{all-IdX}_{locked,v}=D^{U=}_{locked,v}`.

Every represented persistent artifact in the completed locked domain contains
at least one exact pair carrying its definition's sole C113-A one-coordinate
type and changing every supported Soul's assigned noncore map. This closes the
token-artifact and per-copy comparison-lottery loopholes left by C118-C, but
one edge may leave other forms in that same artifact uncovered.

Tight floors remain seven definitions, fifteen exact keys, and ninety six-Soul
rows. Soul counts remain `{6,9,10,12,14,15}` and tight maximum degree remains
six. Each actual aligned edge is partial at the same-persistent-artifact/fixed-
coordinate/across-snapshot boundary, while universal fiber prevalence is
aggregate. The authoritative decision record remains unchanged.

## Active owner choice — EP-D02-C3c.120

For represented ID `r` of locked definition `d`, define

`K^X_v(d,r)={b in B_{v,r}(d):there exists b' in B_{v,r}(d) with {b,b'} in X^{1cW}_v(d)}`,

`Ids^{KX}_v(d)={r in Ids_v(d):K^X_v(d,r)=B_{v,r}(d)}`,

and

`D^{exists-IdKX}_{locked,v}={d in D^{U=}_{locked,v}:Ids^{KX}_v(d) is nonempty}`.

C119-C makes every represented fiber aligned-positive. It does not cover every
key. An aligned-uncovered key still has a sole-type same-ID partner from C111-C
and an internal all-Soul-changing partner from C117-C, but those partners are
different: its typed edge retains at least one Soul map and its whole-roster
edge is diagonal.

### A — no locked definition has a key-complete aligned fiber

`D^{exists-IdKX}_{locked,v}` is empty. Every represented artifact contains an
aligned teaching edge and at least one aligned-uncovered compound specialist
form. This is the strongest principled countercase if the specialist is a
visible, accessible, agentic, non-dominated source of play. Otherwise A forces
an orphan form into every artifact and forbids any fully self-contained form-
by-form comparison carrier.

### B — qualifying and wholly incomplete names coexist

`D^{exists-IdKX}_{locked,v}` is nonempty and proper. At least one name contains
a key-complete aligned artifact, while another name's every represented
artifact is aligned-positive but incomplete. This can support explicit
carrier and specialist name languages, but adds hidden taxonomy and premium-
name risk unless both are disclosed and valuable peers.

### C — every locked definition has a key-complete aligned fiber — recommended

`D^{exists-IdKX}_{locked,v}=D^{U=}_{locked,v}`. Every locked name has at least
one represented persistent artifact whose every completed form has an aligned
partner. Other artifacts may remain positive but incomplete.

C gives each name one structurally self-contained comparison carrier without
requiring every artifact to lose specialist forms. Costs are token two-key-
fiber laundering, a premium-copy or inaccessible-carrier risk, and additional
authoring and validation. It is a structural promise, not yet a guarantee that
the player owns, sees, reaches, or values the carrier.

## Verified witnesses and floors

C105's four-key families give the tight positive-but-incomplete fiber:

```text
R17={b0,b1,b2,b3}
sole-type blocks: {b0,b1}, {b2,b3}
typed edges:      b0--b1, b2--b3
common edges:     b0--b1 and all four cross-block edges
non-common edge:  b2--b3

X={b0--b1}; b2 and b3 are aligned-uncovered.
```

Both constituent relations cover every key. The exact intersection does not.
The state-supporting and boundary-supporting versions differ only in which
coordinate is held fixed.

A complete four-key carrier uses C105's `[2,2]` aligned partition:

```text
R17={b0,b3}       aligned overlap-bearing dyad
R44={b1,b2}       aligned overlap-bearing dyad
```

The member-disjoint partition `{b0,b2}/{b1,b3}` also works. A strict C120-C
versus later all-fiber separator uses six keys:

```text
R17={b3,u1}             aligned and key-complete
R44={b0,u0,u2,u3}       typed b0--u0 is common
                         typed u2--u3 is not common
```

R44 is aligned-positive but leaves `u2,u3` aligned-uncovered; its diagonal
common edges still cover every key. All six families are distinct and retain
the inherited overlap-bearing and member-disjoint coverage requirements.

A positive-but-incomplete fiber needs four keys. The proof depends on C113-A:
inside a sole-type definition, exact-tuple uniqueness makes the typed graph a
disjoint union of fixed-coordinate cliques, and C111-C forbids singleton
blocks. On three vertices the graph must be one triangle. C117-C covers every
vertex with a common edge, all such edges lie in the triangle, and therefore
the aligned intersection covers all three. The four-key witness proves the
bound tight.

| Choice | Soul counts | Definitions | Keys | Minimum rows | Maximum degree |
| --- | --- | ---: | ---: | ---: | ---: |
| A | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| B | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |
| C | `{6,9,10,12,14,15}` | 7 | 15 | 90 at six Souls | 6 |

C113-A already requires two four-key locked definitions above the inherited
five-definition/seven-key outside base. A uses two incomplete `[4]` names, B
uses one incomplete `[4]` and one complete `[2,2]`, and C uses two complete
`[2,2]` names. Coordinate decoration changes no complete family, Soul roster,
or palette degree.

Independent read-only frontier, construction, and gameplay audits confirmed
that no coarser card is missing, four is the tight incomplete-fiber size, all
three branches are feasible at every retained Soul profile, and C is the
recommended branch while A is the strongest principled countercase.

## Ideal ratings and exclusions

At one fixed exact-key/Soul relationship, inherited fit remains **direct**. An
actual aligned edge is **partial** at the same-persistent-artifact/fixed-
coordinate/across-snapshot boundary. An aligned-uncovered key is **neutral/
protective** only at the combined aligned-incidence projection: its typed edge
is still **partial** at the narrow fixed-coordinate boundary while retaining a
Soul map, and its diagonal common edge is **partial** at the broader artifact
boundary and **neutral** at the omitted fixed-coordinate projection. Fiber
completeness and definition prevalence are **aggregate**. C improves systematic
participation; it does not upgrade the relationship to direct.

C120 selects no number or share of complete fibers beyond existence; no all-
fiber or global per-key aligned coverage, edge count, partner count, every-pair
rule, matching, connectedness, or common anchor; no overlap-bearing versus
member-disjoint aligned identity; no exact state or boundary semantics, cause,
path, transition, access, disclosure, agency, ease, value, power, non-
dominance, persistence, release, or implementation. It quantifies only over
represented completed-domain IDs, not every inventory instance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C120 only. Record
the selected branch and its derived consequences, recompute the frontier, and
present exactly one next prerequisite-ready choice. Do not infer acceptance of
the whole EP-D02 replacement or start implementation.
