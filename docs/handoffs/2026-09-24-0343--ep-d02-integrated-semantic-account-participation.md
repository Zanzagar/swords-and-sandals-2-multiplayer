# Handoff — Two-form saturation selected; semantic integration next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-24 03:43:06
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.166: per-tag integrated two-form semantic-account
participation. The owner's latest `A` selected C3c.165-A and nothing in C166.
This remains Soul Relic design, specifically the player-information surface of
the selected same-opportunity, one-hinge result-rearm grammar. Preserve
accepted EP-D02 wording until a complete normative replacement is replayed and
explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready bounded choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Do not batch
semantic integration with per-history saturation, canonical reference-asset
identity, simultaneous presentation, command-holder identity or communication,
hinge disclosure, accessibility, value, or fun.

## Selected direction — EP-D02-C3c.165-A

`U^{pre-all-op2}_v=D^{dual}_v`. Every mechanical joint opportunity on every
legal history has a qualifying P pair and distinct qualifying R pair
intelligible to its one actual command-holder under the same exact authorized
`O^-` before commitment.

At the exact `(k,w,d,u,O^-_u)` boundary, continuing Soul–Relic context and both
distinct forms coexist **directly**. Their later relationship to `q2`, effects,
and payoff remains **partial**; excluding form-hiding is
**neutral/protective**; and universal opportunity/history/tag saturation is
**aggregate**.

C165-A collapses joint-opportunity support states to `{P,R}`. It does not fuse
the forms or distinct pairs, require one shared account, reference, asset, or
screen, select an actual controller, or cover non-joint choices. It adds no
floor: the tight three-tag construction, one dual tag-history, one co-legible
joint opportunity, two form memberships, and two legible-pair incidences
remain sufficient; singleton and opaque opportunity counts are zero.

## Active owner choice — EP-D02-C3c.166

For an exact joint opportunity `(k,w,d)`, let `A^-_u(w,d)` be the complete set
of canonical, versioned player-semantic accounts authorized inside
`O^-_u(w,d)`. An account has one authored causal scope and provenance
independent of localization, layout, asset duplication, page breaks, or
rendering. One account may render through multiple explicitly linked views;
placing independent accounts in one page or panel does not merge them. A
neutral baseline may supply legal-arm identities and already-defined general
vocabulary but no target-specific P/R mapping.

Let `Lambda_F(w,d|xi)` contain qualifying form-`F` pairs made intelligible by
that baseline plus account `xi` alone. Define

`Gamma^{pre-int}_v(w,d)={(xi,pi_P,pi_R) :
xi in A^-_u(w,d), pi_P in Lambda_P(w,d|xi),
pi_R in Lambda_R(w,d|xi), pi_P != pi_R,
and xi explicitly presents those interventions as the distinct predicate and
recurrence faces of the same continuing tag and atomic choice}`,

`J^{pre-int}(w,d) iff J^{pre-both-op}(w,d)
and Gamma^{pre-int}_v(w,d) is nonempty`,

`W^{pre-int}_v(k)={w in W_v(k) : some d in D^{joint}(w)
has J^{pre-int}(w,d)}`,

and

`E^{pre-int}_v={k in D^{dual}_v : W^{pre-int}_v(k) is nonempty}`.

This is semantic integration rather than physical colocation. One versioned
account may render through tooltip, codex, audio, or linked views; one asset or
screen may contain separate accounts. C165-A guarantees that the whole `O^-`
makes both forms legible at every joint opportunity, not that any one account
is jointly sufficient.

```text
INTEGRATED SEMANTIC ACCOUNT
“Converge restores the Covenant and opens its Echo to return.
 Compared with Invoke Only, bond restoration is the predicate face.
 Compared with Rekindle Only, return permission is the recurrence face.”

COMPOSITION-ONLY ACCOUNTS
Covenant primer: Converge vs Invoke Only explains P.
Echo-return primer: Converge vs Rekindle Only explains R.
Both are available before choice, but neither account explains their unity.
```

### A — every dual tag has an integrated two-form account — recommended

`E^{pre-int}_v=D^{dual}_v`. Every dual tag has at least one exact legal history
and joint opportunity with one authorized semantic account that makes a
qualifying P pair and distinct R pair intelligible and explicitly relates them
as two faces of the same continuing Soul–Relic choice. It need not occur on
every history or opportunity, use one reference asset, or appear on one screen.

At the exact `(k,w,d,u,O^-_u,xi)` boundary, continuing relationship unity and
both distinctions coexist **directly**. Their later relation to `q2`, effects,
and payoff is **partial**; rejecting fused or merely colocated pseudo-
integration is **neutral/protective**; and universal tag participation is
**aggregate**.

A lets every dual Relic explicitly articulate unity without erasing causal
difference. Costs are synthesis, localization, and validation per tag, denser
explanations, repetitive “two faces” boilerplate, less interpretive mystery,
and token compliance through one obscure integrated account per tag.

Example: Ashen has the canonical Convergence account above. It may render as
two linked views, but its one versioned semantic identity jointly explains
both exact comparisons.

### B — every dual tag remains integration-negative

`E^{pre-int}_v` is empty. C165-A still makes both forms intelligible at every
joint opportunity, but no authorized account anywhere simultaneously supplies
both exact form accounts and explicitly relates them as two faces. A jointly
sufficient account with two unrelated clauses, or a relational overview
without both exact pair mappings, remains integration-negative.

The strongest intentional countercase is modular teaching. Separate concise
bond/predicate and recurrence/permission modules preserve reusable teaching and
reduce dense-explanation, maintenance, and localization coupling while
inviting players to synthesize mastery. Each form is **direct** in its own
module; cross-account unity is **partial**; explicit integration is
**neutral/unsupported**; modularity may be **neutral/protective**; and
universal integration-negative topology is **aggregate**.

Costs are split attention and memory, weaker thematic synthesis, apparent rule
fragmentation, and reliance on player inference despite both facts being
available.

Example: every Ashen choice can reach a Covenant primer for P and Echo-return
primer for R. They may appear side by side, but no semantic account relates
the two.

### C — integrated and integration-negative dual tags coexist

`E^{pre-int}_v` is a nonempty proper subset of `D^{dual}_v`. At least one dual
tag has an integrated account. At least one distinct tag remains integration-
negative after exhaustive analysis, although C165-A still makes both forms
separately legible at all its joint opportunities.

Local cases inherit A/B's ratings and class coexistence is **aggregate**.
Costs are another information dialect, unequal learning burden, clarity or
interpretive-flexibility premium risk, and token positive/negative class
membership.

Example: Ashen has an integrated Convergence account. Dreamglass exposes Facet
and Echo accounts at every choice but no account relates them.

## Proof, anti-laundering, floors, and deferrals

A/B/C are exhaustive because `E^{pre-int}_v` is a subset of the nonempty dual
domain: whole, empty, or nonempty proper. A separate catalog-existence card is
unnecessary because B covers global absence and C covers nonempty nonuniversal
participation. C165-A prevents a negative tag from hiding either form;
negative means only that no integrated semantic account exists. Same-reference
and same-screen states create no fourth class because they remain orthogonal
later axes.

A requires one exact `(w,d,u,O^-_u,xi,pi_P,pi_R)` witness per dual tag. B
requires exhaustive analysis of every dual tag, legal history, joint
opportunity,
qualifying P/R pair combination, and canonical authorized pre-choice account,
proving no account is jointly sufficient and integrating. C combines one
positive tag with one exhaustively integration-negative tag. Failure to find a
shared tooltip, reference, or panel is not negative proof; incomplete account
or content enumeration remains unresolved.

Same-`O^-` co-legibility is not integration. A shared `referenceId`, asset,
page, menu, heading, or simultaneous screen is insufficient without one
canonical account's cross-form semantic relation. A generic “does both,”
visual adjacency, icon/color similarity, two links under one heading, or
player inference cannot manufacture an integrated witness. Different
renderings do not split one versioned account; arbitrary concatenation does
not create one. Generated text counts only when its canonical grammar emits a
truthful integrated account for the target arms. One bundled pair cannot
satisfy both forms, and duplicate aliases count once.

No cross-opportunity, history, tag, role, seat, version, or post-commit
stitching is allowed. Guides, private AI state, post-choice feedback, data
mining, and uncommunicated other-seat information remain invalid. A stable
generic account may support multiple tags only when truthful for each exact
arm set; those are separate indexed witnesses.

Every branch inherits C165-A's same-role two-form co-legibility and C164-A's
lawfully human-controllable observation contract. C166 adds no controller
count or identity and does not require actual human occupancy. B/C cannot
distribute form-legibility evidence across players; both forms remain within
the same command-holder's `O^-`, and only their semantic provenance differs.

Tight indexed incidences are:

| Branch | Candidate tags | Dual `(k,w)` incidences | Co-legible joint opportunities | Integrated account witnesses | Form memberships / legible pairs | Mechanical pairs / continuations |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| A | 3 | 1 | 1 | 1 | 2 / 2 | 2 / 3 |
| B | 3 | 1 | 1 | 0 | 2 / 2 | 2 / 3 |
| C | 4 | 2 | 2 | 1 | 4 / 4 | 4 / 6 |

These are indexed witnesses, not necessarily distinct global histories,
actions, texts, references, assets, panels, or buttons. No branch adds another
mechanical opportunity, result, receipt, decisive cut, definition, exact key,
Soul row, same-reference identity, or simultaneous presentation surface.

Every branch retains nine definitions, twenty-three exact keys, 138 Soul/key
rows at six Souls, four represented locked IDs, eight lived aligned edges,
four one-history-complete IDs, no incomplete represented ID, Souls
`{6,9,10,12,14,15}`, degree six, and C46-A's same-boundary width two.

C166 defers class identities and prevalence; per-history and every-opportunity
integration saturation; reference-asset identity; simultaneous presentation;
account counts, salience, order, modality, discoverability, accessibility,
detail, and wording; bearer/ally/opponent identity, actual controller, role
stability, or communication; hinge disclosure; effect timing, feedback,
preview, value, non-dominance, payoff, power, fun, UI, release, and
implementation.

Under recommended A, per-history integrated-account saturation is the next
dependent breadth atom. Under B/C, recompute the applicable composition or
participating-class frontier.

## Repository state

The owner packet, rigor audit, living head, and systems map carry C165-A and
make C166 the sole active owner choice. The authoritative decision record was
not edited and should retain SHA-256
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.

This was a documentation-only design update. Validate tracked and untracked
whitespace, SVG XML parsing, one active frontier, and the authoritative-record
hash. Do not run the Node suite because no code or executable contract changed.
