# Handoff — Per-opportunity coverage selected; two-form saturation next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-24 03:31:37
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.165: every-opportunity two-form pre-choice
disclosure saturation. The owner's latest `A` selected C3c.164-A and nothing
in C165. This remains Soul Relic design, specifically the player-information
surface of the selected same-opportunity, one-hinge result-rearm grammar.
Preserve accepted EP-D02 wording until a complete normative replacement is
replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready bounded choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Do not batch two-
form saturation with singleton polarity, shared presentation, command-holder
identity or communication, hinge disclosure, accessibility, value, or fun.

## Selected direction — EP-D02-C3c.164-A

`U^{pre-all-op+}_v=D^{dual}_v`. Every mechanically joint opportunity on every
exact legal history of every dual tag exposes at least one qualifying P or R
account to its actual command-holder before commitment. An opportunity may
expose P only, R only, or both.

At the exact tag/history/role/selection boundary, each disclosed form is
**direct**. A singleton-form opportunity's relationship to the complete dual
grammar is **partial**; excluding total opportunity-level opacity is
**neutral/protective**; and universal opportunity/history/tag coverage is
**aggregate**.

C164-A derives that every joint opportunity's command-holder role has a
lawfully human-controllable observation contract. It does not require actual
human occupancy or select bearer/ally/opponent identity. C164-A adds no floor:
the tight three-tag construction and all fixed catalog and width floors remain
unchanged, and every opaque-opportunity count is zero.

## Active owner choice — EP-D02-C3c.165

For every `k in D^{dual}_v`, `w in W_v(k)`, and
`d in D^{joint}(w)`, define

`S^{pre}_v(k,w,d)={F in {P,R} : Lambda_F(w,d) is nonempty}`,

`D^{pre2}_v(k,w)={d in D^{joint}(w) :
S^{pre}_v(k,w,d)={P,R}}`,

`D^{pre1}_v(k,w)={d in D^{joint}(w) :
|S^{pre}_v(k,w,d)|=1}`,

and

`U^{pre-all-op2}_v={k in D^{dual}_v : for every w in W_v(k),
D^{pre2}_v(k,w)=D^{joint}(w)}`.

C164-A leaves only `{P}`, `{R}`, and `{P,R}` support at every joint
opportunity. Each history's joint set is therefore the disjoint union of its
singleton-form and two-form subsets. C163-A makes the two-form subset nonempty
on every exact legal history. Singleton-form means exactly one causal form has
a qualifying account; it does not mean one text, pair, reference, action, or
continuation.

```text
EVERY-OPPORTUNITY TWO-FORM SATURATION
d_converge: P and R accounts
d_tend:     P and R accounts, perhaps emphasizing P
d_manifest: P and R accounts, perhaps emphasizing R

FOCUSED ONE-FORM OPPORTUNITY
d_converge: P and R accounts
d_tend:     P account only; its mechanical R distinction still exists
```

### A — every joint opportunity makes both forms co-legible — recommended

`U^{pre-all-op2}_v=D^{dual}_v`. At every mechanical joint opportunity on every
legal history, the one actual command-holder's exact authorized `O^-` makes a
qualifying P pair and distinct qualifying R pair intelligible before
commitment.

At the exact `(k,w,d,u,O^-_u)` boundary, continuing Soul–Relic context and both
genuinely distinct causal forms coexist **directly**. Their later relationship
to `q2`, effects, and payoff remains **partial**; excluding form-hiding at a
mechanically dual choice is **neutral/protective**; and universal opportunity/
history/tag saturation is **aggregate**.

A prevents any mechanically active causal face from being hidden at the choice
where it matters. Costs are two-form information density, localization and
exhaustive validation at every joint choice, repetition, less focused teaching
or mystery, and token compliance through a buried second account. A removes
every P-only and R-only joint opportunity, although presentation may still
emphasize one form.

Example: Ashen's Converge, Tend, and Manifest choices each explain both the
bond-predicate substitution and recurrence-permission substitution. Tend may
foreground the bond, but cannot hide its mechanically active recurrence face.

### B — every dual tag mixes co-legible and singleton-form opportunities

`U^{pre-all-op2}_v` is empty. Every dual tag has at least one exact P-only or
R-only joint opportunity. C163-A supplies a distinct co-legible opportunity on
that singleton witness's same history. B does not make every opportunity or
history singleton-supported and does not require both singleton polarities.

This is the strongest focused-teaching countercase. At a singleton choice the
disclosed form is **direct**, the undisclosed mechanically active form is
**neutral/unsupported**, same-history participation with the co-legible anchor
is **partial**, deliberate cognitive focus may be **neutral/protective**, and
universal per-tag mixing is **aggregate**.

Costs are choosing through one undisclosed causal face, apparent explanation
inconsistency, guide pressure, and token singleton membership through one
obscure choice.

Example: one Ashen history has `d_converge={P,R}` and a distinct
`d_tend={P}`. Tend still has an inherited mechanical R pair, but its exact
command-holder receives no qualifying pre-choice R account there.

### C — fully saturated and opportunity-mixed dual tags coexist

`U^{pre-all-op2}_v` is a nonempty proper subset of `D^{dual}_v`. At least one
dual tag exposes both forms at every joint opportunity. At least one distinct
tag retains a P-only or R-only opportunity with a distinct co-legible choice
on that same history.

The local classes inherit A/B's ratings and their coexistence is
**aggregate**. Costs are a hidden information dialect, uneven learning and
clarity, likely clarity/flexibility premiums, and token class membership.

Example: every Ashen joint opportunity is `{P,R}`. Dreamglass has a `{P,R}`
Study choice on every history but also one Facet choice with `{R}` only.

## Proof, topology consequence, floors, and deferrals

A/B/C are exhaustive because the saturated set is a subset of the nonempty
dual domain: whole, empty, or nonempty proper. C164-A rules out empty support,
while C163-A rules out any all-singleton history. A tag outside the saturated
set has a singleton witness plus a distinct co-legible witness on that same
history. A never-co-legible or wholly opaque fourth class is impossible.

A requires, for every exact legal `(k,w,d)` satisfying `J^{both-op}`, one
`pi_P in Lambda_P(w,d)` and distinct
`pi_R in Lambda_R(w,d)` under the same `u(w,d)` and `O^-`. B requires, for
every tag, one exact singleton witness, a positive Lambda pair for its supported
form, and exhaustive proof that the other Lambda set is empty across all
qualifying pairs, authorized pre-choice information, and reachable references.
C combines A-style universal proof for one tag with B's positive-and-negative
singleton witness for another. Missing opportunity enumeration or incomplete
information search remains unresolved.

Accounts at different opportunities or histories cannot be stitched. A common
panel, turn, animation, or menu cannot merge commitment-separated choices, and
cosmetic pages cannot split one atomic choice. One pair cannot establish both
forms; P and R require distinct isolating pairs. Separate references may
combine only when both are authorized and reachable inside the target choice's
same exact `O^-`; one shared asset or screen is not required. Post-choice
feedback, private AI state, external guides, data mining, inference, fused non-
causal “does both” labels, and another seat's uncommunicated information remain
invalid. Duplicate aliases count once. Non-joint choices stay outside the
denominator.

Every branch inherits C164-A's lawful human-control capability at every joint
opportunity. A singleton opportunity under B/C cannot be explained away as AI-
only: the same exact `u(w,d)` and `O^-` carries one qualifying account and lacks
the other. C165-A adds no controller count or identity and does not require
actual human occupancy, but requires both accounts for that same role at every
joint choice.

C165-A would be a genuine strengthening: it collapses joint-opportunity
support states from `{P}`, `{R}`, and `{P,R}` to `{P,R}` alone. It thereby
subsumes C164-A and the earlier co-legibility participation/saturation rules.
It does not collapse P and R into one mechanic or pair, cover non-joint combat
choices, or require one shared text, reference, presentation, salience, actual
controller, or communication path.

Tight indexed incidences are:

| Branch | Candidate tags | Dual `(k,w)` incidences | Positive joint opportunities | Co-legible | Singleton | Opaque | Form memberships / legible pairs | Mechanical pairs / continuations |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| A | 3 | 1 | 1 | 1 | 0 | 0 | 2 / 2 | 2 / 3 |
| B | 3 | 1 per dual tag | 2 per dual tag | 1 per dual tag | 1 per dual tag | 0 | 3 / 3 per dual tag | 4 / 6 per dual tag |
| C | 4 | 2 total | 3 total | 2 total | 1 total | 0 | 5 / 5 total | 6 / 9 total |

Every joint opportunity retains at least two mechanical pairs and three
semantic continuations even if only one form is disclosed. These are indexed
witnesses, not necessarily distinct global histories, actions, texts,
references, assets, or buttons. No branch raises local width or adds a receipt,
result width, decisive cut, definition, exact key, or Soul row.

Every branch retains nine definitions, twenty-three exact keys, 138 Soul/key
rows at six Souls, four represented locked IDs, eight lived aligned edges,
four one-history-complete IDs, no incomplete represented ID, Souls
`{6,9,10,12,14,15}`, degree six, and C46-A's same-boundary width two.

C165 defers class identities, counts, fractions, frequency, access, and
ordinary-route prevalence; P-only versus R-only topology under B/C; number of
legible pairs per form and whether every mechanical pair is explained; one
shared explanation/reference/presentation surface, equal salience or order,
modality, discoverability, accessibility, or wording; bearer/ally/opponent
identity, actual controller, role stability, or communication; hinge
disclosure; effect timing, feedback, preview, value, non-dominance, payoff,
power, fun, UI, release, and implementation.

Under recommended A, the singleton-polarity branch disappears. Recompute the
coarsest shared-explanation/presentation atom next; do not silently choose one
shared tooltip, reference, panel, or wording.

## Repository state

The owner packet, rigor audit, living head, and systems map carry C164-A and
make C165 the sole active owner choice. The authoritative decision record was
not edited and should retain SHA-256
`e7e5c0fb047e42e5852648972f7f57f5539708bf990d97fe597d000ad5ed5358`.

This was a documentation-only design update. Validate tracked and untracked
whitespace, SVG XML parsing, one active frontier, and the authoritative-record
hash. Do not run the Node suite because no code or executable contract changed.
