# Handoff — Universal same-encounter rearm selected; blink exposure next

**Session ID:** `01a074ac-c9e7-7303-8538-c8e392199ac2`
**UTC:** 2026-09-22 07:07:51
**Branch:** `design/endless-progression-owner-packet`

Resume with only EP-D02-C3c.137: whether a same-tag relationship may break and
renew without any materially rearm-relevant participant choice while it is
insufficient. The
owner selected C3c.136-A. Preserve accepted EP-D02 wording until a complete
replacement is replayed and explicitly accepted. Do not implement.

## Standing north star and workflow

The standalone game is **Souls and Simulacra**. Use *Achintya Bheda Abheda*
(*acintya-bhedabheda*) as the enduring design ideal: prefer meaningful
continuity or participation and real, mechanically legible distinction at the
same operative relationship. This is an inspirational game-design analogy,
not theological equivalence, a silent invariant, or an automatic selector.

Present exactly one prerequisite-ready A/B/C choice at a time with a
recommendation, strongest countercase, concrete example, named operative
boundary, and direct/partial/neutral/aggregate ideal rating. Finish the current
full-assurance pass before the standalone expansion/audit/adaptation pass.
Vanilla evidence and the byte-identical oracle remain in their own lane.

## Selected direction — EP-D02-C3c.136-A

`K^{enc-rearm}_v=K^{candidate}_v`.

Every fixed context/receipt tag has at least one fully specified, nonforked,
fixed-version legal encounter history with:

1. an initial authoritative commit `c1`;
2. a strictly later authoritative relationship-insufficient state;
3. renewed sufficiency plus recurrence authorization making a new occurrence
   provisionally complete; and
4. a distinct same-tag commit `c2` before terminal encounter resolution.

Queued/duplicate settlement, rollback, replay, redelivery, polling, evaluator
microsteps, and random divergence do not count. Temporary context unavailability
may participate only if the same fixed tag returns and commits; terminal
teardown cannot supply a same-encounter witness.

C136-A makes every tag rearmable somewhere, not after every break or in every
encounter. It retains `9/23/138`, four represented IDs, eight lived edges, four
complete IDs, no incomplete represented ID, Souls `{6,9,10,12,14,15}`, and
degree six. At the same Soul/same persistent Relic/same tag across loss,
renewal, and remanifestation, the relation is **partial** because it spans a
temporal trajectory; universal capability is **aggregate**. The authoritative
decision record remains unchanged.

## Why material decision exposure is next

A retained-strand or source-replacement rule can be token-satisfied inside an
automatic false/true pulse unexposed to player policy. Material decision
exposure is the coarser implementation-independent reset-quality atom. It uses
the relationship truth already defined by C135/C136 and C133's material
decision semantics. It chooses no exact actor authority, chosen rebuild action,
source atoms, duration, failed evaluation, cadence, or cap.

## Active owner choice — EP-D02-C3c.137

For one C136 witness, let `q2` be the canonical semantic cut where renewed
relationship sufficiency plus recurrence authorization make the second
occurrence provisionally complete:

`c1 < at least one relationship-insufficient cut < q2 <= c2 < T_enc`.

Use adjacent same-tag commits: `c2` is the next same-tag commit after `c1`.
Further commits are evaluated again at each adjacent pair, so an earlier
decision-exposed reset cannot launder a later blink.

A **material rearm opportunity** uses C133's decision test: an authorized
participant chooses among genuinely distinct legal combat continuations from an
authoritative pre-choice snapshot where the fixed relationship is insufficient,
and reachable isolated alternatives can change whether or when the relationship
can renew, recurrence authorization can occur, or the tagged result remains
reachable.
Unrelated choices, forced confirmations, browsing without battle authority,
animations, callbacks, polling, serialization, and resolver microsteps do not
count. The continuation actually chosen need not cause renewal, and the
decision need not belong to the Relic owner.

A C136 witness is **decision-unexposed**, or a **blink witness**, when no
material rearm opportunity occurs after `c1` and before `q2`. Evaluate every
insufficient interval in that span. An early automatic pulse does not make a
history a blink if a later material rearm opportunity occurs while the
relationship is false before `q2`; an unrelated choice or one taken only after
renewed sufficiency does not expose the earlier pulse.

Let `F^0_v` contain exactly the tags with at least one blink witness. Negative
membership requires complete reachable-domain analysis of every legal fixed-tag
same-encounter recommit history. Incomplete analysis is unresolved.

### A — no tag permits decision-unexposed rearm — recommended

`F^0_v` is empty. Every legal same-tag recommit path must contain at least one
material rearm opportunity while the relationship is insufficient before
`q2`. This makes loss a gameplay event and blocks unrelated-choice laundering
as well as free clear/reapply flicker. It can be the very next decision; A sets
no clock duration or cooldown.

Cost: even a deliberate, visible, resource-priced compound Technique cannot
break, rebuild, and rearm one tag wholly inside its own resolution. Temporal
recurrence remains **partial**; blink exclusion is **neutral/protective**, and
universal prevalence is **aggregate**.

### B — every tag permits decision-unexposed rearm — fast-combo countercase

`F^0_v=K^{candidate}_v`. Every tag has at least one witness where an
authoritative false/true pulse leads to recommit without any material rearm
opportunity while the relationship is false. Other routes may still be
decision-exposed.

B supports rapid, resource-costed Echo chains and automatic cycles, but creates
universal burst/proc-loop pressure and makes authored combos difficult to
separate from reset farming. The inherited temporal relation remains
**partial**; blink allowance is **neutral**, and prevalence is **aggregate**.

### C — blink-tolerant and decision-exposed-only tags coexist

`F^0_v` is a nonempty proper subset of `K^{candidate}_v`. Some tags admit fast
blink rearm; at least one other tag has no blink witness and can recommit only
through a decision-exposed break. C offers the richest rhythm but creates two
reset languages, disclosure/testing burden, token witnesses, and premium-
capability risk. Local ratings remain and coexistence is only **aggregate**.

## Concrete separator

Guard + Heat produces P and P commits as `c1`. Heat later becomes
authoritatively false and sufficient again.

- **Decision-exposed:** while Heat is still false, a genuine combat choice has
  legal alternatives capable of affecting its renewal or P's reauthorization.
  The chosen continuation may rebuild it, after which P can commit.
- **Blink:** one compound action or automatic resolution clears and restores
  Heat before any rearm-relevant choice can affect it; P can still reauthorize
  and commit.

A forbids the second route for every tag; B requires every tag to have some such
route; C permits it only for selected tags. None says every break rearms.

## Compatibility, floors, and deferrals

C137's exposed decision is before `q2`, when the second occurrence is not yet
provisionally complete, so it is not a C133 response window. At `q2`, C134-A
seals the candidate and C133-A requires commitment before any later participant
decision can change it. C46-A's same-boundary maximum remains two.

C45-C supplies at least three tags, so C is feasible. Every branch retains
`9/23/138`, four represented IDs, eight lived edges, four complete IDs, no
incomplete represented ID, Souls `{6,9,10,12,14,15}`, and degree six.

C137 selects no exact actor authority or chosen rebuild action, same-context
requirement, false duration,
failed-evaluation requirement, source loss or retained strand, trigger
authority, recurrence phase, cadence, cooldown, broader cap, cross-encounter/
Circuit recurrence, `H` retention, relationship operator, consumption, payoff,
access, power, persistence, migration, release, or implementation.

## Process-doc warning

`docs/overnight-agent-plan.md` still lacks the `## THE ARCHIVE LINE` named by
`AGENTS.md` and contains superseded fan-out/Ruffle guidance. The correction in
`HANDOFF.md`'s living head remains controlling: follow direct `AGENTS.md` rules
and do not treat the overnight plan as current guidance.

## Required next-turn behavior

Treat the owner's next bare `A`, `B`, or `C` as the answer to C137 only. Record
the selected branch and its consequences, recompute the frontier, and present
exactly one next prerequisite-ready choice. Do not infer acceptance of the whole
EP-D02 replacement or start implementation.
